# GENERATED by dagr/codegen/mojo/serde.py — do not edit by hand.
# Serializer (arena -> bytes) for graph 'EventFrozenPackedGraph' — plan 28 Phase 4.

from dagr_writer import Builder, NodeStoreRef, node_offset, resolved_ref, nil_ref, to_zigzag, leb_length
from event_frozen_packed_graph_arena import EventFrozenPackedGraphArena, EventAttr, Event

comptime _VT_MAX = 1   # widest regular-node vtable (§1.19)


# Build a List[Int] of vtable entries from a varargs-free call site.
def _ilist(*vals: Int) -> List[Int]:
    var l = List[Int]()
    for v in vals:
        l.append(v)
    return l^


# Type-namespaced node identity for dedup / cycle keys: a bare packed handle
# collides across node types (index space is per-type), so fold in the type id.
def _nid(tid: Int, idx: Int) -> UInt64:
    return (UInt64(idx) << 8) | UInt64(tid)


def _store_event_attr[o: Origin[mut=False], //](mut b: Builder[_VT_MAX], a: EventFrozenPackedGraphArena, h: EventAttr[o]) raises -> NodeStoreRef:
    var _id = _nid(0, h._index())
    if _id in b.struct_lookup:
        return resolved_ref(b.struct_lookup[_id])
    var _before = b.cursor
    var _rawbits: UInt64 = 0
    var _pr0 = Bool(a._arr_event_attr[h._index()].key)
    var _pr1 = Bool(a._arr_event_attr[h._index()].value)
    if _pr1:
        _ = b.store_utf8(a._arr_event_attr[h._index()].value.value(), False)
    if _pr0:
        _ = b.store_utf8(a._arr_event_attr[h._index()].key.value(), False)
    var _pb: UInt64 = 0
    if _pr0:
        _pb |= UInt64(1) << 0
    if _pr1:
        _pb |= UInt64(1) << 1
    _ = b.store_u8(UInt8(_pb & 0xff))
    _ = b.store_leb(UInt64(b.cursor - _before))
    var _o = b.cursor
    b.struct_lookup[_id] = _o
    return resolved_ref(_o)


def _store_event[o: Origin[mut=False], //](mut b: Builder[_VT_MAX], a: EventFrozenPackedGraphArena, h: Event[o]) raises -> NodeStoreRef:
    var _id = _nid(1, h._index())
    if _id in b.struct_lookup:
        return resolved_ref(b.struct_lookup[_id])
    var _before = b.cursor
    var _rawbits: UInt64 = 0
    var _pr0 = Bool(a._arr_event[h._index()].event_id)
    var _pr1 = Bool(a._arr_event[h._index()].event_type)
    var _pr2 = Bool(h.occurred_at())
    var _pr3 = Bool(a._arr_event[h._index()].producer)
    var _pr4 = True
    ref _row4 = a._arr_event[h._index()].attrs
    var _pcb4 = b.cursor
    var _n4 = 0
    for _k4 in range(len(_row4) - 1, -1, -1):
        var _p4 = _row4[_k4]
        _ = _store_event_attr(b, a, EventAttr[o](h._a, _p4))
        _n4 += 1
    _ = b.store_leb(UInt64(_n4))
    _ = b.store_leb(UInt64(b.cursor - _pcb4))
    if _pr3:
        _ = b.store_utf8(a._arr_event[h._index()].producer.value(), False)
    if _pr2:
        if leb_length(to_zigzag(Int(h.occurred_at().value()))) < 8:
            _ = b.store_leb(to_zigzag(Int(h.occurred_at().value())))
        else:
            _ = b.store_i64(h.occurred_at().value())
            _rawbits |= UInt64(1) << 0
    if _pr1:
        _ = b.store_utf8(a._arr_event[h._index()].event_type.value(), False)
    if _pr0:
        _ = b.store_utf8(a._arr_event[h._index()].event_id.value(), False)
    _ = b.store_u8(UInt8(_rawbits & 0xff))
    var _pb: UInt64 = 0
    if _pr0:
        _pb |= UInt64(1) << 0
    if _pr1:
        _pb |= UInt64(1) << 1
    if _pr2:
        _pb |= UInt64(1) << 2
    if _pr3:
        _pb |= UInt64(1) << 3
    if _pr4:
        _pb |= UInt64(1) << 4
    _ = b.store_u8(UInt8(_pb & 0xff))
    _ = b.store_leb(UInt64(b.cursor - _before))
    var _o = b.cursor
    b.struct_lookup[_id] = _o
    return resolved_ref(_o)


def write_event_graph(mut b: Builder[_VT_MAX], a: EventFrozenPackedGraphArena) raises -> Int:
    var _off = node_offset(_store_event(b, a, a.root().value()))
    return b.store_leb(UInt64((b.cursor - _off) << 2))    # framing (no header)

def serialize_event_graph(a: EventFrozenPackedGraphArena, max_size: Int = 2 * 1024 * 1024) raises -> List[UInt8]:
    var b = Builder[_VT_MAX](hint=len(a._arr_event_attr) + len(a._arr_event), max_size=max_size)
    _ = write_event_graph(b, a)
    return b.make_data()
