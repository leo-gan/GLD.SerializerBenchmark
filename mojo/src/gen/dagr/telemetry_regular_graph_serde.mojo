# GENERATED by dagr/codegen/mojo/serde.py — do not edit by hand.
# Serializer (arena -> bytes) for graph 'TelemetryRegularGraph' — plan 28 Phase 4.

from dagr_writer import Builder, NodeStoreRef, node_offset, resolved_ref, nil_ref, to_zigzag, leb_length
from telemetry_regular_graph_arena import TelemetryRegularGraphArena, Telemetry

comptime _VT_MAX = 4   # widest regular-node vtable (§1.19)


# Build a List[Int] of vtable entries from a varargs-free call site.
def _ilist(*vals: Int) -> List[Int]:
    var l = List[Int]()
    for v in vals:
        l.append(v)
    return l^


# Type-namespaced node identity for dedup / cycle keys: a bare packed handle
# collides across node types (index space is per-type), so fold in the type id.
def _nid(tid: Int, idx: Int) -> UInt64:
    return (UInt64(idx) << 8) | UInt64(tid)


def _store_telemetry[o: Origin[mut=False], //](mut b: Builder[_VT_MAX], a: TelemetryRegularGraphArena, h: Telemetry[o]) raises -> NodeStoreRef:
    var _id = _nid(0, h._index())
    var _r = b.begin_storing_acyclic(_id)
    if _r.is_pending or _r.off != -1:
        return _r
    var _c3 = -1
    var _bi3 = h._index()
    _ = b.store_raw_block(a._arr_telemetry[_bi3].values, 8)
    _c3 = b.store_leb(UInt64(len(a._arr_telemetry[_bi3].values)))
    var _c2 = -1
    var _bi2 = h._index()
    var _offs2 = b.take_ints()
    for _k2 in range(len(a._arr_telemetry[_bi2].tags) - 1, -1, -1):
        _offs2.append(b.store_utf8(a._arr_telemetry[_bi2].tags[_k2], True))
    _c2 = b.store_ptr_table(_offs2)
    b.return_ints(_offs2^)
    var _c0 = -1
    var _p0 = Bool(a._arr_telemetry[h._index()].source)
    if _p0:
        _c0 = b.store_utf8(a._arr_telemetry[h._index()].source.value(), True)
    var _off3 = b.store_forward_pointer(_c3)
    var _off2 = b.store_forward_pointer(_c2)
    var _off1 = -1
    var _v1 = h.ts()
    if _v1:
        _off1 = b.store_i64(_v1.value())
    var _off0 = -1
    if _p0:
        _off0 = b.store_forward_pointer(_c0)
    var _o = b.store_vtable(_off0, _off1, _off2, _off3)
    b.finish_storing_acyclic(_id, _o)
    return resolved_ref(_o)


def write_telemetry_graph(mut b: Builder[_VT_MAX], a: TelemetryRegularGraphArena) raises -> Int:
    var _off = node_offset(_store_telemetry(b, a, a.root().value()))
    return b.store_leb(UInt64((b.cursor - _off) << 2))    # framing (no header)

def serialize_telemetry_graph(a: TelemetryRegularGraphArena, max_size: Int = 2 * 1024 * 1024) raises -> List[UInt8]:
    var b = Builder[_VT_MAX](hint=len(a._arr_telemetry), max_size=max_size)
    _ = write_telemetry_graph(b, a)
    return b.make_data()
