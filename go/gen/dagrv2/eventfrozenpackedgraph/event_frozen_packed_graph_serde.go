// Code generated by dagr (dagr/codegen/go/serde.py); DO NOT EDIT.

package eventfrozenpackedgraph

import "dagrv2/dagr"

func storeEventAttr(n EventAttr, b *dagr.Builder) dagr.NodeStoreRef {
	if d, ok := b.StructLookup[n.key()]; ok {
		return dagr.NodeStoreRef{Off: d}
	}
	before := b.Cursor()
	if x1, ok := n.Value(); ok {
		b.StoreUtf8(x1, false)
	}
	if x0, ok := n.Key(); ok {
		b.StoreUtf8(x0, false)
	}
	var nbits uint64
	if _, ok := n.Key(); ok {
		nbits |= 1 << 0
	}
	if _, ok := n.Value(); ok {
		nbits |= 1 << 1
	}
	b.StoreBytes([]byte{byte(nbits >> 0)})
	b.StoreLEB(uint64(b.Cursor() - before))
	o := b.Cursor()
	b.StructLookup[n.key()] = o
	return dagr.NodeStoreRef{Off: o}
}

func storeEvent(n Event, b *dagr.Builder) dagr.NodeStoreRef {
	if d, ok := b.StructLookup[n.key()]; ok {
		return dagr.NodeStoreRef{Off: d}
	}
	before := b.Cursor()
	var raw [1]bool
	if x4, ok := n.Attrs(); ok {
		dagr.StorePackedNodeArrayPlain(b, x4, func(b *dagr.Builder, v EventAttr) { storeEventAttr(v, b) })
	}
	if x3, ok := n.Producer(); ok {
		b.StoreUtf8(x3, false)
	}
	if x2, ok := n.OccurredAt(); ok {
		zz := dagr.ToZigZag(int64(x2))
		if dagr.LebLength(zz) < 8 {
			b.StoreLEB(zz)
		} else {
			b.StoreI64(x2)
			raw[0] = true
		}
	}
	if x1, ok := n.EventType(); ok {
		b.StoreUtf8(x1, false)
	}
	if x0, ok := n.EventID(); ok {
		b.StoreUtf8(x0, false)
	}
	enc := make([]byte, 1)
	for k, r := range raw {
		if r {
			enc[k>>3] |= 1 << (k & 7)
		}
	}
	b.StoreBytes(enc)
	var nbits uint64
	if _, ok := n.EventID(); ok {
		nbits |= 1 << 0
	}
	if _, ok := n.EventType(); ok {
		nbits |= 1 << 1
	}
	if _, ok := n.OccurredAt(); ok {
		nbits |= 1 << 2
	}
	if _, ok := n.Producer(); ok {
		nbits |= 1 << 3
	}
	if _, ok := n.Attrs(); ok {
		nbits |= 1 << 4
	}
	b.StoreBytes([]byte{byte(nbits >> 0)})
	b.StoreLEB(uint64(b.Cursor() - before))
	o := b.Cursor()
	b.StructLookup[n.key()] = o
	return dagr.NodeStoreRef{Off: o}
}

// ToBytesEventFrozenPackedGraph serializes the graph reachable from root (byte-identical to every
// other Dagr implementation for the same graph). maxSize sets the cycle back-
// reference placeholder width (0 → 2 MiB, i.e. 4 bytes) and must match across
// producers for byte-identity.
func ToBytesEventFrozenPackedGraph(root Event, maxSize int) []byte {
	b := dagr.NewBuilder(maxSize)
	off := dagr.NodeOffset(storeEvent(root, b))
	b.StoreLEB(uint64(b.Cursor()-off) << 2)
	return b.MakeData()
}
