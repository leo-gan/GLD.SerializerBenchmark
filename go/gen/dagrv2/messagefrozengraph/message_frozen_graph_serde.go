// Code generated by dagr (dagr/codegen/go/serde.py); DO NOT EDIT.

package messagefrozengraph

import "dagrv2/dagr"

func storeMessage(n Message, b *dagr.Builder) dagr.NodeStoreRef {
	if r, ok := b.BeginStoring(n.key()); ok {
		return r
	}
	var c7 int
	x7, has7 := n.FString2()
	if has7 {
		c7 = b.StoreUtf8(x7, true)
	}
	var c4 int
	x4, has4 := n.FString()
	if has4 {
		c4 = b.StoreUtf8(x4, true)
	}
	if has7 {
		b.StoreForwardPointer(c7)
	}
	if x6, ok := n.FInt32_2(); ok {
		b.StoreI32(x6)
	}
	if x5, ok := n.FBool2(); ok {
		b.StoreBool(x5)
	}
	if has4 {
		b.StoreForwardPointer(c4)
	}
	if x3, ok := n.FFloat64(); ok {
		b.StoreF64(x3)
	}
	if x2, ok := n.FInt64(); ok {
		b.StoreI64(x2)
	}
	if x1, ok := n.FInt32(); ok {
		b.StoreI32(x1)
	}
	if x0, ok := n.FBool(); ok {
		b.StoreBool(x0)
	}
	var bs uint64
	if _, ok := n.FBool(); ok {
		bs |= 1 << 0
	}
	if _, ok := n.FInt32(); ok {
		bs |= 1 << 1
	}
	if _, ok := n.FInt64(); ok {
		bs |= 1 << 2
	}
	if _, ok := n.FFloat64(); ok {
		bs |= 1 << 3
	}
	if _, ok := n.FString(); ok {
		bs |= 1 << 4
	}
	if _, ok := n.FBool2(); ok {
		bs |= 1 << 5
	}
	if _, ok := n.FInt32_2(); ok {
		bs |= 1 << 6
	}
	if _, ok := n.FString2(); ok {
		bs |= 1 << 7
	}
	b.StoreBytes([]byte{byte(bs >> 0)})
	o := b.Cursor()
	b.FinishStoring(n.key(), o)
	return dagr.NodeStoreRef{Off: o}
}

// ToBytesMessageFrozenGraph serializes the graph reachable from root (byte-identical to every
// other Dagr implementation for the same graph). maxSize sets the cycle back-
// reference placeholder width (0 → 2 MiB, i.e. 4 bytes) and must match across
// producers for byte-identity.
func ToBytesMessageFrozenGraph(root Message, maxSize int) []byte {
	b := dagr.NewBuilder(maxSize)
	off := dagr.NodeOffset(storeMessage(root, b))
	b.StoreLEB(uint64(b.Cursor()-off) << 2)
	return b.MakeData()
}

// AppendToMessageFrozenGraph serializes the graph reachable from root into b (reset first) and
// appends the record to dst: ToBytes without a Builder per call. Bytes are
// identical to ToBytes with the maxSize b was created with.
func AppendToMessageFrozenGraph(b *dagr.Builder, dst []byte, root Message) []byte {
	b.Reset()
	off := dagr.NodeOffset(storeMessage(root, b))
	b.StoreLEB(uint64(b.Cursor()-off) << 2)
	return b.AppendTo(dst)
}
