# GENERATED by dagr/codegen/mojo/arena.py — do not edit by hand.
# Arena (in-memory graph) + deletion + cycle-aware structural methods — plan 28
# Phase 2, Design H. A handle `{Node}[o]` carries a SHARED safe Pointer to its arena
# (origin-typed) + a packed index; getters/setters/structural/delete are HANDLE
# methods (Rust/Swift-identical call site: `alice.name()`, `alice.set_age(31)`,
# `alice.equals(bob)`). Reads go through the safe pointer; writes/factories mutate
# through an Pointer interior-mutability escape. Two arenas get two origins →
# cross-arena handle use is a compile error (no brand needed). Soundness contract:
# a handle borrows the arena (shares its origin) — the arena must not MOVE while
# handles are live (like Rust's borrow / Swift's heap-stable class).

from std.collections import Set
from std.hashlib import Hasher

comptime IDX_MASK = (UInt64(1) << 40) - 1


def _pack(gen: UInt32, idx: Int) -> UInt64:
    return (UInt64(gen) << 40) | (UInt64(idx) & IDX_MASK)


def _uidx(p: UInt64) -> Int:
    return Int(p & IDX_MASK)


def _ugen(p: UInt64) -> UInt32:
    return UInt32(p >> 40)


# Resolve a possibly-nil packed ref against a generation list: live -> Some(packed),
# stale/nil -> None (used by array-of-ref structural equality).
def _resolve_ref(gen: List[UInt32], e: Optional[UInt64]) -> Optional[UInt64]:
    if not e:
        return None
    var p = e.value()
    if gen[_uidx(p)] == _ugen(p):
        return Optional(p)
    return None


# Visited-set key for hash/repr: (type_id, arena identity, index).
@fieldwise_init
struct NodeKey(Copyable, Movable, ImplicitlyCopyable, Hashable, Equatable):
    var t: Int
    var a: Int
    var i: Int

    def __eq__(self, other: Self) -> Bool:
        return self.t == other.t and self.a == other.a and self.i == other.i

    def __ne__(self, other: Self) -> Bool:
        return not (self == other)

    def __hash__[H: Hasher](self, mut hasher: H):
        hasher.update(self.t)
        hasher.update(self.a)
        hasher.update(self.i)


# Visited-set key for cross-arena equality: a (left, right) node pair.
@fieldwise_init
struct EqPair(Copyable, Movable, ImplicitlyCopyable, Hashable, Equatable):
    var lt: Int
    var la: Int
    var li: Int
    var rt: Int
    var ra: Int
    var ri: Int

    def __eq__(self, other: Self) -> Bool:
        return (self.lt == other.lt and self.la == other.la and self.li == other.li
                and self.rt == other.rt and self.ra == other.ra and self.ri == other.ri)

    def __ne__(self, other: Self) -> Bool:
        return not (self == other)

    def __hash__[H: Hasher](self, mut hasher: H):
        hasher.update(self.lt)
        hasher.update(self.la)
        hasher.update(self.li)
        hasher.update(self.rt)
        hasher.update(self.ra)
        hasher.update(self.ri)


@fieldwise_init
struct EventAttrValues(Copyable, Movable):
    var key: Optional[String]
    var value: Optional[String]

@fieldwise_init
struct EventValues(Copyable, Movable):
    var event_id: Optional[String]
    var event_type: Optional[String]
    var occurred_at: Optional[Int64]
    var producer: Optional[String]
    var attrs: List[UInt64]

@fieldwise_init
struct EventAttr[o: Origin[mut=False]](Copyable, Movable, ImplicitlyCopyable, Writable, Hashable, Equatable):
    var _a: Pointer[EventFrozenPackedGraphArena, origin=Self.o]
    var _packed: UInt64
    def _i(self) -> Int:
        return _uidx(self._packed)
    def _index(self) -> Int:
        return _uidx(self._packed)
    def generation(self) -> UInt32:
        return _ugen(self._packed)
    def _aid(self) -> Int:
        return Int(Pointer(to=self._a[]))
    def _mut(self) -> Pointer[EventFrozenPackedGraphArena, MutAnyOrigin]:
        return Pointer[EventFrozenPackedGraphArena, MutAnyOrigin](unsafe_from_address=Int(Pointer(to=self._a[])))
    def key(self) -> Optional[String]:
        return self._a[]._arr_event_attr[self._i()].key.copy()
    def key_slice(self) -> Optional[StringSlice[origin_of(self._a[]._arr_event_attr[self._i()].key.value())]]:
        if self._a[]._arr_event_attr[self._i()].key:
            return Optional(StringSlice(self._a[]._arr_event_attr[self._i()].key.value()))
        return None
    def set_key(self, var v: String):
        self._mut()[]._arr_event_attr[self._i()].key = Optional(v^)
    def value(self) -> Optional[String]:
        return self._a[]._arr_event_attr[self._i()].value.copy()
    def value_slice(self) -> Optional[StringSlice[origin_of(self._a[]._arr_event_attr[self._i()].value.value())]]:
        if self._a[]._arr_event_attr[self._i()].value:
            return Optional(StringSlice(self._a[]._arr_event_attr[self._i()].value.value()))
        return None
    def set_value(self, var v: String):
        self._mut()[]._arr_event_attr[self._i()].value = Optional(v^)
    def write_to[W: Writer](self, mut writer: W):
        var seen = Set[NodeKey]()
        self._repr_cyc(writer, seen)
    def _repr_cyc[W: Writer](self, mut writer: W, mut seen: Set[NodeKey]):
        var i = self._i()
        var key = NodeKey(0, self._aid(), i)
        if key in seen:
            writer.write("EventAttr@", i)
            return
        seen.add(key)
        writer.write("EventAttr@", i, "{")
        ref _o0 = self._a[]._arr_event_attr[i].key
        if _o0:
            writer.write("key=", _o0.value())
        else:
            writer.write("key=None")
        writer.write(", ")
        ref _o1 = self._a[]._arr_event_attr[i].value
        if _o1:
            writer.write("value=", _o1.value())
        else:
            writer.write("value=None")
        writer.write("}")
    def __hash__[H: Hasher](self, mut hasher: H):
        var seen = Set[NodeKey]()
        self._hash_cyc(hasher, seen)
    def _hash_cyc[H: Hasher](self, mut hasher: H, mut seen: Set[NodeKey]):
        var i = self._i()
        var key = NodeKey(0, self._aid(), i)
        if key in seen:
            hasher.update(UInt64(0))
            return
        seen.add(key)
        hasher.update(UInt64(0))
        ref _o0 = self._a[]._arr_event_attr[i].key
        if _o0:
            hasher.update(_o0.value())
        ref _o1 = self._a[]._arr_event_attr[i].value
        if _o1:
            hasher.update(_o1.value())
    def __eq__(self, other: Self) -> Bool:
        return self.equals(other)
    def __ne__(self, other: Self) -> Bool:
        return not self.equals(other)
    def equals[o2: Origin[mut=False]](self, other: EventAttr[o2]) -> Bool:
        var seen = Set[EqPair]()
        return self._eq_cyc(other, seen)
    def _eq_cyc[o2: Origin[mut=False]](self, other: EventAttr[o2], mut seen: Set[EqPair]) -> Bool:
        var si = self._i()
        var oi = other._i()
        var pair = EqPair(0, self._aid(), si, 0, other._aid(), oi)
        if pair in seen:
            return True
        seen.add(pair)
        if self._a[]._arr_event_attr[si].key != other._a[]._arr_event_attr[oi].key:
            return False
        if self._a[]._arr_event_attr[si].value != other._a[]._arr_event_attr[oi].value:
            return False
        return True

@fieldwise_init
struct Event[o: Origin[mut=False]](Copyable, Movable, ImplicitlyCopyable, Writable, Hashable, Equatable):
    var _a: Pointer[EventFrozenPackedGraphArena, origin=Self.o]
    var _packed: UInt64
    def _i(self) -> Int:
        return _uidx(self._packed)
    def _index(self) -> Int:
        return _uidx(self._packed)
    def generation(self) -> UInt32:
        return _ugen(self._packed)
    def _aid(self) -> Int:
        return Int(Pointer(to=self._a[]))
    def _mut(self) -> Pointer[EventFrozenPackedGraphArena, MutAnyOrigin]:
        return Pointer[EventFrozenPackedGraphArena, MutAnyOrigin](unsafe_from_address=Int(Pointer(to=self._a[])))
    def event_id(self) -> Optional[String]:
        return self._a[]._arr_event[self._i()].event_id.copy()
    def event_id_slice(self) -> Optional[StringSlice[origin_of(self._a[]._arr_event[self._i()].event_id.value())]]:
        if self._a[]._arr_event[self._i()].event_id:
            return Optional(StringSlice(self._a[]._arr_event[self._i()].event_id.value()))
        return None
    def set_event_id(self, var v: String):
        self._mut()[]._arr_event[self._i()].event_id = Optional(v^)
    def event_type(self) -> Optional[String]:
        return self._a[]._arr_event[self._i()].event_type.copy()
    def event_type_slice(self) -> Optional[StringSlice[origin_of(self._a[]._arr_event[self._i()].event_type.value())]]:
        if self._a[]._arr_event[self._i()].event_type:
            return Optional(StringSlice(self._a[]._arr_event[self._i()].event_type.value()))
        return None
    def set_event_type(self, var v: String):
        self._mut()[]._arr_event[self._i()].event_type = Optional(v^)
    def occurred_at(self) -> Optional[Int64]:
        return self._a[]._arr_event[self._i()].occurred_at
    def set_occurred_at(self, var v: Int64):
        self._mut()[]._arr_event[self._i()].occurred_at = Optional(v)
    def producer(self) -> Optional[String]:
        return self._a[]._arr_event[self._i()].producer.copy()
    def producer_slice(self) -> Optional[StringSlice[origin_of(self._a[]._arr_event[self._i()].producer.value())]]:
        if self._a[]._arr_event[self._i()].producer:
            return Optional(StringSlice(self._a[]._arr_event[self._i()].producer.value()))
        return None
    def set_producer(self, var v: String):
        self._mut()[]._arr_event[self._i()].producer = Optional(v^)
    def attrs(self) -> EventAttrListView[Self.o, origin_of(self._a[]._arr_event[self._i()].attrs)]:
        return EventAttrListView[Self.o, origin_of(self._a[]._arr_event[self._i()].attrs)](self._a, Pointer(to=self._a[]._arr_event[self._i()].attrs))
    def set_attrs(self, v: List[EventAttr[Self.o]]):
        var packed = List[UInt64]()
        for k in range(len(v)):
            packed.append(v[k]._packed)
        self._mut()[]._arr_event[self._i()].attrs = packed^
    def write_to[W: Writer](self, mut writer: W):
        var seen = Set[NodeKey]()
        self._repr_cyc(writer, seen)
    def _repr_cyc[W: Writer](self, mut writer: W, mut seen: Set[NodeKey]):
        var i = self._i()
        var key = NodeKey(1, self._aid(), i)
        if key in seen:
            writer.write("Event@", i)
            return
        seen.add(key)
        writer.write("Event@", i, "{")
        ref _o0 = self._a[]._arr_event[i].event_id
        if _o0:
            writer.write("event_id=", _o0.value())
        else:
            writer.write("event_id=None")
        writer.write(", ")
        ref _o1 = self._a[]._arr_event[i].event_type
        if _o1:
            writer.write("event_type=", _o1.value())
        else:
            writer.write("event_type=None")
        writer.write(", ")
        ref _o2 = self._a[]._arr_event[i].occurred_at
        if _o2:
            writer.write("occurred_at=", String(_o2.value()))
        else:
            writer.write("occurred_at=None")
        writer.write(", ")
        ref _o3 = self._a[]._arr_event[i].producer
        if _o3:
            writer.write("producer=", _o3.value())
        else:
            writer.write("producer=None")
        writer.write(", ")
        writer.write("attrs=[", len(self._a[]._arr_event[i].attrs), " items]")
        writer.write("}")
    def __hash__[H: Hasher](self, mut hasher: H):
        var seen = Set[NodeKey]()
        self._hash_cyc(hasher, seen)
    def _hash_cyc[H: Hasher](self, mut hasher: H, mut seen: Set[NodeKey]):
        var i = self._i()
        var key = NodeKey(1, self._aid(), i)
        if key in seen:
            hasher.update(UInt64(0))
            return
        seen.add(key)
        hasher.update(UInt64(1))
        ref _o0 = self._a[]._arr_event[i].event_id
        if _o0:
            hasher.update(_o0.value())
        ref _o1 = self._a[]._arr_event[i].event_type
        if _o1:
            hasher.update(_o1.value())
        ref _o2 = self._a[]._arr_event[i].occurred_at
        if _o2:
            hasher.update(_o2.value())
        ref _o3 = self._a[]._arr_event[i].producer
        if _o3:
            hasher.update(_o3.value())
        for _e in self._a[]._arr_event[i].attrs:
            var _p = _e
            EventAttr[Self.o](self._a, _p)._hash_cyc(hasher, seen)
    def __eq__(self, other: Self) -> Bool:
        return self.equals(other)
    def __ne__(self, other: Self) -> Bool:
        return not self.equals(other)
    def equals[o2: Origin[mut=False]](self, other: Event[o2]) -> Bool:
        var seen = Set[EqPair]()
        return self._eq_cyc(other, seen)
    def _eq_cyc[o2: Origin[mut=False]](self, other: Event[o2], mut seen: Set[EqPair]) -> Bool:
        var si = self._i()
        var oi = other._i()
        var pair = EqPair(1, self._aid(), si, 1, other._aid(), oi)
        if pair in seen:
            return True
        seen.add(pair)
        if self._a[]._arr_event[si].event_id != other._a[]._arr_event[oi].event_id:
            return False
        if self._a[]._arr_event[si].event_type != other._a[]._arr_event[oi].event_type:
            return False
        if self._a[]._arr_event[si].occurred_at != other._a[]._arr_event[oi].occurred_at:
            return False
        if self._a[]._arr_event[si].producer != other._a[]._arr_event[oi].producer:
            return False
        ref _sl4 = self._a[]._arr_event[si].attrs
        ref _ol4 = other._a[]._arr_event[oi].attrs
        if len(_sl4) != len(_ol4):
            return False
        for _k in range(len(_sl4)):
            var _sp = _sl4[_k]
            var _op = _ol4[_k]
            if not EventAttr[Self.o](self._a, _sp)._eq_cyc(EventAttr[o2](other._a, _op), seen):
                return False
        return True

@fieldwise_init
struct EventAttrListIter[o: Origin[mut=False], lo: Origin[mut=False]](Copyable, Movable):
    var _a: Pointer[EventFrozenPackedGraphArena, origin=Self.o]
    var _ids: Pointer[List[UInt64], origin=Self.lo]
    var _i: Int
    def __next__(mut self) raises StopIteration -> EventAttr[Self.o]:
        if self._i >= len(self._ids[]):
            raise StopIteration()
        var _h = EventAttr[Self.o](self._a, self._ids[][self._i])
        self._i += 1
        return _h

@fieldwise_init
struct EventAttrListView[o: Origin[mut=False], lo: Origin[mut=False]](Copyable, Movable, Sized):
    var _a: Pointer[EventFrozenPackedGraphArena, origin=Self.o]
    var _ids: Pointer[List[UInt64], origin=Self.lo]
    def __len__(self) -> Int:
        return len(self._ids[])
    def __getitem__(self, i: Int) -> EventAttr[Self.o]:
        return EventAttr[Self.o](self._a, self._ids[][i])
    def __iter__(self) -> EventAttrListIter[Self.o, Self.lo]:
        return EventAttrListIter[Self.o, Self.lo](self._a, self._ids, 0)

struct EventFrozenPackedGraphArena(Movable):
    var _arr_event_attr: List[EventAttrValues]
    var _arr_event: List[EventValues]
    var _root: Optional[UInt64]
    def __init__(out self):
        self._arr_event_attr = List[EventAttrValues]()
        self._arr_event = List[EventValues]()
        self._root = None
    def new_event_attr[o: Origin[mut=False], //](ref [o] self, var key: String = String(""), var value: String = String("")) -> EventAttr[o]:
        var up = Pointer[EventFrozenPackedGraphArena, MutAnyOrigin](unsafe_from_address=Int(Pointer(to=self)))
        var idx = len(self._arr_event_attr)
        up[]._arr_event_attr.append(EventAttrValues(key=Optional(key^), value=Optional(value^)))
        return EventAttr[o](Pointer(to=self), _pack(0, idx))
    def new_event[o: Origin[mut=False], //](ref [o] self, var event_id: String = String(""), var event_type: String = String(""), var occurred_at: Int64 = Int64(0), var producer: String = String("")) -> Event[o]:
        var up = Pointer[EventFrozenPackedGraphArena, MutAnyOrigin](unsafe_from_address=Int(Pointer(to=self)))
        var idx = len(self._arr_event)
        up[]._arr_event.append(EventValues(event_id=Optional(event_id^), event_type=Optional(event_type^), occurred_at=Optional(occurred_at), producer=Optional(producer^), attrs=List[UInt64]()))
        return Event[o](Pointer(to=self), _pack(0, idx))
    def root[o: Origin[mut=False], //](ref [o] self) -> Optional[Event[o]]:
        if not self._root:
            return None
        var p = self._root.value()
        return Event[o](Pointer(to=self), p)
    def set_root[o: Origin[mut=False], //](ref [o] self, h: Event[o]):
        var up = Pointer[EventFrozenPackedGraphArena, MutAnyOrigin](unsafe_from_address=Int(Pointer(to=self)))
        up[]._root = Optional(h._packed)
