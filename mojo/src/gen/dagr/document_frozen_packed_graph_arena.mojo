# GENERATED by dagr/codegen/mojo/arena.py — do not edit by hand.
# Arena (in-memory graph) + deletion + cycle-aware structural methods — plan 28
# Phase 2, Design H. A handle `{Node}[o]` carries a SHARED safe Pointer to its arena
# (origin-typed) + a packed index; getters/setters/structural/delete are HANDLE
# methods (Rust/Swift-identical call site: `alice.name()`, `alice.set_age(31)`,
# `alice.equals(bob)`). Reads go through the safe pointer; writes/factories mutate
# through an Pointer interior-mutability escape. Two arenas get two origins →
# cross-arena handle use is a compile error (no brand needed). Soundness contract:
# a handle borrows the arena (shares its origin) — the arena must not MOVE while
# handles are live (like Rust's borrow / Swift's heap-stable class).

from std.collections import Set
from std.hashlib import Hasher

comptime IDX_MASK = (UInt64(1) << 40) - 1


def _pack(gen: UInt32, idx: Int) -> UInt64:
    return (UInt64(gen) << 40) | (UInt64(idx) & IDX_MASK)


def _uidx(p: UInt64) -> Int:
    return Int(p & IDX_MASK)


def _ugen(p: UInt64) -> UInt32:
    return UInt32(p >> 40)


# Resolve a possibly-nil packed ref against a generation list: live -> Some(packed),
# stale/nil -> None (used by array-of-ref structural equality).
def _resolve_ref(gen: List[UInt32], e: Optional[UInt64]) -> Optional[UInt64]:
    if not e:
        return None
    var p = e.value()
    if gen[_uidx(p)] == _ugen(p):
        return Optional(p)
    return None


# Visited-set key for hash/repr: (type_id, arena identity, index).
@fieldwise_init
struct NodeKey(Copyable, Movable, ImplicitlyCopyable, Hashable, Equatable):
    var t: Int
    var a: Int
    var i: Int

    def __eq__(self, other: Self) -> Bool:
        return self.t == other.t and self.a == other.a and self.i == other.i

    def __ne__(self, other: Self) -> Bool:
        return not (self == other)

    def __hash__[H: Hasher](self, mut hasher: H):
        self.t.__hash__(hasher)
        self.a.__hash__(hasher)
        self.i.__hash__(hasher)


# Visited-set key for cross-arena equality: a (left, right) node pair.
@fieldwise_init
struct EqPair(Copyable, Movable, ImplicitlyCopyable, Hashable, Equatable):
    var lt: Int
    var la: Int
    var li: Int
    var rt: Int
    var ra: Int
    var ri: Int

    def __eq__(self, other: Self) -> Bool:
        return (self.lt == other.lt and self.la == other.la and self.li == other.li
                and self.rt == other.rt and self.ra == other.ra and self.ri == other.ri)

    def __ne__(self, other: Self) -> Bool:
        return not (self == other)

    def __hash__[H: Hasher](self, mut hasher: H):
        self.lt.__hash__(hasher)
        self.la.__hash__(hasher)
        self.li.__hash__(hasher)
        self.rt.__hash__(hasher)
        self.ra.__hash__(hasher)
        self.ri.__hash__(hasher)


@fieldwise_init
struct DocumentMetaValues(Copyable, Movable):
    var region: Optional[String]
    var version: Optional[Int32]

@fieldwise_init
struct DocumentItemValues(Copyable, Movable):
    var sku: Optional[String]
    var qty: Optional[Int32]
    var price_minor: Optional[Int64]

@fieldwise_init
struct DocumentValues(Copyable, Movable):
    var id: Optional[String]
    var status: Optional[Int32]
    var meta: Optional[UInt64]
    var items: List[UInt64]

@fieldwise_init
struct DocumentMeta[o: Origin[mut=False]](Copyable, Movable, ImplicitlyCopyable, Writable, Hashable, Equatable):
    var _a: Pointer[DocumentFrozenPackedGraphArena, origin=Self.o]
    var _packed: UInt64
    def _i(self) -> Int:
        return _uidx(self._packed)
    def _index(self) -> Int:
        return _uidx(self._packed)
    def generation(self) -> UInt32:
        return _ugen(self._packed)
    def _aid(self) -> Int:
        return Int(Pointer(to=self._a[]))
    def _mut(self) -> Pointer[DocumentFrozenPackedGraphArena, MutAnyOrigin]:
        return Pointer[DocumentFrozenPackedGraphArena, MutAnyOrigin](unsafe_from_address=Int(Pointer(to=self._a[])))
    def region(self) -> Optional[String]:
        return self._a[]._arr_document_meta[self._i()].region.copy()
    def region_slice(self) -> Optional[StringSlice[origin_of(self._a[]._arr_document_meta[self._i()].region.value())]]:
        if self._a[]._arr_document_meta[self._i()].region:
            return Optional(StringSlice(self._a[]._arr_document_meta[self._i()].region.value()))
        return None
    def set_region(self, var v: String):
        self._mut()[]._arr_document_meta[self._i()].region = Optional(v^)
    def version(self) -> Optional[Int32]:
        return self._a[]._arr_document_meta[self._i()].version
    def set_version(self, var v: Int32):
        self._mut()[]._arr_document_meta[self._i()].version = Optional(v)
    def write_to[W: Writer](self, mut writer: W):
        var seen = Set[NodeKey]()
        self._repr_cyc(writer, seen)
    def _repr_cyc[W: Writer](self, mut writer: W, mut seen: Set[NodeKey]):
        var i = self._i()
        var key = NodeKey(0, self._aid(), i)
        if key in seen:
            writer.write("DocumentMeta@", i)
            return
        seen.add(key)
        writer.write("DocumentMeta@", i, "{")
        ref _o0 = self._a[]._arr_document_meta[i].region
        if _o0:
            writer.write("region=", _o0.value())
        else:
            writer.write("region=None")
        writer.write(", ")
        ref _o1 = self._a[]._arr_document_meta[i].version
        if _o1:
            writer.write("version=", String(_o1.value()))
        else:
            writer.write("version=None")
        writer.write("}")
    def __hash__[H: Hasher](self, mut hasher: H):
        var seen = Set[NodeKey]()
        self._hash_cyc(hasher, seen)
    def _hash_cyc[H: Hasher](self, mut hasher: H, mut seen: Set[NodeKey]):
        var i = self._i()
        var key = NodeKey(0, self._aid(), i)
        if key in seen:
            UInt64(0).__hash__(hasher)
            return
        seen.add(key)
        UInt64(0).__hash__(hasher)
        ref _o0 = self._a[]._arr_document_meta[i].region
        if _o0:
            _o0.value().__hash__(hasher)
        ref _o1 = self._a[]._arr_document_meta[i].version
        if _o1:
            _o1.value().__hash__(hasher)
    def __eq__(self, other: Self) -> Bool:
        return self.equals(other)
    def __ne__(self, other: Self) -> Bool:
        return not self.equals(other)
    def equals[o2: Origin[mut=False]](self, other: DocumentMeta[o2]) -> Bool:
        var seen = Set[EqPair]()
        return self._eq_cyc(other, seen)
    def _eq_cyc[o2: Origin[mut=False]](self, other: DocumentMeta[o2], mut seen: Set[EqPair]) -> Bool:
        var si = self._i()
        var oi = other._i()
        var pair = EqPair(0, self._aid(), si, 0, other._aid(), oi)
        if pair in seen:
            return True
        seen.add(pair)
        if self._a[]._arr_document_meta[si].region != other._a[]._arr_document_meta[oi].region:
            return False
        if self._a[]._arr_document_meta[si].version != other._a[]._arr_document_meta[oi].version:
            return False
        return True

@fieldwise_init
struct DocumentItem[o: Origin[mut=False]](Copyable, Movable, ImplicitlyCopyable, Writable, Hashable, Equatable):
    var _a: Pointer[DocumentFrozenPackedGraphArena, origin=Self.o]
    var _packed: UInt64
    def _i(self) -> Int:
        return _uidx(self._packed)
    def _index(self) -> Int:
        return _uidx(self._packed)
    def generation(self) -> UInt32:
        return _ugen(self._packed)
    def _aid(self) -> Int:
        return Int(Pointer(to=self._a[]))
    def _mut(self) -> Pointer[DocumentFrozenPackedGraphArena, MutAnyOrigin]:
        return Pointer[DocumentFrozenPackedGraphArena, MutAnyOrigin](unsafe_from_address=Int(Pointer(to=self._a[])))
    def sku(self) -> Optional[String]:
        return self._a[]._arr_document_item[self._i()].sku.copy()
    def sku_slice(self) -> Optional[StringSlice[origin_of(self._a[]._arr_document_item[self._i()].sku.value())]]:
        if self._a[]._arr_document_item[self._i()].sku:
            return Optional(StringSlice(self._a[]._arr_document_item[self._i()].sku.value()))
        return None
    def set_sku(self, var v: String):
        self._mut()[]._arr_document_item[self._i()].sku = Optional(v^)
    def qty(self) -> Optional[Int32]:
        return self._a[]._arr_document_item[self._i()].qty
    def set_qty(self, var v: Int32):
        self._mut()[]._arr_document_item[self._i()].qty = Optional(v)
    def price_minor(self) -> Optional[Int64]:
        return self._a[]._arr_document_item[self._i()].price_minor
    def set_price_minor(self, var v: Int64):
        self._mut()[]._arr_document_item[self._i()].price_minor = Optional(v)
    def write_to[W: Writer](self, mut writer: W):
        var seen = Set[NodeKey]()
        self._repr_cyc(writer, seen)
    def _repr_cyc[W: Writer](self, mut writer: W, mut seen: Set[NodeKey]):
        var i = self._i()
        var key = NodeKey(1, self._aid(), i)
        if key in seen:
            writer.write("DocumentItem@", i)
            return
        seen.add(key)
        writer.write("DocumentItem@", i, "{")
        ref _o0 = self._a[]._arr_document_item[i].sku
        if _o0:
            writer.write("sku=", _o0.value())
        else:
            writer.write("sku=None")
        writer.write(", ")
        ref _o1 = self._a[]._arr_document_item[i].qty
        if _o1:
            writer.write("qty=", String(_o1.value()))
        else:
            writer.write("qty=None")
        writer.write(", ")
        ref _o2 = self._a[]._arr_document_item[i].price_minor
        if _o2:
            writer.write("price_minor=", String(_o2.value()))
        else:
            writer.write("price_minor=None")
        writer.write("}")
    def __hash__[H: Hasher](self, mut hasher: H):
        var seen = Set[NodeKey]()
        self._hash_cyc(hasher, seen)
    def _hash_cyc[H: Hasher](self, mut hasher: H, mut seen: Set[NodeKey]):
        var i = self._i()
        var key = NodeKey(1, self._aid(), i)
        if key in seen:
            UInt64(0).__hash__(hasher)
            return
        seen.add(key)
        UInt64(1).__hash__(hasher)
        ref _o0 = self._a[]._arr_document_item[i].sku
        if _o0:
            _o0.value().__hash__(hasher)
        ref _o1 = self._a[]._arr_document_item[i].qty
        if _o1:
            _o1.value().__hash__(hasher)
        ref _o2 = self._a[]._arr_document_item[i].price_minor
        if _o2:
            _o2.value().__hash__(hasher)
    def __eq__(self, other: Self) -> Bool:
        return self.equals(other)
    def __ne__(self, other: Self) -> Bool:
        return not self.equals(other)
    def equals[o2: Origin[mut=False]](self, other: DocumentItem[o2]) -> Bool:
        var seen = Set[EqPair]()
        return self._eq_cyc(other, seen)
    def _eq_cyc[o2: Origin[mut=False]](self, other: DocumentItem[o2], mut seen: Set[EqPair]) -> Bool:
        var si = self._i()
        var oi = other._i()
        var pair = EqPair(1, self._aid(), si, 1, other._aid(), oi)
        if pair in seen:
            return True
        seen.add(pair)
        if self._a[]._arr_document_item[si].sku != other._a[]._arr_document_item[oi].sku:
            return False
        if self._a[]._arr_document_item[si].qty != other._a[]._arr_document_item[oi].qty:
            return False
        if self._a[]._arr_document_item[si].price_minor != other._a[]._arr_document_item[oi].price_minor:
            return False
        return True

@fieldwise_init
struct Document[o: Origin[mut=False]](Copyable, Movable, ImplicitlyCopyable, Writable, Hashable, Equatable):
    var _a: Pointer[DocumentFrozenPackedGraphArena, origin=Self.o]
    var _packed: UInt64
    def _i(self) -> Int:
        return _uidx(self._packed)
    def _index(self) -> Int:
        return _uidx(self._packed)
    def generation(self) -> UInt32:
        return _ugen(self._packed)
    def _aid(self) -> Int:
        return Int(Pointer(to=self._a[]))
    def _mut(self) -> Pointer[DocumentFrozenPackedGraphArena, MutAnyOrigin]:
        return Pointer[DocumentFrozenPackedGraphArena, MutAnyOrigin](unsafe_from_address=Int(Pointer(to=self._a[])))
    def id(self) -> Optional[String]:
        return self._a[]._arr_document[self._i()].id.copy()
    def id_slice(self) -> Optional[StringSlice[origin_of(self._a[]._arr_document[self._i()].id.value())]]:
        if self._a[]._arr_document[self._i()].id:
            return Optional(StringSlice(self._a[]._arr_document[self._i()].id.value()))
        return None
    def set_id(self, var v: String):
        self._mut()[]._arr_document[self._i()].id = Optional(v^)
    def status(self) -> Optional[Int32]:
        return self._a[]._arr_document[self._i()].status
    def set_status(self, var v: Int32):
        self._mut()[]._arr_document[self._i()].status = Optional(v)
    def meta(self) -> Optional[DocumentMeta[Self.o]]:
        var v = self._a[]._arr_document[self._i()].meta
        if not v:
            return None
        var p = v.value()
        return DocumentMeta[Self.o](self._a, p)
    def set_meta(self, v: DocumentMeta[Self.o]):
        self._mut()[]._arr_document[self._i()].meta = Optional(v._packed)
    def items(self) -> DocumentItemListView[Self.o, origin_of(self._a[]._arr_document[self._i()].items)]:
        return DocumentItemListView[Self.o, origin_of(self._a[]._arr_document[self._i()].items)](self._a, Pointer(to=self._a[]._arr_document[self._i()].items))
    def set_items(self, v: List[DocumentItem[Self.o]]):
        var packed = List[UInt64]()
        for k in range(len(v)):
            packed.append(v[k]._packed)
        self._mut()[]._arr_document[self._i()].items = packed^
    def write_to[W: Writer](self, mut writer: W):
        var seen = Set[NodeKey]()
        self._repr_cyc(writer, seen)
    def _repr_cyc[W: Writer](self, mut writer: W, mut seen: Set[NodeKey]):
        var i = self._i()
        var key = NodeKey(2, self._aid(), i)
        if key in seen:
            writer.write("Document@", i)
            return
        seen.add(key)
        writer.write("Document@", i, "{")
        ref _o0 = self._a[]._arr_document[i].id
        if _o0:
            writer.write("id=", _o0.value())
        else:
            writer.write("id=None")
        writer.write(", ")
        ref _o1 = self._a[]._arr_document[i].status
        if _o1:
            writer.write("status=", String(_o1.value()))
        else:
            writer.write("status=None")
        writer.write(", ")
        var _r2 = self.meta()
        if _r2:
            writer.write("meta=")
            _r2.value()._repr_cyc(writer, seen)
        else:
            writer.write("meta=None")
        writer.write(", ")
        writer.write("items=[", len(self._a[]._arr_document[i].items), " items]")
        writer.write("}")
    def __hash__[H: Hasher](self, mut hasher: H):
        var seen = Set[NodeKey]()
        self._hash_cyc(hasher, seen)
    def _hash_cyc[H: Hasher](self, mut hasher: H, mut seen: Set[NodeKey]):
        var i = self._i()
        var key = NodeKey(2, self._aid(), i)
        if key in seen:
            UInt64(0).__hash__(hasher)
            return
        seen.add(key)
        UInt64(2).__hash__(hasher)
        ref _o0 = self._a[]._arr_document[i].id
        if _o0:
            _o0.value().__hash__(hasher)
        ref _o1 = self._a[]._arr_document[i].status
        if _o1:
            _o1.value().__hash__(hasher)
        var _r2 = self.meta()
        if _r2:
            _r2.value()._hash_cyc(hasher, seen)
        for _e in self._a[]._arr_document[i].items:
            var _p = _e
            DocumentItem[Self.o](self._a, _p)._hash_cyc(hasher, seen)
    def __eq__(self, other: Self) -> Bool:
        return self.equals(other)
    def __ne__(self, other: Self) -> Bool:
        return not self.equals(other)
    def equals[o2: Origin[mut=False]](self, other: Document[o2]) -> Bool:
        var seen = Set[EqPair]()
        return self._eq_cyc(other, seen)
    def _eq_cyc[o2: Origin[mut=False]](self, other: Document[o2], mut seen: Set[EqPair]) -> Bool:
        var si = self._i()
        var oi = other._i()
        var pair = EqPair(2, self._aid(), si, 2, other._aid(), oi)
        if pair in seen:
            return True
        seen.add(pair)
        if self._a[]._arr_document[si].id != other._a[]._arr_document[oi].id:
            return False
        if self._a[]._arr_document[si].status != other._a[]._arr_document[oi].status:
            return False
        var _sa2 = self.meta()
        var _ob2 = other.meta()
        if Bool(_sa2) != Bool(_ob2):
            return False
        if _sa2 and _ob2:
            if not _sa2.value()._eq_cyc(_ob2.value(), seen):
                return False
        ref _sl3 = self._a[]._arr_document[si].items
        ref _ol3 = other._a[]._arr_document[oi].items
        if len(_sl3) != len(_ol3):
            return False
        for _k in range(len(_sl3)):
            var _sp = _sl3[_k]
            var _op = _ol3[_k]
            if not DocumentItem[Self.o](self._a, _sp)._eq_cyc(DocumentItem[o2](other._a, _op), seen):
                return False
        return True

@fieldwise_init
struct DocumentItemListIter[o: Origin[mut=False], lo: Origin[mut=False]](Copyable, Movable):
    var _a: Pointer[DocumentFrozenPackedGraphArena, origin=Self.o]
    var _ids: Pointer[List[UInt64], origin=Self.lo]
    var _i: Int
    def __next__(mut self) raises StopIteration -> DocumentItem[Self.o]:
        if self._i >= len(self._ids[]):
            raise StopIteration()
        var _h = DocumentItem[Self.o](self._a, self._ids[][self._i])
        self._i += 1
        return _h

@fieldwise_init
struct DocumentItemListView[o: Origin[mut=False], lo: Origin[mut=False]](Copyable, Movable, Sized):
    var _a: Pointer[DocumentFrozenPackedGraphArena, origin=Self.o]
    var _ids: Pointer[List[UInt64], origin=Self.lo]
    def __len__(self) -> Int:
        return len(self._ids[])
    def __getitem__(self, i: Int) -> DocumentItem[Self.o]:
        return DocumentItem[Self.o](self._a, self._ids[][i])
    def __iter__(self) -> DocumentItemListIter[Self.o, Self.lo]:
        return DocumentItemListIter[Self.o, Self.lo](self._a, self._ids, 0)

struct DocumentFrozenPackedGraphArena(Movable):
    var _arr_document_meta: List[DocumentMetaValues]
    var _arr_document_item: List[DocumentItemValues]
    var _arr_document: List[DocumentValues]
    var _root: Optional[UInt64]
    def __init__(out self):
        self._arr_document_meta = List[DocumentMetaValues]()
        self._arr_document_item = List[DocumentItemValues]()
        self._arr_document = List[DocumentValues]()
        self._root = None
    def new_document_meta[o: Origin[mut=False], //](ref [o] self, var region: String = String(""), var version: Int32 = Int32(0)) -> DocumentMeta[o]:
        var up = Pointer[DocumentFrozenPackedGraphArena, MutAnyOrigin](unsafe_from_address=Int(Pointer(to=self)))
        var idx = len(self._arr_document_meta)
        up[]._arr_document_meta.append(DocumentMetaValues(region=Optional(region^), version=Optional(version)))
        return DocumentMeta[o](Pointer(to=self), _pack(0, idx))
    def new_document_item[o: Origin[mut=False], //](ref [o] self, var sku: String = String(""), var qty: Int32 = Int32(0), var price_minor: Int64 = Int64(0)) -> DocumentItem[o]:
        var up = Pointer[DocumentFrozenPackedGraphArena, MutAnyOrigin](unsafe_from_address=Int(Pointer(to=self)))
        var idx = len(self._arr_document_item)
        up[]._arr_document_item.append(DocumentItemValues(sku=Optional(sku^), qty=Optional(qty), price_minor=Optional(price_minor)))
        return DocumentItem[o](Pointer(to=self), _pack(0, idx))
    def new_document[o: Origin[mut=False], //](ref [o] self, var id: String = String(""), var status: Int32 = Int32(0)) -> Document[o]:
        var up = Pointer[DocumentFrozenPackedGraphArena, MutAnyOrigin](unsafe_from_address=Int(Pointer(to=self)))
        var idx = len(self._arr_document)
        up[]._arr_document.append(DocumentValues(id=Optional(id^), status=Optional(status), meta=None, items=List[UInt64]()))
        return Document[o](Pointer(to=self), _pack(0, idx))
    def root[o: Origin[mut=False], //](ref [o] self) -> Optional[Document[o]]:
        if not self._root:
            return None
        var p = self._root.value()
        return Document[o](Pointer(to=self), p)
    def set_root[o: Origin[mut=False], //](ref [o] self, h: Document[o]):
        var up = Pointer[DocumentFrozenPackedGraphArena, MutAnyOrigin](unsafe_from_address=Int(Pointer(to=self)))
        up[]._root = Optional(h._packed)
