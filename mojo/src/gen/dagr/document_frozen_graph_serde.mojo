# GENERATED by dagr/codegen/mojo/serde.py — do not edit by hand.
# Serializer (arena -> bytes) for graph 'DocumentFrozenGraph' — plan 28 Phase 4.

from dagr_writer import Builder, NodeStoreRef, node_offset, resolved_ref, nil_ref, to_zigzag, leb_length
from document_frozen_graph_arena import DocumentFrozenGraphArena, DocumentMeta, DocumentItem, Document

comptime _VT_MAX = 1   # widest regular-node vtable (§1.19)


# Build a List[Int] of vtable entries from a varargs-free call site.
def _ilist(*vals: Int) -> List[Int]:
    var l = List[Int]()
    for v in vals:
        l.append(v)
    return l^


# Type-namespaced node identity for dedup / cycle keys: a bare packed handle
# collides across node types (index space is per-type), so fold in the type id.
def _nid(tid: Int, idx: Int) -> UInt64:
    return (UInt64(idx) << 8) | UInt64(tid)


def _store_document_meta[o: Origin[mut=False], //](mut b: Builder[_VT_MAX], a: DocumentFrozenGraphArena, h: DocumentMeta[o]) raises -> NodeStoreRef:
    var _id = _nid(0, h._index())
    var _r = b.begin_storing_acyclic(_id)
    if _r.is_pending or _r.off != -1:
        return _r
    var _pr0 = Bool(a._arr_document_meta[h._index()].region)
    var _pr1 = Bool(h.version())
    var _c0 = -1
    if _pr0:
        _c0 = b.store_utf8(a._arr_document_meta[h._index()].region.value(), True)
    if _pr1:
        _ = b.store_i32(h.version().value())
    if _pr0:
        _ = b.store_forward_pointer(_c0)
    var _bs: UInt64 = 0
    if _pr0:
        _bs |= UInt64(1) << 0
    if _pr1:
        _bs |= UInt64(1) << 1
    _ = b.store_u8(UInt8(_bs & 0xff))
    var _o = b.cursor
    b.finish_storing_acyclic(_id, _o)
    return resolved_ref(_o)


def _store_document_item[o: Origin[mut=False], //](mut b: Builder[_VT_MAX], a: DocumentFrozenGraphArena, h: DocumentItem[o]) raises -> NodeStoreRef:
    var _id = _nid(1, h._index())
    var _r = b.begin_storing_acyclic(_id)
    if _r.is_pending or _r.off != -1:
        return _r
    var _pr0 = Bool(a._arr_document_item[h._index()].sku)
    var _pr1 = Bool(h.qty())
    var _pr2 = Bool(h.price_minor())
    var _c0 = -1
    if _pr0:
        _c0 = b.store_utf8(a._arr_document_item[h._index()].sku.value(), True)
    if _pr2:
        _ = b.store_i64(h.price_minor().value())
    if _pr1:
        _ = b.store_i32(h.qty().value())
    if _pr0:
        _ = b.store_forward_pointer(_c0)
    var _bs: UInt64 = 0
    if _pr0:
        _bs |= UInt64(1) << 0
    if _pr1:
        _bs |= UInt64(1) << 1
    if _pr2:
        _bs |= UInt64(1) << 2
    _ = b.store_u8(UInt8(_bs & 0xff))
    var _o = b.cursor
    b.finish_storing_acyclic(_id, _o)
    return resolved_ref(_o)


def _store_document[o: Origin[mut=False], //](mut b: Builder[_VT_MAX], a: DocumentFrozenGraphArena, h: Document[o]) raises -> NodeStoreRef:
    var _id = _nid(2, h._index())
    var _r = b.begin_storing_acyclic(_id)
    if _r.is_pending or _r.off != -1:
        return _r
    var _pr0 = Bool(a._arr_document[h._index()].id)
    var _pr1 = Bool(h.status())
    var _pr2 = Bool(h.meta())
    var _pr3 = True
    var _c3 = -1
    if _pr3:
        var _bi3 = h._index()
        var _refs3 = b.take_refs()
        for _k3 in range(len(a._arr_document[_bi3].items) - 1, -1, -1):
            _refs3.append(_store_document_item(b, a, DocumentItem[o](h._a, a._arr_document[_bi3].items[_k3])))
        _c3 = b.store_node_ref_array(_refs3)
        b.return_refs(_refs3^)
    var _c2 = nil_ref()
    if _pr2:
        _c2 = _store_document_meta(b, a, h.meta().value())
    var _c0 = -1
    if _pr0:
        _c0 = b.store_utf8(a._arr_document[h._index()].id.value(), True)
    if _pr3:
        _ = b.store_forward_pointer(_c3)
    if _pr2:
        _ = b.store_bidirectional_pointer(_c2)
    if _pr1:
        _ = b.store_i32(h.status().value())
    if _pr0:
        _ = b.store_forward_pointer(_c0)
    var _bs: UInt64 = 0
    if _pr0:
        _bs |= UInt64(1) << 0
    if _pr1:
        _bs |= UInt64(1) << 1
    if _pr2:
        _bs |= UInt64(1) << 2
    if _pr3:
        _bs |= UInt64(1) << 3
    _ = b.store_u8(UInt8(_bs & 0xff))
    var _o = b.cursor
    b.finish_storing_acyclic(_id, _o)
    return resolved_ref(_o)


def write_document_graph(mut b: Builder[_VT_MAX], a: DocumentFrozenGraphArena) raises -> Int:
    var _off = node_offset(_store_document(b, a, a.root().value()))
    return b.store_leb(UInt64((b.cursor - _off) << 2))    # framing (no header)

def serialize_document_graph(a: DocumentFrozenGraphArena, max_size: Int = 2 * 1024 * 1024) raises -> List[UInt8]:
    var b = Builder[_VT_MAX](hint=len(a._arr_document_meta) + len(a._arr_document_item) + len(a._arr_document), max_size=max_size)
    _ = write_document_graph(b, a)
    return b.make_data()
