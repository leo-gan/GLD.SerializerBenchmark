# GENERATED by dagr/codegen/mojo/core.py — do not edit by hand.
# Lazy reader for graph 'DocumentFrozenGraph' (regular-node scalars/utf8/data/enum/node-ref/arrays).

from dagr_reader import read_bitset, read_i32, read_i64, read_leb, read_rel_offset_s, read_utf8, read_v62, read_zigzag_v62, root_offset


@fieldwise_init
struct _Document_items_RefArray[o: ImmOrigin](Copyable, Movable, ImplicitlyCopyable, Sized):
    var buf: Span[UInt8, Self.o]
    var table_base: Int
    var base: Int
    var count: Int
    var es: Int

    def __len__(self) -> Int:
        return self.count

    def get(self, i: Int) raises -> DocumentItemAccessor[Self.o]:
        if i < 0 or i >= self.count:
            raise Error("array index out of range")
        var ro = read_rel_offset_s(self.buf, self.table_base + i * self.es, self.es)
        if ro == 0:
            raise Error("nil slot in non-optional node array")
        return DocumentItemAccessor[Self.o](self.buf, self.base + ro - 1)

    def iter(self) -> _Document_items_RefArrayIter[Self.o]:
        return _Document_items_RefArrayIter[Self.o](self, 0)


@fieldwise_init
struct _Document_items_RefArrayIter[o: ImmOrigin](Copyable, Movable, ImplicitlyCopyable):
    var arr: _Document_items_RefArray[Self.o]
    var i: Int

    def next(mut self) raises -> DocumentItemAccessor[Self.o]:
        var _r = self.arr.get(self.i)
        self.i += 1
        return _r

struct DocumentMetaAccessor[o: ImmOrigin](Copyable, Movable, ImplicitlyCopyable):
    var buf: Span[UInt8, Self.o]
    var pos: Int     # node start position (dedup key for eager restore)
    var _p0: Int   # position of field 0 (region); -1 = absent
    var _p1: Int   # position of field 1 (version); -1 = absent

    def __init__(out self, buf: Span[UInt8, Self.o], start: Int) raises:
        self.buf = buf
        self.pos = start
        var bs = read_bitset(buf, start, 1)
        var cur = start + 1
        if (bs >> 0) & 1:
            self._p0 = cur
            cur += read_v62(self.buf, cur)[1]
        else:
            self._p0 = -1
        if (bs >> 1) & 1:
            self._p1 = cur
            cur += 4
        else:
            self._p1 = -1

    def region(self) raises -> Optional[String]:
        if self._p0 < 0:
            return None
        var pos = self._p0
        var p = read_v62(self.buf, pos)
        return String(read_utf8(self.buf, pos + p[1] + Int(p[0]))[0])

    def version(self) raises -> Optional[Int32]:
        if self._p1 < 0:
            return None
        var pos = self._p1
        return read_i32(self.buf, pos)


struct DocumentItemAccessor[o: ImmOrigin](Copyable, Movable, ImplicitlyCopyable):
    var buf: Span[UInt8, Self.o]
    var pos: Int     # node start position (dedup key for eager restore)
    var _p0: Int   # position of field 0 (sku); -1 = absent
    var _p1: Int   # position of field 1 (qty); -1 = absent
    var _p2: Int   # position of field 2 (price_minor); -1 = absent

    def __init__(out self, buf: Span[UInt8, Self.o], start: Int) raises:
        self.buf = buf
        self.pos = start
        var bs = read_bitset(buf, start, 1)
        var cur = start + 1
        if (bs >> 0) & 1:
            self._p0 = cur
            cur += read_v62(self.buf, cur)[1]
        else:
            self._p0 = -1
        if (bs >> 1) & 1:
            self._p1 = cur
            cur += 4
        else:
            self._p1 = -1
        if (bs >> 2) & 1:
            self._p2 = cur
            cur += 8
        else:
            self._p2 = -1

    def sku(self) raises -> Optional[String]:
        if self._p0 < 0:
            return None
        var pos = self._p0
        var p = read_v62(self.buf, pos)
        return String(read_utf8(self.buf, pos + p[1] + Int(p[0]))[0])

    def qty(self) raises -> Optional[Int32]:
        if self._p1 < 0:
            return None
        var pos = self._p1
        return read_i32(self.buf, pos)

    def price_minor(self) raises -> Optional[Int64]:
        if self._p2 < 0:
            return None
        var pos = self._p2
        return read_i64(self.buf, pos)


struct DocumentAccessor[o: ImmOrigin](Copyable, Movable, ImplicitlyCopyable):
    var buf: Span[UInt8, Self.o]
    var pos: Int     # node start position (dedup key for eager restore)
    var _p0: Int   # position of field 0 (id); -1 = absent
    var _p1: Int   # position of field 1 (status); -1 = absent
    var _p2: Int   # position of field 2 (meta); -1 = absent
    var _p3: Int   # position of field 3 (items); -1 = absent

    def __init__(out self, buf: Span[UInt8, Self.o], start: Int) raises:
        self.buf = buf
        self.pos = start
        var bs = read_bitset(buf, start, 1)
        var cur = start + 1
        if (bs >> 0) & 1:
            self._p0 = cur
            cur += read_v62(self.buf, cur)[1]
        else:
            self._p0 = -1
        if (bs >> 1) & 1:
            self._p1 = cur
            cur += 4
        else:
            self._p1 = -1
        if (bs >> 2) & 1:
            self._p2 = cur
            cur += read_zigzag_v62(self.buf, cur)[1]
        else:
            self._p2 = -1
        if (bs >> 3) & 1:
            self._p3 = cur
            cur += read_v62(self.buf, cur)[1]
        else:
            self._p3 = -1

    def id(self) raises -> Optional[String]:
        if self._p0 < 0:
            return None
        var pos = self._p0
        var p = read_v62(self.buf, pos)
        return String(read_utf8(self.buf, pos + p[1] + Int(p[0]))[0])

    def status(self) raises -> Optional[Int32]:
        if self._p1 < 0:
            return None
        var pos = self._p1
        return read_i32(self.buf, pos)

    def meta(self) raises -> Optional[DocumentMetaAccessor[Self.o]]:
        if self._p2 < 0:
            return None
        var pos = self._p2
        var p = read_zigzag_v62(self.buf, pos)
        return DocumentMetaAccessor[Self.o](self.buf, pos + p[1] + Int(p[0]))

    def items(self) raises -> Optional[_Document_items_RefArray[Self.o]]:
        if self._p3 < 0:
            return None
        var pos = self._p3
        var p = read_v62(self.buf, pos)
        var payload = pos + p[1] + Int(p[0])
        var h = read_leb(self.buf, payload)
        var count = Int(h[0]) >> 2
        var es = 1 << (Int(h[0]) & 3)
        var table_base = payload + h[1]
        return _Document_items_RefArray[Self.o](self.buf, table_base, table_base + count * es, count, es)

def read_document_root[o: ImmOrigin, //](buf: Span[UInt8, o]) raises -> DocumentAccessor[o]:
    return DocumentAccessor[o](buf, root_offset(buf))
