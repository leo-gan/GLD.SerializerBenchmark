# GENERATED by dagr/codegen/mojo/serde.py — do not edit by hand.
# Serializer (arena -> bytes) for graph 'TelemetryFrozenPackedGraph' — plan 28 Phase 4.

from dagr_writer import Builder, NodeStoreRef, node_offset, resolved_ref, nil_ref, to_zigzag, leb_length
from telemetry_frozen_packed_graph_arena import TelemetryFrozenPackedGraphArena, Telemetry

comptime _VT_MAX = 1   # widest regular-node vtable (§1.19)


# Build a List[Int] of vtable entries from a varargs-free call site.
def _ilist(*vals: Int) -> List[Int]:
    var l = List[Int]()
    for v in vals:
        l.append(v)
    return l^


# Type-namespaced node identity for dedup / cycle keys: a bare packed handle
# collides across node types (index space is per-type), so fold in the type id.
def _nid(tid: Int, idx: Int) -> UInt64:
    return (UInt64(idx) << 8) | UInt64(tid)


def _store_telemetry[o: Origin[mut=False], //](mut b: Builder[_VT_MAX], a: TelemetryFrozenPackedGraphArena, h: Telemetry[o]) raises -> NodeStoreRef:
    var _id = _nid(0, h._index())
    if _id in b.struct_lookup:
        return resolved_ref(b.struct_lookup[_id])
    var _before = b.cursor
    var _rawbits: UInt64 = 0
    var _pr0 = Bool(a._arr_telemetry[h._index()].source)
    var _pr1 = Bool(h.ts())
    var _pr2 = True
    var _pr3 = True
    ref _pa3 = a._arr_telemetry[h._index()].values
    _ = b.store_packed_float64_array(_pa3, True)
    ref _pa2 = a._arr_telemetry[h._index()].tags
    var _pcb2 = b.cursor
    for _k2 in range(len(_pa2) - 1, -1, -1):
        _ = b.store_utf8(_pa2[_k2], False)
    _ = b.store_leb(UInt64(len(_pa2)))
    _ = b.store_leb(UInt64(b.cursor - _pcb2))
    if _pr1:
        if leb_length(to_zigzag(Int(h.ts().value()))) < 8:
            _ = b.store_leb(to_zigzag(Int(h.ts().value())))
        else:
            _ = b.store_i64(h.ts().value())
            _rawbits |= UInt64(1) << 0
    if _pr0:
        _ = b.store_utf8(a._arr_telemetry[h._index()].source.value(), False)
    _ = b.store_u8(UInt8(_rawbits & 0xff))
    var _pb: UInt64 = 0
    if _pr0:
        _pb |= UInt64(1) << 0
    if _pr1:
        _pb |= UInt64(1) << 1
    if _pr2:
        _pb |= UInt64(1) << 2
    if _pr3:
        _pb |= UInt64(1) << 3
    _ = b.store_u8(UInt8(_pb & 0xff))
    _ = b.store_leb(UInt64(b.cursor - _before))
    var _o = b.cursor
    b.struct_lookup[_id] = _o
    return resolved_ref(_o)


def write_telemetry_graph(mut b: Builder[_VT_MAX], a: TelemetryFrozenPackedGraphArena) raises -> Int:
    var _off = node_offset(_store_telemetry(b, a, a.root().value()))
    return b.store_leb(UInt64((b.cursor - _off) << 2))    # framing (no header)

def serialize_telemetry_graph(a: TelemetryFrozenPackedGraphArena, max_size: Int = 2 * 1024 * 1024) raises -> List[UInt8]:
    var b = Builder[_VT_MAX](hint=len(a._arr_telemetry), max_size=max_size)
    _ = write_telemetry_graph(b, a)
    return b.make_data()
