# GENERATED by dagr/codegen/mojo/serde.py — do not edit by hand.
# Serializer (arena -> bytes) for graph 'DocumentGraph' — plan 28 Phase 4.

from dagr_writer import Builder, NodeStoreRef, node_offset, resolved_ref, nil_ref, to_zigzag, leb_length
from document_graph_arena import DocumentGraphArena, DocumentMeta, DocumentItem, Document, _uidx, _ugen

comptime _VT_MAX = 1   # widest regular-node vtable (§1.19)


# Build a List[Int] of vtable entries from a varargs-free call site.
def _ilist(*vals: Int) -> List[Int]:
    var l = List[Int]()
    for v in vals:
        l.append(v)
    return l^


# Type-namespaced node identity for dedup / cycle keys: a bare packed handle
# collides across node types (index space is per-type), so fold in the type id.
def _nid(tid: Int, idx: Int) -> UInt64:
    return (UInt64(idx) << 8) | UInt64(tid)


def _store_document_meta[o: Origin[mut=False], //](mut b: Builder[_VT_MAX], a: DocumentGraphArena, h: DocumentMeta[o]) raises -> NodeStoreRef:
    var _before = b.cursor
    var _v1 = h.version()
    if _v1:
        if leb_length(to_zigzag(Int(_v1.value()))) < 4:
            _ = b.store_leb(to_zigzag(Int(_v1.value())))
            _ = b.store_leb(UInt64(2))
        else:
            _ = b.store_i32(_v1.value())
            _ = b.store_leb(UInt64(3))
    if Bool(a._arr_document_meta[h._index()].region):
        _ = b.store_utf8(a._arr_document_meta[h._index()].region.value(), False)
        _ = b.store_leb(UInt64(1))
    _ = b.store_leb(UInt64(b.cursor - _before))
    return resolved_ref(b.cursor)


def _store_document_item[o: Origin[mut=False], //](mut b: Builder[_VT_MAX], a: DocumentGraphArena, h: DocumentItem[o]) raises -> NodeStoreRef:
    var _before = b.cursor
    var _v2 = h.price_minor()
    if _v2:
        if leb_length(to_zigzag(Int(_v2.value()))) < 8:
            _ = b.store_leb(to_zigzag(Int(_v2.value())))
            _ = b.store_leb(UInt64(4))
        else:
            _ = b.store_i64(_v2.value())
            _ = b.store_leb(UInt64(5))
    var _v1 = h.qty()
    if _v1:
        if leb_length(to_zigzag(Int(_v1.value()))) < 4:
            _ = b.store_leb(to_zigzag(Int(_v1.value())))
            _ = b.store_leb(UInt64(2))
        else:
            _ = b.store_i32(_v1.value())
            _ = b.store_leb(UInt64(3))
    if Bool(a._arr_document_item[h._index()].sku):
        _ = b.store_utf8(a._arr_document_item[h._index()].sku.value(), False)
        _ = b.store_leb(UInt64(1))
    _ = b.store_leb(UInt64(b.cursor - _before))
    return resolved_ref(b.cursor)


def _store_document[o: Origin[mut=False], //](mut b: Builder[_VT_MAX], a: DocumentGraphArena, h: Document[o]) raises -> NodeStoreRef:
    var _before = b.cursor
    ref _row3 = a._arr_document[h._index()].items
    var _pcb3 = b.cursor
    var _n3 = 0
    for _k3 in range(len(_row3) - 1, -1, -1):
        var _p3 = _row3[_k3]
        if a._gen_document_item[_uidx(_p3)] != _ugen(_p3):
            continue
        _ = _store_document_item(b, a, DocumentItem[o](h._a, _p3))
        _n3 += 1
    _ = b.store_leb(UInt64(_n3))
    _ = b.store_leb(UInt64(b.cursor - _pcb3))
    _ = b.store_leb(UInt64(7))
    var _v2 = h.meta()
    if _v2:
        _ = _store_document_meta(b, a, _v2.value())
        _ = b.store_leb(UInt64(5))
    var _v1 = h.status()
    if _v1:
        if leb_length(to_zigzag(Int(_v1.value()))) < 4:
            _ = b.store_leb(to_zigzag(Int(_v1.value())))
            _ = b.store_leb(UInt64(2))
        else:
            _ = b.store_i32(_v1.value())
            _ = b.store_leb(UInt64(3))
    if Bool(a._arr_document[h._index()].id):
        _ = b.store_utf8(a._arr_document[h._index()].id.value(), False)
        _ = b.store_leb(UInt64(1))
    _ = b.store_leb(UInt64(b.cursor - _before))
    return resolved_ref(b.cursor)


def write_document_graph(mut b: Builder[_VT_MAX], a: DocumentGraphArena) raises -> Int:
    var _off = node_offset(_store_document(b, a, a.root().value()))
    return b.store_leb(UInt64((b.cursor - _off) << 2))    # framing (no header)

def serialize_document_graph(a: DocumentGraphArena, max_size: Int = 2 * 1024 * 1024) raises -> List[UInt8]:
    var b = Builder[_VT_MAX](hint=len(a._arr_document_meta) + len(a._arr_document_item) + len(a._arr_document), max_size=max_size)
    _ = write_document_graph(b, a)
    return b.make_data()
