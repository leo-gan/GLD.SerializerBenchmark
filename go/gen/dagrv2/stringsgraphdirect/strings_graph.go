// Code generated by dagr (dagr/codegen/go/direct.py); DO NOT EDIT.

package stringsgraphdirect

import (
	"iter"

	"dagrv2/dagr"
)

type StringsAccessor struct {
	buf []byte
	pos int
	p   [1]int
	raw uint64
}

func NewStringsAccessor(buf []byte, pos int) StringsAccessor {
	a := StringsAccessor{buf: buf, pos: pos}
	for i := range a.p {
		a.p[i] = -1
	}
	es, ee := dagr.PackedBounds(buf, pos)
	cursor := es
	last := -1
scan:
	for cursor < ee {
		tagv, tb := dagr.ReadLEB(buf, cursor)
		fidx := int(tagv >> 1)
		// spec 07: tags are strictly ascending; anything else is the unknown tail
		// (or a spec-15 header's zero padding) and ends the scan.
		if fidx <= last {
			break scan
		}
		last = fidx
		raw := (tagv & 1) == 1
		_ = raw
		pl := cursor + tb
		switch fidx {
		case 0:
			a.p[0] = pl
			if raw {
				a.raw |= 1 << 0
			}
			cursor = dagr.SkipBlob(buf, pl)
		default:
			break scan
		}
	}
	return a
}

// BufferPos is where this node starts in the buffer — its identity in a graph.
func (a StringsAccessor) BufferPos() int { return a.pos }

// BufferBytes is the buffer this accessor reads — with BufferPos, enough to reopen
// the node through another package's accessor (a DataSink does, spec/11 §12).
func (a StringsAccessor) BufferBytes() []byte { return a.buf }

func (a StringsAccessor) ItemsLen() (int, bool) {
	pl := a.p[0]
	if pl < 0 {
		return 0, false
	}
	_, blb := dagr.ReadLEB(a.buf, pl)
	c := pl + blb
	h, _ := dagr.ReadLEB(a.buf, c)
	return int(h), true
}

func (a StringsAccessor) ItemsAt(i int) string {
	pl := a.p[0]
	_, blb := dagr.ReadLEB(a.buf, pl)
	c := pl + blb
	_, hb := dagr.ReadLEB(a.buf, c)
	p := c + hb
	for j := 0; j < i; j++ {
		p = dagr.SkipBlob(a.buf, p)
	}
	s, _ := dagr.ReadUtf8(a.buf, p)
	return s
}

type StringsItemsIter struct {
	a    StringsAccessor
	buf  []byte
	p    int
	i    int
	n    int
	walk bool
}

// ItemsIter walks the array once, keeping its position between elements —
// O(n) for the whole array, where ItemsAt(i) is O(i) per call.
func (a StringsAccessor) ItemsIter() StringsItemsIter {
	n, lok := a.ItemsLen()
	if !lok {
		n = 0
	}
	it := StringsItemsIter{a: a, buf: a.buf, n: n}
	pl := a.p[0]
	if pl < 0 || n == 0 {
		return it
	}
	_, blb := dagr.ReadLEB(a.buf, pl)
	c := pl + blb
	_, hb := dagr.ReadLEB(a.buf, c)
	p := c + hb
	it.p = p
	it.walk = true
	return it
}

func (it *StringsItemsIter) Next() (string, bool) {
	var v string
	if it.i >= it.n {
		return v, false
	}
	if !it.walk {
		v = it.a.ItemsAt(it.i)
		it.i++
		return v, true
	}
	i := it.i
	it.i++
	_ = i
	v, _ = dagr.ReadUtf8(it.buf, it.p)
	it.p = dagr.SkipBlob(it.buf, it.p)
	return v, true
}

func (it *StringsItemsIter) Len() int { return it.n }

// ItemsAll ranges over the array in one pass.
func (a StringsAccessor) ItemsAll() iter.Seq[string] {
	return func(yield func(string) bool) {
		it := a.ItemsIter()
		for {
			v, ok := it.Next()
			if !ok || !yield(v) {
				return
			}
		}
	}
}

// Strings is a plain value record (spec 11 §4: no arena).
type Strings struct {
	Items dagr.Opt[[]string]
}

func restoreStrings(acc StringsAccessor) Strings {
	var rec Strings
	{
		if cnt, ok := acc.ItemsLen(); ok {
			s := make([]string, cnt)
			it := acc.ItemsIter()
			for i := 0; i < cnt; i++ {
				e, _ := it.Next()
				s[i] = e
			}
			rec.Items = dagr.Some(s)
		}
	}
	return rec
}

// RestoreStrings decodes one Strings (and its subtree) into plain values — the entry
// a DataSink uses for a record field that holds this graph's node (spec/11 §12).
func RestoreStrings(acc StringsAccessor) Strings { return restoreStrings(acc) }

func storeStrings(n Strings, b *dagr.Builder) dagr.NodeStoreRef {
	buf, i := b.Take()
	buf, i = PutStrings(n, b, buf, i)
	b.Put(buf, i)
	return dagr.NodeStoreRef{Off: b.Cursor()}
}

func PutStrings(n Strings, b *dagr.Builder, buf []byte, i int) ([]byte, int) {
	before := len(buf) - i
	if x0, ok := n.Items.V, n.Items.Ok; ok {
		b.Put(buf, i)
		dagr.StorePackedComplexArrayPlain(b, x0, func(b *dagr.Builder, v string) { b.StoreUtf8(v, false) })
		buf, i = b.Take()
		buf, i = dagr.PutLEBAt(buf, i, 1)
	}
	return dagr.PutLEBAt(buf, i, uint64((len(buf)-i)-before))
}

// StringsGraphBuilder is the reusable form (spec 33 §4.3): one builder, reset per Build.
type StringsGraphBuilder struct {
	b *dagr.Builder
}

func NewStringsGraphBuilder() *StringsGraphBuilder {
	return &StringsGraphBuilder{b: dagr.NewBuilder(0)}
}

// Grow makes room for a record of n bytes up front, so the first Build does not
// have to double its way up from 64. A reused builder keeps its buffer.
func (w *StringsGraphBuilder) Grow(n int) { w.b.Grow(n) }

// Build serializes the value tree under root — byte-identical to the arena's ToBytes.
func (w *StringsGraphBuilder) Build(root Strings) []byte {
	w.build(root)
	return w.b.MakeData()
}

// BuildAppend is Build without the copy: it appends the record to dst and returns
// the extended slice. A caller that keeps dst between batches reuses its capacity,
// so steady-state encoding allocates nothing — this is the Go analogue of Rust's
// write_into (spec 33 §4.3).
func (w *StringsGraphBuilder) BuildAppend(dst []byte, root Strings) []byte {
	w.build(root)
	return w.b.AppendTo(dst)
}

// BuildBytes is BuildAppend with no copy at all: the returned slice aliases the
// builder's buffer and is invalidated by the next Build on it.
func (w *StringsGraphBuilder) BuildBytes(root Strings) []byte {
	w.build(root)
	return w.b.Bytes()
}

func (w *StringsGraphBuilder) build(root Strings) {
	b := w.b
	b.Reset()
	off := dagr.NodeOffset(storeStrings(root, b))
	b.StoreLEB(uint64(b.Cursor()-off) << 2)
}

// BuildStringsGraph is the one-shot form (spec 33 §4.2).
func BuildStringsGraph(root Strings) []byte {
	return NewStringsGraphBuilder().Build(root)
}

// RestoreStringsGraphValues decodes a buffer into plain value structs (no arena);
// malformed bytes yield dagr.ErrMalformed.
func RestoreStringsGraphValues(buf []byte) (v Strings, err error) {
	pos, err := dagr.RootOffset(buf)
	if err != nil {
		return Strings{}, err
	}
	defer func() {
		if r := recover(); r != nil {
			v, err = Strings{}, dagr.ErrMalformed
		}
	}()
	return restoreStrings(NewStringsAccessor(buf, pos)), nil
}
