// Generated by dagr/codegen/typescript/arena.py — do not edit by hand.
// Graph: EventGraph
import { EventAccessor } from "./EventGraph";
import type { EventAttrAccessor } from "./EventGraph";


function _pack(gen: number, idx: number): bigint { return (BigInt(gen) << 40n) | BigInt(idx); }

function _strHash(s: string): number {
  let h = 5381;
  for (let i = 0; i < s.length; i++) h = (Math.imul(h, 33) + s.charCodeAt(i)) | 0;
  return h;
}

type _Structural = { _describe(s: Set<string>): string; _hash(s: Set<string>): number; _eq(o: unknown, s: Set<string>): boolean };
function _hasMethod<K extends string>(v: unknown, k: K): v is Record<K, (...a: never[]) => unknown> {
  return typeof v === "object" && v !== null && typeof (v as Record<string, unknown>)[k] === "function";
}
function _isTagged(v: unknown): v is { type: unknown; value: unknown } {
  return typeof v === "object" && v !== null && "type" in v && "value" in v;
}

function _dagrDescribe(v: unknown, seen: Set<string>): string {
  if (v === null || v === undefined) return "null";
  if (_hasMethod(v, "_describe")) return (v as _Structural)._describe(seen);
  if (Array.isArray(v)) return "[" + v.map((e) => _dagrDescribe(e, seen)).join(", ") + "]";
  if (v instanceof Uint8Array) return "[" + Array.from(v).join(", ") + "]";
  if (_isTagged(v)) return String(v.type) + "(" + _dagrDescribe(v.value, seen) + ")";
  if (typeof v === "string") return JSON.stringify(v);
  if (typeof v === "bigint") return v.toString();
  if (typeof v === "number" || typeof v === "boolean") return String(v);
  return "";
}

function _dagrHash(v: unknown, seen: Set<string>): number {
  if (v === null || v === undefined) return 0;
  if (_hasMethod(v, "_hash")) return (v as _Structural)._hash(seen);
  if (Array.isArray(v)) { let h = 7; for (const e of v) h = (Math.imul(h, 31) + _dagrHash(e, seen)) | 0; return h; }
  if (v instanceof Uint8Array) { let h = 7; for (const b of v) h = (Math.imul(h, 31) + b) | 0; return h; }
  if (_isTagged(v)) return (Math.imul(_strHash(String(v.type)), 31) + _dagrHash(v.value, seen)) | 0;
  if (typeof v === "string") return _strHash(v);
  if (typeof v === "bigint") return Number(BigInt.asUintN(32, v)) | 0;
  if (typeof v === "boolean") return v ? 1 : 0;
  return typeof v === "number" ? v | 0 : 0;
}

function _dagrEquals(a: unknown, b: unknown, seen: Set<string>): boolean {
  if (a === null || a === undefined) return b === null || b === undefined;
  if (b === null || b === undefined) return false;
  if (_hasMethod(a, "_eq")) return _hasMethod(b, "_eq") && (a as _Structural)._eq(b, seen);
  if (Array.isArray(a)) { if (!Array.isArray(b) || a.length !== b.length) return false; for (let i = 0; i < a.length; i++) if (!_dagrEquals(a[i], b[i], seen)) return false; return true; }
  if (a instanceof Uint8Array) { if (!(b instanceof Uint8Array) || a.length !== b.length) return false; for (let i = 0; i < a.length; i++) if (a[i] !== b[i]) return false; return true; }
  if (_isTagged(a)) return _isTagged(b) && a.type === b.type && _dagrEquals(a.value, b.value, seen);
  return a === b;
}

interface EventAttrValues {
  key: string;
  value: string;
}

interface EventValues {
  event_id: string;
  event_type: string;
  occurred_at: bigint;
  producer: string;
  attrs: (bigint | null)[];
}

export class EventAttr<B = unknown> {
  constructor(readonly _packed: bigint, readonly _arena: Arena<B>) {}
  get _index(): number { return Number(this._packed); }
  get _values(): EventAttrValues { return this._arena._arrEventAttr[this._index]!; }
  get key(): string {
    return this._arena._arrEventAttr[this._index]!.key;
  }
  set key(value: string) {
    this._arena._arrEventAttr[this._index]!.key = value;
  }
  get value(): string {
    return this._arena._arrEventAttr[this._index]!.value;
  }
  set value(value: string) {
    this._arena._arrEventAttr[this._index]!.value = value;
  }
  _key(): string { return `EventAttr#${this._index}`; }
  toString(): string { return this._describe(new Set<string>()); }
  _describe(_s: Set<string>): string {
    const _k = this._key();
    if (_s.has(_k)) return `EventAttr@${this._index}`;   // cycle / already visited
    _s.add(_k);
    return `EventAttr@${this._index} { key: ${_dagrDescribe(this.key, _s)}, value: ${_dagrDescribe(this.value, _s)} }`;
  }
  hash(): number { return this._hash(new Set<string>()); }
  _hash(_s: Set<string>): number {
    const _k = this._key();
    if (_s.has(_k)) return 0;
    _s.add(_k);
    let _h = _strHash('EventAttr');
    _h = (Math.imul(_h, 31) + _dagrHash(this.key, _s)) | 0;
    _h = (Math.imul(_h, 31) + _dagrHash(this.value, _s)) | 0;
    return _h;
  }
  equals<O>(other: EventAttr<O>): boolean { return this._eq(other, new Set<string>()); }
  _eq<O>(other: EventAttr<O>, _s: Set<string>): boolean {
    const _pk = this._key() + "|" + other._key();
    if (_s.has(_pk)) return true;   // cycle: this pair is already under comparison
    _s.add(_pk);
    if (!_dagrEquals(this.key, other.key, _s)) return false;
    if (!_dagrEquals(this.value, other.value, _s)) return false;
    return true;
  }
}

export class Event<B = unknown> {
  constructor(readonly _packed: bigint, readonly _arena: Arena<B>) {}
  get _index(): number { return Number(this._packed); }
  get _values(): EventValues { return this._arena._arrEvent[this._index]!; }
  get event_id(): string {
    return this._arena._arrEvent[this._index]!.event_id;
  }
  set event_id(value: string) {
    this._arena._arrEvent[this._index]!.event_id = value;
  }
  get event_type(): string {
    return this._arena._arrEvent[this._index]!.event_type;
  }
  set event_type(value: string) {
    this._arena._arrEvent[this._index]!.event_type = value;
  }
  get occurred_at(): bigint {
    return this._arena._arrEvent[this._index]!.occurred_at;
  }
  set occurred_at(value: bigint) {
    this._arena._arrEvent[this._index]!.occurred_at = value;
  }
  get producer(): string {
    return this._arena._arrEvent[this._index]!.producer;
  }
  set producer(value: string) {
    this._arena._arrEvent[this._index]!.producer = value;
  }
  get attrs(): (EventAttr<B> | null)[] {
    return this._arena._arrEvent[this._index]!.attrs.filter((_p): _p is bigint => _p !== null).map((_p) => new EventAttr<B>(_p, this._arena));
  }
  set attrs(value: (EventAttr<B> | null)[]) {
    this._arena._arrEvent[this._index]!.attrs = value.map((_h) => _h === null ? null : _h._packed);
  }
  _key(): string { return `Event#${this._index}`; }
  toString(): string { return this._describe(new Set<string>()); }
  _describe(_s: Set<string>): string {
    const _k = this._key();
    if (_s.has(_k)) return `Event@${this._index}`;   // cycle / already visited
    _s.add(_k);
    return `Event@${this._index} { event_id: ${_dagrDescribe(this.event_id, _s)}, event_type: ${_dagrDescribe(this.event_type, _s)}, occurred_at: ${_dagrDescribe(this.occurred_at, _s)}, producer: ${_dagrDescribe(this.producer, _s)}, attrs: ${_dagrDescribe(this.attrs, _s)} }`;
  }
  hash(): number { return this._hash(new Set<string>()); }
  _hash(_s: Set<string>): number {
    const _k = this._key();
    if (_s.has(_k)) return 0;
    _s.add(_k);
    let _h = _strHash('Event');
    _h = (Math.imul(_h, 31) + _dagrHash(this.event_id, _s)) | 0;
    _h = (Math.imul(_h, 31) + _dagrHash(this.event_type, _s)) | 0;
    _h = (Math.imul(_h, 31) + _dagrHash(this.occurred_at, _s)) | 0;
    _h = (Math.imul(_h, 31) + _dagrHash(this.producer, _s)) | 0;
    _h = (Math.imul(_h, 31) + _dagrHash(this.attrs, _s)) | 0;
    return _h;
  }
  equals<O>(other: Event<O>): boolean { return this._eq(other, new Set<string>()); }
  _eq<O>(other: Event<O>, _s: Set<string>): boolean {
    const _pk = this._key() + "|" + other._key();
    if (_s.has(_pk)) return true;   // cycle: this pair is already under comparison
    _s.add(_pk);
    if (!_dagrEquals(this.event_id, other.event_id, _s)) return false;
    if (!_dagrEquals(this.event_type, other.event_type, _s)) return false;
    if (!_dagrEquals(this.occurred_at, other.occurred_at, _s)) return false;
    if (!_dagrEquals(this.producer, other.producer, _s)) return false;
    if (!_dagrEquals(this.attrs, other.attrs, _s)) return false;
    return true;
  }
}

export class Arena<B = unknown> {
  declare private readonly __brand: B;
  _arrEventAttr: EventAttrValues[] = [];
  _arrEvent: EventValues[] = [];
  #root: bigint | null = null;
  newEventAttr(key: string = "", value: string = ""): EventAttr<B> {
    const _i = this._arrEventAttr.length;
      this._arrEventAttr.push({ key, value });
    return new EventAttr<B>(_pack(0, _i), this);
  }
  newEvent(event_id: string = "", event_type: string = "", occurred_at: bigint = 0n, producer: string = "", attrs: (EventAttr<B> | null)[] = []): Event<B> {
    const _i = this._arrEvent.length;
      this._arrEvent.push({ event_id, event_type, occurred_at, producer, attrs: attrs.map((_h) => _h === null ? null : _h._packed) });
    return new Event<B>(_pack(0, _i), this);
  }
  get root(): Event<B> | null {
    if (this.#root === null) return null;
    return new Event<B>(this.#root, this);
  }
  set root(h: Event<B> | null) { this.#root = h === null ? null : h._packed; }
}

function _fromEventAttr<B>(accU: unknown, a: Arena<B>, seen: Map<number, unknown>): EventAttr<B> {
  const acc = accU as EventAttrAccessor;
  const _hit = seen.get(acc._start);
  if (_hit) return _hit as EventAttr<B>;
  const _n = a.newEventAttr();
  seen.set(acc._start, _n);
  _n.key = acc.key!;
  _n.value = acc.value!;
  return _n;
}

function _fromEvent<B>(accU: unknown, a: Arena<B>, seen: Map<number, unknown>): Event<B> {
  const acc = accU as EventAccessor;
  const _hit = seen.get(acc._start);
  if (_hit) return _hit as Event<B>;
  const _n = a.newEvent();
  seen.set(acc._start, _n);
  _n.event_id = acc.event_id!;
  _n.event_type = acc.event_type!;
  _n.occurred_at = acc.occurred_at!;
  _n.producer = acc.producer!;
  _n.attrs = ((_s: Map<number, unknown>) => acc.attrs!.map((_e) => _e === null ? null : _fromEventAttr<B>(_e, a, _s)))(seen);
  return _n;
}

export function restore<B = unknown>(bytes: Uint8Array): Arena<B> {
  const a = new Arena<B>();
  a.root = _fromEvent<B>(EventAccessor.lazyRoot(bytes), a, new Map<number, unknown>());
  return a;
}

export function restoreWithMap<B = unknown>(bytes: Uint8Array): { arena: Arena<B>; byOffset: Map<number, unknown> } {
  const a = new Arena<B>();
  const byOffset = new Map<number, unknown>();
  a.root = _fromEvent<B>(EventAccessor.lazyRoot(bytes), a, byOffset);
  return { arena: a, byOffset };
}
