// Generated by dagr/codegen/typescript/serde.py — do not edit by hand.
// Graph: EventGraph
import { Builder, nodeOffset, lebLength, zigzag } from "./dagr_writer";
import type { NodeStoreRef } from "./dagr_writer";
import type { EventAttr, Event } from "./EventGraph_arena";

function _storeEventAttr(n: EventAttr<unknown>, b: Builder): NodeStoreRef {
  const _d = b.structLookup.get(n._values); if (_d !== undefined) return { off: _d };
  const _before = b.cursor;
  if (n.value !== null) { b.storeUtf8(n.value, false); b.storeLEB((1 << 1) | 1); }
  if (n.key !== null) { b.storeUtf8(n.key, false); b.storeLEB((0 << 1) | 1); }
  b.storeLEB(b.cursor - _before);  // blockLen (front of the record)
  const _o = b.cursor;
  b.structLookup.set(n._values, _o);
  return { off: _o };
}

function _storeEvent(n: Event<unknown>, b: Builder): NodeStoreRef {
  const _d = b.structLookup.get(n._values); if (_d !== undefined) return { off: _d };
  const _before = b.cursor;
  if (n.attrs !== null) { b.storePackedNodeArray(n.attrs, (b, v) => _storeEventAttr(v, b), false); b.storeLEB((4 << 1) | 1); }
  if (n.producer !== null) { b.storeUtf8(n.producer, false); b.storeLEB((3 << 1) | 1); }
  if (n.occurred_at !== null) { { const _zz = zigzag(n.occurred_at); if (lebLength(_zz) < 8) { b.storeLEB(_zz); b.storeLEB(2 << 1); } else { b.storeI64(n.occurred_at); b.storeLEB((2 << 1) | 1); } } }
  if (n.event_type !== null) { b.storeUtf8(n.event_type, false); b.storeLEB((1 << 1) | 1); }
  if (n.event_id !== null) { b.storeUtf8(n.event_id, false); b.storeLEB((0 << 1) | 1); }
  b.storeLEB(b.cursor - _before);  // blockLen (front of the record)
  const _o = b.cursor;
  b.structLookup.set(n._values, _o);
  return { off: _o };
}

export function writeInto(root: Event, b: Builder): number {
  const off = nodeOffset(_storeEvent(root, b));
  return b.storeLEB((BigInt(b.cursor - off) << 2n));  // framing: rootDist<<2 | headerFlags(0)
}

export function toBytes(root: Event, maxSize: number = 2 * 1024 * 1024): Uint8Array {
  // maxSize sets the back-reference placeholder width (2 MiB -> 4 B, 1024 -> 2 B); it must
  // match across producers for byte-identity.
  const b = new Builder(maxSize);
  writeInto(root, b);
  return b.makeData();
}
