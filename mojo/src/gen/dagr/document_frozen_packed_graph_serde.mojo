# GENERATED by dagr/codegen/mojo/serde.py — do not edit by hand.
# Serializer (arena -> bytes) for graph 'DocumentFrozenPackedGraph' — plan 28 Phase 4.

from dagr_writer import Builder, NodeStoreRef, node_offset, resolved_ref, nil_ref, to_zigzag, leb_length
from document_frozen_packed_graph_arena import DocumentFrozenPackedGraphArena, DocumentMeta, DocumentItem, Document

comptime _VT_MAX = 1   # widest regular-node vtable (§1.19)


# Build a List[Int] of vtable entries from a varargs-free call site.
def _ilist(*vals: Int) -> List[Int]:
    var l = List[Int]()
    for v in vals:
        l.append(v)
    return l^


# Type-namespaced node identity for dedup / cycle keys: a bare packed handle
# collides across node types (index space is per-type), so fold in the type id.
def _nid(tid: Int, idx: Int) -> UInt64:
    return (UInt64(idx) << 8) | UInt64(tid)


def _store_document_meta[o: Origin[mut=False], //](mut b: Builder[_VT_MAX], a: DocumentFrozenPackedGraphArena, h: DocumentMeta[o]) raises -> NodeStoreRef:
    var _id = _nid(0, h._index())
    if _id in b.struct_lookup:
        return resolved_ref(b.struct_lookup[_id])
    var _before = b.cursor
    var _rawbits: UInt64 = 0
    var _pr0 = Bool(a._arr_document_meta[h._index()].region)
    var _pr1 = Bool(h.version())
    if _pr1:
        if leb_length(to_zigzag(Int(h.version().value()))) < 4:
            _ = b.store_leb(to_zigzag(Int(h.version().value())))
        else:
            _ = b.store_i32(h.version().value())
            _rawbits |= UInt64(1) << 0
    if _pr0:
        _ = b.store_utf8(a._arr_document_meta[h._index()].region.value(), False)
    _ = b.store_u8(UInt8(_rawbits & 0xff))
    var _pb: UInt64 = 0
    if _pr0:
        _pb |= UInt64(1) << 0
    if _pr1:
        _pb |= UInt64(1) << 1
    _ = b.store_u8(UInt8(_pb & 0xff))
    _ = b.store_leb(UInt64(b.cursor - _before))
    var _o = b.cursor
    b.struct_lookup[_id] = _o
    return resolved_ref(_o)


def _store_document_item[o: Origin[mut=False], //](mut b: Builder[_VT_MAX], a: DocumentFrozenPackedGraphArena, h: DocumentItem[o]) raises -> NodeStoreRef:
    var _id = _nid(1, h._index())
    if _id in b.struct_lookup:
        return resolved_ref(b.struct_lookup[_id])
    var _before = b.cursor
    var _rawbits: UInt64 = 0
    var _pr0 = Bool(a._arr_document_item[h._index()].sku)
    var _pr1 = Bool(h.qty())
    var _pr2 = Bool(h.price_minor())
    if _pr2:
        if leb_length(to_zigzag(Int(h.price_minor().value()))) < 8:
            _ = b.store_leb(to_zigzag(Int(h.price_minor().value())))
        else:
            _ = b.store_i64(h.price_minor().value())
            _rawbits |= UInt64(1) << 1
    if _pr1:
        if leb_length(to_zigzag(Int(h.qty().value()))) < 4:
            _ = b.store_leb(to_zigzag(Int(h.qty().value())))
        else:
            _ = b.store_i32(h.qty().value())
            _rawbits |= UInt64(1) << 0
    if _pr0:
        _ = b.store_utf8(a._arr_document_item[h._index()].sku.value(), False)
    _ = b.store_u8(UInt8(_rawbits & 0xff))
    var _pb: UInt64 = 0
    if _pr0:
        _pb |= UInt64(1) << 0
    if _pr1:
        _pb |= UInt64(1) << 1
    if _pr2:
        _pb |= UInt64(1) << 2
    _ = b.store_u8(UInt8(_pb & 0xff))
    _ = b.store_leb(UInt64(b.cursor - _before))
    var _o = b.cursor
    b.struct_lookup[_id] = _o
    return resolved_ref(_o)


def _store_document[o: Origin[mut=False], //](mut b: Builder[_VT_MAX], a: DocumentFrozenPackedGraphArena, h: Document[o]) raises -> NodeStoreRef:
    var _id = _nid(2, h._index())
    if _id in b.struct_lookup:
        return resolved_ref(b.struct_lookup[_id])
    var _before = b.cursor
    var _rawbits: UInt64 = 0
    var _pr0 = Bool(a._arr_document[h._index()].id)
    var _pr1 = Bool(h.status())
    var _pr2 = Bool(h.meta())
    var _pr3 = True
    ref _row3 = a._arr_document[h._index()].items
    var _pcb3 = b.cursor
    var _n3 = 0
    for _k3 in range(len(_row3) - 1, -1, -1):
        var _p3 = _row3[_k3]
        _ = _store_document_item(b, a, DocumentItem[o](h._a, _p3))
        _n3 += 1
    _ = b.store_leb(UInt64(_n3))
    _ = b.store_leb(UInt64(b.cursor - _pcb3))
    if _pr2:
        _ = _store_document_meta(b, a, h.meta().value())
    if _pr1:
        if leb_length(to_zigzag(Int(h.status().value()))) < 4:
            _ = b.store_leb(to_zigzag(Int(h.status().value())))
        else:
            _ = b.store_i32(h.status().value())
            _rawbits |= UInt64(1) << 0
    if _pr0:
        _ = b.store_utf8(a._arr_document[h._index()].id.value(), False)
    _ = b.store_u8(UInt8(_rawbits & 0xff))
    var _pb: UInt64 = 0
    if _pr0:
        _pb |= UInt64(1) << 0
    if _pr1:
        _pb |= UInt64(1) << 1
    if _pr2:
        _pb |= UInt64(1) << 2
    if _pr3:
        _pb |= UInt64(1) << 3
    _ = b.store_u8(UInt8(_pb & 0xff))
    _ = b.store_leb(UInt64(b.cursor - _before))
    var _o = b.cursor
    b.struct_lookup[_id] = _o
    return resolved_ref(_o)


def write_document_graph(mut b: Builder[_VT_MAX], a: DocumentFrozenPackedGraphArena) raises -> Int:
    var _off = node_offset(_store_document(b, a, a.root().value()))
    return b.store_leb(UInt64((b.cursor - _off) << 2))    # framing (no header)

def serialize_document_graph(a: DocumentFrozenPackedGraphArena, max_size: Int = 2 * 1024 * 1024) raises -> List[UInt8]:
    var b = Builder[_VT_MAX](hint=len(a._arr_document_meta) + len(a._arr_document_item) + len(a._arr_document), max_size=max_size)
    _ = write_document_graph(b, a)
    return b.make_data()
