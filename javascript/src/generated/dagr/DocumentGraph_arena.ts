// Generated by dagr/codegen/typescript/arena.py — do not edit by hand.
// Graph: DocumentGraph
import { DocumentAccessor } from "./DocumentGraph";
import type { DocumentMetaAccessor, DocumentItemAccessor } from "./DocumentGraph";


function _pack(gen: number, idx: number): bigint { return (BigInt(gen) << 40n) | BigInt(idx); }

function _strHash(s: string): number {
  let h = 5381;
  for (let i = 0; i < s.length; i++) h = (Math.imul(h, 33) + s.charCodeAt(i)) | 0;
  return h;
}

type _Structural = { _describe(s: Set<string>): string; _hash(s: Set<string>): number; _eq(o: unknown, s: Set<string>): boolean };
function _hasMethod<K extends string>(v: unknown, k: K): v is Record<K, (...a: never[]) => unknown> {
  return typeof v === "object" && v !== null && typeof (v as Record<string, unknown>)[k] === "function";
}
function _isTagged(v: unknown): v is { type: unknown; value: unknown } {
  return typeof v === "object" && v !== null && "type" in v && "value" in v;
}

function _dagrDescribe(v: unknown, seen: Set<string>): string {
  if (v === null || v === undefined) return "null";
  if (_hasMethod(v, "_describe")) return (v as _Structural)._describe(seen);
  if (Array.isArray(v)) return "[" + v.map((e) => _dagrDescribe(e, seen)).join(", ") + "]";
  if (v instanceof Uint8Array) return "[" + Array.from(v).join(", ") + "]";
  if (_isTagged(v)) return String(v.type) + "(" + _dagrDescribe(v.value, seen) + ")";
  if (typeof v === "string") return JSON.stringify(v);
  if (typeof v === "bigint") return v.toString();
  if (typeof v === "number" || typeof v === "boolean") return String(v);
  return "";
}

function _dagrHash(v: unknown, seen: Set<string>): number {
  if (v === null || v === undefined) return 0;
  if (_hasMethod(v, "_hash")) return (v as _Structural)._hash(seen);
  if (Array.isArray(v)) { let h = 7; for (const e of v) h = (Math.imul(h, 31) + _dagrHash(e, seen)) | 0; return h; }
  if (v instanceof Uint8Array) { let h = 7; for (const b of v) h = (Math.imul(h, 31) + b) | 0; return h; }
  if (_isTagged(v)) return (Math.imul(_strHash(String(v.type)), 31) + _dagrHash(v.value, seen)) | 0;
  if (typeof v === "string") return _strHash(v);
  if (typeof v === "bigint") return Number(BigInt.asUintN(32, v)) | 0;
  if (typeof v === "boolean") return v ? 1 : 0;
  return typeof v === "number" ? v | 0 : 0;
}

function _dagrEquals(a: unknown, b: unknown, seen: Set<string>): boolean {
  if (a === null || a === undefined) return b === null || b === undefined;
  if (b === null || b === undefined) return false;
  if (_hasMethod(a, "_eq")) return _hasMethod(b, "_eq") && (a as _Structural)._eq(b, seen);
  if (Array.isArray(a)) { if (!Array.isArray(b) || a.length !== b.length) return false; for (let i = 0; i < a.length; i++) if (!_dagrEquals(a[i], b[i], seen)) return false; return true; }
  if (a instanceof Uint8Array) { if (!(b instanceof Uint8Array) || a.length !== b.length) return false; for (let i = 0; i < a.length; i++) if (a[i] !== b[i]) return false; return true; }
  if (_isTagged(a)) return _isTagged(b) && a.type === b.type && _dagrEquals(a.value, b.value, seen);
  return a === b;
}

interface DocumentMetaValues {
  region: string;
  version: number;
}

interface DocumentItemValues {
  sku: string;
  qty: number;
  price_minor: bigint;
}

interface DocumentValues {
  id: string;
  status: number;
  meta: bigint | null;
  items: (bigint | null)[];
}

export class DocumentMeta<B = unknown> {
  constructor(readonly _packed: bigint, readonly _arena: Arena<B>) {}
  get _index(): number { return Number(this._packed); }
  get _values(): DocumentMetaValues { return this._arena._arrDocumentMeta[this._index]!; }
  get region(): string {
    return this._arena._arrDocumentMeta[this._index]!.region;
  }
  set region(value: string) {
    this._arena._arrDocumentMeta[this._index]!.region = value;
  }
  get version(): number {
    return this._arena._arrDocumentMeta[this._index]!.version;
  }
  set version(value: number) {
    this._arena._arrDocumentMeta[this._index]!.version = value;
  }
  _key(): string { return `DocumentMeta#${this._index}`; }
  toString(): string { return this._describe(new Set<string>()); }
  _describe(_s: Set<string>): string {
    const _k = this._key();
    if (_s.has(_k)) return `DocumentMeta@${this._index}`;   // cycle / already visited
    _s.add(_k);
    return `DocumentMeta@${this._index} { region: ${_dagrDescribe(this.region, _s)}, version: ${_dagrDescribe(this.version, _s)} }`;
  }
  hash(): number { return this._hash(new Set<string>()); }
  _hash(_s: Set<string>): number {
    const _k = this._key();
    if (_s.has(_k)) return 0;
    _s.add(_k);
    let _h = _strHash('DocumentMeta');
    _h = (Math.imul(_h, 31) + _dagrHash(this.region, _s)) | 0;
    _h = (Math.imul(_h, 31) + _dagrHash(this.version, _s)) | 0;
    return _h;
  }
  equals<O>(other: DocumentMeta<O>): boolean { return this._eq(other, new Set<string>()); }
  _eq<O>(other: DocumentMeta<O>, _s: Set<string>): boolean {
    const _pk = this._key() + "|" + other._key();
    if (_s.has(_pk)) return true;   // cycle: this pair is already under comparison
    _s.add(_pk);
    if (!_dagrEquals(this.region, other.region, _s)) return false;
    if (!_dagrEquals(this.version, other.version, _s)) return false;
    return true;
  }
}

export class DocumentItem<B = unknown> {
  constructor(readonly _packed: bigint, readonly _arena: Arena<B>) {}
  get _index(): number { return Number(this._packed); }
  get _values(): DocumentItemValues { return this._arena._arrDocumentItem[this._index]!; }
  get sku(): string {
    return this._arena._arrDocumentItem[this._index]!.sku;
  }
  set sku(value: string) {
    this._arena._arrDocumentItem[this._index]!.sku = value;
  }
  get qty(): number {
    return this._arena._arrDocumentItem[this._index]!.qty;
  }
  set qty(value: number) {
    this._arena._arrDocumentItem[this._index]!.qty = value;
  }
  get price_minor(): bigint {
    return this._arena._arrDocumentItem[this._index]!.price_minor;
  }
  set price_minor(value: bigint) {
    this._arena._arrDocumentItem[this._index]!.price_minor = value;
  }
  _key(): string { return `DocumentItem#${this._index}`; }
  toString(): string { return this._describe(new Set<string>()); }
  _describe(_s: Set<string>): string {
    const _k = this._key();
    if (_s.has(_k)) return `DocumentItem@${this._index}`;   // cycle / already visited
    _s.add(_k);
    return `DocumentItem@${this._index} { sku: ${_dagrDescribe(this.sku, _s)}, qty: ${_dagrDescribe(this.qty, _s)}, price_minor: ${_dagrDescribe(this.price_minor, _s)} }`;
  }
  hash(): number { return this._hash(new Set<string>()); }
  _hash(_s: Set<string>): number {
    const _k = this._key();
    if (_s.has(_k)) return 0;
    _s.add(_k);
    let _h = _strHash('DocumentItem');
    _h = (Math.imul(_h, 31) + _dagrHash(this.sku, _s)) | 0;
    _h = (Math.imul(_h, 31) + _dagrHash(this.qty, _s)) | 0;
    _h = (Math.imul(_h, 31) + _dagrHash(this.price_minor, _s)) | 0;
    return _h;
  }
  equals<O>(other: DocumentItem<O>): boolean { return this._eq(other, new Set<string>()); }
  _eq<O>(other: DocumentItem<O>, _s: Set<string>): boolean {
    const _pk = this._key() + "|" + other._key();
    if (_s.has(_pk)) return true;   // cycle: this pair is already under comparison
    _s.add(_pk);
    if (!_dagrEquals(this.sku, other.sku, _s)) return false;
    if (!_dagrEquals(this.qty, other.qty, _s)) return false;
    if (!_dagrEquals(this.price_minor, other.price_minor, _s)) return false;
    return true;
  }
}

export class Document<B = unknown> {
  constructor(readonly _packed: bigint, readonly _arena: Arena<B>) {}
  get _index(): number { return Number(this._packed); }
  get _values(): DocumentValues { return this._arena._arrDocument[this._index]!; }
  get id(): string {
    return this._arena._arrDocument[this._index]!.id;
  }
  set id(value: string) {
    this._arena._arrDocument[this._index]!.id = value;
  }
  get status(): number {
    return this._arena._arrDocument[this._index]!.status;
  }
  set status(value: number) {
    this._arena._arrDocument[this._index]!.status = value;
  }
  get meta(): DocumentMeta<B> | null {
    const _v = this._arena._arrDocument[this._index]!.meta;
    if (_v === null) return null;
    return new DocumentMeta<B>(_v, this._arena);
  }
  set meta(value: DocumentMeta<B> | null) {
    this._arena._arrDocument[this._index]!.meta = value === null ? null : value._packed;
  }
  get items(): (DocumentItem<B> | null)[] {
    return this._arena._arrDocument[this._index]!.items.filter((_p): _p is bigint => _p !== null).map((_p) => new DocumentItem<B>(_p, this._arena));
  }
  set items(value: (DocumentItem<B> | null)[]) {
    this._arena._arrDocument[this._index]!.items = value.map((_h) => _h === null ? null : _h._packed);
  }
  _key(): string { return `Document#${this._index}`; }
  toString(): string { return this._describe(new Set<string>()); }
  _describe(_s: Set<string>): string {
    const _k = this._key();
    if (_s.has(_k)) return `Document@${this._index}`;   // cycle / already visited
    _s.add(_k);
    return `Document@${this._index} { id: ${_dagrDescribe(this.id, _s)}, status: ${_dagrDescribe(this.status, _s)}, meta: ${_dagrDescribe(this.meta, _s)}, items: ${_dagrDescribe(this.items, _s)} }`;
  }
  hash(): number { return this._hash(new Set<string>()); }
  _hash(_s: Set<string>): number {
    const _k = this._key();
    if (_s.has(_k)) return 0;
    _s.add(_k);
    let _h = _strHash('Document');
    _h = (Math.imul(_h, 31) + _dagrHash(this.id, _s)) | 0;
    _h = (Math.imul(_h, 31) + _dagrHash(this.status, _s)) | 0;
    _h = (Math.imul(_h, 31) + _dagrHash(this.meta, _s)) | 0;
    _h = (Math.imul(_h, 31) + _dagrHash(this.items, _s)) | 0;
    return _h;
  }
  equals<O>(other: Document<O>): boolean { return this._eq(other, new Set<string>()); }
  _eq<O>(other: Document<O>, _s: Set<string>): boolean {
    const _pk = this._key() + "|" + other._key();
    if (_s.has(_pk)) return true;   // cycle: this pair is already under comparison
    _s.add(_pk);
    if (!_dagrEquals(this.id, other.id, _s)) return false;
    if (!_dagrEquals(this.status, other.status, _s)) return false;
    if (!_dagrEquals(this.meta, other.meta, _s)) return false;
    if (!_dagrEquals(this.items, other.items, _s)) return false;
    return true;
  }
}

export class Arena<B = unknown> {
  declare private readonly __brand: B;
  _arrDocumentMeta: DocumentMetaValues[] = [];
  _arrDocumentItem: DocumentItemValues[] = [];
  _arrDocument: DocumentValues[] = [];
  #root: bigint | null = null;
  newDocumentMeta(region: string = "", version: number = 0): DocumentMeta<B> {
    const _i = this._arrDocumentMeta.length;
      this._arrDocumentMeta.push({ region, version });
    return new DocumentMeta<B>(_pack(0, _i), this);
  }
  newDocumentItem(sku: string = "", qty: number = 0, price_minor: bigint = 0n): DocumentItem<B> {
    const _i = this._arrDocumentItem.length;
      this._arrDocumentItem.push({ sku, qty, price_minor });
    return new DocumentItem<B>(_pack(0, _i), this);
  }
  newDocument(id: string = "", status: number = 0, meta: DocumentMeta<B> | null = null, items: (DocumentItem<B> | null)[] = []): Document<B> {
    const _i = this._arrDocument.length;
      this._arrDocument.push({ id, status, meta: meta === null ? null : meta._packed, items: items.map((_h) => _h === null ? null : _h._packed) });
    return new Document<B>(_pack(0, _i), this);
  }
  get root(): Document<B> | null {
    if (this.#root === null) return null;
    return new Document<B>(this.#root, this);
  }
  set root(h: Document<B> | null) { this.#root = h === null ? null : h._packed; }
}

function _fromDocumentMeta<B>(accU: unknown, a: Arena<B>, seen: Map<number, unknown>): DocumentMeta<B> {
  const acc = accU as DocumentMetaAccessor;
  const _hit = seen.get(acc._start);
  if (_hit) return _hit as DocumentMeta<B>;
  const _n = a.newDocumentMeta();
  seen.set(acc._start, _n);
  _n.region = acc.region!;
  _n.version = acc.version!;
  return _n;
}

function _fromDocumentItem<B>(accU: unknown, a: Arena<B>, seen: Map<number, unknown>): DocumentItem<B> {
  const acc = accU as DocumentItemAccessor;
  const _hit = seen.get(acc._start);
  if (_hit) return _hit as DocumentItem<B>;
  const _n = a.newDocumentItem();
  seen.set(acc._start, _n);
  _n.sku = acc.sku!;
  _n.qty = acc.qty!;
  _n.price_minor = acc.price_minor!;
  return _n;
}

function _fromDocument<B>(accU: unknown, a: Arena<B>, seen: Map<number, unknown>): Document<B> {
  const acc = accU as DocumentAccessor;
  const _hit = seen.get(acc._start);
  if (_hit) return _hit as Document<B>;
  const _n = a.newDocument();
  seen.set(acc._start, _n);
  _n.id = acc.id!;
  _n.status = acc.status!;
  _n.meta = (acc.meta === null ? null : _fromDocumentMeta<B>(acc.meta, a, seen));
  _n.items = ((_s: Map<number, unknown>) => acc.items!.map((_e) => _e === null ? null : _fromDocumentItem<B>(_e, a, _s)))(seen);
  return _n;
}

export function restore<B = unknown>(bytes: Uint8Array): Arena<B> {
  const a = new Arena<B>();
  a.root = _fromDocument<B>(DocumentAccessor.lazyRoot(bytes), a, new Map<number, unknown>());
  return a;
}

export function restoreWithMap<B = unknown>(bytes: Uint8Array): { arena: Arena<B>; byOffset: Map<number, unknown> } {
  const a = new Arena<B>();
  const byOffset = new Map<number, unknown>();
  a.root = _fromDocument<B>(DocumentAccessor.lazyRoot(bytes), a, byOffset);
  return { arena: a, byOffset };
}
