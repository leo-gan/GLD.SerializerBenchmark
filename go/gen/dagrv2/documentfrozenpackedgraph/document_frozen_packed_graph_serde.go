// Code generated by dagr (dagr/codegen/go/serde.py); DO NOT EDIT.

package documentfrozenpackedgraph

import "dagrv2/dagr"

func storeDocumentMeta(n DocumentMeta, b *dagr.Builder) dagr.NodeStoreRef {
	if d, ok := b.StructLookup[n.key()]; ok {
		return dagr.NodeStoreRef{Off: d}
	}
	before := b.Cursor()
	var raw [1]bool
	if x1, ok := n.Version(); ok {
		zz := dagr.ToZigZag(int64(x1))
		if dagr.LebLength(zz) < 4 {
			b.StoreLEB(zz)
		} else {
			b.StoreI32(x1)
			raw[0] = true
		}
	}
	if x0, ok := n.Region(); ok {
		b.StoreUtf8(x0, false)
	}
	enc := make([]byte, 1)
	for k, r := range raw {
		if r {
			enc[k>>3] |= 1 << (k & 7)
		}
	}
	b.StoreBytes(enc)
	var nbits uint64
	if _, ok := n.Region(); ok {
		nbits |= 1 << 0
	}
	if _, ok := n.Version(); ok {
		nbits |= 1 << 1
	}
	b.StoreBytes([]byte{byte(nbits >> 0)})
	b.StoreLEB(uint64(b.Cursor() - before))
	o := b.Cursor()
	b.StructLookup[n.key()] = o
	return dagr.NodeStoreRef{Off: o}
}

func storeDocumentItem(n DocumentItem, b *dagr.Builder) dagr.NodeStoreRef {
	if d, ok := b.StructLookup[n.key()]; ok {
		return dagr.NodeStoreRef{Off: d}
	}
	before := b.Cursor()
	var raw [2]bool
	if x2, ok := n.PriceMinor(); ok {
		zz := dagr.ToZigZag(int64(x2))
		if dagr.LebLength(zz) < 8 {
			b.StoreLEB(zz)
		} else {
			b.StoreI64(x2)
			raw[1] = true
		}
	}
	if x1, ok := n.Qty(); ok {
		zz := dagr.ToZigZag(int64(x1))
		if dagr.LebLength(zz) < 4 {
			b.StoreLEB(zz)
		} else {
			b.StoreI32(x1)
			raw[0] = true
		}
	}
	if x0, ok := n.Sku(); ok {
		b.StoreUtf8(x0, false)
	}
	enc := make([]byte, 1)
	for k, r := range raw {
		if r {
			enc[k>>3] |= 1 << (k & 7)
		}
	}
	b.StoreBytes(enc)
	var nbits uint64
	if _, ok := n.Sku(); ok {
		nbits |= 1 << 0
	}
	if _, ok := n.Qty(); ok {
		nbits |= 1 << 1
	}
	if _, ok := n.PriceMinor(); ok {
		nbits |= 1 << 2
	}
	b.StoreBytes([]byte{byte(nbits >> 0)})
	b.StoreLEB(uint64(b.Cursor() - before))
	o := b.Cursor()
	b.StructLookup[n.key()] = o
	return dagr.NodeStoreRef{Off: o}
}

func storeDocument(n Document, b *dagr.Builder) dagr.NodeStoreRef {
	if d, ok := b.StructLookup[n.key()]; ok {
		return dagr.NodeStoreRef{Off: d}
	}
	before := b.Cursor()
	var raw [1]bool
	if x3, ok := n.Items(); ok {
		dagr.StorePackedNodeArrayPlain(b, x3, func(b *dagr.Builder, v DocumentItem) { storeDocumentItem(v, b) })
	}
	if x2, ok := n.Meta(); ok {
		storeDocumentMeta(x2, b)
	}
	if x1, ok := n.Status(); ok {
		zz := dagr.ToZigZag(int64(x1))
		if dagr.LebLength(zz) < 4 {
			b.StoreLEB(zz)
		} else {
			b.StoreI32(x1)
			raw[0] = true
		}
	}
	if x0, ok := n.ID(); ok {
		b.StoreUtf8(x0, false)
	}
	enc := make([]byte, 1)
	for k, r := range raw {
		if r {
			enc[k>>3] |= 1 << (k & 7)
		}
	}
	b.StoreBytes(enc)
	var nbits uint64
	if _, ok := n.ID(); ok {
		nbits |= 1 << 0
	}
	if _, ok := n.Status(); ok {
		nbits |= 1 << 1
	}
	if _, ok := n.Meta(); ok {
		nbits |= 1 << 2
	}
	if _, ok := n.Items(); ok {
		nbits |= 1 << 3
	}
	b.StoreBytes([]byte{byte(nbits >> 0)})
	b.StoreLEB(uint64(b.Cursor() - before))
	o := b.Cursor()
	b.StructLookup[n.key()] = o
	return dagr.NodeStoreRef{Off: o}
}

// ToBytesDocumentFrozenPackedGraph serializes the graph reachable from root (byte-identical to every
// other Dagr implementation for the same graph). maxSize sets the cycle back-
// reference placeholder width (0 → 2 MiB, i.e. 4 bytes) and must match across
// producers for byte-identity.
func ToBytesDocumentFrozenPackedGraph(root Document, maxSize int) []byte {
	b := dagr.NewBuilder(maxSize)
	off := dagr.NodeOffset(storeDocument(root, b))
	b.StoreLEB(uint64(b.Cursor()-off) << 2)
	return b.MakeData()
}
