# GENERATED by dagr/codegen/mojo/core.py — do not edit by hand.
# Lazy reader for graph 'DocumentGraph' (regular-node scalars/utf8/data/enum/node-ref/arrays).

from dagr_reader import read_i32, read_i64, read_leb, read_utf8, read_zigzag_leb, root_offset


@fieldwise_init
struct _Document_items_RefArray[o: ImmOrigin](Copyable, Movable, ImplicitlyCopyable, Sized):
    var buf: Span[UInt8, Self.o]
    var table_base: Int
    var base: Int
    var count: Int
    var es: Int

    def __len__(self) -> Int:
        return self.count

    def get(self, i: Int) raises -> DocumentItemAccessor[Self.o]:
        if i < 0 or i >= self.count:
            raise Error("array index out of range")
        var ro = read_rel_offset_s(self.buf, self.table_base + i * self.es, self.es)
        if ro == 0:
            raise Error("nil slot in non-optional node array")
        return DocumentItemAccessor[Self.o](self.buf, self.base + ro - 1)

    def iter(self) -> _Document_items_RefArrayIter[Self.o]:
        return _Document_items_RefArrayIter[Self.o](self, 0)


@fieldwise_init
struct _Document_items_RefArrayIter[o: ImmOrigin](Copyable, Movable, ImplicitlyCopyable):
    var arr: _Document_items_RefArray[Self.o]
    var i: Int

    def next(mut self) raises -> DocumentItemAccessor[Self.o]:
        var _r = self.arr.get(self.i)
        self.i += 1
        return _r

@fieldwise_init
struct _Document_items_PackedRefArray[o: ImmOrigin](Copyable, Movable, ImplicitlyCopyable, Sized):
    var buf: Span[UInt8, Self.o]
    var base: Int      # first element block position
    var count: Int
    var nil_base: Int  # nil-bitset start (-1 if plain)

    def __len__(self) -> Int:
        return self.count

    def get(self, i: Int) raises -> DocumentItemAccessor[Self.o]:
        if i < 0 or i >= self.count:
            raise Error("array index out of range")
        var p = self.base
        for _ in range(i):
            var _bl = read_leb(self.buf, p)
            p += _bl[1] + Int(_bl[0])
        return DocumentItemAccessor[Self.o](self.buf, p)

    def iter(self) -> _Document_items_PackedRefArrayIter[Self.o]:
        return _Document_items_PackedRefArrayIter[Self.o](self.buf, self.base, self.nil_base, 0)


@fieldwise_init
struct _Document_items_PackedRefArrayIter[o: ImmOrigin](Copyable, Movable, ImplicitlyCopyable):
    var buf: Span[UInt8, Self.o]
    var p: Int         # byte cursor of the next element block
    var nil_base: Int
    var j: Int         # logical element index (for the AWO nil bitset)

    def next(mut self) raises -> DocumentItemAccessor[Self.o]:
        var _acc = DocumentItemAccessor[Self.o](self.buf, self.p)
        var _bl = read_leb(self.buf, self.p)
        self.p += _bl[1] + Int(_bl[0])
        self.j += 1
        return _acc

struct DocumentMetaAccessor[o: ImmOrigin](Copyable, Movable, ImplicitlyCopyable):
    var buf: Span[UInt8, Self.o]
    var pos: Int     # node start position (dedup key for eager restore)
    var _p0: Int   # tag position of field 0 (region); -1 = absent
    var _p1: Int   # tag position of field 1 (version); -1 = absent

    def __init__(out self, buf: Span[UInt8, Self.o], start: Int) raises:
        self.buf = buf
        self.pos = start
        var r = read_leb(buf, start)
        var cursor = start + r[1]
        var end = cursor + Int(r[0])
        self._p0 = -1
        if cursor < end:
            var t0 = read_leb(buf, cursor)
            if (t0[0] >> 1) == 0:
                self._p0 = cursor
                cursor += t0[1]
                var _l0 = read_leb(buf, cursor)
                cursor += _l0[1] + Int(_l0[0])
        self._p1 = -1
        if cursor < end:
            var t1 = read_leb(buf, cursor)
            if (t1[0] >> 1) == 1:
                self._p1 = cursor
                var raw1 = (t1[0] & 1) == 1
                cursor += t1[1]
                if raw1:
                    cursor += 4
                else:
                    cursor += read_leb(buf, cursor)[1]

    def region(self) raises -> Optional[String]:
        if self._p0 < 0:
            return None
        var t = read_leb(self.buf, self._p0)
        var payload = self._p0 + t[1]
        return String(read_utf8(self.buf, payload)[0])

    def version(self) raises -> Optional[Int32]:
        if self._p1 < 0:
            return None
        var t = read_leb(self.buf, self._p1)
        var raw = (t[0] & 1) == 1
        var payload = self._p1 + t[1]
        if raw:
            return read_i32(self.buf, payload)
        else:
            return Int32(read_zigzag_leb(self.buf, payload)[0])


struct DocumentItemAccessor[o: ImmOrigin](Copyable, Movable, ImplicitlyCopyable):
    var buf: Span[UInt8, Self.o]
    var pos: Int     # node start position (dedup key for eager restore)
    var _p0: Int   # tag position of field 0 (sku); -1 = absent
    var _p1: Int   # tag position of field 1 (qty); -1 = absent
    var _p2: Int   # tag position of field 2 (price_minor); -1 = absent

    def __init__(out self, buf: Span[UInt8, Self.o], start: Int) raises:
        self.buf = buf
        self.pos = start
        var r = read_leb(buf, start)
        var cursor = start + r[1]
        var end = cursor + Int(r[0])
        self._p0 = -1
        if cursor < end:
            var t0 = read_leb(buf, cursor)
            if (t0[0] >> 1) == 0:
                self._p0 = cursor
                cursor += t0[1]
                var _l0 = read_leb(buf, cursor)
                cursor += _l0[1] + Int(_l0[0])
        self._p1 = -1
        if cursor < end:
            var t1 = read_leb(buf, cursor)
            if (t1[0] >> 1) == 1:
                self._p1 = cursor
                var raw1 = (t1[0] & 1) == 1
                cursor += t1[1]
                if raw1:
                    cursor += 4
                else:
                    cursor += read_leb(buf, cursor)[1]
        self._p2 = -1
        if cursor < end:
            var t2 = read_leb(buf, cursor)
            if (t2[0] >> 1) == 2:
                self._p2 = cursor
                var raw2 = (t2[0] & 1) == 1
                cursor += t2[1]
                if raw2:
                    cursor += 8
                else:
                    cursor += read_leb(buf, cursor)[1]

    def sku(self) raises -> Optional[String]:
        if self._p0 < 0:
            return None
        var t = read_leb(self.buf, self._p0)
        var payload = self._p0 + t[1]
        return String(read_utf8(self.buf, payload)[0])

    def qty(self) raises -> Optional[Int32]:
        if self._p1 < 0:
            return None
        var t = read_leb(self.buf, self._p1)
        var raw = (t[0] & 1) == 1
        var payload = self._p1 + t[1]
        if raw:
            return read_i32(self.buf, payload)
        else:
            return Int32(read_zigzag_leb(self.buf, payload)[0])

    def price_minor(self) raises -> Optional[Int64]:
        if self._p2 < 0:
            return None
        var t = read_leb(self.buf, self._p2)
        var raw = (t[0] & 1) == 1
        var payload = self._p2 + t[1]
        if raw:
            return read_i64(self.buf, payload)
        else:
            return Int64(read_zigzag_leb(self.buf, payload)[0])


struct DocumentAccessor[o: ImmOrigin](Copyable, Movable, ImplicitlyCopyable):
    var buf: Span[UInt8, Self.o]
    var pos: Int     # node start position (dedup key for eager restore)
    var _p0: Int   # tag position of field 0 (id); -1 = absent
    var _p1: Int   # tag position of field 1 (status); -1 = absent
    var _p2: Int   # tag position of field 2 (meta); -1 = absent
    var _p3: Int   # tag position of field 3 (items); -1 = absent

    def __init__(out self, buf: Span[UInt8, Self.o], start: Int) raises:
        self.buf = buf
        self.pos = start
        var r = read_leb(buf, start)
        var cursor = start + r[1]
        var end = cursor + Int(r[0])
        self._p0 = -1
        if cursor < end:
            var t0 = read_leb(buf, cursor)
            if (t0[0] >> 1) == 0:
                self._p0 = cursor
                cursor += t0[1]
                var _l0 = read_leb(buf, cursor)
                cursor += _l0[1] + Int(_l0[0])
        self._p1 = -1
        if cursor < end:
            var t1 = read_leb(buf, cursor)
            if (t1[0] >> 1) == 1:
                self._p1 = cursor
                var raw1 = (t1[0] & 1) == 1
                cursor += t1[1]
                if raw1:
                    cursor += 4
                else:
                    cursor += read_leb(buf, cursor)[1]
        self._p2 = -1
        if cursor < end:
            var t2 = read_leb(buf, cursor)
            if (t2[0] >> 1) == 2:
                self._p2 = cursor
                cursor += t2[1]
                var _nl2 = read_leb(buf, cursor)
                cursor += _nl2[1] + Int(_nl2[0])
        self._p3 = -1
        if cursor < end:
            var t3 = read_leb(buf, cursor)
            if (t3[0] >> 1) == 3:
                self._p3 = cursor
                cursor += t3[1]
                var _al3 = read_leb(buf, cursor)
                cursor += _al3[1] + Int(_al3[0])

    def id(self) raises -> Optional[String]:
        if self._p0 < 0:
            return None
        var t = read_leb(self.buf, self._p0)
        var payload = self._p0 + t[1]
        return String(read_utf8(self.buf, payload)[0])

    def status(self) raises -> Optional[Int32]:
        if self._p1 < 0:
            return None
        var t = read_leb(self.buf, self._p1)
        var raw = (t[0] & 1) == 1
        var payload = self._p1 + t[1]
        if raw:
            return read_i32(self.buf, payload)
        else:
            return Int32(read_zigzag_leb(self.buf, payload)[0])

    def meta(self) raises -> Optional[DocumentMetaAccessor[Self.o]]:
        if self._p2 < 0:
            return None
        var t = read_leb(self.buf, self._p2)
        var payload = self._p2 + t[1]
        return DocumentMetaAccessor[Self.o](self.buf, payload)

    def items(self) raises -> Optional[_Document_items_PackedRefArray[Self.o]]:
        if self._p3 < 0:
            return None
        var t = read_leb(self.buf, self._p3)
        var payload = self._p3 + t[1]
        var _abl = read_leb(self.buf, payload)
        var _cd = payload + _abl[1]
        var _cnt = read_leb(self.buf, _cd)
        var _count = Int(_cnt[0])
        var _base = _cd + _cnt[1]
        return _Document_items_PackedRefArray[Self.o](self.buf, _base, _count, -1)

def read_document_root[o: ImmOrigin, //](buf: Span[UInt8, o]) raises -> DocumentAccessor[o]:
    return DocumentAccessor[o](buf, root_offset(buf))
