// Generated by dagr/codegen/typescript/serde.py — do not edit by hand.
// Graph: DocumentGraph
import { Builder, nodeOffset, lebLength, zigzag } from "./dagr_writer";
import type { NodeStoreRef } from "./dagr_writer";
import type { DocumentMeta, DocumentItem, Document } from "./DocumentGraph_arena";

function _storeDocumentMeta(n: DocumentMeta<unknown>, b: Builder): NodeStoreRef {
  const _d = b.structLookup.get(n._values); if (_d !== undefined) return { off: _d };
  const _before = b.cursor;
  if (n.version !== null) { { const _zz = zigzag(n.version); if (lebLength(_zz) < 4) { b.storeLEB(_zz); b.storeLEB(1 << 1); } else { b.storeI32(n.version); b.storeLEB((1 << 1) | 1); } } }
  if (n.region !== null) { b.storeUtf8(n.region, false); b.storeLEB((0 << 1) | 1); }
  b.storeLEB(b.cursor - _before);  // blockLen (front of the record)
  const _o = b.cursor;
  b.structLookup.set(n._values, _o);
  return { off: _o };
}

function _storeDocumentItem(n: DocumentItem<unknown>, b: Builder): NodeStoreRef {
  const _d = b.structLookup.get(n._values); if (_d !== undefined) return { off: _d };
  const _before = b.cursor;
  if (n.price_minor !== null) { { const _zz = zigzag(n.price_minor); if (lebLength(_zz) < 8) { b.storeLEB(_zz); b.storeLEB(2 << 1); } else { b.storeI64(n.price_minor); b.storeLEB((2 << 1) | 1); } } }
  if (n.qty !== null) { { const _zz = zigzag(n.qty); if (lebLength(_zz) < 4) { b.storeLEB(_zz); b.storeLEB(1 << 1); } else { b.storeI32(n.qty); b.storeLEB((1 << 1) | 1); } } }
  if (n.sku !== null) { b.storeUtf8(n.sku, false); b.storeLEB((0 << 1) | 1); }
  b.storeLEB(b.cursor - _before);  // blockLen (front of the record)
  const _o = b.cursor;
  b.structLookup.set(n._values, _o);
  return { off: _o };
}

function _storeDocument(n: Document<unknown>, b: Builder): NodeStoreRef {
  const _d = b.structLookup.get(n._values); if (_d !== undefined) return { off: _d };
  const _before = b.cursor;
  if (n.items !== null) { b.storePackedNodeArray(n.items, (b, v) => _storeDocumentItem(v, b), false); b.storeLEB((3 << 1) | 1); }
  if (n.meta !== null) { _storeDocumentMeta(n.meta, b); b.storeLEB((2 << 1) | 1); }
  if (n.status !== null) { { const _zz = zigzag(n.status); if (lebLength(_zz) < 4) { b.storeLEB(_zz); b.storeLEB(1 << 1); } else { b.storeI32(n.status); b.storeLEB((1 << 1) | 1); } } }
  if (n.id !== null) { b.storeUtf8(n.id, false); b.storeLEB((0 << 1) | 1); }
  b.storeLEB(b.cursor - _before);  // blockLen (front of the record)
  const _o = b.cursor;
  b.structLookup.set(n._values, _o);
  return { off: _o };
}

export function writeInto(root: Document, b: Builder): number {
  const off = nodeOffset(_storeDocument(root, b));
  return b.storeLEB((BigInt(b.cursor - off) << 2n));  // framing: rootDist<<2 | headerFlags(0)
}

export function toBytes(root: Document, maxSize: number = 2 * 1024 * 1024): Uint8Array {
  // maxSize sets the back-reference placeholder width (2 MiB -> 4 B, 1024 -> 2 B); it must
  // match across producers for byte-identity.
  const b = new Builder(maxSize);
  writeInto(root, b);
  return b.makeData();
}
