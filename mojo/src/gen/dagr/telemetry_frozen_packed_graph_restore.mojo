# GENERATED by dagr/codegen/mojo/restore.py — do not edit by hand.
# Eager restore (bytes -> arena) for graph 'TelemetryFrozenPackedGraph' — plan 28 Phase 3.

from telemetry_frozen_packed_graph_arena import TelemetryFrozenPackedGraphArena, Telemetry
from telemetry_frozen_packed_graph_reader import TelemetryAccessor
from dagr_reader import root_offset
from std.collections import Dict


def restore_telemetry_graph[o: ImmOrigin](buf: Span[UInt8, o]) raises -> TelemetryFrozenPackedGraphArena:
    var a = TelemetryFrozenPackedGraphArena()
    var seen = Dict[Int, UInt64]()
    var racc = TelemetryAccessor[o](buf, root_offset(buf))
    var rh = _restore_telemetry(a, racc, seen)
    a.set_root(rh)
    return a^


def _restore_telemetry[co: ImmOrigin, ao: Origin[mut=False], //](ref [ao] a: TelemetryFrozenPackedGraphArena, acc: TelemetryAccessor[co], mut seen: Dict[Int, UInt64]) raises -> Telemetry[ao]:
    var _pos = acc.pos
    if _pos in seen:
        return Telemetry[ao](Pointer(to=a), seen[_pos])
    var h = a.new_telemetry()
    var i = h._index()
    var _wa = Pointer[TelemetryFrozenPackedGraphArena, MutAnyOrigin](unsafe_from_address=Int(Pointer(to=a)))
    seen[_pos] = h._packed
    _wa[]._arr_telemetry[i].source = acc.source()
    _wa[]._arr_telemetry[i].ts = acc.ts()
    var _oa2 = acc.tags()
    if _oa2:
        _wa[]._arr_telemetry[i].tags = _oa2.take().into_list()
    else:
        _wa[]._arr_telemetry[i].tags = List[String]()
    var _oa3 = acc.values()
    if _oa3:
        _wa[]._arr_telemetry[i].values = _oa3.take().into_list()
    else:
        _wa[]._arr_telemetry[i].values = List[Float64]()
    return h


