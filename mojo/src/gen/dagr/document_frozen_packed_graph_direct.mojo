# GENERATED by dagr/codegen/mojo/direct.py — do not edit by hand.
# Direct Graph Builder ("spec/33-direct-graph-builder.md") for graph 'DocumentFrozenPackedGraph'.
from dagr_writer import Builder, NodeStoreRef, node_offset, resolved_ref, leb_length, to_zigzag

comptime _VT_MAX = 1

struct DirectDocumentMeta(Copyable, Movable):
    var region: Optional[String]
    var version: Optional[Int32]
    def __init__(out self, var region: Optional[String], var version: Optional[Int32]):
        self.region = region^
        self.version = version^

struct DirectDocumentItem(Copyable, Movable):
    var sku: Optional[String]
    var qty: Optional[Int32]
    var price_minor: Optional[Int64]
    def __init__(out self, var sku: Optional[String], var qty: Optional[Int32], var price_minor: Optional[Int64]):
        self.sku = sku^
        self.qty = qty^
        self.price_minor = price_minor^

struct DirectDocument(Copyable, Movable):
    var id: Optional[String]
    var status: Optional[Int32]
    var meta: Optional[DirectDocumentMeta]
    var items: List[DirectDocumentItem]
    def __init__(out self, var id: Optional[String], var status: Optional[Int32], var meta: Optional[DirectDocumentMeta], var items: List[DirectDocumentItem]):
        self.id = id^
        self.status = status^
        self.meta = meta^
        self.items = items^

def _store_document_meta_d(mut b: Builder[_VT_MAX], n: DirectDocumentMeta) raises -> NodeStoreRef:
    var _before = b.cursor
    var _rawbits: UInt64 = 0
    var _pr0 = Bool(n.region)
    var _pr1 = Bool(n.version)
    if _pr1:
        if leb_length(to_zigzag(Int(n.version.value()))) < 4:
            _ = b.store_leb(to_zigzag(Int(n.version.value())))
        else:
            _ = b.store_i32(n.version.value())
            _rawbits |= UInt64(1) << 0
    if _pr0:
        _ = b.store_utf8(n.region.value(), False)
    _ = b.store_u8(UInt8((_rawbits >> 0) & 0xff))
    var _pb: UInt64 = 0
    if _pr0:
        _pb |= UInt64(1) << 0
    if _pr1:
        _pb |= UInt64(1) << 1
    _ = b.store_u8(UInt8((_pb >> 0) & 0xff))
    _ = b.store_leb(UInt64(b.cursor - _before))
    return resolved_ref(b.cursor)

def _store_document_item_d(mut b: Builder[_VT_MAX], n: DirectDocumentItem) raises -> NodeStoreRef:
    var _before = b.cursor
    var _rawbits: UInt64 = 0
    var _pr0 = Bool(n.sku)
    var _pr1 = Bool(n.qty)
    var _pr2 = Bool(n.price_minor)
    if _pr2:
        if leb_length(to_zigzag(Int(n.price_minor.value()))) < 8:
            _ = b.store_leb(to_zigzag(Int(n.price_minor.value())))
        else:
            _ = b.store_i64(n.price_minor.value())
            _rawbits |= UInt64(1) << 1
    if _pr1:
        if leb_length(to_zigzag(Int(n.qty.value()))) < 4:
            _ = b.store_leb(to_zigzag(Int(n.qty.value())))
        else:
            _ = b.store_i32(n.qty.value())
            _rawbits |= UInt64(1) << 0
    if _pr0:
        _ = b.store_utf8(n.sku.value(), False)
    _ = b.store_u8(UInt8((_rawbits >> 0) & 0xff))
    var _pb: UInt64 = 0
    if _pr0:
        _pb |= UInt64(1) << 0
    if _pr1:
        _pb |= UInt64(1) << 1
    if _pr2:
        _pb |= UInt64(1) << 2
    _ = b.store_u8(UInt8((_pb >> 0) & 0xff))
    _ = b.store_leb(UInt64(b.cursor - _before))
    return resolved_ref(b.cursor)

def _store_document_d(mut b: Builder[_VT_MAX], n: DirectDocument) raises -> NodeStoreRef:
    var _before = b.cursor
    var _rawbits: UInt64 = 0
    var _pr0 = Bool(n.id)
    var _pr1 = Bool(n.status)
    var _pr2 = Bool(n.meta)
    var _pr3 = True
    ref _pa3 = n.items
    var _pcb3 = b.cursor
    for _k3 in range(len(_pa3) - 1, -1, -1):
        _ = _store_document_item_d(b, _pa3[_k3])
    _ = b.store_leb(UInt64(len(_pa3)))
    _ = b.store_leb(UInt64(b.cursor - _pcb3))
    if _pr2:
        _ = _store_document_meta_d(b, n.meta.value())
    if _pr1:
        if leb_length(to_zigzag(Int(n.status.value()))) < 4:
            _ = b.store_leb(to_zigzag(Int(n.status.value())))
        else:
            _ = b.store_i32(n.status.value())
            _rawbits |= UInt64(1) << 0
    if _pr0:
        _ = b.store_utf8(n.id.value(), False)
    _ = b.store_u8(UInt8((_rawbits >> 0) & 0xff))
    var _pb: UInt64 = 0
    if _pr0:
        _pb |= UInt64(1) << 0
    if _pr1:
        _pb |= UInt64(1) << 1
    if _pr2:
        _pb |= UInt64(1) << 2
    if _pr3:
        _pb |= UInt64(1) << 3
    _ = b.store_u8(UInt8((_pb >> 0) & 0xff))
    _ = b.store_leb(UInt64(b.cursor - _before))
    return resolved_ref(b.cursor)

def _push_leb(mut out: List[UInt8], v: UInt64):
    var x = v
    while True:
        var byte = UInt8(x & 0x7f)
        x >>= 7
        if x != 0:
            out.append(byte | 0x80)
        else:
            out.append(byte)
            break

def _push_bytes(mut out: List[UInt8], src: List[UInt8]):
    for i in range(len(src)):
        out.append(src[i])

comptime DirectBuilder = Builder[_VT_MAX]

def write_document_graph_direct(mut b: Builder[_VT_MAX], n: DirectDocument) raises -> Int:
    var _off = node_offset(_store_document_d(b, n))
    return b.store_leb(UInt64((b.cursor - _off) << 2))

def serialize_document_graph_direct(var n: DirectDocument) raises -> List[UInt8]:
    var b = Builder[_VT_MAX]()
    _ = write_document_graph_direct(b, n)
    return b.make_data()

