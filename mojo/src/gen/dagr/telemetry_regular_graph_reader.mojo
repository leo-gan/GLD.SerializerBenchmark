# GENERATED by dagr/codegen/mojo/core.py — do not edit by hand.
# Lazy reader for graph 'TelemetryRegularGraph' (regular-node scalars/utf8/data/enum/node-ref/arrays).

from dagr_reader import NumArray, Utf8Array, read_i64, read_leb, read_utf8, read_v62, root_offset, rtype_vtable_slot


@fieldwise_init
struct TelemetryAccessor[o: ImmOrigin](Copyable, Movable, ImplicitlyCopyable):
    var buf: Span[UInt8, Self.o]
    var pos: Int

    def source_slice(self) raises -> Optional[StringSlice[Self.o]]:
        var _slot = rtype_vtable_slot(self.buf, self.pos, 0)
        if _slot < 0:
            return None
        var sp = self.pos + _slot
        var p = read_v62(self.buf, sp)
        return Optional(read_utf8(self.buf, sp + p[1] + Int(p[0]))[0])

    def source(self) raises -> Optional[String]:
        var _slot = rtype_vtable_slot(self.buf, self.pos, 0)
        if _slot < 0:
            return None
        var sp = self.pos + _slot
        var p = read_v62(self.buf, sp)
        return String(read_utf8(self.buf, sp + p[1] + Int(p[0]))[0])

    def ts(self) raises -> Optional[Int64]:
        var _slot = rtype_vtable_slot(self.buf, self.pos, 1)
        if _slot < 0:
            return None
        var sp = self.pos + _slot
        return read_i64(self.buf, sp)

    def tags(self) raises -> Optional[Utf8Array[Self.o]]:
        var _slot = rtype_vtable_slot(self.buf, self.pos, 2)
        if _slot < 0:
            return None
        var sp = self.pos + _slot
        var p = read_v62(self.buf, sp)
        var payload = sp + p[1] + Int(p[0])
        var h = read_leb(self.buf, payload)
        var count = Int(h[0]) >> 2
        var es = 1 << (Int(h[0]) & 3)
        var table_base = payload + h[1]
        return Utf8Array[Self.o](self.buf, table_base, table_base + count * es, count, es)

    def values(self) raises -> Optional[NumArray[Self.o, DType.float64, 8]]:
        var _slot = rtype_vtable_slot(self.buf, self.pos, 3)
        if _slot < 0:
            return None
        var sp = self.pos + _slot
        var p = read_v62(self.buf, sp)
        var payload = sp + p[1] + Int(p[0])
        var h = read_leb(self.buf, payload)
        return NumArray[Self.o, DType.float64, 8](self.buf, payload + h[1], Int(h[0]))

def read_telemetry_root[o: ImmOrigin, //](buf: Span[UInt8, o]) raises -> TelemetryAccessor[o]:
    return TelemetryAccessor[o](buf, root_offset(buf))
