# GENERATED by dagr/codegen/mojo/serde.py — do not edit by hand.
# Serializer (arena -> bytes) for graph 'MessageRegularGraph' — plan 28 Phase 4.

from dagr_writer import Builder, NodeStoreRef, node_offset, resolved_ref, nil_ref, to_zigzag, leb_length
from message_regular_graph_arena import MessageRegularGraphArena, Message

comptime _VT_MAX = 8   # widest regular-node vtable (§1.19)


# Build a List[Int] of vtable entries from a varargs-free call site.
def _ilist(*vals: Int) -> List[Int]:
    var l = List[Int]()
    for v in vals:
        l.append(v)
    return l^


# Type-namespaced node identity for dedup / cycle keys: a bare packed handle
# collides across node types (index space is per-type), so fold in the type id.
def _nid(tid: Int, idx: Int) -> UInt64:
    return (UInt64(idx) << 8) | UInt64(tid)


def _store_message[o: Origin[mut=False], //](mut b: Builder[_VT_MAX], a: MessageRegularGraphArena, h: Message[o]) raises -> NodeStoreRef:
    var _id = _nid(0, h._index())
    var _r = b.begin_storing_acyclic(_id)
    if _r.is_pending or _r.off != -1:
        return _r
    var _c7 = -1
    var _p7 = Bool(a._arr_message[h._index()].f_string_2)
    if _p7:
        _c7 = b.store_utf8(a._arr_message[h._index()].f_string_2.value(), True)
    var _c4 = -1
    var _p4 = Bool(a._arr_message[h._index()].f_string)
    if _p4:
        _c4 = b.store_utf8(a._arr_message[h._index()].f_string.value(), True)
    var _off7 = -1
    if _p7:
        _off7 = b.store_forward_pointer(_c7)
    var _off6 = -1
    var _v6 = h.f_int32_2()
    if _v6:
        _off6 = b.store_i32(_v6.value())
    var _off5 = -1
    var _v5 = h.f_bool_2()
    if _v5:
        _off5 = b.store_u8(UInt8(1) if _v5.value() else UInt8(0))
    var _off4 = -1
    if _p4:
        _off4 = b.store_forward_pointer(_c4)
    var _off3 = -1
    var _v3 = h.f_float64()
    if _v3:
        _off3 = b.store_f64(_v3.value())
    var _off2 = -1
    var _v2 = h.f_int64()
    if _v2:
        _off2 = b.store_i64(_v2.value())
    var _off1 = -1
    var _v1 = h.f_int32()
    if _v1:
        _off1 = b.store_i32(_v1.value())
    var _off0 = -1
    var _v0 = h.f_bool()
    if _v0:
        _off0 = b.store_u8(UInt8(1) if _v0.value() else UInt8(0))
    var _o = b.store_vtable(_off0, _off1, _off2, _off3, _off4, _off5, _off6, _off7)
    b.finish_storing_acyclic(_id, _o)
    return resolved_ref(_o)


def write_message_graph(mut b: Builder[_VT_MAX], a: MessageRegularGraphArena) raises -> Int:
    var _off = node_offset(_store_message(b, a, a.root().value()))
    return b.store_leb(UInt64((b.cursor - _off) << 2))    # framing (no header)

def serialize_message_graph(a: MessageRegularGraphArena, max_size: Int = 2 * 1024 * 1024) raises -> List[UInt8]:
    var b = Builder[_VT_MAX](hint=len(a._arr_message), max_size=max_size)
    _ = write_message_graph(b, a)
    return b.make_data()
