from std.utils.numerics import FPUtils, isinf, isnan
from std.memory.unsafe import bitcast
from .helpers import (
    is_small_integer,
    remove_trailing_zeros,
    log10_pow2,
    log10_pow2_residual,
    mshift,
    div10,
    wins_tiebreak,
    is_tie,
    is_tie_uncentered,
)
from .tables import MULTIPLIERS
from ..utils import StackArray, lut, DIGIT_PAIRS
from emberjson.constants import `-`, `0`, `.`, `e`
from std.sys.intrinsics import unlikely

# Mojo port of the Teju Jagua algorithm by Cassio Neri.
# Original implementation: https://github.com/cassioneri/teju_jagua
# Licensed under the Apache License, Version 2.0.

comptime STORAGE_INDEX_OFFSET = -324


@always_inline
def _get_sig_digits[dtype: DType]() -> Int:
    # 0.30103 is log10(2). We add 24 to bound the maximum number of decimal significant digits generated by teju internally
    return Int((FPUtils[dtype].mantissa_width() + 1) * 30103 // 100000) + 24


@always_inline
def _get_exp_digits[dtype: DType]() -> Int:
    var max_exp = (1 << (FPUtils[dtype].exponent_width() - 1)) - 1
    var max_dec_exp = max_exp * 30103 // 100000
    if max_dec_exp < 10:
        return 1
    if max_dec_exp < 100:
        return 2
    if max_dec_exp < 1000:
        return 3
    if max_dec_exp < 10000:
        return 4
    return 5


@always_inline
def _get_buffer_size[dtype: DType]() -> Int:
    # The true output significant digits bound for the string is smaller than the internal TEJU mantissa processing bound.
    # It is log10(2) + 2.
    var true_sig_digits = (
        Int((FPUtils[dtype].mantissa_width() + 1) * 30103 // 100000) + 2
    )
    var exp_digits = _get_exp_digits[dtype]()

    # 1(sign) + 1(d) + 1(.) + (sig_digits - 1)(d) + 1(e) + 1(+/-) + exp_digits
    var scientific = true_sig_digits + exp_digits + 4

    # 1(sign) + 2(0.) + 3(000) + true_sig_digits
    var decimal = true_sig_digits + 6

    # max padded standard notation: exp <= 15
    # e.g., f64 `-1.23e15` padded out could be `-1230000000000000.0`
    # 1(sign) + 16(digits before dec) + 2(.0)
    # We allocate 24 here for absolute safety margin.
    var padded = 24

    var size = scientific
    if decimal > size:
        size = decimal
    if padded > size:
        size = padded

    return size


@always_inline
def write_float[dtype: DType](d: Scalar[dtype], mut writer: Some[Writer]):
    comptime if dtype != DType.float64 and dtype != DType.float32 and dtype != DType.float16:
        # Let stdlib handle exotic float types in case someone is bold enough to use
        # them in JSON.
        writer.write(d)
        return

    if isinf(d) or isnan(d):
        writer.write("null")
        return

    comptime buf_size = 64
    var buffer = StackArray[Byte, buf_size](uninitialized=True)
    var buf_idx = 0

    if FPUtils[dtype].get_sign(d):
        buffer.unsafe_get(buf_idx) = `-`
        buf_idx += 1

    if d == 0.0:
        buffer.unsafe_get(buf_idx) = `0`
        buf_idx += 1
        buffer.unsafe_get(buf_idx) = `.`
        buf_idx += 1
        buffer.unsafe_get(buf_idx) = `0`
        buf_idx += 1
        var str_slice = StringSlice(
            unsafe_from_utf8=Span(
                unsafe_ptr=buffer.unsafe_ptr(), length=buf_idx
            )
        )
        writer.write(str_slice)
        return

    var fields = teju[dtype](fp_to_binary(abs(d)))

    var sig = fields.mantissa
    var exp = fields.exponent

    var sig_len = 0
    var temp_sig = sig

    comptime for p in range(4, -1, -1):
        comptime step = 1 << p
        comptime pow10 = UInt64(10**step)
        if temp_sig >= pow10:
            temp_sig //= pow10
            sig_len += step

    sig_len += 1

    var digit_start = buf_idx
    var cur_idx = digit_start + sig_len

    # Fast 2-digit extraction directly into buffer
    while sig >= 100:
        var q = sig // 100
        var r = Int(sig - (q * 100))
        var pair = lut[DIGIT_PAIRS](r)
        cur_idx -= 2
        buffer.unsafe_get(cur_idx) = pair[0]
        buffer.unsafe_get(cur_idx + 1) = pair[1]
        sig = q

    if sig > 0:
        if sig >= 10:
            var pair = lut[DIGIT_PAIRS](Int(sig))
            cur_idx -= 2
            buffer.unsafe_get(cur_idx) = pair[0]
            buffer.unsafe_get(cur_idx + 1) = pair[1]
        else:
            cur_idx -= 1
            buffer.unsafe_get(cur_idx) = Byte(sig) + `0`

    var leading_zeroes = -exp - Int32(sig_len)
    var str_exp = exp + Int32(sig_len - 1)

    if (exp < 0 and leading_zeroes > 3) or str_exp > 15:
        # Scientific notation
        if sig_len > 1:
            for i in reversed(range(1, sig_len)):
                buffer.unsafe_get(digit_start + i + 1) = buffer.unsafe_get(
                    digit_start + i
                )
            buffer.unsafe_get(digit_start + 1) = `.`
            buf_idx += sig_len + 1
        else:
            buf_idx += 1

        buffer.unsafe_get(buf_idx) = `e`
        buf_idx += 1

        var abs_e = Int(str_exp) if str_exp >= 0 else Int(-str_exp)
        if str_exp < 0:
            buffer.unsafe_get(buf_idx) = `-`
            buf_idx += 1

        if abs_e < 10:
            buffer.unsafe_get(buf_idx) = Byte(abs_e) + `0`
            buf_idx += 1
        elif abs_e < 100:
            var pair = lut[DIGIT_PAIRS](abs_e)
            buffer.unsafe_get(buf_idx) = pair[0]
            buffer.unsafe_get(buf_idx + 1) = pair[1]
            buf_idx += 2
        else:
            var q = abs_e // 100
            var r = Int(abs_e - (q * 100))
            var pair = lut[DIGIT_PAIRS](r)
            buffer.unsafe_get(buf_idx) = Byte(q) + `0`
            buffer.unsafe_get(buf_idx + 1) = pair[0]
            buffer.unsafe_get(buf_idx + 2) = pair[1]
            buf_idx += 3

    elif exp < 0 and leading_zeroes >= 0:
        # Between 0.0001 and 0.9999
        var shift_amount = 2 + Int(leading_zeroes)
        for i in reversed(range(sig_len)):
            buffer.unsafe_get(
                digit_start + shift_amount + i
            ) = buffer.unsafe_get(digit_start + i)

        buffer.unsafe_get(digit_start) = `0`
        buffer.unsafe_get(digit_start + 1) = `.`
        for i in range(Int(leading_zeroes)):
            buffer.unsafe_get(digit_start + 2 + i) = `0`

        buf_idx += sig_len + shift_amount

    else:
        var digits_before = Int(exp) + sig_len
        var trailing = digits_before - sig_len
        if trailing > 0:
            buf_idx += sig_len
            for _ in range(trailing):
                buffer.unsafe_get(buf_idx) = `0`
                buf_idx += 1
            buffer.unsafe_get(buf_idx) = `.`
            buf_idx += 1
            buffer.unsafe_get(buf_idx) = `0`
            buf_idx += 1
        elif digits_before < sig_len:
            for i in reversed(range(digits_before, sig_len)):
                buffer.unsafe_get(digit_start + i + 1) = buffer.unsafe_get(
                    digit_start + i
                )
            buffer.unsafe_get(digit_start + digits_before) = `.`
            buf_idx += sig_len + 1
        else:
            buf_idx += sig_len
            buffer.unsafe_get(buf_idx) = `.`
            buf_idx += 1
            buffer.unsafe_get(buf_idx) = `0`
            buf_idx += 1

    var str_slice = StringSlice(
        unsafe_from_utf8=Span(unsafe_ptr=buffer.unsafe_ptr(), length=buf_idx)
    )
    writer.write(str_slice)


@fieldwise_init
struct Fields(TrivialRegisterPassable):
    var mantissa: UInt64
    var exponent: Int32


@always_inline
def teju[dtype: DType](binary: Fields, out dec: Fields):
    comptime mantissa_size = UInt64(FPUtils[dtype].mantissa_width() + 1)
    comptime min_exponent = Int32(
        1 - FPUtils[dtype].exponent_bias() - FPUtils[dtype].mantissa_width()
    )

    var e = binary.exponent
    var m = binary.mantissa

    if is_small_integer(m, e, Int32(mantissa_size)):
        if e < 0:
            return remove_trailing_zeros(m >> UInt64(-e), 0)
        else:
            return remove_trailing_zeros(m << UInt64(e), 0)

    var f = log10_pow2(e)
    var r = log10_pow2_residual(e)
    var i = f - STORAGE_INDEX_OFFSET

    var mult = lut[MULTIPLIERS](i)
    var u = mult[0]
    var l = mult[1]

    var m_0: UInt64 = 1 << (mantissa_size - 1)

    if m != m_0 or e == min_exponent:
        var m_a = UInt64(2 * m - 1) << UInt64(r)
        var a = mshift(m_a, u, l)
        var m_b = UInt64(2 * m + 1) << UInt64(r)
        var b = mshift(m_b, u, l)
        var q = div10(b)
        var s = 10 * q

        if a < s:
            if s < b or wins_tiebreak(m) or not is_tie(m_b, f):
                return remove_trailing_zeros(q, f + 1)
            elif s == a and wins_tiebreak(m) and is_tie(m_a, f):
                return remove_trailing_zeros(q, f + 1)

        if (a + b) & 1 == 1:
            return Fields((a + b) // 2 + 1, f)

        var m_c = UInt64(4) * m << UInt64(r)
        var c_2 = mshift(m_c, u, l)
        var c = c_2 // 2

        if wins_tiebreak(c_2) or (wins_tiebreak(c) and is_tie(c_2, -f)):
            return Fields(c, f)
        return Fields(c + 1, f)

    var m_a = (4 * m_0 - 1) << UInt64(r)
    var a = mshift(m_a, u, l) // 2
    var m_b = (2 * m_0 + 1) << UInt64(r)
    var b = mshift(m_b, u, l)

    if a < b:
        var q = div10(b)
        var s = 10 * q

        if a < s:
            if s < b or wins_tiebreak(m_0) or not is_tie_uncentered(m_b, f):
                return remove_trailing_zeros(q, f + 1)
        elif s == a and wins_tiebreak(m_0) and is_tie_uncentered(m_a, f):
            return remove_trailing_zeros(q, f + 1)

        var log2_m_c = mantissa_size + UInt64(r) + 1
        var c_2 = mshift(UInt64(1) << log2_m_c, u, l)
        var c = c_2 // 2

        if c == a and not is_tie_uncentered(m_a, f):
            return Fields(c + 1, f)

        if wins_tiebreak(c_2) or (wins_tiebreak(c) and is_tie(c_2, -f)):
            return Fields(c, f)

        return Fields(c + 1, f)

    elif is_tie_uncentered(m_a, f):
        return remove_trailing_zeros(a, f)

    var m_c = 40 * m_0 << UInt64(r)
    var c_2 = mshift(m_c, u, l)
    var c = c_2 // 2

    if wins_tiebreak(c_2) or (wins_tiebreak(c) and is_tie(c_2, -f)):
        return Fields(c, f - 1)
    return Fields(c + 1, f - 1)


@always_inline
def fp_to_binary[dtype: DType](d: Scalar[dtype], out bin: Fields):
    comptime mantissa_width = FPUtils[dtype].mantissa_width()
    comptime min_exponent = 1 - FPUtils[dtype].exponent_bias() - mantissa_width

    var mantissa = UInt64(FPUtils[dtype].get_mantissa_uint(d))
    var exponent = FPUtils[dtype].get_exponent_biased(d)

    if exponent != 0:
        exponent -= 1
        mantissa |= UInt64(1) << UInt64(mantissa_width)

    exponent += min_exponent

    return Fields(mantissa, Int32(exponent))
