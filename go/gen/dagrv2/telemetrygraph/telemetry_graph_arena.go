// Code generated by dagr (dagr/codegen/go/arena.py); DO NOT EDIT.

package telemetrygraph

import (
	"strconv"
	"strings"

	"dagrv2/dagr"
)

type telemetryValues struct {
	source dagr.Opt[string]
	ts     dagr.Opt[int64]
	tags   dagr.Opt[[]string]
	values dagr.Opt[[]float64]
}

// Arena owns every node of one graph instance (struct-of-arrays storage; handles
// are indices into it, so the graph may be cyclic and share nodes freely).
type Arena struct {
	telemetrys []telemetryValues
	root       dagr.Opt[uint64]
	id         uint32
}

var arenaIDs uint32

// NewArena creates an empty arena.
func NewArena() *Arena {
	arenaIDs++
	return &Arena{id: arenaIDs}
}

func (a *Arena) Root() (Telemetry, bool) {
	if !a.root.Ok {
		return Telemetry{}, false
	}
	h := Telemetry{a: a, h: a.root.V}
	return h, true
}

func (a *Arena) SetRoot(h Telemetry) { a.root = dagr.Some(h.h) }

func (a *Arena) ClearRoot() { a.root = dagr.None[uint64]() }

func (a *Arena) allocTelemetry() (int, uint64) {
	i := len(a.telemetrys)
	a.telemetrys = append(a.telemetrys, telemetryValues{})
	return i, dagr.Pack(0, i)
}

// NewTelemetry allocates a Telemetry with construct-time defaults.
func (a *Arena) NewTelemetry() Telemetry {
	idx, h := a.allocTelemetry()
	_ = idx
	return Telemetry{a: a, h: h}
}

// Telemetry is a value handle into an Arena (spec 09: generation<<40 | index).
type Telemetry struct {
	a *Arena
	h uint64
}

func (n Telemetry) Arena() *Arena { return n.a }

func (n Telemetry) Handle() uint64 { return n.h }

func (n Telemetry) Index() int { return dagr.UnpackIdx(n.h) }

func (n Telemetry) vals() *telemetryValues {
	return &n.a.telemetrys[dagr.UnpackIdx(n.h)]
}

func (n Telemetry) key() dagr.NodeKey {
	return dagr.NodeKey{Type: 0, Handle: n.h}
}

func (n Telemetry) IsValid() bool {
	return dagr.UnpackIdx(n.h) < len(n.a.telemetrys)
}

func (n Telemetry) Source() (string, bool) {
	o := n.vals().source
	return o.V, o.Ok
}

func (n Telemetry) SourceBytes() ([]byte, bool) {
	o := n.vals().source
	return []byte(o.V), o.Ok
}

func (n Telemetry) SetSource(v string) { n.vals().source = dagr.Some(v) }

func (n Telemetry) ClearSource() { n.vals().source = dagr.None[string]() }

func (n Telemetry) Ts() (int64, bool) {
	o := n.vals().ts
	return o.V, o.Ok
}

func (n Telemetry) SetTs(v int64) { n.vals().ts = dagr.Some(v) }

func (n Telemetry) ClearTs() { n.vals().ts = dagr.None[int64]() }

func (n Telemetry) Tags() ([]string, bool) {
	o := n.vals().tags
	if !o.Ok {
		return nil, false
	}
	s := o.V
	return s, true
}

func (n Telemetry) TagsLen() (int, bool) {
	s, ok := n.Tags()
	return len(s), ok
}

func (n Telemetry) TagsAt(i int) string {
	s, _ := n.Tags()
	return s[i]
}

func (n Telemetry) SetTags(v []string) { n.vals().tags = dagr.Some(v) }

func (n Telemetry) ClearTags() { n.vals().tags = dagr.None[[]string]() }

func (n Telemetry) Values() ([]float64, bool) {
	o := n.vals().values
	if !o.Ok {
		return nil, false
	}
	s := o.V
	return s, true
}

func (n Telemetry) ValuesLen() (int, bool) {
	s, ok := n.Values()
	return len(s), ok
}

func (n Telemetry) ValuesAt(i int) float64 {
	s, _ := n.Values()
	return s[i]
}

func (n Telemetry) SetValues(v []float64) { n.vals().values = dagr.Some(v) }

func (n Telemetry) ClearValues() { n.vals().values = dagr.None[[]float64]() }

func (n Telemetry) describe(sb *strings.Builder, seen map[dagr.NodeKey]int) {
	k := dagr.NodeKey{Type: 0, Handle: n.h}
	if ord, ok := seen[k]; ok {
		sb.WriteString("Telemetry#")
		sb.WriteString(strconv.Itoa(ord))
		return
	}
	seen[k] = len(seen)
	sb.WriteString("Telemetry#")
	sb.WriteString(strconv.Itoa(len(seen) - 1))
	sb.WriteString("{")
	sb.WriteString("source: ")
	if n.vals().source.Ok {
		sb.WriteString(strconv.Quote(n.vals().source.V))
	} else {
		sb.WriteString("nil")
	}
	sb.WriteString(", ")
	sb.WriteString("ts: ")
	if n.vals().ts.Ok {
		sb.WriteString(strconv.FormatInt(int64(n.vals().ts.V), 10))
	} else {
		sb.WriteString("nil")
	}
	sb.WriteString(", ")
	sb.WriteString("tags: ")
	if n.vals().tags.Ok {
		sb.WriteByte('[')
		for i, e := range n.vals().tags.V {
			if i > 0 {
				sb.WriteString(", ")
			}
			sb.WriteString(strconv.Quote(e))
		}
		sb.WriteByte(']')
	} else {
		sb.WriteString("nil")
	}
	sb.WriteString(", ")
	sb.WriteString("values: ")
	if n.vals().values.Ok {
		sb.WriteByte('[')
		for i, e := range n.vals().values.V {
			if i > 0 {
				sb.WriteString(", ")
			}
			sb.WriteString(strconv.FormatFloat(e, 'g', -1, 64))
		}
		sb.WriteByte(']')
	} else {
		sb.WriteString("nil")
	}
	sb.WriteByte('}')
}

// String renders the node and everything reachable from it; a repeated node prints
// as `Telemetry#<ordinal>` so cycles terminate and sharing is visible.
func (n Telemetry) String() string {
	var sb strings.Builder
	n.describe(&sb, map[dagr.NodeKey]int{})
	return sb.String()
}

// Hash is a structural hash (FNV-1a of the canonical description): isomorphic graphs
// in different arenas hash equal.
func (n Telemetry) Hash() uint64 { return dagr.Fnv1a([]byte(n.String())) }

// Equals is cross-arena structural equality (cycle- and sharing-aware).
func (n Telemetry) Equals(o Telemetry) bool { return n.String() == o.String() }

func restoreTelemetry(acc TelemetryAccessor, a *Arena, seen map[dagr.SeenKey]uint64) Telemetry {
	sk := dagr.SeenAt(acc.buf, acc.pos)
	if h, ok := seen[sk]; ok {
		return Telemetry{a: a, h: h}
	}
	idx, h := a.allocTelemetry()
	seen[sk] = h
	var tmpSource dagr.Opt[string]
	{
		if v, ok := acc.Source(); ok {
			tmpSource = dagr.Some(v)
		}
	}
	a.telemetrys[idx].source = tmpSource
	var tmpTs dagr.Opt[int64]
	{
		if v, ok := acc.Ts(); ok {
			tmpTs = dagr.Some(v)
		}
	}
	a.telemetrys[idx].ts = tmpTs
	var tmpTags dagr.Opt[[]string]
	{
		if cnt, ok := acc.TagsLen(); ok {
			s := make([]string, cnt)
			it := acc.TagsIter()
			for i := 0; i < cnt; i++ {
				e, _ := it.Next()
				s[i] = e
			}
			tmpTags = dagr.Some(s)
		}
	}
	a.telemetrys[idx].tags = tmpTags
	var tmpValues dagr.Opt[[]float64]
	{
		if cnt, ok := acc.ValuesLen(); ok {
			s := make([]float64, cnt)
			it := acc.ValuesIter()
			for i := 0; i < cnt; i++ {
				e, _ := it.Next()
				s[i] = e
			}
			tmpValues = dagr.Some(s)
		}
	}
	a.telemetrys[idx].values = tmpValues
	return Telemetry{a: a, h: h}
}

// RestoreTelemetryGraph eagerly materializes a buffer into a fresh Arena (shared and cyclic
// nodes materialize once).
func RestoreTelemetryGraph(buf []byte) (*Arena, error) {
	acc, err := OpenTelemetryGraph(buf)
	if err != nil {
		return nil, err
	}
	a := NewArena()
	a.SetRoot(restoreTelemetry(acc, a, map[dagr.SeenKey]uint64{}))
	return a, nil
}
