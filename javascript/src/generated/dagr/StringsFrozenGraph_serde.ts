// Generated by dagr/codegen/typescript/serde.py — do not edit by hand.
// Graph: StringsFrozenGraph
import { Builder, nodeOffset } from "./dagr_writer";
import type { NodeStoreRef } from "./dagr_writer";
import type { Strings } from "./StringsFrozenGraph_arena";

function _storeStrings(n: Strings<unknown>, b: Builder): NodeStoreRef {
  const _r = b.beginStoring(n._values); if (_r) return _r;
  const _c0: number | null = n.items === null ? null : b.storePtrTableArray(n.items, (b, v: string) => b.storeUtf8(v, true), false);
  if (_c0 !== null) b.storeForwardPointer(_c0);
  let _bs = 0;
  if (n.items !== null) _bs |= 1 << 0;
  b.storeBytes([(_bs >>> 0) & 0xff]);
  const _o = b.cursor;
  b.finishStoring(n._values, _o);
  return { off: _o };
}

export function writeInto(root: Strings, b: Builder): number {
  const off = nodeOffset(_storeStrings(root, b));
  return b.storeLEB((BigInt(b.cursor - off) << 2n));  // framing: rootDist<<2 | headerFlags(0)
}

export function toBytes(root: Strings, maxSize: number = 2 * 1024 * 1024): Uint8Array {
  // maxSize sets the back-reference placeholder width (2 MiB -> 4 B, 1024 -> 2 B); it must
  // match across producers for byte-identity.
  const b = new Builder(maxSize);
  writeInto(root, b);
  return b.makeData();
}
