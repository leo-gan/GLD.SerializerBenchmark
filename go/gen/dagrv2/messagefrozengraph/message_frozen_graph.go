// Code generated by dagr (dagr/codegen/go/core.py); DO NOT EDIT.

package messagefrozengraph

import "dagrv2/dagr"

type MessageAccessor struct {
	buf []byte
	pos int
	p   [8]int
}

func NewMessageAccessor(buf []byte, pos int) MessageAccessor {
	a := MessageAccessor{buf: buf, pos: pos}
	bs := dagr.ReadBitset(buf, pos, 1)
	cur := pos + 1
	if (bs>>0)&1 == 1 {
		a.p[0] = cur
		cur += 1
	} else {
		a.p[0] = -1
	}
	if (bs>>1)&1 == 1 {
		a.p[1] = cur
		cur += 4
	} else {
		a.p[1] = -1
	}
	if (bs>>2)&1 == 1 {
		a.p[2] = cur
		cur += 8
	} else {
		a.p[2] = -1
	}
	if (bs>>3)&1 == 1 {
		a.p[3] = cur
		cur += 8
	} else {
		a.p[3] = -1
	}
	if (bs>>4)&1 == 1 {
		a.p[4] = cur
		cur += dagr.V62Bytes(buf, cur)
	} else {
		a.p[4] = -1
	}
	if (bs>>5)&1 == 1 {
		a.p[5] = cur
		cur += 1
	} else {
		a.p[5] = -1
	}
	if (bs>>6)&1 == 1 {
		a.p[6] = cur
		cur += 4
	} else {
		a.p[6] = -1
	}
	if (bs>>7)&1 == 1 {
		a.p[7] = cur
		cur += dagr.V62Bytes(buf, cur)
	} else {
		a.p[7] = -1
	}
	_ = cur
	return a
}

// BufferPos is where this node starts in the buffer — its identity in a graph.
func (a MessageAccessor) BufferPos() int { return a.pos }

// BufferBytes is the buffer this accessor reads — with BufferPos, enough to reopen
// the node through another package's accessor (a DataSink does, spec/11 §12).
func (a MessageAccessor) BufferBytes() []byte { return a.buf }

func (a MessageAccessor) FBool() (bool, bool) {
	pl := a.p[0]
	if pl < 0 {
		return false, false
	}
	return dagr.ReadBool(a.buf, pl), true
}

func (a MessageAccessor) FInt32() (int32, bool) {
	pl := a.p[1]
	if pl < 0 {
		return 0, false
	}
	return dagr.ReadI32(a.buf, pl), true
}

func (a MessageAccessor) FInt64() (int64, bool) {
	pl := a.p[2]
	if pl < 0 {
		return 0, false
	}
	return dagr.ReadI64(a.buf, pl), true
}

func (a MessageAccessor) FFloat64() (float64, bool) {
	pl := a.p[3]
	if pl < 0 {
		return 0, false
	}
	return dagr.ReadF64(a.buf, pl), true
}

func (a MessageAccessor) FString() (string, bool) {
	pl := a.p[4]
	if pl < 0 {
		return "", false
	}
	s, _ := dagr.ReadUtf8(a.buf, dagr.FwdTarget(a.buf, pl))
	return s, true
}

func (a MessageAccessor) FStringBytes() ([]byte, bool) {
	pl := a.p[4]
	if pl < 0 {
		return nil, false
	}
	d, _ := dagr.ReadBytes(a.buf, dagr.FwdTarget(a.buf, pl))
	return d, true
}

func (a MessageAccessor) FBool2() (bool, bool) {
	pl := a.p[5]
	if pl < 0 {
		return false, false
	}
	return dagr.ReadBool(a.buf, pl), true
}

func (a MessageAccessor) FInt32_2() (int32, bool) {
	pl := a.p[6]
	if pl < 0 {
		return 0, false
	}
	return dagr.ReadI32(a.buf, pl), true
}

func (a MessageAccessor) FString2() (string, bool) {
	pl := a.p[7]
	if pl < 0 {
		return "", false
	}
	s, _ := dagr.ReadUtf8(a.buf, dagr.FwdTarget(a.buf, pl))
	return s, true
}

func (a MessageAccessor) FString2Bytes() ([]byte, bool) {
	pl := a.p[7]
	if pl < 0 {
		return nil, false
	}
	d, _ := dagr.ReadBytes(a.buf, dagr.FwdTarget(a.buf, pl))
	return d, true
}

func walkMessage(a MessageAccessor, seen map[dagr.SeenKey]bool) {
	sk := dagr.SeenAt(a.buf, a.pos)
	if seen[sk] {
		return
	}
	seen[sk] = true
	_, _ = a.FBool()
	_, _ = a.FInt32()
	_, _ = a.FInt64()
	_, _ = a.FFloat64()
	_, _ = a.FString()
	_, _ = a.FBool2()
	_, _ = a.FInt32_2()
	_, _ = a.FString2()
}

// ValidateMessage exercises every accessor path below a once, under recover, as Open does
// for the root: nil means reading this subtree is panic-free, malformed bytes
// yield dagr.ErrMalformed.
func ValidateMessage(a MessageAccessor) (err error) {
	defer func() {
		if r := recover(); r != nil {
			err = dagr.ErrMalformed
		}
	}()
	walkMessage(a, map[dagr.SeenKey]bool{})
	return nil
}

// OpenMessageFrozenGraph parses the framing word, then validates the whole reachable graph once
// (every accessor path is exercised under recover), so a buffer that passed Open is
// read panic-free by construction; malformed input yields dagr.ErrMalformed. A spec-15
// header is skipped. The buffer must outlive every accessor derived from it.
func OpenMessageFrozenGraph(buf []byte) (root MessageAccessor, err error) {
	root, err = OpenMessageFrozenGraphUnchecked(buf)
	if err != nil {
		return MessageAccessor{}, err
	}
	defer func() {
		if r := recover(); r != nil {
			root, err = MessageAccessor{}, dagr.ErrMalformed
		}
	}()
	walkMessage(root, map[dagr.SeenKey]bool{})
	return root, nil
}

// OpenMessageFrozenGraphUnchecked returns the root accessor after checking only the framing word —
// O(1), for buffers already known to be well-formed (a Go-written buffer, or one
// validated by an earlier Open).
func OpenMessageFrozenGraphUnchecked(buf []byte) (MessageAccessor, error) {
	root, err := dagr.RootOffset(buf)
	if err != nil {
		return MessageAccessor{}, err
	}
	return NewMessageAccessor(buf, root), nil
}
