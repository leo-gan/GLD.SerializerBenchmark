# GENERATED by dagr/codegen/mojo/core.py — do not edit by hand.
# Lazy reader for graph 'StringsFrozenPackedGraph' (regular-node scalars/utf8/data/enum/node-ref/arrays).

from dagr_reader import OwnedArray, decode_utf8_list, read_bitset, read_leb, root_offset


struct StringsAccessor[o: ImmOrigin](Copyable, Movable, ImplicitlyCopyable):
    var buf: Span[UInt8, Self.o]
    var pos: Int     # node start position (dedup key for eager restore)
    var _p0: Int   # value position of field 0 (items); -1 = absent

    def __init__(out self, buf: Span[UInt8, Self.o], start: Int) raises:
        self.buf = buf
        self.pos = start
        var r = read_leb(buf, start)
        var cursor = start + r[1]
        var pres = read_bitset(buf, cursor, 1)
        cursor += 1
        if (pres >> 0) & 1:
            self._p0 = cursor
            var _al0 = read_leb(buf, cursor)
            cursor += _al0[1] + Int(_al0[0])
        else:
            self._p0 = -1

    def items(self) raises -> Optional[OwnedArray[String]]:
        if self._p0 < 0:
            return None
        var pos = self._p0
        var _abl = read_leb(self.buf, pos)
        return OwnedArray[String](decode_utf8_list(self.buf, pos + _abl[1]))

def read_strings_root[o: ImmOrigin, //](buf: Span[UInt8, o]) raises -> StringsAccessor[o]:
    return StringsAccessor[o](buf, root_offset(buf))
