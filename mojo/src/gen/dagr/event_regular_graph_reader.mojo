# GENERATED by dagr/codegen/mojo/core.py — do not edit by hand.
# Lazy reader for graph 'EventRegularGraph' (regular-node scalars/utf8/data/enum/node-ref/arrays).

from dagr_reader import read_i64, read_leb, read_rel_offset_s, read_utf8, read_v62, root_offset, rtype_vtable_slot


@fieldwise_init
struct _Event_attrs_RefArray[o: ImmOrigin](Copyable, Movable, ImplicitlyCopyable, Sized):
    var buf: Span[UInt8, Self.o]
    var table_base: Int
    var base: Int
    var count: Int
    var es: Int

    def __len__(self) -> Int:
        return self.count

    def get(self, i: Int) raises -> EventAttrAccessor[Self.o]:
        if i < 0 or i >= self.count:
            raise Error("array index out of range")
        var ro = read_rel_offset_s(self.buf, self.table_base + i * self.es, self.es)
        if ro == 0:
            raise Error("nil slot in non-optional node array")
        return EventAttrAccessor[Self.o](self.buf, self.base + ro - 1)

    def iter(self) -> _Event_attrs_RefArrayIter[Self.o]:
        return _Event_attrs_RefArrayIter[Self.o](self, 0)


@fieldwise_init
struct _Event_attrs_RefArrayIter[o: ImmOrigin](Copyable, Movable, ImplicitlyCopyable):
    var arr: _Event_attrs_RefArray[Self.o]
    var i: Int

    def next(mut self) raises -> EventAttrAccessor[Self.o]:
        var _r = self.arr.get(self.i)
        self.i += 1
        return _r

@fieldwise_init
struct EventAttrAccessor[o: ImmOrigin](Copyable, Movable, ImplicitlyCopyable):
    var buf: Span[UInt8, Self.o]
    var pos: Int

    def key_slice(self) raises -> Optional[StringSlice[Self.o]]:
        var _slot = rtype_vtable_slot(self.buf, self.pos, 0)
        if _slot < 0:
            return None
        var sp = self.pos + _slot
        var p = read_v62(self.buf, sp)
        return Optional(read_utf8(self.buf, sp + p[1] + Int(p[0]))[0])

    def key(self) raises -> Optional[String]:
        var _slot = rtype_vtable_slot(self.buf, self.pos, 0)
        if _slot < 0:
            return None
        var sp = self.pos + _slot
        var p = read_v62(self.buf, sp)
        return String(read_utf8(self.buf, sp + p[1] + Int(p[0]))[0])

    def value_slice(self) raises -> Optional[StringSlice[Self.o]]:
        var _slot = rtype_vtable_slot(self.buf, self.pos, 1)
        if _slot < 0:
            return None
        var sp = self.pos + _slot
        var p = read_v62(self.buf, sp)
        return Optional(read_utf8(self.buf, sp + p[1] + Int(p[0]))[0])

    def value(self) raises -> Optional[String]:
        var _slot = rtype_vtable_slot(self.buf, self.pos, 1)
        if _slot < 0:
            return None
        var sp = self.pos + _slot
        var p = read_v62(self.buf, sp)
        return String(read_utf8(self.buf, sp + p[1] + Int(p[0]))[0])


@fieldwise_init
struct EventAccessor[o: ImmOrigin](Copyable, Movable, ImplicitlyCopyable):
    var buf: Span[UInt8, Self.o]
    var pos: Int

    def event_id_slice(self) raises -> Optional[StringSlice[Self.o]]:
        var _slot = rtype_vtable_slot(self.buf, self.pos, 0)
        if _slot < 0:
            return None
        var sp = self.pos + _slot
        var p = read_v62(self.buf, sp)
        return Optional(read_utf8(self.buf, sp + p[1] + Int(p[0]))[0])

    def event_id(self) raises -> Optional[String]:
        var _slot = rtype_vtable_slot(self.buf, self.pos, 0)
        if _slot < 0:
            return None
        var sp = self.pos + _slot
        var p = read_v62(self.buf, sp)
        return String(read_utf8(self.buf, sp + p[1] + Int(p[0]))[0])

    def event_type_slice(self) raises -> Optional[StringSlice[Self.o]]:
        var _slot = rtype_vtable_slot(self.buf, self.pos, 1)
        if _slot < 0:
            return None
        var sp = self.pos + _slot
        var p = read_v62(self.buf, sp)
        return Optional(read_utf8(self.buf, sp + p[1] + Int(p[0]))[0])

    def event_type(self) raises -> Optional[String]:
        var _slot = rtype_vtable_slot(self.buf, self.pos, 1)
        if _slot < 0:
            return None
        var sp = self.pos + _slot
        var p = read_v62(self.buf, sp)
        return String(read_utf8(self.buf, sp + p[1] + Int(p[0]))[0])

    def occurred_at(self) raises -> Optional[Int64]:
        var _slot = rtype_vtable_slot(self.buf, self.pos, 2)
        if _slot < 0:
            return None
        var sp = self.pos + _slot
        return read_i64(self.buf, sp)

    def producer_slice(self) raises -> Optional[StringSlice[Self.o]]:
        var _slot = rtype_vtable_slot(self.buf, self.pos, 3)
        if _slot < 0:
            return None
        var sp = self.pos + _slot
        var p = read_v62(self.buf, sp)
        return Optional(read_utf8(self.buf, sp + p[1] + Int(p[0]))[0])

    def producer(self) raises -> Optional[String]:
        var _slot = rtype_vtable_slot(self.buf, self.pos, 3)
        if _slot < 0:
            return None
        var sp = self.pos + _slot
        var p = read_v62(self.buf, sp)
        return String(read_utf8(self.buf, sp + p[1] + Int(p[0]))[0])

    def attrs(self) raises -> Optional[_Event_attrs_RefArray[Self.o]]:
        var _slot = rtype_vtable_slot(self.buf, self.pos, 4)
        if _slot < 0:
            return None
        var sp = self.pos + _slot
        var p = read_v62(self.buf, sp)
        var payload = sp + p[1] + Int(p[0])
        var h = read_leb(self.buf, payload)
        var count = Int(h[0]) >> 2
        var es = 1 << (Int(h[0]) & 3)
        var table_base = payload + h[1]
        return _Event_attrs_RefArray[Self.o](self.buf, table_base, table_base + count * es, count, es)

def read_event_root[o: ImmOrigin, //](buf: Span[UInt8, o]) raises -> EventAccessor[o]:
    return EventAccessor[o](buf, root_offset(buf))
