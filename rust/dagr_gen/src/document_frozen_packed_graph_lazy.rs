// lazy.rs — generated by dagr/codegen/rust/lazy.py
#![allow(dead_code, unused_variables, unused_mut, unused_imports)]
#![allow(unsafe_code)]
use crate::dagr_runtime;

#[derive(Debug)]
pub struct DocumentItemArrayAccessor<'a> {
    pub data: &'a [u8],
    pub slot_start: usize,
    pub es: usize,
    pub base: usize,
    pub count: usize,
}

impl<'a> DocumentItemArrayAccessor<'a> {
    pub fn empty(data: &'a [u8]) -> Self {
        Self { data, slot_start: 0, es: 1, base: 0, count: 0 }
    }
    pub fn new(data: &'a [u8], at: usize) -> Result<Self, dagr_runtime::DagrError> {
        if at >= data.len() { return Err(dagr_runtime::DagrError::InvalidData); }
        let (hdr, hl) = dagr_runtime::try_read_leb(data, at).ok_or(dagr_runtime::DagrError::InvalidData)?;
        let count = (hdr >> 2) as usize;
        let es = [1usize, 2, 4, 8][(hdr & 3) as usize];
        let slot_start = at + hl;
        let base = slot_start + count * es;
        if base > data.len() { return Err(dagr_runtime::DagrError::InvalidData); }
        Ok(Self { data, slot_start, es, base, count })
    }
    pub fn get(&self, idx: usize) -> Result<DocumentItemAccessor<'a>, dagr_runtime::DagrError> {
        if idx >= self.count { return Err(dagr_runtime::DagrError::InvalidData); }
        let slot_pos = self.slot_start + idx * self.es;
        let d = dagr_runtime::read_signed_rel_offset(self.data, slot_pos, self.es)?;
        if d == 0 { return Err(dagr_runtime::DagrError::InvalidData); }
        let target = (self.base as i64 + d - 1) as usize;
        DocumentItemAccessor::new(self.data, target)
    }
    pub fn iter(&self) -> DocumentItemArrayIter<'a> {
        DocumentItemArrayIter { data: self.data, slot_start: self.slot_start, es: self.es, base: self.base, count: self.count, idx: 0 }
    }
}

pub struct DocumentItemArrayIter<'a> {
    data: &'a [u8],
    slot_start: usize,
    es: usize,
    base: usize,
    count: usize,
    idx: usize,
}

impl<'a> Iterator for DocumentItemArrayIter<'a> {
    type Item = Result<DocumentItemAccessor<'a>, dagr_runtime::DagrError>;
    fn next(&mut self) -> Option<Self::Item> {
        if self.idx >= self.count { return None; }
        let acc = DocumentItemArrayAccessor { data: self.data, slot_start: self.slot_start, es: self.es, base: self.base, count: self.count };
        let item = acc.get(self.idx);
        self.idx += 1;
        Some(item)
    }
}

#[derive(Debug)]
pub struct DocumentItemOptArrayAccessor<'a> {
    pub data: &'a [u8],
    pub slot_start: usize,
    pub es: usize,
    pub base: usize,
    pub count: usize,
}

impl<'a> DocumentItemOptArrayAccessor<'a> {
    pub fn empty(data: &'a [u8]) -> Self {
        Self { data, slot_start: 0, es: 1, base: 0, count: 0 }
    }
    pub fn new(data: &'a [u8], at: usize) -> Result<Self, dagr_runtime::DagrError> {
        if at >= data.len() { return Err(dagr_runtime::DagrError::InvalidData); }
        let (hdr, hl) = dagr_runtime::try_read_leb(data, at).ok_or(dagr_runtime::DagrError::InvalidData)?;
        let count = (hdr >> 2) as usize;
        let es = [1usize, 2, 4, 8][(hdr & 3) as usize];
        let slot_start = at + hl;
        let base = slot_start + count * es;
        if base > data.len() { return Err(dagr_runtime::DagrError::InvalidData); }
        Ok(Self { data, slot_start, es, base, count })
    }
    pub fn get(&self, idx: usize) -> Result<Option<DocumentItemAccessor<'a>>, dagr_runtime::DagrError> {
        if idx >= self.count { return Ok(None); }
        let slot_pos = self.slot_start + idx * self.es;
        let d = dagr_runtime::read_signed_rel_offset(self.data, slot_pos, self.es)?;
        if d == 0 { return Ok(None); }
        let target = (self.base as i64 + d - 1) as usize;
        DocumentItemAccessor::new(self.data, target).map(Some)
    }
    pub fn iter(&self) -> DocumentItemOptArrayIter<'a> {
        DocumentItemOptArrayIter { data: self.data, slot_start: self.slot_start, es: self.es, base: self.base, count: self.count, idx: 0 }
    }
}

pub struct DocumentItemOptArrayIter<'a> {
    data: &'a [u8],
    slot_start: usize,
    es: usize,
    base: usize,
    count: usize,
    idx: usize,
}

impl<'a> Iterator for DocumentItemOptArrayIter<'a> {
    type Item = Result<Option<DocumentItemAccessor<'a>>, dagr_runtime::DagrError>;
    fn next(&mut self) -> Option<Self::Item> {
        if self.idx >= self.count { return None; }
        let acc = DocumentItemOptArrayAccessor { data: self.data, slot_start: self.slot_start, es: self.es, base: self.base, count: self.count };
        let item = acc.get(self.idx);
        self.idx += 1;
        Some(item)
    }
}

pub struct DocumentItemPackedNodeArray<'a> { data: &'a [u8], base: usize, count: usize }
impl<'a> DocumentItemPackedNodeArray<'a> {
    pub fn empty(data: &'a [u8]) -> Self { Self { data, base: 0, count: 0 } }
    pub fn new(data: &'a [u8], at: usize) -> Result<Self, dagr_runtime::DagrError> {
        let (count, cl) = dagr_runtime::try_read_leb(data, at).ok_or(dagr_runtime::DagrError::InvalidData)?;
        Ok(Self { data, base: at + cl, count: count as usize })
    }
    pub fn len(&self) -> usize { self.count }
    pub fn is_empty(&self) -> bool { self.count == 0 }
    pub fn iter(&self) -> DocumentItemPackedNodeArrayIter<'a> { DocumentItemPackedNodeArrayIter { data: self.data, ap: self.base, count: self.count, idx: 0 } }
}
impl<'a> IntoIterator for &DocumentItemPackedNodeArray<'a> { type Item = Result<DocumentItemAccessor<'a>, dagr_runtime::DagrError>; type IntoIter = DocumentItemPackedNodeArrayIter<'a>; fn into_iter(self) -> DocumentItemPackedNodeArrayIter<'a> { self.iter() } }
pub struct DocumentItemPackedNodeArrayIter<'a> { data: &'a [u8], ap: usize, count: usize, idx: usize }
impl<'a> Iterator for DocumentItemPackedNodeArrayIter<'a> {
    type Item = Result<DocumentItemAccessor<'a>, dagr_runtime::DagrError>;
    fn next(&mut self) -> Option<Self::Item> {
        if self.idx >= self.count { return None; }
        self.idx += 1;
        let (_el_sz, _el_sl) = match dagr_runtime::read_leb(self.data, self.ap) { Ok(v) => v, Err(e) => { self.idx = self.count; return Some(Err(e)); } };
        match DocumentItemAccessor::new(self.data, self.ap) {
            Ok(a) => { self.ap += _el_sl + _el_sz as usize; Some(Ok(a)) }
            Err(e) => { self.idx = self.count; Some(Err(e)) }
        }
    }
    fn size_hint(&self) -> (usize, Option<usize>) { let r = self.count - self.idx; (r, Some(r)) }
}
impl<'a> ExactSizeIterator for DocumentItemPackedNodeArrayIter<'a> {}

pub struct DocumentItemPackedNodeOptArray<'a> { data: &'a [u8], nil_base: usize, base: usize, count: usize }
impl<'a> DocumentItemPackedNodeOptArray<'a> {
    pub fn empty(data: &'a [u8]) -> Self { Self { data, nil_base: 0, base: 0, count: 0 } }
    pub fn new(data: &'a [u8], at: usize) -> Result<Self, dagr_runtime::DagrError> {
        let (count, cl) = dagr_runtime::try_read_leb(data, at).ok_or(dagr_runtime::DagrError::InvalidData)?;
        let count = count as usize;
        let nil_base = at + cl;
        Ok(Self { data, nil_base, base: nil_base + (count + 7) / 8, count })
    }
    pub fn len(&self) -> usize { self.count }
    pub fn is_empty(&self) -> bool { self.count == 0 }
    pub fn iter(&self) -> DocumentItemPackedNodeOptArrayIter<'a> { DocumentItemPackedNodeOptArrayIter { data: self.data, nil_base: self.nil_base, ap: self.base, count: self.count, idx: 0 } }
}
impl<'a> IntoIterator for &DocumentItemPackedNodeOptArray<'a> { type Item = Result<Option<DocumentItemAccessor<'a>>, dagr_runtime::DagrError>; type IntoIter = DocumentItemPackedNodeOptArrayIter<'a>; fn into_iter(self) -> DocumentItemPackedNodeOptArrayIter<'a> { self.iter() } }
pub struct DocumentItemPackedNodeOptArrayIter<'a> { data: &'a [u8], nil_base: usize, ap: usize, count: usize, idx: usize }
impl<'a> Iterator for DocumentItemPackedNodeOptArrayIter<'a> {
    type Item = Result<Option<DocumentItemAccessor<'a>>, dagr_runtime::DagrError>;
    fn next(&mut self) -> Option<Self::Item> {
        if self.idx >= self.count { return None; }
        let k = self.idx; self.idx += 1;
        if self.data.get(self.nil_base + k / 8).map_or(false, |&b| (b >> (k % 8)) & 1 != 0) { return Some(Ok(None)); }
        let (_el_sz, _el_sl) = match dagr_runtime::read_leb(self.data, self.ap) { Ok(v) => v, Err(e) => { self.idx = self.count; return Some(Err(e)); } };
        match DocumentItemAccessor::new(self.data, self.ap) {
            Ok(a) => { self.ap += _el_sl + _el_sz as usize; Some(Ok(Some(a))) }
            Err(e) => { self.idx = self.count; Some(Err(e)) }
        }
    }
    fn size_hint(&self) -> (usize, Option<usize>) { let r = self.count - self.idx; (r, Some(r)) }
}
impl<'a> ExactSizeIterator for DocumentItemPackedNodeOptArrayIter<'a> {}

pub struct DocumentMetaAccessor<'a> {
    data: &'a [u8],
    _at: usize,
    v1: Option<i32>,
    r0: Option<core::ops::Range<usize>>,
}

impl<'a> DocumentMetaAccessor<'a> {
    pub fn new(data: &'a [u8], at: usize) -> Result<Self, dagr_runtime::DagrError> {
        if at >= data.len() { return Err(dagr_runtime::DagrError::InvalidData); }
        let (_, _fp_leb_sz) = dagr_runtime::read_leb(data, at)?;
        let _start = at + _fp_leb_sz;
        let mut v1: Option<i32> = None;
        let mut r0: Option<core::ops::Range<usize>> = None;
        let _obs0 = data.get(_start + 0).copied().unwrap_or(0);
        let _ebs0 = data.get(_start + 1).copied().unwrap_or(0);
        let mut _cur = _start + 2;
        if _obs0 & 1 != 0 {
            if let Ok((_bsz, _blb)) = dagr_runtime::read_leb(data, _cur) { let _s = _cur + _blb; if let Some(_e) = _s.checked_add(_bsz as usize) { if _e <= data.len() { r0 = Some(_s.._e); } } _cur = _s.saturating_add(_bsz as usize); }
        }
        if _obs0 & 2 != 0 {
            if _ebs0 & 1 != 0 {
                let _v = dagr_runtime::read_i32(data, _cur)? as i32; _cur += 4;
                v1 = Some(_v);
            } else {
                let (_lv, _lb) = dagr_runtime::read_leb(data, _cur)?; _cur += _lb;
                v1 = Some(dagr_runtime::from_zigzag(_lv) as i32);
            }
        }
        let _ = _cur;
        Ok(Self { data, _at: at, v1, r0 })
    }
    /// Byte offset of this node in the buffer — its within-buffer identity.
    pub fn _start(&self) -> usize { self._at }
    pub fn _has_region(&self) -> bool { self.r0.is_some() }
    pub fn _has_version(&self) -> bool { self.v1.is_some() }

    pub fn version(&self) -> Result<i32, dagr_runtime::DagrError> {
        self.v1.ok_or(dagr_runtime::DagrError::InvalidData)
    }

    pub fn region(&self) -> Option<&'a str> {
        dagr_runtime::str_from(&self.data[self.r0.clone()?])
    }

}

pub struct DocumentItemAccessor<'a> {
    data: &'a [u8],
    _at: usize,
    v1: Option<i32>,
    v2: Option<i64>,
    r0: Option<core::ops::Range<usize>>,
}

impl<'a> DocumentItemAccessor<'a> {
    pub fn new(data: &'a [u8], at: usize) -> Result<Self, dagr_runtime::DagrError> {
        if at >= data.len() { return Err(dagr_runtime::DagrError::InvalidData); }
        let (_, _fp_leb_sz) = dagr_runtime::read_leb(data, at)?;
        let _start = at + _fp_leb_sz;
        let mut v1: Option<i32> = None;
        let mut v2: Option<i64> = None;
        let mut r0: Option<core::ops::Range<usize>> = None;
        let _obs0 = data.get(_start + 0).copied().unwrap_or(0);
        let _ebs0 = data.get(_start + 1).copied().unwrap_or(0);
        let mut _cur = _start + 2;
        if _obs0 & 1 != 0 {
            if let Ok((_bsz, _blb)) = dagr_runtime::read_leb(data, _cur) { let _s = _cur + _blb; if let Some(_e) = _s.checked_add(_bsz as usize) { if _e <= data.len() { r0 = Some(_s.._e); } } _cur = _s.saturating_add(_bsz as usize); }
        }
        if _obs0 & 2 != 0 {
            if _ebs0 & 1 != 0 {
                let _v = dagr_runtime::read_i32(data, _cur)? as i32; _cur += 4;
                v1 = Some(_v);
            } else {
                let (_lv, _lb) = dagr_runtime::read_leb(data, _cur)?; _cur += _lb;
                v1 = Some(dagr_runtime::from_zigzag(_lv) as i32);
            }
        }
        if _obs0 & 4 != 0 {
            if _ebs0 & 2 != 0 {
                let _v = dagr_runtime::read_i64(data, _cur)? as i64; _cur += 8;
                v2 = Some(_v);
            } else {
                let (_lv, _lb) = dagr_runtime::read_leb(data, _cur)?; _cur += _lb;
                v2 = Some(dagr_runtime::from_zigzag(_lv) as i64);
            }
        }
        let _ = _cur;
        Ok(Self { data, _at: at, v1, v2, r0 })
    }
    /// Byte offset of this node in the buffer — its within-buffer identity.
    pub fn _start(&self) -> usize { self._at }
    pub fn _has_sku(&self) -> bool { self.r0.is_some() }
    pub fn _has_qty(&self) -> bool { self.v1.is_some() }
    pub fn _has_price_minor(&self) -> bool { self.v2.is_some() }

    pub fn qty(&self) -> Result<i32, dagr_runtime::DagrError> {
        self.v1.ok_or(dagr_runtime::DagrError::InvalidData)
    }

    pub fn price_minor(&self) -> Result<i64, dagr_runtime::DagrError> {
        self.v2.ok_or(dagr_runtime::DagrError::InvalidData)
    }

    pub fn sku(&self) -> Option<&'a str> {
        dagr_runtime::str_from(&self.data[self.r0.clone()?])
    }

}

pub struct DocumentAccessor<'a> {
    data: &'a [u8],
    _at: usize,
    v1: Option<i32>,
    r0: Option<core::ops::Range<usize>>,
    n2: Option<usize>,  // nested packed node position ([size LEB] start)
    a3: Option<usize>,
}

impl<'a> DocumentAccessor<'a> {
    pub fn new(data: &'a [u8], at: usize) -> Result<Self, dagr_runtime::DagrError> {
        if at >= data.len() { return Err(dagr_runtime::DagrError::InvalidData); }
        let (_, _fp_leb_sz) = dagr_runtime::read_leb(data, at)?;
        let _start = at + _fp_leb_sz;
        let mut v1: Option<i32> = None;
        let mut r0: Option<core::ops::Range<usize>> = None;
        let mut n2: Option<usize> = None;
        let mut a3: Option<usize> = None;
        let _obs0 = data.get(_start + 0).copied().unwrap_or(0);
        let _ebs0 = data.get(_start + 1).copied().unwrap_or(0);
        let mut _cur = _start + 2;
        if _obs0 & 1 != 0 {
            if let Ok((_bsz, _blb)) = dagr_runtime::read_leb(data, _cur) { let _s = _cur + _blb; if let Some(_e) = _s.checked_add(_bsz as usize) { if _e <= data.len() { r0 = Some(_s.._e); } } _cur = _s.saturating_add(_bsz as usize); }
        }
        if _obs0 & 2 != 0 {
            if _ebs0 & 1 != 0 {
                let _v = dagr_runtime::read_i32(data, _cur)? as i32; _cur += 4;
                v1 = Some(_v);
            } else {
                let (_lv, _lb) = dagr_runtime::read_leb(data, _cur)?; _cur += _lb;
                v1 = Some(dagr_runtime::from_zigzag(_lv) as i32);
            }
        }
        if _obs0 & 4 != 0 {
            n2 = Some(_cur);
            if let Ok((_bsz, _blb)) = dagr_runtime::read_leb(data, _cur) { _cur += _blb + _bsz as usize; }
        }
        if _obs0 & 8 != 0 {
            if let Ok((_bsz3, _blb3)) = dagr_runtime::read_leb(data, _cur) {
                a3 = Some(_cur + _blb3);
                _cur += _blb3 + _bsz3 as usize;
            }
        }
        let _ = _cur;
        Ok(Self { data, _at: at, v1, r0, n2, a3 })
    }
    /// Byte offset of this node in the buffer — its within-buffer identity.
    pub fn _start(&self) -> usize { self._at }
    pub fn _has_id(&self) -> bool { self.r0.is_some() }
    pub fn _has_status(&self) -> bool { self.v1.is_some() }
    pub fn _has_meta(&self) -> bool { self.n2.is_some() }
    pub fn _has_items(&self) -> bool { self.a3.is_some() }

    pub fn status(&self) -> Result<i32, dagr_runtime::DagrError> {
        self.v1.ok_or(dagr_runtime::DagrError::InvalidData)
    }

    pub fn id(&self) -> Option<&'a str> {
        dagr_runtime::str_from(&self.data[self.r0.clone()?])
    }

    pub fn meta(&self) -> Option<DocumentMetaAccessor<'a>> {
        DocumentMetaAccessor::new(self.data, self.n2?).ok()
    }

    pub fn items(&self) -> Result<DocumentItemPackedNodeArray<'a>, dagr_runtime::DagrError> {
        let pos = match self.a3 { None => return Ok(DocumentItemPackedNodeArray::empty(self.data)), Some(p) => p };
        DocumentItemPackedNodeArray::new(self.data, pos)
    }

}

pub fn read_root(data: &[u8]) -> Result<DocumentAccessor<'_>, dagr_runtime::DagrError> {
    if data.is_empty() { return Err(dagr_runtime::DagrError::InvalidData); }
    let (root_dist, rl) = dagr_runtime::read_leb(data, 0)?;
    if (root_dist & 1) != 0 { return Err(dagr_runtime::DagrError::InvalidData); }
    let _at = rl + (root_dist >> 2) as usize;
    if _at >= data.len() { return Err(dagr_runtime::DagrError::InvalidData); }
    DocumentAccessor::new(data, _at)
}
