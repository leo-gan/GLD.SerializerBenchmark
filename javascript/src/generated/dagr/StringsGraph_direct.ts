// Generated by dagr/codegen/typescript/direct.py — do not edit by hand.
// Graph: StringsGraph — Direct Graph Builder ("spec/33-direct-graph-builder.md")
import { Builder, nodeOffset } from "./dagr_writer";
import type { NodeStoreRef } from "./dagr_writer";

export interface Strings {
  items: string[] | null;
}

function _storeStrings(n: Strings, b: Builder): NodeStoreRef {
  const _before = b.cursor;
  if (n.items !== null) { b.storePackedComplexArray(n.items, (b, v: string) => b.storeUtf8(v, false), false); b.storeLEB((0 << 1) | 1); }
  b.storeLEB(b.cursor - _before);  // blockLen (front of the record)
  const _o = b.cursor;
  return { off: _o };
}

export function writeInto(root: Strings, b: Builder): number {
  const off = nodeOffset(_storeStrings(root, b));
  return b.storeLEB((b.cursor - off) * 4);  // framing: rootDist<<2 | headerFlags(0)
}

export function toBytes(root: Strings, maxSize: number = 2 * 1024 * 1024): Uint8Array {
  const b = new Builder(maxSize, 1024);   // direct = tree → small buffer, grows if needed
  writeInto(root, b);
  return b.makeData();
}
