# GENERATED by dagr/codegen/mojo/core.py — do not edit by hand.
# Lazy reader for graph 'MessageRegularGraph' (regular-node scalars/utf8/data/enum/node-ref/arrays).

from dagr_reader import read_bool, read_f64, read_i32, read_i64, read_utf8, read_v62, root_offset, rtype_vtable_slot


@fieldwise_init
struct MessageAccessor[o: ImmOrigin](Copyable, Movable, ImplicitlyCopyable):
    var buf: Span[UInt8, Self.o]
    var pos: Int

    def f_bool(self) raises -> Optional[Bool]:
        var _slot = rtype_vtable_slot(self.buf, self.pos, 0)
        if _slot < 0:
            return None
        var sp = self.pos + _slot
        return read_bool(self.buf, sp)

    def f_int32(self) raises -> Optional[Int32]:
        var _slot = rtype_vtable_slot(self.buf, self.pos, 1)
        if _slot < 0:
            return None
        var sp = self.pos + _slot
        return read_i32(self.buf, sp)

    def f_int64(self) raises -> Optional[Int64]:
        var _slot = rtype_vtable_slot(self.buf, self.pos, 2)
        if _slot < 0:
            return None
        var sp = self.pos + _slot
        return read_i64(self.buf, sp)

    def f_float64(self) raises -> Optional[Float64]:
        var _slot = rtype_vtable_slot(self.buf, self.pos, 3)
        if _slot < 0:
            return None
        var sp = self.pos + _slot
        return read_f64(self.buf, sp)

    def f_string_slice(self) raises -> Optional[StringSlice[Self.o]]:
        var _slot = rtype_vtable_slot(self.buf, self.pos, 4)
        if _slot < 0:
            return None
        var sp = self.pos + _slot
        var p = read_v62(self.buf, sp)
        return Optional(read_utf8(self.buf, sp + p[1] + Int(p[0]))[0])

    def f_string(self) raises -> Optional[String]:
        var _slot = rtype_vtable_slot(self.buf, self.pos, 4)
        if _slot < 0:
            return None
        var sp = self.pos + _slot
        var p = read_v62(self.buf, sp)
        return String(read_utf8(self.buf, sp + p[1] + Int(p[0]))[0])

    def f_bool_2(self) raises -> Optional[Bool]:
        var _slot = rtype_vtable_slot(self.buf, self.pos, 5)
        if _slot < 0:
            return None
        var sp = self.pos + _slot
        return read_bool(self.buf, sp)

    def f_int32_2(self) raises -> Optional[Int32]:
        var _slot = rtype_vtable_slot(self.buf, self.pos, 6)
        if _slot < 0:
            return None
        var sp = self.pos + _slot
        return read_i32(self.buf, sp)

    def f_string_2_slice(self) raises -> Optional[StringSlice[Self.o]]:
        var _slot = rtype_vtable_slot(self.buf, self.pos, 7)
        if _slot < 0:
            return None
        var sp = self.pos + _slot
        var p = read_v62(self.buf, sp)
        return Optional(read_utf8(self.buf, sp + p[1] + Int(p[0]))[0])

    def f_string_2(self) raises -> Optional[String]:
        var _slot = rtype_vtable_slot(self.buf, self.pos, 7)
        if _slot < 0:
            return None
        var sp = self.pos + _slot
        var p = read_v62(self.buf, sp)
        return String(read_utf8(self.buf, sp + p[1] + Int(p[0]))[0])

def read_message_root[o: ImmOrigin, //](buf: Span[UInt8, o]) raises -> MessageAccessor[o]:
    return MessageAccessor[o](buf, root_offset(buf))
