# GENERATED by dagr/codegen/mojo/restore.py — do not edit by hand.
# Eager restore (bytes -> arena) for graph 'StringsFrozenGraph' — plan 28 Phase 3.

from strings_frozen_graph_arena import StringsFrozenGraphArena, Strings
from strings_frozen_graph_reader import StringsAccessor
from dagr_reader import root_offset
from std.collections import Dict


def restore_strings_graph[o: ImmOrigin](buf: Span[UInt8, o]) raises -> StringsFrozenGraphArena:
    var a = StringsFrozenGraphArena()
    var seen = Dict[Int, UInt64]()
    var racc = StringsAccessor[o](buf, root_offset(buf))
    var rh = _restore_strings(a, racc, seen)
    a.set_root(rh)
    return a^


def _restore_strings[co: ImmOrigin, ao: Origin[mut=False], //](ref [ao] a: StringsFrozenGraphArena, acc: StringsAccessor[co], mut seen: Dict[Int, UInt64]) raises -> Strings[ao]:
    var _pos = acc.pos
    if _pos in seen:
        return Strings[ao](Pointer(to=a), seen[_pos])
    var h = a.new_strings()
    var i = h._index()
    var _wa = Pointer[StringsFrozenGraphArena, MutAnyOrigin](unsafe_from_address=Int(Pointer(to=a)))
    seen[_pos] = h._packed
    var _oa0 = acc.items()
    if _oa0:
        var _l0 = List[String]()
        for _k0 in range(len(_oa0.value())):
            _l0.append(_oa0.value().get(_k0))
        _wa[]._arr_strings[i].items = _l0^
    else:
        _wa[]._arr_strings[i].items = List[String]()
    return h


