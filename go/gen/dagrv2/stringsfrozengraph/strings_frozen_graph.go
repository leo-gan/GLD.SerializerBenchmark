// Code generated by dagr (dagr/codegen/go/core.py); DO NOT EDIT.

package stringsfrozengraph

import (
	"iter"

	"dagrv2/dagr"
)

type StringsAccessor struct {
	buf []byte
	pos int
	p   [1]int
}

func NewStringsAccessor(buf []byte, pos int) StringsAccessor {
	a := StringsAccessor{buf: buf, pos: pos}
	bs := dagr.ReadBitset(buf, pos, 1)
	cur := pos + 1
	if (bs>>0)&1 == 1 {
		a.p[0] = cur
		cur += dagr.V62Bytes(buf, cur)
	} else {
		a.p[0] = -1
	}
	_ = cur
	return a
}

// BufferPos is where this node starts in the buffer — its identity in a graph.
func (a StringsAccessor) BufferPos() int { return a.pos }

// BufferBytes is the buffer this accessor reads — with BufferPos, enough to reopen
// the node through another package's accessor (a DataSink does, spec/11 §12).
func (a StringsAccessor) BufferBytes() []byte { return a.buf }

func (a StringsAccessor) ItemsLen() (int, bool) {
	pl := a.p[0]
	if pl < 0 {
		return 0, false
	}
	_, _, count, _ := dagr.PtrTable(a.buf, pl)
	return count, true
}

func (a StringsAccessor) ItemsAt(i int) string {
	pl := a.p[0]
	tb, db, _, es := dagr.PtrTable(a.buf, pl)
	ro := dagr.ReadUintLE(a.buf, tb+i*es, es)
	s, _ := dagr.ReadUtf8(a.buf, db+ro-1)
	return s
}

type StringsItemsIter struct {
	buf []byte
	i   int
	n   int
	tb  int
	db  int
	es  int
}

// ItemsIter resolves the array once; Next reports ok=false past the end.
func (a StringsAccessor) ItemsIter() StringsItemsIter {
	pl := a.p[0]
	if pl < 0 {
		return StringsItemsIter{}
	}
	tb, db, n, es := dagr.PtrTable(a.buf, pl)
	return StringsItemsIter{buf: a.buf, n: n, tb: tb, db: db, es: es}
}

func (it *StringsItemsIter) at(i int) string {
	buf := it.buf
	tb := it.tb
	db := it.db
	es := it.es
	ro := dagr.ReadUintLE(buf, tb+i*es, es)
	s, _ := dagr.ReadUtf8(buf, db+ro-1)
	return s
}

func (it *StringsItemsIter) Next() (string, bool) {
	if it.i >= it.n {
		var z string
		return z, false
	}
	v := it.at(it.i)
	it.i++
	return v, true
}

func (it *StringsItemsIter) Len() int { return it.n }

// ItemsAll ranges over the array in one pass.
func (a StringsAccessor) ItemsAll() iter.Seq[string] {
	return func(yield func(string) bool) {
		it := a.ItemsIter()
		for {
			v, ok := it.Next()
			if !ok || !yield(v) {
				return
			}
		}
	}
}

func walkStrings(a StringsAccessor, seen map[dagr.SeenKey]bool) {
	sk := dagr.SeenAt(a.buf, a.pos)
	if seen[sk] {
		return
	}
	seen[sk] = true
	{
		if n, ok := a.ItemsLen(); ok {
			for i := 0; i < n; i++ {
				_ = a.ItemsAt(i)
			}
		}
	}
}

// ValidateStrings exercises every accessor path below a once, under recover, as Open does
// for the root: nil means reading this subtree is panic-free, malformed bytes
// yield dagr.ErrMalformed.
func ValidateStrings(a StringsAccessor) (err error) {
	defer func() {
		if r := recover(); r != nil {
			err = dagr.ErrMalformed
		}
	}()
	walkStrings(a, map[dagr.SeenKey]bool{})
	return nil
}

// OpenStringsFrozenGraph parses the framing word, then validates the whole reachable graph once
// (every accessor path is exercised under recover), so a buffer that passed Open is
// read panic-free by construction; malformed input yields dagr.ErrMalformed. A spec-15
// header is skipped. The buffer must outlive every accessor derived from it.
func OpenStringsFrozenGraph(buf []byte) (root StringsAccessor, err error) {
	root, err = OpenStringsFrozenGraphUnchecked(buf)
	if err != nil {
		return StringsAccessor{}, err
	}
	defer func() {
		if r := recover(); r != nil {
			root, err = StringsAccessor{}, dagr.ErrMalformed
		}
	}()
	walkStrings(root, map[dagr.SeenKey]bool{})
	return root, nil
}

// OpenStringsFrozenGraphUnchecked returns the root accessor after checking only the framing word —
// O(1), for buffers already known to be well-formed (a Go-written buffer, or one
// validated by an earlier Open).
func OpenStringsFrozenGraphUnchecked(buf []byte) (StringsAccessor, error) {
	root, err := dagr.RootOffset(buf)
	if err != nil {
		return StringsAccessor{}, err
	}
	return NewStringsAccessor(buf, root), nil
}
