// Code generated by dagr (dagr/codegen/go/core.py); DO NOT EDIT.

package documentfrozengraph

import (
	"iter"

	"dagrv2/dagr"
)

type DocumentMetaAccessor struct {
	buf []byte
	pos int
	p   [2]int
}

func NewDocumentMetaAccessor(buf []byte, pos int) DocumentMetaAccessor {
	a := DocumentMetaAccessor{buf: buf, pos: pos}
	bs := dagr.ReadBitset(buf, pos, 1)
	cur := pos + 1
	if (bs>>0)&1 == 1 {
		a.p[0] = cur
		cur += dagr.V62Bytes(buf, cur)
	} else {
		a.p[0] = -1
	}
	if (bs>>1)&1 == 1 {
		a.p[1] = cur
		cur += 4
	} else {
		a.p[1] = -1
	}
	_ = cur
	return a
}

// BufferPos is where this node starts in the buffer — its identity in a graph.
func (a DocumentMetaAccessor) BufferPos() int { return a.pos }

// BufferBytes is the buffer this accessor reads — with BufferPos, enough to reopen
// the node through another package's accessor (a DataSink does, spec/11 §12).
func (a DocumentMetaAccessor) BufferBytes() []byte { return a.buf }

func (a DocumentMetaAccessor) Region() (string, bool) {
	pl := a.p[0]
	if pl < 0 {
		return "", false
	}
	s, _ := dagr.ReadUtf8(a.buf, dagr.FwdTarget(a.buf, pl))
	return s, true
}

func (a DocumentMetaAccessor) RegionBytes() ([]byte, bool) {
	pl := a.p[0]
	if pl < 0 {
		return nil, false
	}
	d, _ := dagr.ReadBytes(a.buf, dagr.FwdTarget(a.buf, pl))
	return d, true
}

func (a DocumentMetaAccessor) Version() (int32, bool) {
	pl := a.p[1]
	if pl < 0 {
		return 0, false
	}
	return dagr.ReadI32(a.buf, pl), true
}

type DocumentItemAccessor struct {
	buf []byte
	pos int
	p   [3]int
}

func NewDocumentItemAccessor(buf []byte, pos int) DocumentItemAccessor {
	a := DocumentItemAccessor{buf: buf, pos: pos}
	bs := dagr.ReadBitset(buf, pos, 1)
	cur := pos + 1
	if (bs>>0)&1 == 1 {
		a.p[0] = cur
		cur += dagr.V62Bytes(buf, cur)
	} else {
		a.p[0] = -1
	}
	if (bs>>1)&1 == 1 {
		a.p[1] = cur
		cur += 4
	} else {
		a.p[1] = -1
	}
	if (bs>>2)&1 == 1 {
		a.p[2] = cur
		cur += 8
	} else {
		a.p[2] = -1
	}
	_ = cur
	return a
}

// BufferPos is where this node starts in the buffer — its identity in a graph.
func (a DocumentItemAccessor) BufferPos() int { return a.pos }

// BufferBytes is the buffer this accessor reads — with BufferPos, enough to reopen
// the node through another package's accessor (a DataSink does, spec/11 §12).
func (a DocumentItemAccessor) BufferBytes() []byte { return a.buf }

func (a DocumentItemAccessor) Sku() (string, bool) {
	pl := a.p[0]
	if pl < 0 {
		return "", false
	}
	s, _ := dagr.ReadUtf8(a.buf, dagr.FwdTarget(a.buf, pl))
	return s, true
}

func (a DocumentItemAccessor) SkuBytes() ([]byte, bool) {
	pl := a.p[0]
	if pl < 0 {
		return nil, false
	}
	d, _ := dagr.ReadBytes(a.buf, dagr.FwdTarget(a.buf, pl))
	return d, true
}

func (a DocumentItemAccessor) Qty() (int32, bool) {
	pl := a.p[1]
	if pl < 0 {
		return 0, false
	}
	return dagr.ReadI32(a.buf, pl), true
}

func (a DocumentItemAccessor) PriceMinor() (int64, bool) {
	pl := a.p[2]
	if pl < 0 {
		return 0, false
	}
	return dagr.ReadI64(a.buf, pl), true
}

type DocumentAccessor struct {
	buf []byte
	pos int
	p   [4]int
}

func NewDocumentAccessor(buf []byte, pos int) DocumentAccessor {
	a := DocumentAccessor{buf: buf, pos: pos}
	bs := dagr.ReadBitset(buf, pos, 1)
	cur := pos + 1
	if (bs>>0)&1 == 1 {
		a.p[0] = cur
		cur += dagr.V62Bytes(buf, cur)
	} else {
		a.p[0] = -1
	}
	if (bs>>1)&1 == 1 {
		a.p[1] = cur
		cur += 4
	} else {
		a.p[1] = -1
	}
	if (bs>>2)&1 == 1 {
		a.p[2] = cur
		cur += dagr.V62Bytes(buf, cur)
	} else {
		a.p[2] = -1
	}
	if (bs>>3)&1 == 1 {
		a.p[3] = cur
		cur += dagr.V62Bytes(buf, cur)
	} else {
		a.p[3] = -1
	}
	_ = cur
	return a
}

// BufferPos is where this node starts in the buffer — its identity in a graph.
func (a DocumentAccessor) BufferPos() int { return a.pos }

// BufferBytes is the buffer this accessor reads — with BufferPos, enough to reopen
// the node through another package's accessor (a DataSink does, spec/11 §12).
func (a DocumentAccessor) BufferBytes() []byte { return a.buf }

func (a DocumentAccessor) ID() (string, bool) {
	pl := a.p[0]
	if pl < 0 {
		return "", false
	}
	s, _ := dagr.ReadUtf8(a.buf, dagr.FwdTarget(a.buf, pl))
	return s, true
}

func (a DocumentAccessor) IDBytes() ([]byte, bool) {
	pl := a.p[0]
	if pl < 0 {
		return nil, false
	}
	d, _ := dagr.ReadBytes(a.buf, dagr.FwdTarget(a.buf, pl))
	return d, true
}

func (a DocumentAccessor) Status() (int32, bool) {
	pl := a.p[1]
	if pl < 0 {
		return 0, false
	}
	return dagr.ReadI32(a.buf, pl), true
}

func (a DocumentAccessor) Meta() (DocumentMetaAccessor, bool) {
	pl := a.p[2]
	if pl < 0 {
		return DocumentMetaAccessor{}, false
	}
	bd, bdB := dagr.ReadZigZagV62(a.buf, pl)
	return NewDocumentMetaAccessor(a.buf, pl+bdB+int(bd)), true
}

func (a DocumentAccessor) ItemsLen() (int, bool) {
	pl := a.p[3]
	if pl < 0 {
		return 0, false
	}
	_, _, count, _ := dagr.PtrTable(a.buf, pl)
	return count, true
}

func (a DocumentAccessor) ItemsAt(i int) DocumentItemAccessor {
	pl := a.p[3]
	tb, db, _, es := dagr.PtrTable(a.buf, pl)
	ro := dagr.ReadIntLE(a.buf, tb+i*es, es)
	return NewDocumentItemAccessor(a.buf, db+ro-1)
}

type DocumentItemsIter struct {
	buf []byte
	i   int
	n   int
	tb  int
	db  int
	es  int
}

// ItemsIter resolves the array once; Next reports ok=false past the end.
func (a DocumentAccessor) ItemsIter() DocumentItemsIter {
	pl := a.p[3]
	if pl < 0 {
		return DocumentItemsIter{}
	}
	tb, db, n, es := dagr.PtrTable(a.buf, pl)
	return DocumentItemsIter{buf: a.buf, n: n, tb: tb, db: db, es: es}
}

func (it *DocumentItemsIter) at(i int) DocumentItemAccessor {
	buf := it.buf
	tb := it.tb
	db := it.db
	es := it.es
	ro := dagr.ReadIntLE(buf, tb+i*es, es)
	return NewDocumentItemAccessor(buf, db+ro-1)
}

func (it *DocumentItemsIter) Next() (DocumentItemAccessor, bool) {
	if it.i >= it.n {
		var z DocumentItemAccessor
		return z, false
	}
	v := it.at(it.i)
	it.i++
	return v, true
}

func (it *DocumentItemsIter) Len() int { return it.n }

// ItemsAll ranges over the array in one pass.
func (a DocumentAccessor) ItemsAll() iter.Seq[DocumentItemAccessor] {
	return func(yield func(DocumentItemAccessor) bool) {
		it := a.ItemsIter()
		for {
			v, ok := it.Next()
			if !ok || !yield(v) {
				return
			}
		}
	}
}

func walkDocumentMeta(a DocumentMetaAccessor, seen map[dagr.SeenKey]bool) {
	sk := dagr.SeenAt(a.buf, a.pos)
	if seen[sk] {
		return
	}
	seen[sk] = true
	_, _ = a.Region()
	_, _ = a.Version()
}

// ValidateDocumentMeta exercises every accessor path below a once, under recover, as Open does
// for the root: nil means reading this subtree is panic-free, malformed bytes
// yield dagr.ErrMalformed.
func ValidateDocumentMeta(a DocumentMetaAccessor) (err error) {
	defer func() {
		if r := recover(); r != nil {
			err = dagr.ErrMalformed
		}
	}()
	walkDocumentMeta(a, map[dagr.SeenKey]bool{})
	return nil
}

func walkDocumentItem(a DocumentItemAccessor, seen map[dagr.SeenKey]bool) {
	sk := dagr.SeenAt(a.buf, a.pos)
	if seen[sk] {
		return
	}
	seen[sk] = true
	_, _ = a.Sku()
	_, _ = a.Qty()
	_, _ = a.PriceMinor()
}

// ValidateDocumentItem exercises every accessor path below a once, under recover, as Open does
// for the root: nil means reading this subtree is panic-free, malformed bytes
// yield dagr.ErrMalformed.
func ValidateDocumentItem(a DocumentItemAccessor) (err error) {
	defer func() {
		if r := recover(); r != nil {
			err = dagr.ErrMalformed
		}
	}()
	walkDocumentItem(a, map[dagr.SeenKey]bool{})
	return nil
}

func walkDocument(a DocumentAccessor, seen map[dagr.SeenKey]bool) {
	sk := dagr.SeenAt(a.buf, a.pos)
	if seen[sk] {
		return
	}
	seen[sk] = true
	_, _ = a.ID()
	_, _ = a.Status()
	{
		if c, ok := a.Meta(); ok {
			walkDocumentMeta(c, seen)
		}
	}
	{
		if n, ok := a.ItemsLen(); ok {
			for i := 0; i < n; i++ {
				walkDocumentItem(a.ItemsAt(i), seen)
			}
		}
	}
}

// ValidateDocument exercises every accessor path below a once, under recover, as Open does
// for the root: nil means reading this subtree is panic-free, malformed bytes
// yield dagr.ErrMalformed.
func ValidateDocument(a DocumentAccessor) (err error) {
	defer func() {
		if r := recover(); r != nil {
			err = dagr.ErrMalformed
		}
	}()
	walkDocument(a, map[dagr.SeenKey]bool{})
	return nil
}

// OpenDocumentFrozenGraph parses the framing word, then validates the whole reachable graph once
// (every accessor path is exercised under recover), so a buffer that passed Open is
// read panic-free by construction; malformed input yields dagr.ErrMalformed. A spec-15
// header is skipped. The buffer must outlive every accessor derived from it.
func OpenDocumentFrozenGraph(buf []byte) (root DocumentAccessor, err error) {
	root, err = OpenDocumentFrozenGraphUnchecked(buf)
	if err != nil {
		return DocumentAccessor{}, err
	}
	defer func() {
		if r := recover(); r != nil {
			root, err = DocumentAccessor{}, dagr.ErrMalformed
		}
	}()
	walkDocument(root, map[dagr.SeenKey]bool{})
	return root, nil
}

// OpenDocumentFrozenGraphUnchecked returns the root accessor after checking only the framing word —
// O(1), for buffers already known to be well-formed (a Go-written buffer, or one
// validated by an earlier Open).
func OpenDocumentFrozenGraphUnchecked(buf []byte) (DocumentAccessor, error) {
	root, err := dagr.RootOffset(buf)
	if err != nil {
		return DocumentAccessor{}, err
	}
	return NewDocumentAccessor(buf, root), nil
}
