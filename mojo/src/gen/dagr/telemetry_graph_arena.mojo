# GENERATED by dagr/codegen/mojo/arena.py — do not edit by hand.
# Arena (in-memory graph) + deletion + cycle-aware structural methods — plan 28
# Phase 2, Design H. A handle `{Node}[o]` carries a SHARED safe Pointer to its arena
# (origin-typed) + a packed index; getters/setters/structural/delete are HANDLE
# methods (Rust/Swift-identical call site: `alice.name()`, `alice.set_age(31)`,
# `alice.equals(bob)`). Reads go through the safe pointer; writes/factories mutate
# through an Pointer interior-mutability escape. Two arenas get two origins →
# cross-arena handle use is a compile error (no brand needed). Soundness contract:
# a handle borrows the arena (shares its origin) — the arena must not MOVE while
# handles are live (like Rust's borrow / Swift's heap-stable class).

from std.collections import Set
from std.hashlib import Hasher

comptime IDX_MASK = (UInt64(1) << 40) - 1


def _pack(gen: UInt32, idx: Int) -> UInt64:
    return (UInt64(gen) << 40) | (UInt64(idx) & IDX_MASK)


def _uidx(p: UInt64) -> Int:
    return Int(p & IDX_MASK)


def _ugen(p: UInt64) -> UInt32:
    return UInt32(p >> 40)


# Resolve a possibly-nil packed ref against a generation list: live -> Some(packed),
# stale/nil -> None (used by array-of-ref structural equality).
def _resolve_ref(gen: List[UInt32], e: Optional[UInt64]) -> Optional[UInt64]:
    if not e:
        return None
    var p = e.value()
    if gen[_uidx(p)] == _ugen(p):
        return Optional(p)
    return None


# Visited-set key for hash/repr: (type_id, arena identity, index).
@fieldwise_init
struct NodeKey(Copyable, Movable, ImplicitlyCopyable, Hashable, Equatable):
    var t: Int
    var a: Int
    var i: Int

    def __eq__(self, other: Self) -> Bool:
        return self.t == other.t and self.a == other.a and self.i == other.i

    def __ne__(self, other: Self) -> Bool:
        return not (self == other)

    def __hash__[H: Hasher](self, mut hasher: H):
        hasher.update(self.t)
        hasher.update(self.a)
        hasher.update(self.i)


# Visited-set key for cross-arena equality: a (left, right) node pair.
@fieldwise_init
struct EqPair(Copyable, Movable, ImplicitlyCopyable, Hashable, Equatable):
    var lt: Int
    var la: Int
    var li: Int
    var rt: Int
    var ra: Int
    var ri: Int

    def __eq__(self, other: Self) -> Bool:
        return (self.lt == other.lt and self.la == other.la and self.li == other.li
                and self.rt == other.rt and self.ra == other.ra and self.ri == other.ri)

    def __ne__(self, other: Self) -> Bool:
        return not (self == other)

    def __hash__[H: Hasher](self, mut hasher: H):
        hasher.update(self.lt)
        hasher.update(self.la)
        hasher.update(self.li)
        hasher.update(self.rt)
        hasher.update(self.ra)
        hasher.update(self.ri)


@fieldwise_init
struct TelemetryValues(Copyable, Movable):
    var source: Optional[String]
    var ts: Optional[Int64]
    var tags: List[String]
    var values: List[Float64]

@fieldwise_init
struct Telemetry[o: Origin[mut=False]](Copyable, Movable, ImplicitlyCopyable, Writable, Hashable, Equatable):
    var _a: Pointer[TelemetryGraphArena, origin=Self.o]
    var _packed: UInt64
    def _i(self) -> Int:
        return _uidx(self._packed)
    def _index(self) -> Int:
        return _uidx(self._packed)
    def generation(self) -> UInt32:
        return _ugen(self._packed)
    def _aid(self) -> Int:
        return Int(Pointer(to=self._a[]))
    def _mut(self) -> Pointer[TelemetryGraphArena, MutAnyOrigin]:
        return Pointer[TelemetryGraphArena, MutAnyOrigin](unsafe_from_address=Int(Pointer(to=self._a[])))
    def source(self) -> Optional[String]:
        return self._a[]._arr_telemetry[self._i()].source.copy()
    def source_slice(self) -> Optional[StringSlice[origin_of(self._a[]._arr_telemetry[self._i()].source.value())]]:
        if self._a[]._arr_telemetry[self._i()].source:
            return Optional(StringSlice(self._a[]._arr_telemetry[self._i()].source.value()))
        return None
    def set_source(self, var v: String):
        self._mut()[]._arr_telemetry[self._i()].source = Optional(v^)
    def ts(self) -> Optional[Int64]:
        return self._a[]._arr_telemetry[self._i()].ts
    def set_ts(self, var v: Int64):
        self._mut()[]._arr_telemetry[self._i()].ts = Optional(v)
    def tags(self) -> List[String]:
        return self._a[]._arr_telemetry[self._i()].tags.copy()
    def set_tags(self, var v: List[String]):
        self._mut()[]._arr_telemetry[self._i()].tags = v^
    def values(self) -> List[Float64]:
        return self._a[]._arr_telemetry[self._i()].values.copy()
    def set_values(self, var v: List[Float64]):
        self._mut()[]._arr_telemetry[self._i()].values = v^
    def is_valid(self) -> Bool:
        return self._a[]._gen_telemetry[self._i()] == self.generation()
    def delete(self):
        var i = self._i()
        if self._a[]._gen_telemetry[i] != self.generation():
            return
        var up = self._mut()
        up[]._gen_telemetry[i] = (up[]._gen_telemetry[i] + 1) & 0xffffff
        up[]._arr_telemetry[i] = TelemetryValues(source=None, ts=None, tags=List[String](), values=List[Float64]())
        up[]._free_telemetry.append(i)
    def write_to[W: Writer](self, mut writer: W):
        var seen = Set[NodeKey]()
        self._repr_cyc(writer, seen)
    def _repr_cyc[W: Writer](self, mut writer: W, mut seen: Set[NodeKey]):
        var i = self._i()
        var key = NodeKey(0, self._aid(), i)
        if key in seen:
            writer.write("Telemetry@", i)
            return
        seen.add(key)
        writer.write("Telemetry@", i, "{")
        ref _o0 = self._a[]._arr_telemetry[i].source
        if _o0:
            writer.write("source=", _o0.value())
        else:
            writer.write("source=None")
        writer.write(", ")
        ref _o1 = self._a[]._arr_telemetry[i].ts
        if _o1:
            writer.write("ts=", String(_o1.value()))
        else:
            writer.write("ts=None")
        writer.write(", ")
        writer.write("tags=[", len(self._a[]._arr_telemetry[i].tags), " items]")
        writer.write(", ")
        writer.write("values=[", len(self._a[]._arr_telemetry[i].values), " items]")
        writer.write("}")
    def __hash__[H: Hasher](self, mut hasher: H):
        var seen = Set[NodeKey]()
        self._hash_cyc(hasher, seen)
    def _hash_cyc[H: Hasher](self, mut hasher: H, mut seen: Set[NodeKey]):
        var i = self._i()
        var key = NodeKey(0, self._aid(), i)
        if key in seen:
            hasher.update(UInt64(0))
            return
        seen.add(key)
        hasher.update(UInt64(0))
        ref _o0 = self._a[]._arr_telemetry[i].source
        if _o0:
            hasher.update(_o0.value())
        ref _o1 = self._a[]._arr_telemetry[i].ts
        if _o1:
            hasher.update(_o1.value())
        for _i128 in range(len(self._a[]._arr_telemetry[i].tags)):
            hasher.update(self._a[]._arr_telemetry[i].tags[_i128])
        for _i192 in range(len(self._a[]._arr_telemetry[i].values)):
            hasher.update(String(self._a[]._arr_telemetry[i].values[_i192]))
    def __eq__(self, other: Self) -> Bool:
        return self.equals(other)
    def __ne__(self, other: Self) -> Bool:
        return not self.equals(other)
    def equals[o2: Origin[mut=False]](self, other: Telemetry[o2]) -> Bool:
        var seen = Set[EqPair]()
        return self._eq_cyc(other, seen)
    def _eq_cyc[o2: Origin[mut=False]](self, other: Telemetry[o2], mut seen: Set[EqPair]) -> Bool:
        var si = self._i()
        var oi = other._i()
        var pair = EqPair(0, self._aid(), si, 0, other._aid(), oi)
        if pair in seen:
            return True
        seen.add(pair)
        if self._a[]._arr_telemetry[si].source != other._a[]._arr_telemetry[oi].source:
            return False
        if self._a[]._arr_telemetry[si].ts != other._a[]._arr_telemetry[oi].ts:
            return False
        ref _sl128 = self._a[]._arr_telemetry[si].tags
        ref _ol128 = other._a[]._arr_telemetry[oi].tags
        if len(_sl128) != len(_ol128):
            return False
        for _k128 in range(len(_sl128)):
            if _sl128[_k128] != _ol128[_k128]:
                return False
        ref _sl192 = self._a[]._arr_telemetry[si].values
        ref _ol192 = other._a[]._arr_telemetry[oi].values
        if len(_sl192) != len(_ol192):
            return False
        for _k192 in range(len(_sl192)):
            if _sl192[_k192] != _ol192[_k192]:
                return False
        return True

struct TelemetryGraphArena(Movable):
    var _arr_telemetry: List[TelemetryValues]
    var _gen_telemetry: List[UInt32]
    var _free_telemetry: List[Int]
    var _root: Optional[UInt64]
    def __init__(out self):
        self._arr_telemetry = List[TelemetryValues]()
        self._gen_telemetry = List[UInt32]()
        self._free_telemetry = List[Int]()
        self._root = None
    def new_telemetry[o: Origin[mut=False], //](ref [o] self, var source: String = String(""), var ts: Int64 = Int64(0)) -> Telemetry[o]:
        var up = Pointer[TelemetryGraphArena, MutAnyOrigin](unsafe_from_address=Int(Pointer(to=self)))
        var idx: Int
        var gen: UInt32
        if len(self._free_telemetry) > 0:
            idx = up[]._free_telemetry.pop()
            gen = up[]._gen_telemetry[idx]
            up[]._arr_telemetry[idx] = TelemetryValues(source=Optional(source^), ts=Optional(ts), tags=List[String](), values=List[Float64]())
        else:
            idx = len(self._arr_telemetry)
            gen = 0
            up[]._arr_telemetry.append(TelemetryValues(source=Optional(source^), ts=Optional(ts), tags=List[String](), values=List[Float64]()))
            up[]._gen_telemetry.append(0)
        return Telemetry[o](Pointer(to=self), _pack(gen, idx))
    def root[o: Origin[mut=False], //](ref [o] self) -> Optional[Telemetry[o]]:
        if not self._root:
            return None
        var p = self._root.value()
        if self._gen_telemetry[_uidx(p)] != _ugen(p):
            return None
        return Telemetry[o](Pointer(to=self), p)
    def set_root[o: Origin[mut=False], //](ref [o] self, h: Telemetry[o]):
        var up = Pointer[TelemetryGraphArena, MutAnyOrigin](unsafe_from_address=Int(Pointer(to=self)))
        up[]._root = Optional(h._packed)
