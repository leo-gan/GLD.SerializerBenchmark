// lazy.rs — generated by dagr/codegen/rust/lazy.py
#![allow(dead_code, unused_variables, unused_mut, unused_imports)]
#![allow(unsafe_code)]
use crate::dagr_runtime;

#[derive(Debug)]
pub struct EventAttrArrayAccessor<'a> {
    pub data: &'a [u8],
    pub slot_start: usize,
    pub es: usize,
    pub base: usize,
    pub count: usize,
}

impl<'a> EventAttrArrayAccessor<'a> {
    pub fn empty(data: &'a [u8]) -> Self {
        Self { data, slot_start: 0, es: 1, base: 0, count: 0 }
    }
    pub fn new(data: &'a [u8], at: usize) -> Result<Self, dagr_runtime::DagrError> {
        if at >= data.len() { return Err(dagr_runtime::DagrError::InvalidData); }
        let (hdr, hl) = dagr_runtime::try_read_leb(data, at).ok_or(dagr_runtime::DagrError::InvalidData)?;
        let count = (hdr >> 2) as usize;
        let es = [1usize, 2, 4, 8][(hdr & 3) as usize];
        let slot_start = at + hl;
        let base = slot_start + count * es;
        if base > data.len() { return Err(dagr_runtime::DagrError::InvalidData); }
        Ok(Self { data, slot_start, es, base, count })
    }
    pub fn get(&self, idx: usize) -> Result<EventAttrAccessor<'a>, dagr_runtime::DagrError> {
        if idx >= self.count { return Err(dagr_runtime::DagrError::InvalidData); }
        let slot_pos = self.slot_start + idx * self.es;
        let d = dagr_runtime::read_signed_rel_offset(self.data, slot_pos, self.es)?;
        if d == 0 { return Err(dagr_runtime::DagrError::InvalidData); }
        let target = (self.base as i64 + d - 1) as usize;
        EventAttrAccessor::new(self.data, target)
    }
    pub fn iter(&self) -> EventAttrArrayIter<'a> {
        EventAttrArrayIter { data: self.data, slot_start: self.slot_start, es: self.es, base: self.base, count: self.count, idx: 0 }
    }
}

pub struct EventAttrArrayIter<'a> {
    data: &'a [u8],
    slot_start: usize,
    es: usize,
    base: usize,
    count: usize,
    idx: usize,
}

impl<'a> Iterator for EventAttrArrayIter<'a> {
    type Item = Result<EventAttrAccessor<'a>, dagr_runtime::DagrError>;
    fn next(&mut self) -> Option<Self::Item> {
        if self.idx >= self.count { return None; }
        let acc = EventAttrArrayAccessor { data: self.data, slot_start: self.slot_start, es: self.es, base: self.base, count: self.count };
        let item = acc.get(self.idx);
        self.idx += 1;
        Some(item)
    }
}

#[derive(Debug)]
pub struct EventAttrOptArrayAccessor<'a> {
    pub data: &'a [u8],
    pub slot_start: usize,
    pub es: usize,
    pub base: usize,
    pub count: usize,
}

impl<'a> EventAttrOptArrayAccessor<'a> {
    pub fn empty(data: &'a [u8]) -> Self {
        Self { data, slot_start: 0, es: 1, base: 0, count: 0 }
    }
    pub fn new(data: &'a [u8], at: usize) -> Result<Self, dagr_runtime::DagrError> {
        if at >= data.len() { return Err(dagr_runtime::DagrError::InvalidData); }
        let (hdr, hl) = dagr_runtime::try_read_leb(data, at).ok_or(dagr_runtime::DagrError::InvalidData)?;
        let count = (hdr >> 2) as usize;
        let es = [1usize, 2, 4, 8][(hdr & 3) as usize];
        let slot_start = at + hl;
        let base = slot_start + count * es;
        if base > data.len() { return Err(dagr_runtime::DagrError::InvalidData); }
        Ok(Self { data, slot_start, es, base, count })
    }
    pub fn get(&self, idx: usize) -> Result<Option<EventAttrAccessor<'a>>, dagr_runtime::DagrError> {
        if idx >= self.count { return Ok(None); }
        let slot_pos = self.slot_start + idx * self.es;
        let d = dagr_runtime::read_signed_rel_offset(self.data, slot_pos, self.es)?;
        if d == 0 { return Ok(None); }
        let target = (self.base as i64 + d - 1) as usize;
        EventAttrAccessor::new(self.data, target).map(Some)
    }
    pub fn iter(&self) -> EventAttrOptArrayIter<'a> {
        EventAttrOptArrayIter { data: self.data, slot_start: self.slot_start, es: self.es, base: self.base, count: self.count, idx: 0 }
    }
}

pub struct EventAttrOptArrayIter<'a> {
    data: &'a [u8],
    slot_start: usize,
    es: usize,
    base: usize,
    count: usize,
    idx: usize,
}

impl<'a> Iterator for EventAttrOptArrayIter<'a> {
    type Item = Result<Option<EventAttrAccessor<'a>>, dagr_runtime::DagrError>;
    fn next(&mut self) -> Option<Self::Item> {
        if self.idx >= self.count { return None; }
        let acc = EventAttrOptArrayAccessor { data: self.data, slot_start: self.slot_start, es: self.es, base: self.base, count: self.count };
        let item = acc.get(self.idx);
        self.idx += 1;
        Some(item)
    }
}

pub struct EventAttrPackedNodeArray<'a> { data: &'a [u8], base: usize, count: usize }
impl<'a> EventAttrPackedNodeArray<'a> {
    pub fn empty(data: &'a [u8]) -> Self { Self { data, base: 0, count: 0 } }
    pub fn new(data: &'a [u8], at: usize) -> Result<Self, dagr_runtime::DagrError> {
        let (count, cl) = dagr_runtime::try_read_leb(data, at).ok_or(dagr_runtime::DagrError::InvalidData)?;
        Ok(Self { data, base: at + cl, count: count as usize })
    }
    pub fn len(&self) -> usize { self.count }
    pub fn is_empty(&self) -> bool { self.count == 0 }
    pub fn iter(&self) -> EventAttrPackedNodeArrayIter<'a> { EventAttrPackedNodeArrayIter { data: self.data, ap: self.base, count: self.count, idx: 0 } }
}
impl<'a> IntoIterator for &EventAttrPackedNodeArray<'a> { type Item = Result<EventAttrAccessor<'a>, dagr_runtime::DagrError>; type IntoIter = EventAttrPackedNodeArrayIter<'a>; fn into_iter(self) -> EventAttrPackedNodeArrayIter<'a> { self.iter() } }
pub struct EventAttrPackedNodeArrayIter<'a> { data: &'a [u8], ap: usize, count: usize, idx: usize }
impl<'a> Iterator for EventAttrPackedNodeArrayIter<'a> {
    type Item = Result<EventAttrAccessor<'a>, dagr_runtime::DagrError>;
    fn next(&mut self) -> Option<Self::Item> {
        if self.idx >= self.count { return None; }
        self.idx += 1;
        let (_el_sz, _el_sl) = match dagr_runtime::read_leb(self.data, self.ap) { Ok(v) => v, Err(e) => { self.idx = self.count; return Some(Err(e)); } };
        match EventAttrAccessor::new(self.data, self.ap) {
            Ok(a) => { self.ap += _el_sl + _el_sz as usize; Some(Ok(a)) }
            Err(e) => { self.idx = self.count; Some(Err(e)) }
        }
    }
    fn size_hint(&self) -> (usize, Option<usize>) { let r = self.count - self.idx; (r, Some(r)) }
}
impl<'a> ExactSizeIterator for EventAttrPackedNodeArrayIter<'a> {}

pub struct EventAttrPackedNodeOptArray<'a> { data: &'a [u8], nil_base: usize, base: usize, count: usize }
impl<'a> EventAttrPackedNodeOptArray<'a> {
    pub fn empty(data: &'a [u8]) -> Self { Self { data, nil_base: 0, base: 0, count: 0 } }
    pub fn new(data: &'a [u8], at: usize) -> Result<Self, dagr_runtime::DagrError> {
        let (count, cl) = dagr_runtime::try_read_leb(data, at).ok_or(dagr_runtime::DagrError::InvalidData)?;
        let count = count as usize;
        let nil_base = at + cl;
        Ok(Self { data, nil_base, base: nil_base + (count + 7) / 8, count })
    }
    pub fn len(&self) -> usize { self.count }
    pub fn is_empty(&self) -> bool { self.count == 0 }
    pub fn iter(&self) -> EventAttrPackedNodeOptArrayIter<'a> { EventAttrPackedNodeOptArrayIter { data: self.data, nil_base: self.nil_base, ap: self.base, count: self.count, idx: 0 } }
}
impl<'a> IntoIterator for &EventAttrPackedNodeOptArray<'a> { type Item = Result<Option<EventAttrAccessor<'a>>, dagr_runtime::DagrError>; type IntoIter = EventAttrPackedNodeOptArrayIter<'a>; fn into_iter(self) -> EventAttrPackedNodeOptArrayIter<'a> { self.iter() } }
pub struct EventAttrPackedNodeOptArrayIter<'a> { data: &'a [u8], nil_base: usize, ap: usize, count: usize, idx: usize }
impl<'a> Iterator for EventAttrPackedNodeOptArrayIter<'a> {
    type Item = Result<Option<EventAttrAccessor<'a>>, dagr_runtime::DagrError>;
    fn next(&mut self) -> Option<Self::Item> {
        if self.idx >= self.count { return None; }
        let k = self.idx; self.idx += 1;
        if self.data.get(self.nil_base + k / 8).map_or(false, |&b| (b >> (k % 8)) & 1 != 0) { return Some(Ok(None)); }
        let (_el_sz, _el_sl) = match dagr_runtime::read_leb(self.data, self.ap) { Ok(v) => v, Err(e) => { self.idx = self.count; return Some(Err(e)); } };
        match EventAttrAccessor::new(self.data, self.ap) {
            Ok(a) => { self.ap += _el_sl + _el_sz as usize; Some(Ok(Some(a))) }
            Err(e) => { self.idx = self.count; Some(Err(e)) }
        }
    }
    fn size_hint(&self) -> (usize, Option<usize>) { let r = self.count - self.idx; (r, Some(r)) }
}
impl<'a> ExactSizeIterator for EventAttrPackedNodeOptArrayIter<'a> {}

pub struct EventAttrAccessor<'a> {
    data: &'a [u8],
    _at: usize,
    r0: Option<core::ops::Range<usize>>,
    r1: Option<core::ops::Range<usize>>,
}

impl<'a> EventAttrAccessor<'a> {
    pub fn new(data: &'a [u8], at: usize) -> Result<Self, dagr_runtime::DagrError> {
        if at >= data.len() { return Err(dagr_runtime::DagrError::InvalidData); }
        let (_, _fp_leb_sz) = dagr_runtime::read_leb(data, at)?;
        let _start = at + _fp_leb_sz;
        let mut r0: Option<core::ops::Range<usize>> = None;
        let mut r1: Option<core::ops::Range<usize>> = None;
        let _obs0 = data.get(_start + 0).copied().unwrap_or(0);
        let mut _cur = _start + 1;
        if _obs0 & 1 != 0 {
            if let Ok((_bsz, _blb)) = dagr_runtime::read_leb(data, _cur) { let _s = _cur + _blb; if let Some(_e) = _s.checked_add(_bsz as usize) { if _e <= data.len() { r0 = Some(_s.._e); } } _cur = _s.saturating_add(_bsz as usize); }
        }
        if _obs0 & 2 != 0 {
            if let Ok((_bsz, _blb)) = dagr_runtime::read_leb(data, _cur) { let _s = _cur + _blb; if let Some(_e) = _s.checked_add(_bsz as usize) { if _e <= data.len() { r1 = Some(_s.._e); } } _cur = _s.saturating_add(_bsz as usize); }
        }
        let _ = _cur;
        Ok(Self { data, _at: at, r0, r1 })
    }
    /// Byte offset of this node in the buffer — its within-buffer identity.
    pub fn _start(&self) -> usize { self._at }
    pub fn _has_key(&self) -> bool { self.r0.is_some() }
    pub fn _has_value(&self) -> bool { self.r1.is_some() }

    pub fn key(&self) -> Option<&'a str> {
        dagr_runtime::str_from(&self.data[self.r0.clone()?])
    }

    pub fn value(&self) -> Option<&'a str> {
        dagr_runtime::str_from(&self.data[self.r1.clone()?])
    }

}

pub struct EventAccessor<'a> {
    data: &'a [u8],
    _at: usize,
    v2: Option<i64>,
    r0: Option<core::ops::Range<usize>>,
    r1: Option<core::ops::Range<usize>>,
    r3: Option<core::ops::Range<usize>>,
    a4: Option<usize>,
}

impl<'a> EventAccessor<'a> {
    pub fn new(data: &'a [u8], at: usize) -> Result<Self, dagr_runtime::DagrError> {
        if at >= data.len() { return Err(dagr_runtime::DagrError::InvalidData); }
        let (_, _fp_leb_sz) = dagr_runtime::read_leb(data, at)?;
        let _start = at + _fp_leb_sz;
        let mut v2: Option<i64> = None;
        let mut r0: Option<core::ops::Range<usize>> = None;
        let mut r1: Option<core::ops::Range<usize>> = None;
        let mut r3: Option<core::ops::Range<usize>> = None;
        let mut a4: Option<usize> = None;
        let _obs0 = data.get(_start + 0).copied().unwrap_or(0);
        let _ebs0 = data.get(_start + 1).copied().unwrap_or(0);
        let mut _cur = _start + 2;
        if _obs0 & 1 != 0 {
            if let Ok((_bsz, _blb)) = dagr_runtime::read_leb(data, _cur) { let _s = _cur + _blb; if let Some(_e) = _s.checked_add(_bsz as usize) { if _e <= data.len() { r0 = Some(_s.._e); } } _cur = _s.saturating_add(_bsz as usize); }
        }
        if _obs0 & 2 != 0 {
            if let Ok((_bsz, _blb)) = dagr_runtime::read_leb(data, _cur) { let _s = _cur + _blb; if let Some(_e) = _s.checked_add(_bsz as usize) { if _e <= data.len() { r1 = Some(_s.._e); } } _cur = _s.saturating_add(_bsz as usize); }
        }
        if _obs0 & 4 != 0 {
            if _ebs0 & 1 != 0 {
                let _v = dagr_runtime::read_i64(data, _cur)? as i64; _cur += 8;
                v2 = Some(_v);
            } else {
                let (_lv, _lb) = dagr_runtime::read_leb(data, _cur)?; _cur += _lb;
                v2 = Some(dagr_runtime::from_zigzag(_lv) as i64);
            }
        }
        if _obs0 & 8 != 0 {
            if let Ok((_bsz, _blb)) = dagr_runtime::read_leb(data, _cur) { let _s = _cur + _blb; if let Some(_e) = _s.checked_add(_bsz as usize) { if _e <= data.len() { r3 = Some(_s.._e); } } _cur = _s.saturating_add(_bsz as usize); }
        }
        if _obs0 & 16 != 0 {
            if let Ok((_bsz4, _blb4)) = dagr_runtime::read_leb(data, _cur) {
                a4 = Some(_cur + _blb4);
                _cur += _blb4 + _bsz4 as usize;
            }
        }
        let _ = _cur;
        Ok(Self { data, _at: at, v2, r0, r1, r3, a4 })
    }
    /// Byte offset of this node in the buffer — its within-buffer identity.
    pub fn _start(&self) -> usize { self._at }
    pub fn _has_event_id(&self) -> bool { self.r0.is_some() }
    pub fn _has_event_type(&self) -> bool { self.r1.is_some() }
    pub fn _has_occurred_at(&self) -> bool { self.v2.is_some() }
    pub fn _has_producer(&self) -> bool { self.r3.is_some() }
    pub fn _has_attrs(&self) -> bool { self.a4.is_some() }

    pub fn occurred_at(&self) -> Result<i64, dagr_runtime::DagrError> {
        self.v2.ok_or(dagr_runtime::DagrError::InvalidData)
    }

    pub fn event_id(&self) -> Option<&'a str> {
        dagr_runtime::str_from(&self.data[self.r0.clone()?])
    }

    pub fn event_type(&self) -> Option<&'a str> {
        dagr_runtime::str_from(&self.data[self.r1.clone()?])
    }

    pub fn producer(&self) -> Option<&'a str> {
        dagr_runtime::str_from(&self.data[self.r3.clone()?])
    }

    pub fn attrs(&self) -> Result<EventAttrPackedNodeArray<'a>, dagr_runtime::DagrError> {
        let pos = match self.a4 { None => return Ok(EventAttrPackedNodeArray::empty(self.data)), Some(p) => p };
        EventAttrPackedNodeArray::new(self.data, pos)
    }

}

pub fn read_root(data: &[u8]) -> Result<EventAccessor<'_>, dagr_runtime::DagrError> {
    if data.is_empty() { return Err(dagr_runtime::DagrError::InvalidData); }
    let (root_dist, rl) = dagr_runtime::read_leb(data, 0)?;
    if (root_dist & 1) != 0 { return Err(dagr_runtime::DagrError::InvalidData); }
    let _at = rl + (root_dist >> 2) as usize;
    if _at >= data.len() { return Err(dagr_runtime::DagrError::InvalidData); }
    EventAccessor::new(data, _at)
}
