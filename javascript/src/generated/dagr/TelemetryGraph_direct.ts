// Generated by dagr/codegen/typescript/direct.py — do not edit by hand.
// Graph: TelemetryGraph — Direct Graph Builder ("spec/33-direct-graph-builder.md")
import { Builder, nodeOffset, lebLength, zigzag } from "./dagr_writer";
import type { NodeStoreRef } from "./dagr_writer";

export interface Telemetry {
  source: string | null;
  ts: bigint | null;
  tags: string[] | null;
  values: number[] | null;
}

function _storeTelemetry(n: Telemetry, b: Builder): NodeStoreRef {
  const _before = b.cursor;
  if (n.values !== null) { b.storePackedFloatArray(n.values, true, true); b.storeLEB((3 << 1) | 1); }
  if (n.tags !== null) { b.storePackedComplexArray(n.tags, (b, v: string) => b.storeUtf8(v, false), false); b.storeLEB((2 << 1) | 1); }
  if (n.ts !== null) { { const _zz = zigzag(n.ts); if (lebLength(_zz) < 8) { b.storeLEB(_zz); b.storeLEB(1 << 1); } else { b.storeI64(n.ts); b.storeLEB((1 << 1) | 1); } } }
  if (n.source !== null) { b.storeUtf8(n.source, false); b.storeLEB((0 << 1) | 1); }
  b.storeLEB(b.cursor - _before);  // blockLen (front of the record)
  const _o = b.cursor;
  return { off: _o };
}

export function writeInto(root: Telemetry, b: Builder): number {
  const off = nodeOffset(_storeTelemetry(root, b));
  return b.storeLEB((b.cursor - off) * 4);  // framing: rootDist<<2 | headerFlags(0)
}

export function toBytes(root: Telemetry, maxSize: number = 2 * 1024 * 1024): Uint8Array {
  const b = new Builder(maxSize, 1024);   // direct = tree → small buffer, grows if needed
  writeInto(root, b);
  return b.makeData();
}
