// Code generated by dagr (dagr/codegen/go/serde.py); DO NOT EDIT.

package eventfrozengraph

import "dagrv2/dagr"

func storeEventAttr(n EventAttr, b *dagr.Builder) dagr.NodeStoreRef {
	if r, ok := b.BeginStoring(n.key()); ok {
		return r
	}
	var c1 int
	x1, has1 := n.Value()
	if has1 {
		c1 = b.StoreUtf8(x1, true)
	}
	var c0 int
	x0, has0 := n.Key()
	if has0 {
		c0 = b.StoreUtf8(x0, true)
	}
	if has1 {
		b.StoreForwardPointer(c1)
	}
	if has0 {
		b.StoreForwardPointer(c0)
	}
	var bs uint64
	if _, ok := n.Key(); ok {
		bs |= 1 << 0
	}
	if _, ok := n.Value(); ok {
		bs |= 1 << 1
	}
	b.StoreBytes([]byte{byte(bs >> 0)})
	o := b.Cursor()
	b.FinishStoring(n.key(), o)
	return dagr.NodeStoreRef{Off: o}
}

func storeEvent(n Event, b *dagr.Builder) dagr.NodeStoreRef {
	if r, ok := b.BeginStoring(n.key()); ok {
		return r
	}
	var c4 int
	x4, has4 := n.Attrs()
	if has4 {
		c4 = dagr.StoreNodeRefArrayPlain(b, x4, func(b *dagr.Builder, v EventAttr) dagr.NodeStoreRef { return storeEventAttr(v, b) })
	}
	var c3 int
	x3, has3 := n.Producer()
	if has3 {
		c3 = b.StoreUtf8(x3, true)
	}
	var c1 int
	x1, has1 := n.EventType()
	if has1 {
		c1 = b.StoreUtf8(x1, true)
	}
	var c0 int
	x0, has0 := n.EventID()
	if has0 {
		c0 = b.StoreUtf8(x0, true)
	}
	if has4 {
		b.StoreForwardPointer(c4)
	}
	if has3 {
		b.StoreForwardPointer(c3)
	}
	if x2, ok := n.OccurredAt(); ok {
		b.StoreI64(x2)
	}
	if has1 {
		b.StoreForwardPointer(c1)
	}
	if has0 {
		b.StoreForwardPointer(c0)
	}
	var bs uint64
	if _, ok := n.EventID(); ok {
		bs |= 1 << 0
	}
	if _, ok := n.EventType(); ok {
		bs |= 1 << 1
	}
	if _, ok := n.OccurredAt(); ok {
		bs |= 1 << 2
	}
	if _, ok := n.Producer(); ok {
		bs |= 1 << 3
	}
	if _, ok := n.Attrs(); ok {
		bs |= 1 << 4
	}
	b.StoreBytes([]byte{byte(bs >> 0)})
	o := b.Cursor()
	b.FinishStoring(n.key(), o)
	return dagr.NodeStoreRef{Off: o}
}

// ToBytesEventFrozenGraph serializes the graph reachable from root (byte-identical to every
// other Dagr implementation for the same graph). maxSize sets the cycle back-
// reference placeholder width (0 → 2 MiB, i.e. 4 bytes) and must match across
// producers for byte-identity.
func ToBytesEventFrozenGraph(root Event, maxSize int) []byte {
	b := dagr.NewBuilder(maxSize)
	off := dagr.NodeOffset(storeEvent(root, b))
	b.StoreLEB(uint64(b.Cursor()-off) << 2)
	return b.MakeData()
}

// AppendToEventFrozenGraph serializes the graph reachable from root into b (reset first) and
// appends the record to dst: ToBytes without a Builder per call. Bytes are
// identical to ToBytes with the maxSize b was created with.
func AppendToEventFrozenGraph(b *dagr.Builder, dst []byte, root Event) []byte {
	b.Reset()
	off := dagr.NodeOffset(storeEvent(root, b))
	b.StoreLEB(uint64(b.Cursor()-off) << 2)
	return b.AppendTo(dst)
}
