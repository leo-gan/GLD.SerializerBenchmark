// Code generated by dagr (dagr/codegen/go/serde.py); DO NOT EDIT.

package telemetryfrozenpackedgraph

import "dagrv2/dagr"

func storeTelemetry(n Telemetry, b *dagr.Builder) dagr.NodeStoreRef {
	if d, ok := b.StructLookup[n.key()]; ok {
		return dagr.NodeStoreRef{Off: d}
	}
	before := b.Cursor()
	var raw [1]bool
	if x3, ok := n.Values(); ok {
		b.StorePackedFloatArray(nil, x3, true)
	}
	if x2, ok := n.Tags(); ok {
		dagr.StorePackedComplexArrayPlain(b, x2, func(b *dagr.Builder, v string) { b.StoreUtf8(v, false) })
	}
	if x1, ok := n.Ts(); ok {
		zz := dagr.ToZigZag(int64(x1))
		if dagr.LebLength(zz) < 8 {
			b.StoreLEB(zz)
		} else {
			b.StoreI64(x1)
			raw[0] = true
		}
	}
	if x0, ok := n.Source(); ok {
		b.StoreUtf8(x0, false)
	}
	enc := make([]byte, 1)
	for k, r := range raw {
		if r {
			enc[k>>3] |= 1 << (k & 7)
		}
	}
	b.StoreBytes(enc)
	var nbits uint64
	if _, ok := n.Source(); ok {
		nbits |= 1 << 0
	}
	if _, ok := n.Ts(); ok {
		nbits |= 1 << 1
	}
	if _, ok := n.Tags(); ok {
		nbits |= 1 << 2
	}
	if _, ok := n.Values(); ok {
		nbits |= 1 << 3
	}
	b.StoreBytes([]byte{byte(nbits >> 0)})
	b.StoreLEB(uint64(b.Cursor() - before))
	o := b.Cursor()
	b.StructLookup[n.key()] = o
	return dagr.NodeStoreRef{Off: o}
}

// ToBytesTelemetryFrozenPackedGraph serializes the graph reachable from root (byte-identical to every
// other Dagr implementation for the same graph). maxSize sets the cycle back-
// reference placeholder width (0 → 2 MiB, i.e. 4 bytes) and must match across
// producers for byte-identity.
func ToBytesTelemetryFrozenPackedGraph(root Telemetry, maxSize int) []byte {
	b := dagr.NewBuilder(maxSize)
	off := dagr.NodeOffset(storeTelemetry(root, b))
	b.StoreLEB(uint64(b.Cursor()-off) << 2)
	return b.MakeData()
}

// AppendToTelemetryFrozenPackedGraph serializes the graph reachable from root into b (reset first) and
// appends the record to dst: ToBytes without a Builder per call. Bytes are
// identical to ToBytes with the maxSize b was created with.
func AppendToTelemetryFrozenPackedGraph(b *dagr.Builder, dst []byte, root Telemetry) []byte {
	b.Reset()
	off := dagr.NodeOffset(storeTelemetry(root, b))
	b.StoreLEB(uint64(b.Cursor()-off) << 2)
	return b.AppendTo(dst)
}
