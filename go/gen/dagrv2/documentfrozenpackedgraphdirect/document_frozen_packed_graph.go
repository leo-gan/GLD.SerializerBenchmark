// Code generated by dagr (dagr/codegen/go/direct.py); DO NOT EDIT.

package documentfrozenpackedgraphdirect

import (
	"iter"

	"dagrv2/dagr"
)

type DocumentMetaAccessor struct {
	buf []byte
	pos int
	p   [2]int
	raw uint64
}

func NewDocumentMetaAccessor(buf []byte, pos int) DocumentMetaAccessor {
	a := DocumentMetaAccessor{buf: buf, pos: pos}
	_, r := dagr.ReadLEB(buf, pos)
	cur := pos + r
	pres := dagr.ReadBitset(buf, cur, 1)
	cur += 1
	enc := dagr.ReadBitset(buf, cur, 1)
	cur += 1
	if (pres>>0)&1 == 1 {
		a.p[0] = cur
		cur = dagr.SkipBlob(buf, cur)
	} else {
		a.p[0] = -1
	}
	if (pres>>1)&1 == 1 {
		if (enc>>0)&1 == 1 {
			a.raw |= 1 << 1
		}
		a.p[1] = cur
		if (enc>>0)&1 == 1 {
			cur = cur + 4
		} else {
			cur = dagr.LebEnd(buf, cur)
		}
	} else {
		a.p[1] = -1
	}
	_ = cur
	return a
}

// BufferPos is where this node starts in the buffer — its identity in a graph.
func (a DocumentMetaAccessor) BufferPos() int { return a.pos }

// BufferBytes is the buffer this accessor reads — with BufferPos, enough to reopen
// the node through another package's accessor (a DataSink does, spec/11 §12).
func (a DocumentMetaAccessor) BufferBytes() []byte { return a.buf }

func (a DocumentMetaAccessor) Region() (string, bool) {
	pl := a.p[0]
	if pl < 0 {
		return "", false
	}
	s, _ := dagr.ReadUtf8(a.buf, pl)
	return s, true
}

func (a DocumentMetaAccessor) RegionBytes() ([]byte, bool) {
	pl := a.p[0]
	if pl < 0 {
		return nil, false
	}
	d, _ := dagr.ReadBytes(a.buf, pl)
	return d, true
}

func (a DocumentMetaAccessor) Version() (int32, bool) {
	pl := a.p[1]
	raw := (a.raw>>1)&1 == 1
	if pl < 0 {
		return 0, false
	}
	if raw {
		return dagr.ReadI32(a.buf, pl), true
	}
	x, _ := dagr.ReadZigZagLEB(a.buf, pl)
	return int32(x), true
}

// DocumentMeta is a plain value record (spec 11 §4: no arena).
type DocumentMeta struct {
	Region  dagr.Opt[string]
	Version dagr.Opt[int32]
}

func restoreDocumentMeta(acc DocumentMetaAccessor) DocumentMeta {
	var rec DocumentMeta
	{
		if v, ok := acc.Region(); ok {
			rec.Region = dagr.Some(v)
		}
	}
	{
		if v, ok := acc.Version(); ok {
			rec.Version = dagr.Some(v)
		}
	}
	return rec
}

// RestoreDocumentMeta decodes one DocumentMeta (and its subtree) into plain values — the entry
// a DataSink uses for a record field that holds this graph's node (spec/11 §12).
func RestoreDocumentMeta(acc DocumentMetaAccessor) DocumentMeta {
	return restoreDocumentMeta(acc)
}

func storeDocumentMeta(n DocumentMeta, b *dagr.Builder) dagr.NodeStoreRef {
	before := b.Cursor()
	var raw [1]bool
	if x1, ok := n.Version.V, n.Version.Ok; ok {
		zz := dagr.ToZigZag(int64(x1))
		if dagr.LebLength(zz) < 4 {
			b.StoreLEB(zz)
		} else {
			b.StoreI32(x1)
			raw[0] = true
		}
	}
	if x0, ok := n.Region.V, n.Region.Ok; ok {
		b.StoreUtf8(x0, false)
	}
	enc := make([]byte, 1)
	for k, r := range raw {
		if r {
			enc[k>>3] |= 1 << (k & 7)
		}
	}
	b.StoreBytes(enc)
	var nbits uint64
	if n.Region.Ok {
		nbits |= 1 << 0
	}
	if n.Version.Ok {
		nbits |= 1 << 1
	}
	b.StoreBytes([]byte{byte(nbits >> 0)})
	b.StoreLEB(uint64(b.Cursor() - before))
	o := b.Cursor()
	return dagr.NodeStoreRef{Off: o}
}

type DocumentItemAccessor struct {
	buf []byte
	pos int
	p   [3]int
	raw uint64
}

func NewDocumentItemAccessor(buf []byte, pos int) DocumentItemAccessor {
	a := DocumentItemAccessor{buf: buf, pos: pos}
	_, r := dagr.ReadLEB(buf, pos)
	cur := pos + r
	pres := dagr.ReadBitset(buf, cur, 1)
	cur += 1
	enc := dagr.ReadBitset(buf, cur, 1)
	cur += 1
	if (pres>>0)&1 == 1 {
		a.p[0] = cur
		cur = dagr.SkipBlob(buf, cur)
	} else {
		a.p[0] = -1
	}
	if (pres>>1)&1 == 1 {
		if (enc>>0)&1 == 1 {
			a.raw |= 1 << 1
		}
		a.p[1] = cur
		if (enc>>0)&1 == 1 {
			cur = cur + 4
		} else {
			cur = dagr.LebEnd(buf, cur)
		}
	} else {
		a.p[1] = -1
	}
	if (pres>>2)&1 == 1 {
		if (enc>>1)&1 == 1 {
			a.raw |= 1 << 2
		}
		a.p[2] = cur
		if (enc>>1)&1 == 1 {
			cur = cur + 8
		} else {
			cur = dagr.LebEnd(buf, cur)
		}
	} else {
		a.p[2] = -1
	}
	_ = cur
	return a
}

// BufferPos is where this node starts in the buffer — its identity in a graph.
func (a DocumentItemAccessor) BufferPos() int { return a.pos }

// BufferBytes is the buffer this accessor reads — with BufferPos, enough to reopen
// the node through another package's accessor (a DataSink does, spec/11 §12).
func (a DocumentItemAccessor) BufferBytes() []byte { return a.buf }

func (a DocumentItemAccessor) Sku() (string, bool) {
	pl := a.p[0]
	if pl < 0 {
		return "", false
	}
	s, _ := dagr.ReadUtf8(a.buf, pl)
	return s, true
}

func (a DocumentItemAccessor) SkuBytes() ([]byte, bool) {
	pl := a.p[0]
	if pl < 0 {
		return nil, false
	}
	d, _ := dagr.ReadBytes(a.buf, pl)
	return d, true
}

func (a DocumentItemAccessor) Qty() (int32, bool) {
	pl := a.p[1]
	raw := (a.raw>>1)&1 == 1
	if pl < 0 {
		return 0, false
	}
	if raw {
		return dagr.ReadI32(a.buf, pl), true
	}
	x, _ := dagr.ReadZigZagLEB(a.buf, pl)
	return int32(x), true
}

func (a DocumentItemAccessor) PriceMinor() (int64, bool) {
	pl := a.p[2]
	raw := (a.raw>>2)&1 == 1
	if pl < 0 {
		return 0, false
	}
	if raw {
		return dagr.ReadI64(a.buf, pl), true
	}
	x, _ := dagr.ReadZigZagLEB(a.buf, pl)
	return int64(x), true
}

// DocumentItem is a plain value record (spec 11 §4: no arena).
type DocumentItem struct {
	Sku        dagr.Opt[string]
	Qty        dagr.Opt[int32]
	PriceMinor dagr.Opt[int64]
}

func restoreDocumentItem(acc DocumentItemAccessor) DocumentItem {
	var rec DocumentItem
	{
		if v, ok := acc.Sku(); ok {
			rec.Sku = dagr.Some(v)
		}
	}
	{
		if v, ok := acc.Qty(); ok {
			rec.Qty = dagr.Some(v)
		}
	}
	{
		if v, ok := acc.PriceMinor(); ok {
			rec.PriceMinor = dagr.Some(v)
		}
	}
	return rec
}

// RestoreDocumentItem decodes one DocumentItem (and its subtree) into plain values — the entry
// a DataSink uses for a record field that holds this graph's node (spec/11 §12).
func RestoreDocumentItem(acc DocumentItemAccessor) DocumentItem {
	return restoreDocumentItem(acc)
}

func storeDocumentItem(n DocumentItem, b *dagr.Builder) dagr.NodeStoreRef {
	before := b.Cursor()
	var raw [2]bool
	if x2, ok := n.PriceMinor.V, n.PriceMinor.Ok; ok {
		zz := dagr.ToZigZag(int64(x2))
		if dagr.LebLength(zz) < 8 {
			b.StoreLEB(zz)
		} else {
			b.StoreI64(x2)
			raw[1] = true
		}
	}
	if x1, ok := n.Qty.V, n.Qty.Ok; ok {
		zz := dagr.ToZigZag(int64(x1))
		if dagr.LebLength(zz) < 4 {
			b.StoreLEB(zz)
		} else {
			b.StoreI32(x1)
			raw[0] = true
		}
	}
	if x0, ok := n.Sku.V, n.Sku.Ok; ok {
		b.StoreUtf8(x0, false)
	}
	enc := make([]byte, 1)
	for k, r := range raw {
		if r {
			enc[k>>3] |= 1 << (k & 7)
		}
	}
	b.StoreBytes(enc)
	var nbits uint64
	if n.Sku.Ok {
		nbits |= 1 << 0
	}
	if n.Qty.Ok {
		nbits |= 1 << 1
	}
	if n.PriceMinor.Ok {
		nbits |= 1 << 2
	}
	b.StoreBytes([]byte{byte(nbits >> 0)})
	b.StoreLEB(uint64(b.Cursor() - before))
	o := b.Cursor()
	return dagr.NodeStoreRef{Off: o}
}

type DocumentAccessor struct {
	buf []byte
	pos int
	p   [4]int
	raw uint64
}

func NewDocumentAccessor(buf []byte, pos int) DocumentAccessor {
	a := DocumentAccessor{buf: buf, pos: pos}
	_, r := dagr.ReadLEB(buf, pos)
	cur := pos + r
	pres := dagr.ReadBitset(buf, cur, 1)
	cur += 1
	enc := dagr.ReadBitset(buf, cur, 1)
	cur += 1
	if (pres>>0)&1 == 1 {
		a.p[0] = cur
		cur = dagr.SkipBlob(buf, cur)
	} else {
		a.p[0] = -1
	}
	if (pres>>1)&1 == 1 {
		if (enc>>0)&1 == 1 {
			a.raw |= 1 << 1
		}
		a.p[1] = cur
		if (enc>>0)&1 == 1 {
			cur = cur + 4
		} else {
			cur = dagr.LebEnd(buf, cur)
		}
	} else {
		a.p[1] = -1
	}
	if (pres>>2)&1 == 1 {
		a.p[2] = cur
		cur = dagr.SkipBlob(buf, cur)
	} else {
		a.p[2] = -1
	}
	if (pres>>3)&1 == 1 {
		a.p[3] = cur
		cur = dagr.SkipBlob(buf, cur)
	} else {
		a.p[3] = -1
	}
	_ = cur
	return a
}

// BufferPos is where this node starts in the buffer — its identity in a graph.
func (a DocumentAccessor) BufferPos() int { return a.pos }

// BufferBytes is the buffer this accessor reads — with BufferPos, enough to reopen
// the node through another package's accessor (a DataSink does, spec/11 §12).
func (a DocumentAccessor) BufferBytes() []byte { return a.buf }

func (a DocumentAccessor) ID() (string, bool) {
	pl := a.p[0]
	if pl < 0 {
		return "", false
	}
	s, _ := dagr.ReadUtf8(a.buf, pl)
	return s, true
}

func (a DocumentAccessor) IDBytes() ([]byte, bool) {
	pl := a.p[0]
	if pl < 0 {
		return nil, false
	}
	d, _ := dagr.ReadBytes(a.buf, pl)
	return d, true
}

func (a DocumentAccessor) Status() (int32, bool) {
	pl := a.p[1]
	raw := (a.raw>>1)&1 == 1
	if pl < 0 {
		return 0, false
	}
	if raw {
		return dagr.ReadI32(a.buf, pl), true
	}
	x, _ := dagr.ReadZigZagLEB(a.buf, pl)
	return int32(x), true
}

func (a DocumentAccessor) Meta() (DocumentMetaAccessor, bool) {
	pl := a.p[2]
	if pl < 0 {
		return DocumentMetaAccessor{}, false
	}
	return NewDocumentMetaAccessor(a.buf, pl), true
}

func (a DocumentAccessor) ItemsLen() (int, bool) {
	pl := a.p[3]
	if pl < 0 {
		return 0, false
	}
	_, blb := dagr.ReadLEB(a.buf, pl)
	c := pl + blb
	h, _ := dagr.ReadLEB(a.buf, c)
	return int(h), true
}

func (a DocumentAccessor) ItemsAt(i int) DocumentItemAccessor {
	pl := a.p[3]
	_, blb := dagr.ReadLEB(a.buf, pl)
	c := pl + blb
	cnt, hb := dagr.ReadLEB(a.buf, c)
	_ = cnt
	p := c + hb
	for j := 0; j < i; j++ {
		p = dagr.SkipBlob(a.buf, p)
	}
	return NewDocumentItemAccessor(a.buf, p)
}

type DocumentItemsIter struct {
	a    DocumentAccessor
	buf  []byte
	p    int
	i    int
	n    int
	walk bool
}

// ItemsIter walks the array once, keeping its position between elements —
// O(n) for the whole array, where ItemsAt(i) is O(i) per call.
func (a DocumentAccessor) ItemsIter() DocumentItemsIter {
	n, lok := a.ItemsLen()
	if !lok {
		n = 0
	}
	it := DocumentItemsIter{a: a, buf: a.buf, n: n}
	pl := a.p[3]
	if pl < 0 || n == 0 {
		return it
	}
	_, blb := dagr.ReadLEB(a.buf, pl)
	c := pl + blb
	cnt, hb := dagr.ReadLEB(a.buf, c)
	_ = cnt
	p := c + hb
	it.p = p
	it.walk = true
	return it
}

func (it *DocumentItemsIter) Next() (DocumentItemAccessor, bool) {
	var v DocumentItemAccessor
	if it.i >= it.n {
		return v, false
	}
	if !it.walk {
		v = it.a.ItemsAt(it.i)
		it.i++
		return v, true
	}
	i := it.i
	it.i++
	_ = i
	v = NewDocumentItemAccessor(it.buf, it.p)
	it.p = dagr.SkipBlob(it.buf, it.p)
	return v, true
}

func (it *DocumentItemsIter) Len() int { return it.n }

// ItemsAll ranges over the array in one pass.
func (a DocumentAccessor) ItemsAll() iter.Seq[DocumentItemAccessor] {
	return func(yield func(DocumentItemAccessor) bool) {
		it := a.ItemsIter()
		for {
			v, ok := it.Next()
			if !ok || !yield(v) {
				return
			}
		}
	}
}

// Document is a plain value record (spec 11 §4: no arena).
type Document struct {
	ID     dagr.Opt[string]
	Status dagr.Opt[int32]
	Meta   dagr.Opt[DocumentMeta]
	Items  dagr.Opt[[]DocumentItem]
}

func restoreDocument(acc DocumentAccessor) Document {
	var rec Document
	{
		if v, ok := acc.ID(); ok {
			rec.ID = dagr.Some(v)
		}
	}
	{
		if v, ok := acc.Status(); ok {
			rec.Status = dagr.Some(v)
		}
	}
	{
		if v, ok := acc.Meta(); ok {
			rec.Meta = dagr.Some(restoreDocumentMeta(v))
		}
	}
	{
		if cnt, ok := acc.ItemsLen(); ok {
			s := make([]DocumentItem, cnt)
			it := acc.ItemsIter()
			for i := 0; i < cnt; i++ {
				e, _ := it.Next()
				s[i] = restoreDocumentItem(e)
			}
			rec.Items = dagr.Some(s)
		}
	}
	return rec
}

// RestoreDocument decodes one Document (and its subtree) into plain values — the entry
// a DataSink uses for a record field that holds this graph's node (spec/11 §12).
func RestoreDocument(acc DocumentAccessor) Document {
	return restoreDocument(acc)
}

func storeDocument(n Document, b *dagr.Builder) dagr.NodeStoreRef {
	before := b.Cursor()
	var raw [1]bool
	if x3, ok := n.Items.V, n.Items.Ok; ok {
		dagr.StorePackedNodeArrayPlain(b, x3, func(b *dagr.Builder, v DocumentItem) { storeDocumentItem(v, b) })
	}
	if x2, ok := n.Meta.V, n.Meta.Ok; ok {
		storeDocumentMeta(x2, b)
	}
	if x1, ok := n.Status.V, n.Status.Ok; ok {
		zz := dagr.ToZigZag(int64(x1))
		if dagr.LebLength(zz) < 4 {
			b.StoreLEB(zz)
		} else {
			b.StoreI32(x1)
			raw[0] = true
		}
	}
	if x0, ok := n.ID.V, n.ID.Ok; ok {
		b.StoreUtf8(x0, false)
	}
	enc := make([]byte, 1)
	for k, r := range raw {
		if r {
			enc[k>>3] |= 1 << (k & 7)
		}
	}
	b.StoreBytes(enc)
	var nbits uint64
	if n.ID.Ok {
		nbits |= 1 << 0
	}
	if n.Status.Ok {
		nbits |= 1 << 1
	}
	if n.Meta.Ok {
		nbits |= 1 << 2
	}
	if n.Items.Ok {
		nbits |= 1 << 3
	}
	b.StoreBytes([]byte{byte(nbits >> 0)})
	b.StoreLEB(uint64(b.Cursor() - before))
	o := b.Cursor()
	return dagr.NodeStoreRef{Off: o}
}

// DocumentFrozenPackedGraphBuilder is the reusable form (spec 33 §4.3): one builder, reset per Build.
type DocumentFrozenPackedGraphBuilder struct {
	b *dagr.Builder
}

func NewDocumentFrozenPackedGraphBuilder() *DocumentFrozenPackedGraphBuilder {
	return &DocumentFrozenPackedGraphBuilder{b: dagr.NewBuilder(0)}
}

// Grow makes room for a record of n bytes up front, so the first Build does not
// have to double its way up from 64. A reused builder keeps its buffer.
func (w *DocumentFrozenPackedGraphBuilder) Grow(n int) { w.b.Grow(n) }

// Build serializes the value tree under root — byte-identical to the arena's ToBytes.
func (w *DocumentFrozenPackedGraphBuilder) Build(root Document) []byte {
	w.build(root)
	return w.b.MakeData()
}

// BuildAppend is Build without the copy: it appends the record to dst and returns
// the extended slice. A caller that keeps dst between batches reuses its capacity,
// so steady-state encoding allocates nothing — this is the Go analogue of Rust's
// write_into (spec 33 §4.3).
func (w *DocumentFrozenPackedGraphBuilder) BuildAppend(dst []byte, root Document) []byte {
	w.build(root)
	return w.b.AppendTo(dst)
}

// BuildBytes is BuildAppend with no copy at all: the returned slice aliases the
// builder's buffer and is invalidated by the next Build on it.
func (w *DocumentFrozenPackedGraphBuilder) BuildBytes(root Document) []byte {
	w.build(root)
	return w.b.Bytes()
}

func (w *DocumentFrozenPackedGraphBuilder) build(root Document) {
	b := w.b
	b.Reset()
	off := dagr.NodeOffset(storeDocument(root, b))
	b.StoreLEB(uint64(b.Cursor()-off) << 2)
}

// BuildDocumentFrozenPackedGraph is the one-shot form (spec 33 §4.2).
func BuildDocumentFrozenPackedGraph(root Document) []byte {
	return NewDocumentFrozenPackedGraphBuilder().Build(root)
}

// RestoreDocumentFrozenPackedGraphValues decodes a buffer into plain value structs (no arena);
// malformed bytes yield dagr.ErrMalformed.
func RestoreDocumentFrozenPackedGraphValues(buf []byte) (v Document, err error) {
	pos, err := dagr.RootOffset(buf)
	if err != nil {
		return Document{}, err
	}
	defer func() {
		if r := recover(); r != nil {
			v, err = Document{}, dagr.ErrMalformed
		}
	}()
	return restoreDocument(NewDocumentAccessor(buf, pos)), nil
}
