// Code generated by dagr (dagr/codegen/go/direct.py); DO NOT EDIT.

package eventgraphdirect

import (
	"iter"

	"dagrv2/dagr"
)

type EventAttrAccessor struct {
	buf []byte
	pos int
	p   [2]int
	raw uint64
}

func NewEventAttrAccessor(buf []byte, pos int) EventAttrAccessor {
	a := EventAttrAccessor{buf: buf, pos: pos}
	for i := range a.p {
		a.p[i] = -1
	}
	es, ee := dagr.PackedBounds(buf, pos)
	cursor := es
	last := -1
scan:
	for cursor < ee {
		tagv, tb := dagr.ReadLEB(buf, cursor)
		fidx := int(tagv >> 1)
		// spec 07: tags are strictly ascending; anything else is the unknown tail
		// (or a spec-15 header's zero padding) and ends the scan.
		if fidx <= last {
			break scan
		}
		last = fidx
		raw := (tagv & 1) == 1
		_ = raw
		pl := cursor + tb
		switch fidx {
		case 0:
			a.p[0] = pl
			if raw {
				a.raw |= 1 << 0
			}
			cursor = dagr.SkipBlob(buf, pl)
		case 1:
			a.p[1] = pl
			if raw {
				a.raw |= 1 << 1
			}
			cursor = dagr.SkipBlob(buf, pl)
		default:
			break scan
		}
	}
	return a
}

// BufferPos is where this node starts in the buffer — its identity in a graph.
func (a EventAttrAccessor) BufferPos() int { return a.pos }

// BufferBytes is the buffer this accessor reads — with BufferPos, enough to reopen
// the node through another package's accessor (a DataSink does, spec/11 §12).
func (a EventAttrAccessor) BufferBytes() []byte { return a.buf }

func (a EventAttrAccessor) Key() (string, bool) {
	pl := a.p[0]
	if pl < 0 {
		return "", false
	}
	s, _ := dagr.ReadUtf8(a.buf, pl)
	return s, true
}

func (a EventAttrAccessor) KeyBytes() ([]byte, bool) {
	pl := a.p[0]
	if pl < 0 {
		return nil, false
	}
	d, _ := dagr.ReadBytes(a.buf, pl)
	return d, true
}

func (a EventAttrAccessor) Value() (string, bool) {
	pl := a.p[1]
	if pl < 0 {
		return "", false
	}
	s, _ := dagr.ReadUtf8(a.buf, pl)
	return s, true
}

func (a EventAttrAccessor) ValueBytes() ([]byte, bool) {
	pl := a.p[1]
	if pl < 0 {
		return nil, false
	}
	d, _ := dagr.ReadBytes(a.buf, pl)
	return d, true
}

// EventAttr is a plain value record (spec 11 §4: no arena).
type EventAttr struct {
	Key   dagr.Opt[string]
	Value dagr.Opt[string]
}

func restoreEventAttr(acc EventAttrAccessor) EventAttr {
	var rec EventAttr
	{
		if v, ok := acc.Key(); ok {
			rec.Key = dagr.Some(v)
		}
	}
	{
		if v, ok := acc.Value(); ok {
			rec.Value = dagr.Some(v)
		}
	}
	return rec
}

// RestoreEventAttr decodes one EventAttr (and its subtree) into plain values — the entry
// a DataSink uses for a record field that holds this graph's node (spec/11 §12).
func RestoreEventAttr(acc EventAttrAccessor) EventAttr {
	return restoreEventAttr(acc)
}

func storeEventAttr(n EventAttr, b *dagr.Builder) dagr.NodeStoreRef {
	buf, i := b.Take()
	buf, i = PutEventAttr(n, b, buf, i)
	b.Put(buf, i)
	return dagr.NodeStoreRef{Off: b.Cursor()}
}

func PutEventAttr(n EventAttr, b *dagr.Builder, buf []byte, i int) ([]byte, int) {
	before := len(buf) - i
	if x1, ok := n.Value.V, n.Value.Ok; ok {
		buf, i = dagr.PutUtf8TaggedAt(buf, i, x1, 3)
	}
	if x0, ok := n.Key.V, n.Key.Ok; ok {
		buf, i = dagr.PutUtf8TaggedAt(buf, i, x0, 1)
	}
	return dagr.PutLEBAt(buf, i, uint64((len(buf)-i)-before))
}

type EventAccessor struct {
	buf []byte
	pos int
	p   [5]int
	raw uint64
}

func NewEventAccessor(buf []byte, pos int) EventAccessor {
	a := EventAccessor{buf: buf, pos: pos}
	for i := range a.p {
		a.p[i] = -1
	}
	es, ee := dagr.PackedBounds(buf, pos)
	cursor := es
	last := -1
scan:
	for cursor < ee {
		tagv, tb := dagr.ReadLEB(buf, cursor)
		fidx := int(tagv >> 1)
		// spec 07: tags are strictly ascending; anything else is the unknown tail
		// (or a spec-15 header's zero padding) and ends the scan.
		if fidx <= last {
			break scan
		}
		last = fidx
		raw := (tagv & 1) == 1
		_ = raw
		pl := cursor + tb
		switch fidx {
		case 0:
			a.p[0] = pl
			if raw {
				a.raw |= 1 << 0
			}
			cursor = dagr.SkipBlob(buf, pl)
		case 1:
			a.p[1] = pl
			if raw {
				a.raw |= 1 << 1
			}
			cursor = dagr.SkipBlob(buf, pl)
		case 2:
			a.p[2] = pl
			if raw {
				a.raw |= 1 << 2
			}
			if raw {
				cursor = pl + 8
			} else {
				cursor = dagr.LebEnd(buf, pl)
			}
		case 3:
			a.p[3] = pl
			if raw {
				a.raw |= 1 << 3
			}
			cursor = dagr.SkipBlob(buf, pl)
		case 4:
			a.p[4] = pl
			if raw {
				a.raw |= 1 << 4
			}
			cursor = dagr.SkipBlob(buf, pl)
		default:
			break scan
		}
	}
	return a
}

// BufferPos is where this node starts in the buffer — its identity in a graph.
func (a EventAccessor) BufferPos() int { return a.pos }

// BufferBytes is the buffer this accessor reads — with BufferPos, enough to reopen
// the node through another package's accessor (a DataSink does, spec/11 §12).
func (a EventAccessor) BufferBytes() []byte { return a.buf }

func (a EventAccessor) EventID() (string, bool) {
	pl := a.p[0]
	if pl < 0 {
		return "", false
	}
	s, _ := dagr.ReadUtf8(a.buf, pl)
	return s, true
}

func (a EventAccessor) EventIDBytes() ([]byte, bool) {
	pl := a.p[0]
	if pl < 0 {
		return nil, false
	}
	d, _ := dagr.ReadBytes(a.buf, pl)
	return d, true
}

func (a EventAccessor) EventType() (string, bool) {
	pl := a.p[1]
	if pl < 0 {
		return "", false
	}
	s, _ := dagr.ReadUtf8(a.buf, pl)
	return s, true
}

func (a EventAccessor) EventTypeBytes() ([]byte, bool) {
	pl := a.p[1]
	if pl < 0 {
		return nil, false
	}
	d, _ := dagr.ReadBytes(a.buf, pl)
	return d, true
}

func (a EventAccessor) OccurredAt() (int64, bool) {
	pl := a.p[2]
	raw := (a.raw>>2)&1 == 1
	if pl < 0 {
		return 0, false
	}
	if raw {
		return dagr.ReadI64(a.buf, pl), true
	}
	x, _ := dagr.ReadZigZagLEB(a.buf, pl)
	return int64(x), true
}

func (a EventAccessor) Producer() (string, bool) {
	pl := a.p[3]
	if pl < 0 {
		return "", false
	}
	s, _ := dagr.ReadUtf8(a.buf, pl)
	return s, true
}

func (a EventAccessor) ProducerBytes() ([]byte, bool) {
	pl := a.p[3]
	if pl < 0 {
		return nil, false
	}
	d, _ := dagr.ReadBytes(a.buf, pl)
	return d, true
}

func (a EventAccessor) AttrsLen() (int, bool) {
	pl := a.p[4]
	if pl < 0 {
		return 0, false
	}
	_, blb := dagr.ReadLEB(a.buf, pl)
	c := pl + blb
	h, _ := dagr.ReadLEB(a.buf, c)
	return int(h), true
}

func (a EventAccessor) AttrsAt(i int) EventAttrAccessor {
	pl := a.p[4]
	_, blb := dagr.ReadLEB(a.buf, pl)
	c := pl + blb
	cnt, hb := dagr.ReadLEB(a.buf, c)
	_ = cnt
	p := c + hb
	for j := 0; j < i; j++ {
		p = dagr.SkipBlob(a.buf, p)
	}
	return NewEventAttrAccessor(a.buf, p)
}

type EventAttrsIter struct {
	a    EventAccessor
	buf  []byte
	p    int
	i    int
	n    int
	walk bool
}

// AttrsIter walks the array once, keeping its position between elements —
// O(n) for the whole array, where AttrsAt(i) is O(i) per call.
func (a EventAccessor) AttrsIter() EventAttrsIter {
	n, lok := a.AttrsLen()
	if !lok {
		n = 0
	}
	it := EventAttrsIter{a: a, buf: a.buf, n: n}
	pl := a.p[4]
	if pl < 0 || n == 0 {
		return it
	}
	_, blb := dagr.ReadLEB(a.buf, pl)
	c := pl + blb
	cnt, hb := dagr.ReadLEB(a.buf, c)
	_ = cnt
	p := c + hb
	it.p = p
	it.walk = true
	return it
}

func (it *EventAttrsIter) Next() (EventAttrAccessor, bool) {
	var v EventAttrAccessor
	if it.i >= it.n {
		return v, false
	}
	if !it.walk {
		v = it.a.AttrsAt(it.i)
		it.i++
		return v, true
	}
	i := it.i
	it.i++
	_ = i
	v = NewEventAttrAccessor(it.buf, it.p)
	it.p = dagr.SkipBlob(it.buf, it.p)
	return v, true
}

func (it *EventAttrsIter) Len() int { return it.n }

// AttrsAll ranges over the array in one pass.
func (a EventAccessor) AttrsAll() iter.Seq[EventAttrAccessor] {
	return func(yield func(EventAttrAccessor) bool) {
		it := a.AttrsIter()
		for {
			v, ok := it.Next()
			if !ok || !yield(v) {
				return
			}
		}
	}
}

// Event is a plain value record (spec 11 §4: no arena).
type Event struct {
	EventID    dagr.Opt[string]
	EventType  dagr.Opt[string]
	OccurredAt dagr.Opt[int64]
	Producer   dagr.Opt[string]
	Attrs      dagr.Opt[[]EventAttr]
}

func restoreEvent(acc EventAccessor) Event {
	var rec Event
	{
		if v, ok := acc.EventID(); ok {
			rec.EventID = dagr.Some(v)
		}
	}
	{
		if v, ok := acc.EventType(); ok {
			rec.EventType = dagr.Some(v)
		}
	}
	{
		if v, ok := acc.OccurredAt(); ok {
			rec.OccurredAt = dagr.Some(v)
		}
	}
	{
		if v, ok := acc.Producer(); ok {
			rec.Producer = dagr.Some(v)
		}
	}
	{
		if cnt, ok := acc.AttrsLen(); ok {
			s := make([]EventAttr, cnt)
			it := acc.AttrsIter()
			for i := 0; i < cnt; i++ {
				e, _ := it.Next()
				s[i] = restoreEventAttr(e)
			}
			rec.Attrs = dagr.Some(s)
		}
	}
	return rec
}

// RestoreEvent decodes one Event (and its subtree) into plain values — the entry
// a DataSink uses for a record field that holds this graph's node (spec/11 §12).
func RestoreEvent(acc EventAccessor) Event { return restoreEvent(acc) }

func storeEvent(n Event, b *dagr.Builder) dagr.NodeStoreRef {
	buf, i := b.Take()
	buf, i = PutEvent(n, b, buf, i)
	b.Put(buf, i)
	return dagr.NodeStoreRef{Off: b.Cursor()}
}

func PutEvent(n Event, b *dagr.Builder, buf []byte, i int) ([]byte, int) {
	before := len(buf) - i
	if x4, ok := n.Attrs.V, n.Attrs.Ok; ok {
		{
			a4 := len(buf) - i
			for k := len(x4) - 1; k >= 0; k-- {
				buf, i = PutEventAttr(x4[k], b, buf, i)
			}
			buf, i = dagr.PutLEBAt(buf, i, uint64(len(x4)))
			buf, i = dagr.PutLEBAt(buf, i, uint64((len(buf)-i)-a4))
		}
		buf, i = dagr.PutLEBAt(buf, i, 9)
	}
	if x3, ok := n.Producer.V, n.Producer.Ok; ok {
		buf, i = dagr.PutUtf8TaggedAt(buf, i, x3, 7)
	}
	if x2, ok := n.OccurredAt.V, n.OccurredAt.Ok; ok {
		zz := dagr.ToZigZag(int64(x2))
		if dagr.LebLength(zz) < 8 {
			buf, i = dagr.PutLEBTaggedAt(buf, i, zz, 4)
		} else {
			buf, i = dagr.PutFixedAt(buf, i, uint64(uint64(x2)), 8)
			buf, i = dagr.PutLEBAt(buf, i, 5)
		}
	}
	if x1, ok := n.EventType.V, n.EventType.Ok; ok {
		buf, i = dagr.PutUtf8TaggedAt(buf, i, x1, 3)
	}
	if x0, ok := n.EventID.V, n.EventID.Ok; ok {
		buf, i = dagr.PutUtf8TaggedAt(buf, i, x0, 1)
	}
	return dagr.PutLEBAt(buf, i, uint64((len(buf)-i)-before))
}

// EventGraphBuilder is the reusable form (spec 33 §4.3): one builder, reset per Build.
type EventGraphBuilder struct {
	b *dagr.Builder
}

func NewEventGraphBuilder() *EventGraphBuilder {
	return &EventGraphBuilder{b: dagr.NewBuilder(0)}
}

// Grow makes room for a record of n bytes up front, so the first Build does not
// have to double its way up from 64. A reused builder keeps its buffer.
func (w *EventGraphBuilder) Grow(n int) { w.b.Grow(n) }

// Build serializes the value tree under root — byte-identical to the arena's ToBytes.
func (w *EventGraphBuilder) Build(root Event) []byte {
	w.build(root)
	return w.b.MakeData()
}

// BuildAppend is Build without the copy: it appends the record to dst and returns
// the extended slice. A caller that keeps dst between batches reuses its capacity,
// so steady-state encoding allocates nothing — this is the Go analogue of Rust's
// write_into (spec 33 §4.3).
func (w *EventGraphBuilder) BuildAppend(dst []byte, root Event) []byte {
	w.build(root)
	return w.b.AppendTo(dst)
}

// BuildBytes is BuildAppend with no copy at all: the returned slice aliases the
// builder's buffer and is invalidated by the next Build on it.
func (w *EventGraphBuilder) BuildBytes(root Event) []byte {
	w.build(root)
	return w.b.Bytes()
}

func (w *EventGraphBuilder) build(root Event) {
	b := w.b
	b.Reset()
	off := dagr.NodeOffset(storeEvent(root, b))
	b.StoreLEB(uint64(b.Cursor()-off) << 2)
}

// BuildEventGraph is the one-shot form (spec 33 §4.2).
func BuildEventGraph(root Event) []byte {
	return NewEventGraphBuilder().Build(root)
}

// RestoreEventGraphValues decodes a buffer into plain value structs (no arena);
// malformed bytes yield dagr.ErrMalformed.
func RestoreEventGraphValues(buf []byte) (v Event, err error) {
	pos, err := dagr.RootOffset(buf)
	if err != nil {
		return Event{}, err
	}
	defer func() {
		if r := recover(); r != nil {
			v, err = Event{}, dagr.ErrMalformed
		}
	}()
	return restoreEvent(NewEventAccessor(buf, pos)), nil
}
