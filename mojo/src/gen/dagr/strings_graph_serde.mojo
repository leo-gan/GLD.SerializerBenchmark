# GENERATED by dagr/codegen/mojo/serde.py — do not edit by hand.
# Serializer (arena -> bytes) for graph 'StringsGraph' — plan 28 Phase 4.

from dagr_writer import Builder, NodeStoreRef, node_offset, resolved_ref, nil_ref, to_zigzag, leb_length
from strings_graph_arena import StringsGraphArena, Strings

comptime _VT_MAX = 1   # widest regular-node vtable (§1.19)


# Build a List[Int] of vtable entries from a varargs-free call site.
def _ilist(*vals: Int) -> List[Int]:
    var l = List[Int]()
    for v in vals:
        l.append(v)
    return l^


# Type-namespaced node identity for dedup / cycle keys: a bare packed handle
# collides across node types (index space is per-type), so fold in the type id.
def _nid(tid: Int, idx: Int) -> UInt64:
    return (UInt64(idx) << 8) | UInt64(tid)


def _store_strings[o: Origin[mut=False], //](mut b: Builder[_VT_MAX], a: StringsGraphArena, h: Strings[o]) raises -> NodeStoreRef:
    var _before = b.cursor
    var _bi0 = h._index()
    ref _pa0 = a._arr_strings[_bi0].items
    var _pcb0 = b.cursor
    for _k0 in range(len(_pa0) - 1, -1, -1):
        _ = b.store_utf8(_pa0[_k0], False)
    _ = b.store_leb(UInt64(len(_pa0)))
    _ = b.store_leb(UInt64(b.cursor - _pcb0))
    _ = b.store_leb(UInt64(1))
    _ = b.store_leb(UInt64(b.cursor - _before))
    return resolved_ref(b.cursor)


def write_strings_graph(mut b: Builder[_VT_MAX], a: StringsGraphArena) raises -> Int:
    var _off = node_offset(_store_strings(b, a, a.root().value()))
    return b.store_leb(UInt64((b.cursor - _off) << 2))    # framing (no header)

def serialize_strings_graph(a: StringsGraphArena, max_size: Int = 2 * 1024 * 1024) raises -> List[UInt8]:
    var b = Builder[_VT_MAX](hint=len(a._arr_strings), max_size=max_size)
    _ = write_strings_graph(b, a)
    return b.make_data()
