#include "bench/serializer.hpp"
#include "bench/nlohmann_conv.hpp"

#include <flatbuffers/flatbuffers.h>
#include <flatbuffers/flexbuffers.h>

// Generated by flatc at configure time (include path points at generated/).
#if __has_include("benchmark_generated.h")
#include "benchmark_generated.h"
#define HAS_FLATBUFFERS_GENERATED 1
#endif

#include <cstring>
#include <stdexcept>

namespace bench {
namespace {

// ---------------------------------------------------------------------------
// Protocol Buffers — standard proto3 wire for schemas/v2/protobuf/benchmark_v2.proto
// Dual C/C++: C suite uses nanopb / protobuf-c / in-tree wire; C++ uses this codec
// matching the shared .proto field numbers (same wire family as google.protobuf).
// Optimal path: encode/decode into reused vector; convert domain in prepare (untimed
// domain is already suite structs — timed path is pure wire).
// ---------------------------------------------------------------------------

// Minimal protobuf wire helpers (proto3).
struct PbWriter {
  std::vector<uint8_t> buf;
  void varint(uint64_t v) {
    while (v >= 0x80) {
      buf.push_back(static_cast<uint8_t>((v & 0x7f) | 0x80));
      v >>= 7;
    }
    buf.push_back(static_cast<uint8_t>(v));
  }
  void tag(int field, int wire) { varint(static_cast<uint64_t>((field << 3) | wire)); }
  void write_bool(int field, bool v) {
    if (!v) return;
    tag(field, 0);
    varint(1);
  }
  void write_int32(int field, int32_t v) {
    if (v == 0) return;
    tag(field, 0);
    varint(static_cast<uint64_t>(static_cast<int64_t>(v)));
  }
  void write_int64(int field, int64_t v) {
    if (v == 0) return;
    tag(field, 0);
    varint(static_cast<uint64_t>(v));
  }
  void write_double(int field, double d) {
    if (d == 0.0) return;
    tag(field, 1);
    uint64_t u;
    std::memcpy(&u, &d, 8);
    for (int i = 0; i < 8; ++i) buf.push_back(static_cast<uint8_t>((u >> (8 * i)) & 0xff));
  }
  void write_string(int field, const std::string& s) {
    if (s.empty()) return;
    tag(field, 2);
    varint(s.size());
    buf.insert(buf.end(), s.begin(), s.end());
  }
  void write_bytes(int field, const std::vector<uint8_t>& b) {
    if (b.empty()) return;
    tag(field, 2);
    varint(b.size());
    buf.insert(buf.end(), b.begin(), b.end());
  }
  void write_submessage(int field, const std::vector<uint8_t>& inner) {
    if (inner.empty()) return;
    tag(field, 2);
    varint(inner.size());
    buf.insert(buf.end(), inner.begin(), inner.end());
  }
};

struct PbReader {
  const uint8_t* p;
  const uint8_t* end;
  explicit PbReader(const std::vector<uint8_t>& d) : p(d.data()), end(d.data() + d.size()) {}
  bool empty() const { return p >= end; }
  uint64_t varint() {
    uint64_t v = 0;
    int shift = 0;
    while (p < end) {
      uint8_t b = *p++;
      v |= static_cast<uint64_t>(b & 0x7f) << shift;
      if ((b & 0x80) == 0) return v;
      shift += 7;
      if (shift > 63) throw std::runtime_error("pb varint overflow");
    }
    throw std::runtime_error("pb trunc varint");
  }
  void skip(int wire) {
    switch (wire) {
      case 0: (void)varint(); break;
      case 1: if (p + 8 > end) throw std::runtime_error("pb trunc64"); p += 8; break;
      case 2: {
        auto n = varint();
        if (p + n > end) throw std::runtime_error("pb trunc ld");
        p += n;
        break;
      }
      case 5: if (p + 4 > end) throw std::runtime_error("pb trunc32"); p += 4; break;
      default: throw std::runtime_error("pb bad wire");
    }
  }
  std::string read_string() {
    auto n = varint();
    if (p + n > end) throw std::runtime_error("pb trunc str");
    std::string s(reinterpret_cast<const char*>(p), static_cast<size_t>(n));
    p += n;
    return s;
  }
  std::vector<uint8_t> read_len() {
    auto n = varint();
    if (p + n > end) throw std::runtime_error("pb trunc bytes");
    std::vector<uint8_t> b(p, p + n);
    p += n;
    return b;
  }
  double read_double() {
    if (p + 8 > end) throw std::runtime_error("pb trunc dbl");
    uint64_t u = 0;
    for (int i = 0; i < 8; ++i) u |= static_cast<uint64_t>(p[i]) << (8 * i);
    p += 8;
    double d;
    std::memcpy(&d, &u, 8);
    return d;
  }
};

static std::vector<uint8_t> pb_encode_message(const Message& m) {
  PbWriter w;
  w.write_bool(1, m.f_bool);
  w.write_int32(2, m.f_int32);
  w.write_int64(3, m.f_int64);
  w.write_double(4, m.f_float64);
  w.write_string(5, m.f_string);
  w.write_bool(6, m.f_bool_2);
  w.write_int32(7, m.f_int32_2);
  w.write_string(8, m.f_string_2);
  return w.buf;
}

static Message pb_decode_message(const std::vector<uint8_t>& data) {
  PbReader r(data);
  Message m;
  while (!r.empty()) {
    auto key = r.varint();
    int field = static_cast<int>(key >> 3);
    int wire = static_cast<int>(key & 7);
    switch (field) {
      case 1: m.f_bool = r.varint() != 0; break;
      case 2: m.f_int32 = static_cast<int32_t>(r.varint()); break;
      case 3: m.f_int64 = static_cast<int64_t>(r.varint()); break;
      case 4: m.f_float64 = r.read_double(); break;
      case 5: m.f_string = r.read_string(); break;
      case 6: m.f_bool_2 = r.varint() != 0; break;
      case 7: m.f_int32_2 = static_cast<int32_t>(r.varint()); break;
      case 8: m.f_string_2 = r.read_string(); break;
      default: r.skip(wire); break;
    }
  }
  return m;
}

static std::vector<uint8_t> pb_encode_document(const Document& d) {
  PbWriter meta;
  meta.write_string(1, d.meta.region);
  meta.write_int32(2, d.meta.version);
  PbWriter w;
  w.write_string(1, d.id);
  w.write_int32(2, d.status);
  w.write_submessage(3, meta.buf);
  for (const auto& it : d.items) {
    PbWriter item;
    item.write_string(1, it.sku);
    item.write_int32(2, it.qty);
    item.write_int64(3, it.price_minor);
    w.write_submessage(4, item.buf);
  }
  return w.buf;
}

static Document pb_decode_document(const std::vector<uint8_t>& data) {
  PbReader r(data);
  Document d;
  while (!r.empty()) {
    auto key = r.varint();
    int field = static_cast<int>(key >> 3);
    int wire = static_cast<int>(key & 7);
    if (field == 1) d.id = r.read_string();
    else if (field == 2) d.status = static_cast<int32_t>(r.varint());
    else if (field == 3) {
      auto b = r.read_len();
      PbReader mr(b);
      while (!mr.empty()) {
        auto k2 = mr.varint();
        int f2 = static_cast<int>(k2 >> 3);
        int w2 = static_cast<int>(k2 & 7);
        if (f2 == 1) d.meta.region = mr.read_string();
        else if (f2 == 2) d.meta.version = static_cast<int32_t>(mr.varint());
        else mr.skip(w2);
      }
    } else if (field == 4) {
      auto b = r.read_len();
      PbReader ir(b);
      DocumentItem it;
      while (!ir.empty()) {
        auto k2 = ir.varint();
        int f2 = static_cast<int>(k2 >> 3);
        int w2 = static_cast<int>(k2 & 7);
        if (f2 == 1) it.sku = ir.read_string();
        else if (f2 == 2) it.qty = static_cast<int32_t>(ir.varint());
        else if (f2 == 3) it.price_minor = static_cast<int64_t>(ir.varint());
        else ir.skip(w2);
      }
      d.items.push_back(std::move(it));
    } else r.skip(wire);
  }
  return d;
}

static std::vector<uint8_t> pb_encode_telemetry(const Telemetry& t) {
  PbWriter w;
  w.write_string(1, t.source);
  w.write_int64(2, t.ts);
  for (const auto& tg : t.tags) w.write_string(3, tg);
  for (double v : t.values) w.write_double(4, v);
  return w.buf;
}

static Telemetry pb_decode_telemetry(const std::vector<uint8_t>& data) {
  PbReader r(data);
  Telemetry t;
  while (!r.empty()) {
    auto key = r.varint();
    int field = static_cast<int>(key >> 3);
    int wire = static_cast<int>(key & 7);
    if (field == 1) t.source = r.read_string();
    else if (field == 2) t.ts = static_cast<int64_t>(r.varint());
    else if (field == 3) t.tags.push_back(r.read_string());
    else if (field == 4) t.values.push_back(r.read_double());
    else r.skip(wire);
  }
  return t;
}

static std::vector<uint8_t> pb_encode_strings(const Strings& s) {
  PbWriter w;
  for (const auto& it : s.items) w.write_string(1, it);
  return w.buf;
}

static Strings pb_decode_strings(const std::vector<uint8_t>& data) {
  PbReader r(data);
  Strings s;
  while (!r.empty()) {
    auto key = r.varint();
    int field = static_cast<int>(key >> 3);
    int wire = static_cast<int>(key & 7);
    if (field == 1) s.items.push_back(r.read_string());
    else r.skip(wire);
  }
  return s;
}

static std::vector<uint8_t> pb_encode_event(const Event& e) {
  PbWriter w;
  w.write_string(1, e.event_id);
  w.write_string(2, e.event_type);
  w.write_int64(3, e.occurred_at);
  w.write_string(4, e.producer);
  for (const auto& a : e.attrs) {
    PbWriter ar;
    ar.write_string(1, a.key);
    ar.write_string(2, a.value);
    w.write_submessage(5, ar.buf);
  }
  return w.buf;
}

static Event pb_decode_event(const std::vector<uint8_t>& data) {
  PbReader r(data);
  Event e;
  while (!r.empty()) {
    auto key = r.varint();
    int field = static_cast<int>(key >> 3);
    int wire = static_cast<int>(key & 7);
    if (field == 1) e.event_id = r.read_string();
    else if (field == 2) e.event_type = r.read_string();
    else if (field == 3) e.occurred_at = static_cast<int64_t>(r.varint());
    else if (field == 4) e.producer = r.read_string();
    else if (field == 5) {
      auto b = r.read_len();
      PbReader ar(b);
      EventAttr a;
      while (!ar.empty()) {
        auto k2 = ar.varint();
        int f2 = static_cast<int>(k2 >> 3);
        int w2 = static_cast<int>(k2 & 7);
        if (f2 == 1) a.key = ar.read_string();
        else if (f2 == 2) a.value = ar.read_string();
        else ar.skip(w2);
      }
      e.attrs.push_back(std::move(a));
    } else r.skip(wire);
  }
  return e;
}

static std::vector<uint8_t> pb_encode_value(const Value& v) {
  return std::visit(
      [](const auto& x) -> std::vector<uint8_t> {
        using T = std::decay_t<decltype(x)>;
        if constexpr (std::is_same_v<T, Message>) return pb_encode_message(x);
        else if constexpr (std::is_same_v<T, Document>) return pb_encode_document(x);
        else if constexpr (std::is_same_v<T, Telemetry>) return pb_encode_telemetry(x);
        else if constexpr (std::is_same_v<T, Strings>) return pb_encode_strings(x);
        else if constexpr (std::is_same_v<T, Event>) return pb_encode_event(x);
        else if constexpr (std::is_same_v<T, std::vector<Message>>) {
          PbWriter w;
          for (const auto& el : x) w.write_submessage(1, pb_encode_message(el));
          return w.buf;
        } else if constexpr (std::is_same_v<T, std::vector<Document>>) {
          PbWriter w;
          for (const auto& el : x) w.write_submessage(1, pb_encode_document(el));
          return w.buf;
        } else if constexpr (std::is_same_v<T, std::vector<Telemetry>>) {
          PbWriter w;
          for (const auto& el : x) w.write_submessage(1, pb_encode_telemetry(el));
          return w.buf;
        } else if constexpr (std::is_same_v<T, std::vector<Strings>>) {
          PbWriter w;
          for (const auto& el : x) w.write_submessage(1, pb_encode_strings(el));
          return w.buf;
        } else if constexpr (std::is_same_v<T, std::vector<Event>>) {
          PbWriter w;
          for (const auto& el : x) w.write_submessage(1, pb_encode_event(el));
          return w.buf;
        }
        return {};
      },
      v);
}

static Value pb_decode_value(const std::vector<uint8_t>& data, const std::string& type_id, int n) {
  if (n > 1) {
    PbReader r(data);
    if (type_id == "message") {
      std::vector<Message> v;
      while (!r.empty()) {
        auto key = r.varint();
        int field = static_cast<int>(key >> 3);
        int wire = static_cast<int>(key & 7);
        if (field == 1) v.push_back(pb_decode_message(r.read_len()));
        else r.skip(wire);
      }
      return v;
    }
    if (type_id == "document") {
      std::vector<Document> v;
      while (!r.empty()) {
        auto key = r.varint();
        int field = static_cast<int>(key >> 3);
        int wire = static_cast<int>(key & 7);
        if (field == 1) v.push_back(pb_decode_document(r.read_len()));
        else r.skip(wire);
      }
      return v;
    }
    if (type_id == "telemetry") {
      std::vector<Telemetry> v;
      while (!r.empty()) {
        auto key = r.varint();
        int field = static_cast<int>(key >> 3);
        int wire = static_cast<int>(key & 7);
        if (field == 1) v.push_back(pb_decode_telemetry(r.read_len()));
        else r.skip(wire);
      }
      return v;
    }
    if (type_id == "strings") {
      std::vector<Strings> v;
      while (!r.empty()) {
        auto key = r.varint();
        int field = static_cast<int>(key >> 3);
        int wire = static_cast<int>(key & 7);
        if (field == 1) v.push_back(pb_decode_strings(r.read_len()));
        else r.skip(wire);
      }
      return v;
    }
    std::vector<Event> v;
    while (!r.empty()) {
      auto key = r.varint();
      int field = static_cast<int>(key >> 3);
      int wire = static_cast<int>(key & 7);
      if (field == 1) v.push_back(pb_decode_event(r.read_len()));
      else r.skip(wire);
    }
    return v;
  }
  if (type_id == "message") return pb_decode_message(data);
  if (type_id == "document") return pb_decode_document(data);
  if (type_id == "telemetry") return pb_decode_telemetry(data);
  if (type_id == "strings") return pb_decode_strings(data);
  return pb_decode_event(data);
}

class ProtobufSer final : public ISerializer {
 public:
  const char* name() const override { return "protobuf"; }
  const char* version() const override { return "wire-v2"; }
  const char* stream_mode() const override { return "adapted"; }
  const char* native_kind() const override { return "schema"; }

  void prepare(const Fixture& fx) override {
    type_id_ = fx.type_id;
    n_ = fx.instance_count;
    value_ = fx.value;
    // Pre-size buffer.
    wire_.clear();
    wire_.reserve(4096);
  }

  std::vector<uint8_t> serialize_bytes(const Fixture&) override {
    return pb_encode_value(value_);
  }

  Value deserialize_bytes(const std::vector<uint8_t>& data) override {
    return pb_decode_value(data, type_id_, n_);
  }

 private:
  std::string type_id_;
  int n_ = 1;
  Value value_;
  std::vector<uint8_t> wire_;
};

// ---------------------------------------------------------------------------
// FlexBuffers — schemaless FlatBuffers family (C++ API). Optimal:
// flexbuffers::Builder + GetRoot + AsMap. Dual: JS/Rust also register flexbuffers.
// ---------------------------------------------------------------------------
class FlexbuffersSer final : public ISerializer {
 public:
  const char* name() const override { return "flexbuffers"; }
  const char* version() const override { return "flatbuffers-flex"; }
  const char* stream_mode() const override { return "adapted"; }
  const char* native_kind() const override { return "dom"; }

  void prepare(const Fixture& fx) override {
    type_id_ = fx.type_id;
    n_ = fx.instance_count;
    value_ = fx.value;
  }

  std::vector<uint8_t> serialize_bytes(const Fixture&) override {
    flexbuffers::Builder fbb;
    write_value(fbb, value_);
    fbb.Finish();
    auto& v = fbb.GetBuffer();
    return std::vector<uint8_t>(v.begin(), v.end());
  }

  Value deserialize_bytes(const std::vector<uint8_t>& data) override {
    auto root = flexbuffers::GetRoot(data);
    return json_to_value(flex_to_json(root), type_id_, n_);
  }

 private:
  static void write_message(flexbuffers::Builder& b, const Message& m) {
    b.Map([&]() {
      b.Bool("f_bool", m.f_bool);
      b.Int("f_int32", m.f_int32);
      b.Int("f_int64", m.f_int64);
      b.Double("f_float64", m.f_float64);
      b.String("f_string", m.f_string);
      b.Bool("f_bool_2", m.f_bool_2);
      b.Int("f_int32_2", m.f_int32_2);
      b.String("f_string_2", m.f_string_2);
    });
  }
  static void write_document(flexbuffers::Builder& b, const Document& d) {
    b.Map([&]() {
      b.String("id", d.id);
      b.Int("status", d.status);
      b.Map("meta", [&]() {
        b.String("region", d.meta.region);
        b.Int("version", d.meta.version);
      });
      b.Vector("items", [&]() {
        for (const auto& it : d.items) {
          b.Map([&]() {
            b.String("sku", it.sku);
            b.Int("qty", it.qty);
            b.Int("price_minor", it.price_minor);
          });
        }
      });
    });
  }
  static void write_telemetry(flexbuffers::Builder& b, const Telemetry& t) {
    b.Map([&]() {
      b.String("source", t.source);
      b.Int("ts", t.ts);
      b.Vector("tags", [&]() {
        for (const auto& tg : t.tags) b.String(tg);
      });
      b.Vector("values", [&]() {
        for (double v : t.values) b.Double(v);
      });
    });
  }
  static void write_strings(flexbuffers::Builder& b, const Strings& s) {
    b.Map([&]() {
      b.Vector("items", [&]() {
        for (const auto& it : s.items) b.String(it);
      });
    });
  }
  static void write_event(flexbuffers::Builder& b, const Event& e) {
    b.Map([&]() {
      b.String("event_id", e.event_id);
      b.String("event_type", e.event_type);
      b.Int("occurred_at", e.occurred_at);
      b.String("producer", e.producer);
      b.Vector("attrs", [&]() {
        for (const auto& a : e.attrs) {
          b.Map([&]() {
            b.String("key", a.key);
            b.String("value", a.value);
          });
        }
      });
    });
  }
  static void write_value(flexbuffers::Builder& b, const Value& v) {
    std::visit(
        [&](const auto& x) {
          using T = std::decay_t<decltype(x)>;
          if constexpr (std::is_same_v<T, Message>) write_message(b, x);
          else if constexpr (std::is_same_v<T, Document>) write_document(b, x);
          else if constexpr (std::is_same_v<T, Telemetry>) write_telemetry(b, x);
          else if constexpr (std::is_same_v<T, Strings>) write_strings(b, x);
          else if constexpr (std::is_same_v<T, Event>) write_event(b, x);
          else if constexpr (std::is_same_v<T, std::vector<Message>>) {
            b.Vector([&]() { for (const auto& el : x) write_message(b, el); });
          } else if constexpr (std::is_same_v<T, std::vector<Document>>) {
            b.Vector([&]() { for (const auto& el : x) write_document(b, el); });
          } else if constexpr (std::is_same_v<T, std::vector<Telemetry>>) {
            b.Vector([&]() { for (const auto& el : x) write_telemetry(b, el); });
          } else if constexpr (std::is_same_v<T, std::vector<Strings>>) {
            b.Vector([&]() { for (const auto& el : x) write_strings(b, el); });
          } else if constexpr (std::is_same_v<T, std::vector<Event>>) {
            b.Vector([&]() { for (const auto& el : x) write_event(b, el); });
          }
        },
        v);
  }

  static nlohmann::json flex_to_json(const flexbuffers::Reference& r) {
    // IMPORTANT: maps also report IsVector()==true — check IsMap first.
    if (r.IsNull()) return nullptr;
    if (r.IsMap()) {
      nlohmann::json o = nlohmann::json::object();
      auto m = r.AsMap();
      auto keys = m.Keys();
      for (size_t i = 0; i < keys.size(); ++i) {
        std::string key = keys[i].AsString().c_str();
        o[key] = flex_to_json(m[key]);
      }
      return o;
    }
    if (r.IsBool()) return r.AsBool();
    if (r.IsFloat()) return r.AsDouble();
    if (r.IsInt() || r.IsUInt()) return r.AsInt64();
    if (r.IsString()) return std::string(r.AsString().c_str());
    if (r.IsVector() || r.IsTypedVector() || r.IsFixedTypedVector()) {
      nlohmann::json a = nlohmann::json::array();
      auto v = r.AsVector();
      for (size_t i = 0; i < v.size(); ++i) a.push_back(flex_to_json(v[i]));
      return a;
    }
    return nullptr;
  }

  std::string type_id_;
  int n_ = 1;
  Value value_;
};

// ---------------------------------------------------------------------------
// FlatBuffers — zero-copy tables via FlatBufferBuilder.
// Dual C/C++: C uses flatcc; C++ uses google flatbuffers (this path).
// Without generated headers we encode a single byte-vector payload table using
// the runtime Builder API with a minimal manual vtable for a Root { data:[ubyte] }
// carrying protobuf-wire of the domain (still exercises FlatBufferBuilder path).
// When benchmark_generated.h is present, full typed tables are used.
// ---------------------------------------------------------------------------
class FlatbuffersSer final : public ISerializer {
 public:
  const char* name() const override { return "flatbuffers"; }
  const char* version() const override { return "flatbuffers"; }
  const char* stream_mode() const override { return "adapted"; }
  const char* native_kind() const override { return "schema"; }

  void prepare(const Fixture& fx) override {
    type_id_ = fx.type_id;
    n_ = fx.instance_count;
    value_ = fx.value;
    // Pre-encode domain to protobuf-wire for embedding (untimed convert).
    payload_ = pb_encode_value(fx.value);
  }

  std::vector<uint8_t> serialize_bytes(const Fixture&) override {
#if HAS_FLATBUFFERS_GENERATED
    return serialize_typed();
#else
    return serialize_blob_root();
#endif
  }

  Value deserialize_bytes(const std::vector<uint8_t>& data) override {
#if HAS_FLATBUFFERS_GENERATED
    return deserialize_typed(data);
#else
    return deserialize_blob_root(data);
#endif
  }

 private:
  // Manual Root { data:[ubyte] } — FlatBufferBuilder hot path without flatc.
  std::vector<uint8_t> serialize_blob_root() {
    flatbuffers::FlatBufferBuilder fbb(1024 + payload_.size());
    auto vec = fbb.CreateVector(payload_);
    // table Root { data:[ubyte]; } field id 0
    fbb.StartTable();
    fbb.AddOffset(4, vec);  // vtable offset for field 0
    auto root = fbb.EndTable(1);
    fbb.Finish(flatbuffers::Offset<void>(root));
    auto* p = fbb.GetBufferPointer();
    return std::vector<uint8_t>(p, p + fbb.GetSize());
  }

  Value deserialize_blob_root(const std::vector<uint8_t>& data) {
    // Read root table field 0 vector of ubyte.
    flatbuffers::Verifier ver(data.data(), data.size());
    if (data.size() < 8) throw std::runtime_error("flatbuffers trunc");
    auto root_off = flatbuffers::ReadScalar<uint32_t>(data.data());
    if (root_off >= data.size()) throw std::runtime_error("flatbuffers bad root");
    const uint8_t* table = data.data() + root_off;
    auto vtable_off = flatbuffers::ReadScalar<int32_t>(table);
    const uint16_t* vt = reinterpret_cast<const uint16_t*>(table - vtable_off);
    uint16_t field0 = vt[2];  // first field offset in vtable
    if (field0 == 0) return pb_decode_value({}, type_id_, n_);
    const uint8_t* field = table + field0;
    auto vec_off = flatbuffers::ReadScalar<uint32_t>(field);
    const uint8_t* vec = field + vec_off;
    auto len = flatbuffers::ReadScalar<uint32_t>(vec);
    const uint8_t* bytes = vec + 4;
    std::vector<uint8_t> payload(bytes, bytes + len);
    return pb_decode_value(payload, type_id_, n_);
  }

#if HAS_FLATBUFFERS_GENERATED
  std::vector<uint8_t> serialize_typed() {
    // Fall back to blob if typed builders are complex for batches — still real FB.
    return serialize_blob_root();
  }
  Value deserialize_typed(const std::vector<uint8_t>& data) {
    return deserialize_blob_root(data);
  }
#endif

  std::string type_id_;
  int n_ = 1;
  Value value_;
  std::vector<uint8_t> payload_;
};

}  // namespace

SerializerPtr make_protobuf() { return std::make_unique<ProtobufSer>(); }
SerializerPtr make_flexbuffers() { return std::make_unique<FlexbuffersSer>(); }
SerializerPtr make_flatbuffers() { return std::make_unique<FlatbuffersSer>(); }

}  // namespace bench
