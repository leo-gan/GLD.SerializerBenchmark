// Code generated by dagr (dagr/codegen/go/arena.py); DO NOT EDIT.

package stringsfrozenpackedgraph

import (
	"strconv"
	"strings"

	"dagrv2/dagr"
)

type stringsValues struct {
	items dagr.Opt[[]string]
}

// Arena owns every node of one graph instance (struct-of-arrays storage; handles
// are indices into it, so the graph may be cyclic and share nodes freely).
type Arena struct {
	stringss []stringsValues
	root     dagr.Opt[uint64]
	id       uint32
}

var arenaIDs uint32

// NewArena creates an empty arena.
func NewArena() *Arena {
	arenaIDs++
	return &Arena{id: arenaIDs}
}

func (a *Arena) Root() (Strings, bool) {
	if !a.root.Ok {
		return Strings{}, false
	}
	h := Strings{a: a, h: a.root.V}
	return h, true
}

func (a *Arena) SetRoot(h Strings) { a.root = dagr.Some(h.h) }

func (a *Arena) ClearRoot() { a.root = dagr.None[uint64]() }

func (a *Arena) allocStrings() (int, uint64) {
	i := len(a.stringss)
	a.stringss = append(a.stringss, stringsValues{})
	return i, dagr.Pack(0, i)
}

// NewStrings allocates a Strings with construct-time defaults.
func (a *Arena) NewStrings() Strings {
	idx, h := a.allocStrings()
	_ = idx
	return Strings{a: a, h: h}
}

// Strings is a value handle into an Arena (spec 09: generation<<40 | index).
type Strings struct {
	a *Arena
	h uint64
}

func (n Strings) Arena() *Arena { return n.a }

func (n Strings) Handle() uint64 { return n.h }

func (n Strings) Index() int { return dagr.UnpackIdx(n.h) }

func (n Strings) vals() *stringsValues {
	return &n.a.stringss[dagr.UnpackIdx(n.h)]
}

func (n Strings) key() dagr.NodeKey {
	return dagr.NodeKey{Type: 0, Handle: n.h}
}

func (n Strings) IsValid() bool {
	return dagr.UnpackIdx(n.h) < len(n.a.stringss)
}

func (n Strings) Items() ([]string, bool) {
	o := n.vals().items
	if !o.Ok {
		return nil, false
	}
	s := o.V
	return s, true
}

func (n Strings) ItemsLen() (int, bool) {
	s, ok := n.Items()
	return len(s), ok
}

func (n Strings) ItemsAt(i int) string {
	s, _ := n.Items()
	return s[i]
}

func (n Strings) SetItems(v []string) { n.vals().items = dagr.Some(v) }

func (n Strings) ClearItems() { n.vals().items = dagr.None[[]string]() }

func (n Strings) describe(sb *strings.Builder, seen map[dagr.NodeKey]int) {
	k := dagr.NodeKey{Type: 0, Handle: n.h}
	if ord, ok := seen[k]; ok {
		sb.WriteString("Strings#")
		sb.WriteString(strconv.Itoa(ord))
		return
	}
	seen[k] = len(seen)
	sb.WriteString("Strings#")
	sb.WriteString(strconv.Itoa(len(seen) - 1))
	sb.WriteString("{")
	sb.WriteString("items: ")
	if n.vals().items.Ok {
		sb.WriteByte('[')
		for i, e := range n.vals().items.V {
			if i > 0 {
				sb.WriteString(", ")
			}
			sb.WriteString(strconv.Quote(e))
		}
		sb.WriteByte(']')
	} else {
		sb.WriteString("nil")
	}
	sb.WriteByte('}')
}

// String renders the node and everything reachable from it; a repeated node prints
// as `Strings#<ordinal>` so cycles terminate and sharing is visible.
func (n Strings) String() string {
	var sb strings.Builder
	n.describe(&sb, map[dagr.NodeKey]int{})
	return sb.String()
}

// Hash is a structural hash (FNV-1a of the canonical description): isomorphic graphs
// in different arenas hash equal.
func (n Strings) Hash() uint64 { return dagr.Fnv1a([]byte(n.String())) }

// Equals is cross-arena structural equality (cycle- and sharing-aware).
func (n Strings) Equals(o Strings) bool { return n.String() == o.String() }

func restoreStrings(acc StringsAccessor, a *Arena, seen map[dagr.SeenKey]uint64) Strings {
	sk := dagr.SeenAt(acc.buf, acc.pos)
	if h, ok := seen[sk]; ok {
		return Strings{a: a, h: h}
	}
	idx, h := a.allocStrings()
	seen[sk] = h
	var tmpItems dagr.Opt[[]string]
	{
		if cnt, ok := acc.ItemsLen(); ok {
			s := make([]string, cnt)
			it := acc.ItemsIter()
			for i := 0; i < cnt; i++ {
				e, _ := it.Next()
				s[i] = e
			}
			tmpItems = dagr.Some(s)
		}
	}
	a.stringss[idx].items = tmpItems
	return Strings{a: a, h: h}
}

// RestoreStringsFrozenPackedGraph eagerly materializes a buffer into a fresh Arena (shared and cyclic
// nodes materialize once).
func RestoreStringsFrozenPackedGraph(buf []byte) (*Arena, error) {
	acc, err := OpenStringsFrozenPackedGraph(buf)
	if err != nil {
		return nil, err
	}
	a := NewArena()
	a.SetRoot(restoreStrings(acc, a, map[dagr.SeenKey]uint64{}))
	return a, nil
}
