// Generated by dagr/codegen/typescript/lazy.py — do not edit by hand.
// Graph: MessageGraph
import { Buf, readLEB, readLEBBig, zigzagDecode, zigzagDecodeBig, rootOffset, readU8, readI32, readI64, readF64, decodePackedFloat64, readUtf8At } from "./dagr_reader";

export class MessageAccessor {
  private readonly buf: Buf;
  readonly _start: number;  // node position, for eager-restore dedup
  private _v0: boolean | null = null;  // f_bool
  private _v1: number | null = null;  // f_int32
  private _v2: bigint | null = null;  // f_int64
  private _v3: number | null = null;  // f_float64
  private _v4: string | null = null;  // f_string
  private _v5: boolean | null = null;  // f_bool_2
  private _v6: number | null = null;  // f_int32_2
  private _v7: string | null = null;  // f_string_2
  constructor(buf: Buf, start: number) {
    this.buf = buf;
    this._start = start;
    const [bl, blB] = readLEB(buf, start);
    let cursor = start + blB;
    const end = cursor + bl;
    if (cursor < end) {
      const [tag, tagB] = readLEB(buf, cursor);
      if (tag >> 1 === 0) {
        cursor += tagB;
        if ((tag & 1) === 1) {
          this._v0 = (readU8(buf, cursor) !== 0); cursor += 1;
        } else {
          const [_lv, _lb] = readLEB(buf, cursor);
          this._v0 = _lv !== 0;
          cursor += _lb;
        }
      }
    }
    if (cursor < end) {
      const [tag, tagB] = readLEB(buf, cursor);
      if (tag >> 1 === 1) {
        cursor += tagB;
        if ((tag & 1) === 1) {
          this._v1 = readI32(buf, cursor); cursor += 4;
        } else {
          const [_lv, _lb] = readLEB(buf, cursor);
          this._v1 = zigzagDecode(_lv);
          cursor += _lb;
        }
      }
    }
    if (cursor < end) {
      const [tag, tagB] = readLEB(buf, cursor);
      if (tag >> 1 === 2) {
        cursor += tagB;
        if ((tag & 1) === 1) {
          this._v2 = readI64(buf, cursor); cursor += 8;
        } else {
          const [_lv, _lb] = readLEBBig(buf, cursor);
          this._v2 = zigzagDecodeBig(_lv);
          cursor += _lb;
        }
      }
    }
    if (cursor < end) {
      const [tag, tagB] = readLEB(buf, cursor);
      if (tag >> 1 === 3) {
        cursor += tagB;
        if ((tag & 1) === 1) {
          this._v3 = readF64(buf, cursor); cursor += 8;
        } else {
          const [_fv, _fb] = decodePackedFloat64(buf, cursor);
          this._v3 = _fv;
          cursor += _fb;
        }
      }
    }
    if (cursor < end) {
      const [tag, tagB] = readLEB(buf, cursor);
      if (tag >> 1 === 4) {
        cursor += tagB;
        const [_s, _sb] = readUtf8At(buf, cursor); this._v4 = _s; cursor += _sb;
      }
    }
    if (cursor < end) {
      const [tag, tagB] = readLEB(buf, cursor);
      if (tag >> 1 === 5) {
        cursor += tagB;
        if ((tag & 1) === 1) {
          this._v5 = (readU8(buf, cursor) !== 0); cursor += 1;
        } else {
          const [_lv, _lb] = readLEB(buf, cursor);
          this._v5 = _lv !== 0;
          cursor += _lb;
        }
      }
    }
    if (cursor < end) {
      const [tag, tagB] = readLEB(buf, cursor);
      if (tag >> 1 === 6) {
        cursor += tagB;
        if ((tag & 1) === 1) {
          this._v6 = readI32(buf, cursor); cursor += 4;
        } else {
          const [_lv, _lb] = readLEB(buf, cursor);
          this._v6 = zigzagDecode(_lv);
          cursor += _lb;
        }
      }
    }
    if (cursor < end) {
      const [tag, tagB] = readLEB(buf, cursor);
      if (tag >> 1 === 7) {
        cursor += tagB;
        const [_s] = readUtf8At(buf, cursor); this._v7 = _s;
      }
    }
  }
  get f_bool(): boolean | null { return this._v0; }
  get f_int32(): number | null { return this._v1; }
  get f_int64(): bigint | null { return this._v2; }
  get f_float64(): number | null { return this._v3; }
  get f_string(): string | null { return this._v4; }
  get f_bool_2(): boolean | null { return this._v5; }
  get f_int32_2(): number | null { return this._v6; }
  get f_string_2(): string | null { return this._v7; }
  static lazyRoot(bytes: Uint8Array): MessageAccessor {
    const buf = new Buf(bytes);
    return new MessageAccessor(buf, rootOffset(buf));
  }
}
