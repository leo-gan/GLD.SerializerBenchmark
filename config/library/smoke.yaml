# Run config: smoke (CI / fast path) — Data Model v2 only
id: smoke
description: "Minimal v2 matrix — message + telemetry, single instance"
data_model_version: 2

types:
  - type_id: message
    type_config: {}
  - type_id: telemetry
    type_config: {}

data_type_instance_count: [1]

compression:
  mode: none

execution:
  mode: smoke
  io_modes: [bytes]

