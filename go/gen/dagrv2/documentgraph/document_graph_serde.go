// Code generated by dagr (dagr/codegen/go/serde.py); DO NOT EDIT.

package documentgraph

import "dagrv2/dagr"

func storeDocumentMeta(n DocumentMeta, b *dagr.Builder) dagr.NodeStoreRef {
	if d, ok := b.StructLookup[n.key()]; ok {
		return dagr.NodeStoreRef{Off: d}
	}
	before := b.Cursor()
	if x1, ok := n.Version(); ok {
		zz := dagr.ToZigZag(int64(x1))
		if dagr.LebLength(zz) < 4 {
			b.StoreLEBTagged(zz, 2)
		} else {
			b.StoreI32(x1)
			b.StoreLEB(3)
		}
	}
	if x0, ok := n.Region(); ok {
		b.StoreUtf8Tagged(x0, 1)
	}
	b.StoreLEB(uint64(b.Cursor() - before))
	o := b.Cursor()
	b.StructLookup[n.key()] = o
	return dagr.NodeStoreRef{Off: o}
}

func storeDocumentItem(n DocumentItem, b *dagr.Builder) dagr.NodeStoreRef {
	if d, ok := b.StructLookup[n.key()]; ok {
		return dagr.NodeStoreRef{Off: d}
	}
	before := b.Cursor()
	if x2, ok := n.PriceMinor(); ok {
		zz := dagr.ToZigZag(int64(x2))
		if dagr.LebLength(zz) < 8 {
			b.StoreLEBTagged(zz, 4)
		} else {
			b.StoreI64(x2)
			b.StoreLEB(5)
		}
	}
	if x1, ok := n.Qty(); ok {
		zz := dagr.ToZigZag(int64(x1))
		if dagr.LebLength(zz) < 4 {
			b.StoreLEBTagged(zz, 2)
		} else {
			b.StoreI32(x1)
			b.StoreLEB(3)
		}
	}
	if x0, ok := n.Sku(); ok {
		b.StoreUtf8Tagged(x0, 1)
	}
	b.StoreLEB(uint64(b.Cursor() - before))
	o := b.Cursor()
	b.StructLookup[n.key()] = o
	return dagr.NodeStoreRef{Off: o}
}

func storeDocument(n Document, b *dagr.Builder) dagr.NodeStoreRef {
	if d, ok := b.StructLookup[n.key()]; ok {
		return dagr.NodeStoreRef{Off: d}
	}
	before := b.Cursor()
	if x3, ok := n.Items(); ok {
		dagr.StorePackedNodeArrayPlain(b, x3, func(b *dagr.Builder, v DocumentItem) { storeDocumentItem(v, b) })
		b.StoreLEB(7)
	}
	if x2, ok := n.Meta(); ok {
		storeDocumentMeta(x2, b)
		b.StoreLEB(5)
	}
	if x1, ok := n.Status(); ok {
		zz := dagr.ToZigZag(int64(x1))
		if dagr.LebLength(zz) < 4 {
			b.StoreLEBTagged(zz, 2)
		} else {
			b.StoreI32(x1)
			b.StoreLEB(3)
		}
	}
	if x0, ok := n.ID(); ok {
		b.StoreUtf8Tagged(x0, 1)
	}
	b.StoreLEB(uint64(b.Cursor() - before))
	o := b.Cursor()
	b.StructLookup[n.key()] = o
	return dagr.NodeStoreRef{Off: o}
}

// ToBytesDocumentGraph serializes the graph reachable from root (byte-identical to every
// other Dagr implementation for the same graph). maxSize sets the cycle back-
// reference placeholder width (0 → 2 MiB, i.e. 4 bytes) and must match across
// producers for byte-identity.
func ToBytesDocumentGraph(root Document, maxSize int) []byte {
	b := dagr.NewBuilder(maxSize)
	off := dagr.NodeOffset(storeDocument(root, b))
	b.StoreLEB(uint64(b.Cursor()-off) << 2)
	return b.MakeData()
}
