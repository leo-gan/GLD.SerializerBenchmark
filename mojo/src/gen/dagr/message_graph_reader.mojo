# GENERATED by dagr/codegen/mojo/core.py — do not edit by hand.
# Lazy reader for graph 'MessageGraph' (regular-node scalars/utf8/data/enum/node-ref/arrays).

from dagr_reader import decode_packed_f64, read_bool, read_f64, read_i32, read_i64, read_leb, read_utf8, read_zigzag_leb, root_offset


struct MessageAccessor[o: ImmOrigin](Copyable, Movable, ImplicitlyCopyable):
    var buf: Span[UInt8, Self.o]
    var pos: Int     # node start position (dedup key for eager restore)
    var _p0: Int   # tag position of field 0 (f_bool); -1 = absent
    var _p1: Int   # tag position of field 1 (f_int32); -1 = absent
    var _p2: Int   # tag position of field 2 (f_int64); -1 = absent
    var _p3: Int   # tag position of field 3 (f_float64); -1 = absent
    var _p4: Int   # tag position of field 4 (f_string); -1 = absent
    var _p5: Int   # tag position of field 5 (f_bool_2); -1 = absent
    var _p6: Int   # tag position of field 6 (f_int32_2); -1 = absent
    var _p7: Int   # tag position of field 7 (f_string_2); -1 = absent

    def __init__(out self, buf: Span[UInt8, Self.o], start: Int) raises:
        self.buf = buf
        self.pos = start
        var r = read_leb(buf, start)
        var cursor = start + r[1]
        var end = cursor + Int(r[0])
        self._p0 = -1
        if cursor < end:
            var t0 = read_leb(buf, cursor)
            if (t0[0] >> 1) == 0:
                self._p0 = cursor
                cursor += t0[1]
                cursor += 1
        self._p1 = -1
        if cursor < end:
            var t1 = read_leb(buf, cursor)
            if (t1[0] >> 1) == 1:
                self._p1 = cursor
                var raw1 = (t1[0] & 1) == 1
                cursor += t1[1]
                if raw1:
                    cursor += 4
                else:
                    cursor += read_leb(buf, cursor)[1]
        self._p2 = -1
        if cursor < end:
            var t2 = read_leb(buf, cursor)
            if (t2[0] >> 1) == 2:
                self._p2 = cursor
                var raw2 = (t2[0] & 1) == 1
                cursor += t2[1]
                if raw2:
                    cursor += 8
                else:
                    cursor += read_leb(buf, cursor)[1]
        self._p3 = -1
        if cursor < end:
            var t3 = read_leb(buf, cursor)
            if (t3[0] >> 1) == 3:
                self._p3 = cursor
                var raw3 = (t3[0] & 1) == 1
                cursor += t3[1]
                if raw3:
                    cursor += 8
                else:
                    cursor += decode_packed_f64(buf, cursor)[1]
        self._p4 = -1
        if cursor < end:
            var t4 = read_leb(buf, cursor)
            if (t4[0] >> 1) == 4:
                self._p4 = cursor
                cursor += t4[1]
                var _l4 = read_leb(buf, cursor)
                cursor += _l4[1] + Int(_l4[0])
        self._p5 = -1
        if cursor < end:
            var t5 = read_leb(buf, cursor)
            if (t5[0] >> 1) == 5:
                self._p5 = cursor
                cursor += t5[1]
                cursor += 1
        self._p6 = -1
        if cursor < end:
            var t6 = read_leb(buf, cursor)
            if (t6[0] >> 1) == 6:
                self._p6 = cursor
                var raw6 = (t6[0] & 1) == 1
                cursor += t6[1]
                if raw6:
                    cursor += 4
                else:
                    cursor += read_leb(buf, cursor)[1]
        self._p7 = -1
        if cursor < end:
            var t7 = read_leb(buf, cursor)
            if (t7[0] >> 1) == 7:
                self._p7 = cursor
                cursor += t7[1]
                var _l7 = read_leb(buf, cursor)
                cursor += _l7[1] + Int(_l7[0])

    def f_bool(self) raises -> Optional[Bool]:
        if self._p0 < 0:
            return None
        var t = read_leb(self.buf, self._p0)
        var payload = self._p0 + t[1]
        return read_bool(self.buf, payload)

    def f_int32(self) raises -> Optional[Int32]:
        if self._p1 < 0:
            return None
        var t = read_leb(self.buf, self._p1)
        var raw = (t[0] & 1) == 1
        var payload = self._p1 + t[1]
        if raw:
            return read_i32(self.buf, payload)
        else:
            return Int32(read_zigzag_leb(self.buf, payload)[0])

    def f_int64(self) raises -> Optional[Int64]:
        if self._p2 < 0:
            return None
        var t = read_leb(self.buf, self._p2)
        var raw = (t[0] & 1) == 1
        var payload = self._p2 + t[1]
        if raw:
            return read_i64(self.buf, payload)
        else:
            return Int64(read_zigzag_leb(self.buf, payload)[0])

    def f_float64(self) raises -> Optional[Float64]:
        if self._p3 < 0:
            return None
        var t = read_leb(self.buf, self._p3)
        var raw = (t[0] & 1) == 1
        var payload = self._p3 + t[1]
        if raw:
            return read_f64(self.buf, payload)
        else:
            return decode_packed_f64(self.buf, payload)[0]

    def f_string(self) raises -> Optional[String]:
        if self._p4 < 0:
            return None
        var t = read_leb(self.buf, self._p4)
        var payload = self._p4 + t[1]
        return String(read_utf8(self.buf, payload)[0])

    def f_bool_2(self) raises -> Optional[Bool]:
        if self._p5 < 0:
            return None
        var t = read_leb(self.buf, self._p5)
        var payload = self._p5 + t[1]
        return read_bool(self.buf, payload)

    def f_int32_2(self) raises -> Optional[Int32]:
        if self._p6 < 0:
            return None
        var t = read_leb(self.buf, self._p6)
        var raw = (t[0] & 1) == 1
        var payload = self._p6 + t[1]
        if raw:
            return read_i32(self.buf, payload)
        else:
            return Int32(read_zigzag_leb(self.buf, payload)[0])

    def f_string_2(self) raises -> Optional[String]:
        if self._p7 < 0:
            return None
        var t = read_leb(self.buf, self._p7)
        var payload = self._p7 + t[1]
        return String(read_utf8(self.buf, payload)[0])

def read_message_root[o: ImmOrigin, //](buf: Span[UInt8, o]) raises -> MessageAccessor[o]:
    return MessageAccessor[o](buf, root_offset(buf))
