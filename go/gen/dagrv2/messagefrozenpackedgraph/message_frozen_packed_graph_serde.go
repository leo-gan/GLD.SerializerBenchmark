// Code generated by dagr (dagr/codegen/go/serde.py); DO NOT EDIT.

package messagefrozenpackedgraph

import "dagrv2/dagr"

func storeMessage(n Message, b *dagr.Builder) dagr.NodeStoreRef {
	if d, ok := b.StructLookup[n.key()]; ok {
		return dagr.NodeStoreRef{Off: d}
	}
	before := b.Cursor()
	var raw [4]bool
	if x7, ok := n.FString2(); ok {
		b.StoreUtf8(x7, false)
	}
	if x6, ok := n.FInt32_2(); ok {
		zz := dagr.ToZigZag(int64(x6))
		if dagr.LebLength(zz) < 4 {
			b.StoreLEB(zz)
		} else {
			b.StoreI32(x6)
			raw[3] = true
		}
	}
	if x5, ok := n.FBool2(); ok {
		b.StoreBool(x5)
	}
	if x4, ok := n.FString(); ok {
		b.StoreUtf8(x4, false)
	}
	if x3, ok := n.FFloat64(); ok {
		b.StoreF64(x3)
		raw[2] = true
	}
	if x2, ok := n.FInt64(); ok {
		zz := dagr.ToZigZag(int64(x2))
		if dagr.LebLength(zz) < 8 {
			b.StoreLEB(zz)
		} else {
			b.StoreI64(x2)
			raw[1] = true
		}
	}
	if x1, ok := n.FInt32(); ok {
		zz := dagr.ToZigZag(int64(x1))
		if dagr.LebLength(zz) < 4 {
			b.StoreLEB(zz)
		} else {
			b.StoreI32(x1)
			raw[0] = true
		}
	}
	if x0, ok := n.FBool(); ok {
		b.StoreBool(x0)
	}
	enc := make([]byte, 1)
	for k, r := range raw {
		if r {
			enc[k>>3] |= 1 << (k & 7)
		}
	}
	b.StoreBytes(enc)
	var nbits uint64
	if _, ok := n.FBool(); ok {
		nbits |= 1 << 0
	}
	if _, ok := n.FInt32(); ok {
		nbits |= 1 << 1
	}
	if _, ok := n.FInt64(); ok {
		nbits |= 1 << 2
	}
	if _, ok := n.FFloat64(); ok {
		nbits |= 1 << 3
	}
	if _, ok := n.FString(); ok {
		nbits |= 1 << 4
	}
	if _, ok := n.FBool2(); ok {
		nbits |= 1 << 5
	}
	if _, ok := n.FInt32_2(); ok {
		nbits |= 1 << 6
	}
	if _, ok := n.FString2(); ok {
		nbits |= 1 << 7
	}
	b.StoreBytes([]byte{byte(nbits >> 0)})
	b.StoreLEB(uint64(b.Cursor() - before))
	o := b.Cursor()
	b.StructLookup[n.key()] = o
	return dagr.NodeStoreRef{Off: o}
}

// ToBytesMessageFrozenPackedGraph serializes the graph reachable from root (byte-identical to every
// other Dagr implementation for the same graph). maxSize sets the cycle back-
// reference placeholder width (0 → 2 MiB, i.e. 4 bytes) and must match across
// producers for byte-identity.
func ToBytesMessageFrozenPackedGraph(root Message, maxSize int) []byte {
	b := dagr.NewBuilder(maxSize)
	off := dagr.NodeOffset(storeMessage(root, b))
	b.StoreLEB(uint64(b.Cursor()-off) << 2)
	return b.MakeData()
}
