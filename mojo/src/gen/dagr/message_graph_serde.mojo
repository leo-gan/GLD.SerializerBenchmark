# GENERATED by dagr/codegen/mojo/serde.py — do not edit by hand.
# Serializer (arena -> bytes) for graph 'MessageGraph' — plan 28 Phase 4.

from dagr_writer import Builder, NodeStoreRef, node_offset, resolved_ref, nil_ref, to_zigzag, leb_length
from message_graph_arena import MessageGraphArena, Message

comptime _VT_MAX = 1   # widest regular-node vtable (§1.19)


# Build a List[Int] of vtable entries from a varargs-free call site.
def _ilist(*vals: Int) -> List[Int]:
    var l = List[Int]()
    for v in vals:
        l.append(v)
    return l^


# Type-namespaced node identity for dedup / cycle keys: a bare packed handle
# collides across node types (index space is per-type), so fold in the type id.
def _nid(tid: Int, idx: Int) -> UInt64:
    return (UInt64(idx) << 8) | UInt64(tid)


def _store_message[o: Origin[mut=False], //](mut b: Builder[_VT_MAX], a: MessageGraphArena, h: Message[o]) raises -> NodeStoreRef:
    var _before = b.cursor
    if Bool(a._arr_message[h._index()].f_string_2):
        _ = b.store_utf8(a._arr_message[h._index()].f_string_2.value(), False)
        _ = b.store_leb(UInt64(15))
    var _v6 = h.f_int32_2()
    if _v6:
        if leb_length(to_zigzag(Int(_v6.value()))) < 4:
            _ = b.store_leb(to_zigzag(Int(_v6.value())))
            _ = b.store_leb(UInt64(12))
        else:
            _ = b.store_i32(_v6.value())
            _ = b.store_leb(UInt64(13))
    var _v5 = h.f_bool_2()
    if _v5:
        _ = b.store_u8(UInt8(1) if _v5.value() else UInt8(0))
        _ = b.store_leb(UInt64(11))
    if Bool(a._arr_message[h._index()].f_string):
        _ = b.store_utf8(a._arr_message[h._index()].f_string.value(), False)
        _ = b.store_leb(UInt64(9))
    var _v3 = h.f_float64()
    if _v3:
        _ = b.store_f64(_v3.value())
        _ = b.store_leb(UInt64(7))
    var _v2 = h.f_int64()
    if _v2:
        if leb_length(to_zigzag(Int(_v2.value()))) < 8:
            _ = b.store_leb(to_zigzag(Int(_v2.value())))
            _ = b.store_leb(UInt64(4))
        else:
            _ = b.store_i64(_v2.value())
            _ = b.store_leb(UInt64(5))
    var _v1 = h.f_int32()
    if _v1:
        if leb_length(to_zigzag(Int(_v1.value()))) < 4:
            _ = b.store_leb(to_zigzag(Int(_v1.value())))
            _ = b.store_leb(UInt64(2))
        else:
            _ = b.store_i32(_v1.value())
            _ = b.store_leb(UInt64(3))
    var _v0 = h.f_bool()
    if _v0:
        _ = b.store_u8(UInt8(1) if _v0.value() else UInt8(0))
        _ = b.store_leb(UInt64(1))
    _ = b.store_leb(UInt64(b.cursor - _before))
    return resolved_ref(b.cursor)


def write_message_graph(mut b: Builder[_VT_MAX], a: MessageGraphArena) raises -> Int:
    var _off = node_offset(_store_message(b, a, a.root().value()))
    return b.store_leb(UInt64((b.cursor - _off) << 2))    # framing (no header)

def serialize_message_graph(a: MessageGraphArena, max_size: Int = 2 * 1024 * 1024) raises -> List[UInt8]:
    var b = Builder[_VT_MAX](hint=len(a._arr_message), max_size=max_size)
    _ = write_message_graph(b, a)
    return b.make_data()
