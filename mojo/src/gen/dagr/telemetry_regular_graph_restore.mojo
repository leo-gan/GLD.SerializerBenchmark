# GENERATED by dagr/codegen/mojo/restore.py — do not edit by hand.
# Eager restore (bytes -> arena) for graph 'TelemetryRegularGraph' — plan 28 Phase 3.

from telemetry_regular_graph_arena import TelemetryRegularGraphArena, Telemetry
from telemetry_regular_graph_reader import TelemetryAccessor
from dagr_reader import root_offset
from std.collections import Dict


def restore_telemetry_graph[o: ImmOrigin](buf: Span[UInt8, o]) raises -> TelemetryRegularGraphArena:
    var a = TelemetryRegularGraphArena()
    var seen = Dict[Int, UInt64]()
    var racc = TelemetryAccessor[o](buf, root_offset(buf))
    var rh = _restore_telemetry(a, racc, seen)
    a.set_root(rh)
    return a^


def _restore_telemetry[co: ImmOrigin, ao: Origin[mut=False], //](ref [ao] a: TelemetryRegularGraphArena, acc: TelemetryAccessor[co], mut seen: Dict[Int, UInt64]) raises -> Telemetry[ao]:
    var _pos = acc.pos
    if _pos in seen:
        return Telemetry[ao](Pointer(to=a), seen[_pos])
    var h = a.new_telemetry()
    var i = h._index()
    var _wa = Pointer[TelemetryRegularGraphArena, MutAnyOrigin](unsafe_from_address=Int(Pointer(to=a)))
    seen[_pos] = h._packed
    _wa[]._arr_telemetry[i].source = acc.source()
    _wa[]._arr_telemetry[i].ts = acc.ts()
    var _oa2 = acc.tags()
    if _oa2:
        var _l2 = List[String]()
        for _k2 in range(len(_oa2.value())):
            _l2.append(_oa2.value().get(_k2))
        _wa[]._arr_telemetry[i].tags = _l2^
    else:
        _wa[]._arr_telemetry[i].tags = List[String]()
    var _oa3 = acc.values()
    if _oa3:
        var _l3 = List[Float64]()
        for _k3 in range(len(_oa3.value())):
            _l3.append(_oa3.value().get(_k3))
        _wa[]._arr_telemetry[i].values = _l3^
    else:
        _wa[]._arr_telemetry[i].values = List[Float64]()
    return h


