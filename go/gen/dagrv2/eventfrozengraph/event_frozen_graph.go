// Code generated by dagr (dagr/codegen/go/core.py); DO NOT EDIT.

package eventfrozengraph

import (
	"iter"

	"dagrv2/dagr"
)

type EventAttrAccessor struct {
	buf []byte
	pos int
	p   [2]int
}

func NewEventAttrAccessor(buf []byte, pos int) EventAttrAccessor {
	a := EventAttrAccessor{buf: buf, pos: pos}
	bs := dagr.ReadBitset(buf, pos, 1)
	cur := pos + 1
	if (bs>>0)&1 == 1 {
		a.p[0] = cur
		cur += dagr.V62Bytes(buf, cur)
	} else {
		a.p[0] = -1
	}
	if (bs>>1)&1 == 1 {
		a.p[1] = cur
		cur += dagr.V62Bytes(buf, cur)
	} else {
		a.p[1] = -1
	}
	_ = cur
	return a
}

// BufferPos is where this node starts in the buffer — its identity in a graph.
func (a EventAttrAccessor) BufferPos() int { return a.pos }

// BufferBytes is the buffer this accessor reads — with BufferPos, enough to reopen
// the node through another package's accessor (a DataSink does, spec/11 §12).
func (a EventAttrAccessor) BufferBytes() []byte { return a.buf }

func (a EventAttrAccessor) Key() (string, bool) {
	pl := a.p[0]
	if pl < 0 {
		return "", false
	}
	s, _ := dagr.ReadUtf8(a.buf, dagr.FwdTarget(a.buf, pl))
	return s, true
}

func (a EventAttrAccessor) KeyBytes() ([]byte, bool) {
	pl := a.p[0]
	if pl < 0 {
		return nil, false
	}
	d, _ := dagr.ReadBytes(a.buf, dagr.FwdTarget(a.buf, pl))
	return d, true
}

func (a EventAttrAccessor) Value() (string, bool) {
	pl := a.p[1]
	if pl < 0 {
		return "", false
	}
	s, _ := dagr.ReadUtf8(a.buf, dagr.FwdTarget(a.buf, pl))
	return s, true
}

func (a EventAttrAccessor) ValueBytes() ([]byte, bool) {
	pl := a.p[1]
	if pl < 0 {
		return nil, false
	}
	d, _ := dagr.ReadBytes(a.buf, dagr.FwdTarget(a.buf, pl))
	return d, true
}

type EventAccessor struct {
	buf []byte
	pos int
	p   [5]int
}

func NewEventAccessor(buf []byte, pos int) EventAccessor {
	a := EventAccessor{buf: buf, pos: pos}
	bs := dagr.ReadBitset(buf, pos, 1)
	cur := pos + 1
	if (bs>>0)&1 == 1 {
		a.p[0] = cur
		cur += dagr.V62Bytes(buf, cur)
	} else {
		a.p[0] = -1
	}
	if (bs>>1)&1 == 1 {
		a.p[1] = cur
		cur += dagr.V62Bytes(buf, cur)
	} else {
		a.p[1] = -1
	}
	if (bs>>2)&1 == 1 {
		a.p[2] = cur
		cur += 8
	} else {
		a.p[2] = -1
	}
	if (bs>>3)&1 == 1 {
		a.p[3] = cur
		cur += dagr.V62Bytes(buf, cur)
	} else {
		a.p[3] = -1
	}
	if (bs>>4)&1 == 1 {
		a.p[4] = cur
		cur += dagr.V62Bytes(buf, cur)
	} else {
		a.p[4] = -1
	}
	_ = cur
	return a
}

// BufferPos is where this node starts in the buffer — its identity in a graph.
func (a EventAccessor) BufferPos() int { return a.pos }

// BufferBytes is the buffer this accessor reads — with BufferPos, enough to reopen
// the node through another package's accessor (a DataSink does, spec/11 §12).
func (a EventAccessor) BufferBytes() []byte { return a.buf }

func (a EventAccessor) EventID() (string, bool) {
	pl := a.p[0]
	if pl < 0 {
		return "", false
	}
	s, _ := dagr.ReadUtf8(a.buf, dagr.FwdTarget(a.buf, pl))
	return s, true
}

func (a EventAccessor) EventIDBytes() ([]byte, bool) {
	pl := a.p[0]
	if pl < 0 {
		return nil, false
	}
	d, _ := dagr.ReadBytes(a.buf, dagr.FwdTarget(a.buf, pl))
	return d, true
}

func (a EventAccessor) EventType() (string, bool) {
	pl := a.p[1]
	if pl < 0 {
		return "", false
	}
	s, _ := dagr.ReadUtf8(a.buf, dagr.FwdTarget(a.buf, pl))
	return s, true
}

func (a EventAccessor) EventTypeBytes() ([]byte, bool) {
	pl := a.p[1]
	if pl < 0 {
		return nil, false
	}
	d, _ := dagr.ReadBytes(a.buf, dagr.FwdTarget(a.buf, pl))
	return d, true
}

func (a EventAccessor) OccurredAt() (int64, bool) {
	pl := a.p[2]
	if pl < 0 {
		return 0, false
	}
	return dagr.ReadI64(a.buf, pl), true
}

func (a EventAccessor) Producer() (string, bool) {
	pl := a.p[3]
	if pl < 0 {
		return "", false
	}
	s, _ := dagr.ReadUtf8(a.buf, dagr.FwdTarget(a.buf, pl))
	return s, true
}

func (a EventAccessor) ProducerBytes() ([]byte, bool) {
	pl := a.p[3]
	if pl < 0 {
		return nil, false
	}
	d, _ := dagr.ReadBytes(a.buf, dagr.FwdTarget(a.buf, pl))
	return d, true
}

func (a EventAccessor) AttrsLen() (int, bool) {
	pl := a.p[4]
	if pl < 0 {
		return 0, false
	}
	_, _, count, _ := dagr.PtrTable(a.buf, pl)
	return count, true
}

func (a EventAccessor) AttrsAt(i int) EventAttrAccessor {
	pl := a.p[4]
	tb, db, _, es := dagr.PtrTable(a.buf, pl)
	ro := dagr.ReadIntLE(a.buf, tb+i*es, es)
	return NewEventAttrAccessor(a.buf, db+ro-1)
}

type EventAttrsIter struct {
	buf []byte
	i   int
	n   int
	tb  int
	db  int
	es  int
}

// AttrsIter resolves the array once; Next reports ok=false past the end.
func (a EventAccessor) AttrsIter() EventAttrsIter {
	pl := a.p[4]
	if pl < 0 {
		return EventAttrsIter{}
	}
	tb, db, n, es := dagr.PtrTable(a.buf, pl)
	return EventAttrsIter{buf: a.buf, n: n, tb: tb, db: db, es: es}
}

func (it *EventAttrsIter) at(i int) EventAttrAccessor {
	buf := it.buf
	tb := it.tb
	db := it.db
	es := it.es
	ro := dagr.ReadIntLE(buf, tb+i*es, es)
	return NewEventAttrAccessor(buf, db+ro-1)
}

func (it *EventAttrsIter) Next() (EventAttrAccessor, bool) {
	if it.i >= it.n {
		var z EventAttrAccessor
		return z, false
	}
	v := it.at(it.i)
	it.i++
	return v, true
}

func (it *EventAttrsIter) Len() int { return it.n }

// AttrsAll ranges over the array in one pass.
func (a EventAccessor) AttrsAll() iter.Seq[EventAttrAccessor] {
	return func(yield func(EventAttrAccessor) bool) {
		it := a.AttrsIter()
		for {
			v, ok := it.Next()
			if !ok || !yield(v) {
				return
			}
		}
	}
}

func walkEventAttr(a EventAttrAccessor, seen map[dagr.SeenKey]bool) {
	sk := dagr.SeenAt(a.buf, a.pos)
	if seen[sk] {
		return
	}
	seen[sk] = true
	_, _ = a.Key()
	_, _ = a.Value()
}

// ValidateEventAttr exercises every accessor path below a once, under recover, as Open does
// for the root: nil means reading this subtree is panic-free, malformed bytes
// yield dagr.ErrMalformed.
func ValidateEventAttr(a EventAttrAccessor) (err error) {
	defer func() {
		if r := recover(); r != nil {
			err = dagr.ErrMalformed
		}
	}()
	walkEventAttr(a, map[dagr.SeenKey]bool{})
	return nil
}

func walkEvent(a EventAccessor, seen map[dagr.SeenKey]bool) {
	sk := dagr.SeenAt(a.buf, a.pos)
	if seen[sk] {
		return
	}
	seen[sk] = true
	_, _ = a.EventID()
	_, _ = a.EventType()
	_, _ = a.OccurredAt()
	_, _ = a.Producer()
	{
		if n, ok := a.AttrsLen(); ok {
			for i := 0; i < n; i++ {
				walkEventAttr(a.AttrsAt(i), seen)
			}
		}
	}
}

// ValidateEvent exercises every accessor path below a once, under recover, as Open does
// for the root: nil means reading this subtree is panic-free, malformed bytes
// yield dagr.ErrMalformed.
func ValidateEvent(a EventAccessor) (err error) {
	defer func() {
		if r := recover(); r != nil {
			err = dagr.ErrMalformed
		}
	}()
	walkEvent(a, map[dagr.SeenKey]bool{})
	return nil
}

// OpenEventFrozenGraph parses the framing word, then validates the whole reachable graph once
// (every accessor path is exercised under recover), so a buffer that passed Open is
// read panic-free by construction; malformed input yields dagr.ErrMalformed. A spec-15
// header is skipped. The buffer must outlive every accessor derived from it.
func OpenEventFrozenGraph(buf []byte) (root EventAccessor, err error) {
	root, err = OpenEventFrozenGraphUnchecked(buf)
	if err != nil {
		return EventAccessor{}, err
	}
	defer func() {
		if r := recover(); r != nil {
			root, err = EventAccessor{}, dagr.ErrMalformed
		}
	}()
	walkEvent(root, map[dagr.SeenKey]bool{})
	return root, nil
}

// OpenEventFrozenGraphUnchecked returns the root accessor after checking only the framing word —
// O(1), for buffers already known to be well-formed (a Go-written buffer, or one
// validated by an earlier Open).
func OpenEventFrozenGraphUnchecked(buf []byte) (EventAccessor, error) {
	root, err := dagr.RootOffset(buf)
	if err != nil {
		return EventAccessor{}, err
	}
	return NewEventAccessor(buf, root), nil
}
