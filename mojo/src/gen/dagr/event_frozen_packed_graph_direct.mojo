# GENERATED by dagr/codegen/mojo/direct.py — do not edit by hand.
# Direct Graph Builder ("spec/33-direct-graph-builder.md") for graph 'EventFrozenPackedGraph'.
from dagr_writer import Builder, NodeStoreRef, node_offset, resolved_ref, leb_length, to_zigzag

comptime _VT_MAX = 1

struct DirectEventAttr(Copyable, Movable):
    var key: Optional[String]
    var value: Optional[String]
    def __init__(out self, var key: Optional[String], var value: Optional[String]):
        self.key = key^
        self.value = value^

struct DirectEvent(Copyable, Movable):
    var event_id: Optional[String]
    var event_type: Optional[String]
    var occurred_at: Optional[Int64]
    var producer: Optional[String]
    var attrs: List[DirectEventAttr]
    def __init__(out self, var event_id: Optional[String], var event_type: Optional[String], var occurred_at: Optional[Int64], var producer: Optional[String], var attrs: List[DirectEventAttr]):
        self.event_id = event_id^
        self.event_type = event_type^
        self.occurred_at = occurred_at^
        self.producer = producer^
        self.attrs = attrs^

def _store_event_attr_d(mut b: Builder[_VT_MAX], n: DirectEventAttr) raises -> NodeStoreRef:
    var _before = b.cursor
    var _pr0 = Bool(n.key)
    var _pr1 = Bool(n.value)
    if _pr1:
        _ = b.store_utf8(n.value.value(), False)
    if _pr0:
        _ = b.store_utf8(n.key.value(), False)
    var _pb: UInt64 = 0
    if _pr0:
        _pb |= UInt64(1) << 0
    if _pr1:
        _pb |= UInt64(1) << 1
    _ = b.store_u8(UInt8((_pb >> 0) & 0xff))
    _ = b.store_leb(UInt64(b.cursor - _before))
    return resolved_ref(b.cursor)

def _store_event_d(mut b: Builder[_VT_MAX], n: DirectEvent) raises -> NodeStoreRef:
    var _before = b.cursor
    var _rawbits: UInt64 = 0
    var _pr0 = Bool(n.event_id)
    var _pr1 = Bool(n.event_type)
    var _pr2 = Bool(n.occurred_at)
    var _pr3 = Bool(n.producer)
    var _pr4 = True
    ref _pa4 = n.attrs
    var _pcb4 = b.cursor
    for _k4 in range(len(_pa4) - 1, -1, -1):
        _ = _store_event_attr_d(b, _pa4[_k4])
    _ = b.store_leb(UInt64(len(_pa4)))
    _ = b.store_leb(UInt64(b.cursor - _pcb4))
    if _pr3:
        _ = b.store_utf8(n.producer.value(), False)
    if _pr2:
        if leb_length(to_zigzag(Int(n.occurred_at.value()))) < 8:
            _ = b.store_leb(to_zigzag(Int(n.occurred_at.value())))
        else:
            _ = b.store_i64(n.occurred_at.value())
            _rawbits |= UInt64(1) << 0
    if _pr1:
        _ = b.store_utf8(n.event_type.value(), False)
    if _pr0:
        _ = b.store_utf8(n.event_id.value(), False)
    _ = b.store_u8(UInt8((_rawbits >> 0) & 0xff))
    var _pb: UInt64 = 0
    if _pr0:
        _pb |= UInt64(1) << 0
    if _pr1:
        _pb |= UInt64(1) << 1
    if _pr2:
        _pb |= UInt64(1) << 2
    if _pr3:
        _pb |= UInt64(1) << 3
    if _pr4:
        _pb |= UInt64(1) << 4
    _ = b.store_u8(UInt8((_pb >> 0) & 0xff))
    _ = b.store_leb(UInt64(b.cursor - _before))
    return resolved_ref(b.cursor)

def _push_leb(mut out: List[UInt8], v: UInt64):
    var x = v
    while True:
        var byte = UInt8(x & 0x7f)
        x >>= 7
        if x != 0:
            out.append(byte | 0x80)
        else:
            out.append(byte)
            break

def _push_bytes(mut out: List[UInt8], src: List[UInt8]):
    for i in range(len(src)):
        out.append(src[i])

comptime DirectBuilder = Builder[_VT_MAX]

def write_event_graph_direct(mut b: Builder[_VT_MAX], n: DirectEvent) raises -> Int:
    var _off = node_offset(_store_event_d(b, n))
    return b.store_leb(UInt64((b.cursor - _off) << 2))

def serialize_event_graph_direct(var n: DirectEvent) raises -> List[UInt8]:
    var b = Builder[_VT_MAX]()
    _ = write_event_graph_direct(b, n)
    return b.make_data()

