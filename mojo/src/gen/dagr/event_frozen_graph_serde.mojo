# GENERATED by dagr/codegen/mojo/serde.py — do not edit by hand.
# Serializer (arena -> bytes) for graph 'EventFrozenGraph' — plan 28 Phase 4.

from dagr_writer import Builder, NodeStoreRef, node_offset, resolved_ref, nil_ref, to_zigzag, leb_length
from event_frozen_graph_arena import EventFrozenGraphArena, EventAttr, Event

comptime _VT_MAX = 1   # widest regular-node vtable (§1.19)


# Build a List[Int] of vtable entries from a varargs-free call site.
def _ilist(*vals: Int) -> List[Int]:
    var l = List[Int]()
    for v in vals:
        l.append(v)
    return l^


# Type-namespaced node identity for dedup / cycle keys: a bare packed handle
# collides across node types (index space is per-type), so fold in the type id.
def _nid(tid: Int, idx: Int) -> UInt64:
    return (UInt64(idx) << 8) | UInt64(tid)


def _store_event_attr[o: Origin[mut=False], //](mut b: Builder[_VT_MAX], a: EventFrozenGraphArena, h: EventAttr[o]) raises -> NodeStoreRef:
    var _id = _nid(0, h._index())
    var _r = b.begin_storing_acyclic(_id)
    if _r.is_pending or _r.off != -1:
        return _r
    var _pr0 = Bool(a._arr_event_attr[h._index()].key)
    var _pr1 = Bool(a._arr_event_attr[h._index()].value)
    var _c1 = -1
    if _pr1:
        _c1 = b.store_utf8(a._arr_event_attr[h._index()].value.value(), True)
    var _c0 = -1
    if _pr0:
        _c0 = b.store_utf8(a._arr_event_attr[h._index()].key.value(), True)
    if _pr1:
        _ = b.store_forward_pointer(_c1)
    if _pr0:
        _ = b.store_forward_pointer(_c0)
    var _bs: UInt64 = 0
    if _pr0:
        _bs |= UInt64(1) << 0
    if _pr1:
        _bs |= UInt64(1) << 1
    _ = b.store_u8(UInt8(_bs & 0xff))
    var _o = b.cursor
    b.finish_storing_acyclic(_id, _o)
    return resolved_ref(_o)


def _store_event[o: Origin[mut=False], //](mut b: Builder[_VT_MAX], a: EventFrozenGraphArena, h: Event[o]) raises -> NodeStoreRef:
    var _id = _nid(1, h._index())
    var _r = b.begin_storing_acyclic(_id)
    if _r.is_pending or _r.off != -1:
        return _r
    var _pr0 = Bool(a._arr_event[h._index()].event_id)
    var _pr1 = Bool(a._arr_event[h._index()].event_type)
    var _pr2 = Bool(h.occurred_at())
    var _pr3 = Bool(a._arr_event[h._index()].producer)
    var _pr4 = True
    var _c4 = -1
    if _pr4:
        var _bi4 = h._index()
        var _refs4 = b.take_refs()
        for _k4 in range(len(a._arr_event[_bi4].attrs) - 1, -1, -1):
            _refs4.append(_store_event_attr(b, a, EventAttr[o](h._a, a._arr_event[_bi4].attrs[_k4])))
        _c4 = b.store_node_ref_array(_refs4)
        b.return_refs(_refs4^)
    var _c3 = -1
    if _pr3:
        _c3 = b.store_utf8(a._arr_event[h._index()].producer.value(), True)
    var _c1 = -1
    if _pr1:
        _c1 = b.store_utf8(a._arr_event[h._index()].event_type.value(), True)
    var _c0 = -1
    if _pr0:
        _c0 = b.store_utf8(a._arr_event[h._index()].event_id.value(), True)
    if _pr4:
        _ = b.store_forward_pointer(_c4)
    if _pr3:
        _ = b.store_forward_pointer(_c3)
    if _pr2:
        _ = b.store_i64(h.occurred_at().value())
    if _pr1:
        _ = b.store_forward_pointer(_c1)
    if _pr0:
        _ = b.store_forward_pointer(_c0)
    var _bs: UInt64 = 0
    if _pr0:
        _bs |= UInt64(1) << 0
    if _pr1:
        _bs |= UInt64(1) << 1
    if _pr2:
        _bs |= UInt64(1) << 2
    if _pr3:
        _bs |= UInt64(1) << 3
    if _pr4:
        _bs |= UInt64(1) << 4
    _ = b.store_u8(UInt8(_bs & 0xff))
    var _o = b.cursor
    b.finish_storing_acyclic(_id, _o)
    return resolved_ref(_o)


def write_event_graph(mut b: Builder[_VT_MAX], a: EventFrozenGraphArena) raises -> Int:
    var _off = node_offset(_store_event(b, a, a.root().value()))
    return b.store_leb(UInt64((b.cursor - _off) << 2))    # framing (no header)

def serialize_event_graph(a: EventFrozenGraphArena, max_size: Int = 2 * 1024 * 1024) raises -> List[UInt8]:
    var b = Builder[_VT_MAX](hint=len(a._arr_event_attr) + len(a._arr_event), max_size=max_size)
    _ = write_event_graph(b, a)
    return b.make_data()
