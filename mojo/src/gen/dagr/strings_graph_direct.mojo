# GENERATED by dagr/codegen/mojo/direct.py — do not edit by hand.
# Direct Graph Builder ("spec/33-direct-graph-builder.md") for graph 'StringsGraph'.
from dagr_writer import Builder, NodeStoreRef, node_offset, resolved_ref, leb_length, to_zigzag

comptime _VT_MAX = 1

struct DirectStrings(Copyable, Movable):
    var items: List[String]
    def __init__(out self, var items: List[String]):
        self.items = items^

def _store_strings_d(mut b: Builder[_VT_MAX], n: DirectStrings) raises -> NodeStoreRef:
    var _before = b.cursor
    ref _pa0 = n.items
    var _pcb0 = b.cursor
    for _k0 in range(len(_pa0) - 1, -1, -1):
        _ = b.store_utf8(_pa0[_k0], False)
    _ = b.store_leb(UInt64(len(_pa0)))
    _ = b.store_leb(UInt64(b.cursor - _pcb0))
    _ = b.store_leb(UInt64(1))
    _ = b.store_leb(UInt64(b.cursor - _before))
    return resolved_ref(b.cursor)

def _push_leb(mut out: List[UInt8], v: UInt64):
    var x = v
    while True:
        var byte = UInt8(x & 0x7f)
        x >>= 7
        if x != 0:
            out.append(byte | 0x80)
        else:
            out.append(byte)
            break

def _push_bytes(mut out: List[UInt8], src: List[UInt8]):
    for i in range(len(src)):
        out.append(src[i])

comptime DirectBuilder = Builder[_VT_MAX]

def write_strings_graph_direct(mut b: Builder[_VT_MAX], n: DirectStrings) raises -> Int:
    var _off = node_offset(_store_strings_d(b, n))
    return b.store_leb(UInt64((b.cursor - _off) << 2))

def serialize_strings_graph_direct(var n: DirectStrings) raises -> List[UInt8]:
    var b = Builder[_VT_MAX]()
    _ = write_strings_graph_direct(b, n)
    return b.make_data()

