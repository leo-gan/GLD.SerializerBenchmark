# GENERATED by dagr/codegen/mojo/direct.py — do not edit by hand.
# Direct Graph Builder ("spec/33-direct-graph-builder.md") for graph 'TelemetryFrozenPackedGraph'.
from dagr_writer import Builder, NodeStoreRef, node_offset, resolved_ref, leb_length, to_zigzag

comptime _VT_MAX = 1

struct DirectTelemetry(Copyable, Movable):
    var source: Optional[String]
    var ts: Optional[Int64]
    var tags: List[String]
    var values: List[Float64]
    def __init__(out self, var source: Optional[String], var ts: Optional[Int64], var tags: List[String], var values: List[Float64]):
        self.source = source^
        self.ts = ts^
        self.tags = tags^
        self.values = values^

def _store_telemetry_d(mut b: Builder[_VT_MAX], n: DirectTelemetry) raises -> NodeStoreRef:
    var _before = b.cursor
    var _rawbits: UInt64 = 0
    var _pr0 = Bool(n.source)
    var _pr1 = Bool(n.ts)
    var _pr2 = True
    var _pr3 = True
    ref _pa3 = n.values
    _ = b.store_packed_float64_array(_pa3, True)
    ref _pa2 = n.tags
    var _pcb2 = b.cursor
    for _k2 in range(len(_pa2) - 1, -1, -1):
        _ = b.store_utf8(_pa2[_k2], False)
    _ = b.store_leb(UInt64(len(_pa2)))
    _ = b.store_leb(UInt64(b.cursor - _pcb2))
    if _pr1:
        if leb_length(to_zigzag(Int(n.ts.value()))) < 8:
            _ = b.store_leb(to_zigzag(Int(n.ts.value())))
        else:
            _ = b.store_i64(n.ts.value())
            _rawbits |= UInt64(1) << 0
    if _pr0:
        _ = b.store_utf8(n.source.value(), False)
    _ = b.store_u8(UInt8((_rawbits >> 0) & 0xff))
    var _pb: UInt64 = 0
    if _pr0:
        _pb |= UInt64(1) << 0
    if _pr1:
        _pb |= UInt64(1) << 1
    if _pr2:
        _pb |= UInt64(1) << 2
    if _pr3:
        _pb |= UInt64(1) << 3
    _ = b.store_u8(UInt8((_pb >> 0) & 0xff))
    _ = b.store_leb(UInt64(b.cursor - _before))
    return resolved_ref(b.cursor)

def _push_leb(mut out: List[UInt8], v: UInt64):
    var x = v
    while True:
        var byte = UInt8(x & 0x7f)
        x >>= 7
        if x != 0:
            out.append(byte | 0x80)
        else:
            out.append(byte)
            break

def _push_bytes(mut out: List[UInt8], src: List[UInt8]):
    for i in range(len(src)):
        out.append(src[i])

comptime DirectBuilder = Builder[_VT_MAX]

def write_telemetry_graph_direct(mut b: Builder[_VT_MAX], n: DirectTelemetry) raises -> Int:
    var _off = node_offset(_store_telemetry_d(b, n))
    return b.store_leb(UInt64((b.cursor - _off) << 2))

def serialize_telemetry_graph_direct(var n: DirectTelemetry) raises -> List[UInt8]:
    var b = Builder[_VT_MAX]()
    _ = write_telemetry_graph_direct(b, n)
    return b.make_data()

