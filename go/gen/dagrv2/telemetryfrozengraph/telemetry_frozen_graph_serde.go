// Code generated by dagr (dagr/codegen/go/serde.py); DO NOT EDIT.

package telemetryfrozengraph

import "dagrv2/dagr"

func storeTelemetry(n Telemetry, b *dagr.Builder) dagr.NodeStoreRef {
	if r, ok := b.BeginStoring(n.key()); ok {
		return r
	}
	var c3 int
	x3, has3 := n.Values()
	if has3 {
		c3 = dagr.StoreFixedArray(b, x3, dagr.StF64)
	}
	var c2 int
	x2, has2 := n.Tags()
	if has2 {
		c2 = dagr.StorePtrTableArrayPlain(b, x2, func(b *dagr.Builder, v string) int { return b.StoreUtf8(v, true) })
	}
	var c0 int
	x0, has0 := n.Source()
	if has0 {
		c0 = b.StoreUtf8(x0, true)
	}
	if has3 {
		b.StoreForwardPointer(c3)
	}
	if has2 {
		b.StoreForwardPointer(c2)
	}
	if x1, ok := n.Ts(); ok {
		b.StoreI64(x1)
	}
	if has0 {
		b.StoreForwardPointer(c0)
	}
	var bs uint64
	if _, ok := n.Source(); ok {
		bs |= 1 << 0
	}
	if _, ok := n.Ts(); ok {
		bs |= 1 << 1
	}
	if _, ok := n.Tags(); ok {
		bs |= 1 << 2
	}
	if _, ok := n.Values(); ok {
		bs |= 1 << 3
	}
	b.StoreBytes([]byte{byte(bs >> 0)})
	o := b.Cursor()
	b.FinishStoring(n.key(), o)
	return dagr.NodeStoreRef{Off: o}
}

// ToBytesTelemetryFrozenGraph serializes the graph reachable from root (byte-identical to every
// other Dagr implementation for the same graph). maxSize sets the cycle back-
// reference placeholder width (0 → 2 MiB, i.e. 4 bytes) and must match across
// producers for byte-identity.
func ToBytesTelemetryFrozenGraph(root Telemetry, maxSize int) []byte {
	b := dagr.NewBuilder(maxSize)
	off := dagr.NodeOffset(storeTelemetry(root, b))
	b.StoreLEB(uint64(b.Cursor()-off) << 2)
	return b.MakeData()
}
