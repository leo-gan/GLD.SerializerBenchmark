// lazy.rs — generated by dagr/codegen/rust/lazy.py
#![allow(dead_code, unused_variables, unused_mut, unused_imports)]
#![allow(unsafe_code)]
use crate::dagr_runtime;

pub struct TelemetryAccessor<'a> {
    data: &'a [u8],
    _at: usize,
    _seen: [u64; 1],
    r0: Option<core::ops::Range<usize>>,  // source
    v1: Option<i64>,  // ts
    a2: Option<usize>,  // tags (array content position)
    a3: Option<usize>,  // values (array content position)
}

impl<'a> TelemetryAccessor<'a> {
    pub fn new(data: &'a [u8], at: usize) -> Result<Self, dagr_runtime::DagrError> {
        if at >= data.len() { return Err(dagr_runtime::DagrError::InvalidData); }
        let (_total, ts_len) = dagr_runtime::try_read_leb(data, at).ok_or(dagr_runtime::DagrError::InvalidData)?;
        let payload_end = at + ts_len + _total as usize;
        let mut pos = at + ts_len;
        let mut _seen = [0u64; 1];
        let mut r0: Option<core::ops::Range<usize>> = None;
        let mut v1: Option<i64> = None;
        let mut a2: Option<usize> = None;
        let mut a3: Option<usize> = None;
        while pos < payload_end {
            if pos >= data.len() { break; }
            let (tag, tl) = dagr_runtime::read_leb(data, pos)?;
            let _is_raw = (tag & 1) != 0;
            pos += tl;
            match (tag >> 1) as usize {
                0 => {
                    _seen[0] |= 1u64 << 0;
                    let (len, lb) = dagr_runtime::read_leb(data, pos)?;
                    let start = pos + lb;
                    let end = start.checked_add(len as usize)
                        .ok_or(dagr_runtime::DagrError::InvalidData)?;
                    if end > data.len() { return Err(dagr_runtime::DagrError::InvalidData); }
                    r0 = Some(start..end);
                    pos = end;
                }
                1 => {
                    _seen[0] |= 1u64 << 1;
                    if _is_raw {
                        v1 = Some(dagr_runtime::read_i64(data, pos)? as i64);
                        pos += 8;
                    } else {
                        let (_lv, _lb) = dagr_runtime::read_leb(data, pos)?;
                        v1 = Some(dagr_runtime::from_zigzag(_lv) as i64);
                        pos += _lb;
                    }
                }
                2 => {
                    _seen[0] |= 1u64 << 2;
                    let (_sz, _sl) = dagr_runtime::read_leb(data, pos)?;
                    a2 = Some(pos + _sl);
                    pos += _sl + _sz as usize;
                }
                3 => {
                    _seen[0] |= 1u64 << 3;
                    let (_sz, _sl) = dagr_runtime::read_leb(data, pos)?;
                    a3 = Some(pos + _sl);
                    pos += _sl + _sz as usize;
                }
                _ => { if _is_raw { let (_sz, _sl) = dagr_runtime::read_leb(data, pos)?; pos += _sl + _sz as usize; } else { let (_comb, _cl) = dagr_runtime::read_leb(data, pos)?; pos += _cl; match (_comb & 7) as u8 { 0 => { let (_, _l) = dagr_runtime::read_leb(data, pos)?; pos += _l; } 1 => { pos += 1; } 2 => { pos += 2; } 3 => { pos += 4; } 4 => { pos += 8; } 5 => { if let Ok((_, _fb)) = dagr_runtime::read_packed_f32(data, pos) { pos += _fb; } } _ => { if let Ok((_sz2, _sl2)) = dagr_runtime::read_leb(data, pos) { pos += _sl2 + _sz2 as usize; } } } } }
            }
        }
        Ok(Self { data, _at: at, _seen, r0, v1, a2, a3 })
    }
    /// Byte offset of this node in the buffer — its within-buffer identity.
    pub fn _start(&self) -> usize { self._at }
    pub fn _has_source(&self) -> bool { self._seen[0] & (1u64 << 0) != 0 }
    pub fn _has_ts(&self) -> bool { self._seen[0] & (1u64 << 1) != 0 }
    pub fn _has_tags(&self) -> bool { self._seen[0] & (1u64 << 2) != 0 }
    pub fn _has_values(&self) -> bool { self._seen[0] & (1u64 << 3) != 0 }

    pub fn source(&self) -> Option<&'a str> {
        let r = match self.r0.clone() { Some(r) => r, None => return None };
        dagr_runtime::str_from(&self.data[r])
    }

    pub fn ts(&self) -> Option<i64> { self.v1 }

    pub fn tags(&self) -> Result<dagr_runtime::PackedStrArray<'a>, dagr_runtime::DagrError> {
        let pos = match self.a2 { None => return Ok(dagr_runtime::PackedStrArray::empty(self.data)), Some(p) => p };
        dagr_runtime::PackedStrArray::new(self.data, pos)
    }

    pub fn values(&self) -> Result<dagr_runtime::PackedF64Array<'a>, dagr_runtime::DagrError> {
        let pos = match self.a3 { None => return Ok(dagr_runtime::PackedF64Array::empty(self.data)), Some(p) => p };
        dagr_runtime::PackedF64Array::new(self.data, pos)
    }

}

pub fn read_root(data: &[u8]) -> Result<TelemetryAccessor<'_>, dagr_runtime::DagrError> {
    if data.is_empty() { return Err(dagr_runtime::DagrError::InvalidData); }
    let (root_dist, rl) = dagr_runtime::read_leb(data, 0)?;
    if (root_dist & 1) != 0 { return Err(dagr_runtime::DagrError::InvalidData); }
    let _at = rl + (root_dist >> 2) as usize;
    if _at >= data.len() { return Err(dagr_runtime::DagrError::InvalidData); }
    TelemetryAccessor::new(data, _at)
}
