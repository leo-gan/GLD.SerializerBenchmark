// Generated by dagr/codegen/typescript/direct.py — do not edit by hand.
// Graph: TelemetryFrozenPackedGraph — Direct Graph Builder ("spec/33-direct-graph-builder.md")
import { Builder, nodeOffset, lebLength, zigzag } from "./dagr_writer";
import type { NodeStoreRef } from "./dagr_writer";

export interface Telemetry {
  source: string | null;
  ts: bigint | null;
  tags: string[] | null;
  values: number[] | null;
}

function _storeTelemetry(n: Telemetry, b: Builder): NodeStoreRef {
  const _before = b.cursor;
  const _raw: boolean[] = new Array<boolean>(1).fill(false);
  if (n.values !== null) { b.storePackedFloatArray(n.values, true, true); }
  if (n.tags !== null) { b.storePackedComplexArray(n.tags, (b, v: string) => b.storeUtf8(v, false), false); }
  if (n.ts !== null) { if (lebLength(zigzag(n.ts)) < 8) { b.storeLEB(zigzag(n.ts)); } else { b.storeI64(n.ts); _raw[0] = true; } }
  if (n.source !== null) { b.storeUtf8(n.source, false); }
  { const _e: number[] = []; for (let _k = 0; _k < 1; _k++) { let _x = 0; for (let _bi = 0; _bi < 8 && _k * 8 + _bi < 1; _bi++) if (_raw[_k * 8 + _bi]) _x |= 1 << _bi; _e.push(_x); } b.storeBytes(_e); }
  let _nb = 0;
  if (n.source !== null) _nb |= 1 << 0;
  if (n.ts !== null) _nb |= 1 << 1;
  if (n.tags !== null) _nb |= 1 << 2;
  if (n.values !== null) _nb |= 1 << 3;
  b.storeBytes([(_nb >>> 0) & 0xff]);
  b.storeLEB(b.cursor - _before);  // blockLen
  const _o = b.cursor;
  return { off: _o };
}

export function writeInto(root: Telemetry, b: Builder): number {
  const off = nodeOffset(_storeTelemetry(root, b));
  return b.storeLEB((b.cursor - off) * 4);  // framing: rootDist<<2 | headerFlags(0)
}

export function toBytes(root: Telemetry, maxSize: number = 2 * 1024 * 1024): Uint8Array {
  const b = new Builder(maxSize, 1024);   // direct = tree → small buffer, grows if needed
  writeInto(root, b);
  return b.makeData();
}
