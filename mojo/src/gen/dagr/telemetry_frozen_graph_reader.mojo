# GENERATED by dagr/codegen/mojo/core.py — do not edit by hand.
# Lazy reader for graph 'TelemetryFrozenGraph' (regular-node scalars/utf8/data/enum/node-ref/arrays).

from dagr_reader import NumArray, Utf8Array, read_bitset, read_i64, read_leb, read_utf8, read_v62, root_offset


struct TelemetryAccessor[o: ImmOrigin](Copyable, Movable, ImplicitlyCopyable):
    var buf: Span[UInt8, Self.o]
    var pos: Int     # node start position (dedup key for eager restore)
    var _p0: Int   # position of field 0 (source); -1 = absent
    var _p1: Int   # position of field 1 (ts); -1 = absent
    var _p2: Int   # position of field 2 (tags); -1 = absent
    var _p3: Int   # position of field 3 (values); -1 = absent

    def __init__(out self, buf: Span[UInt8, Self.o], start: Int) raises:
        self.buf = buf
        self.pos = start
        var bs = read_bitset(buf, start, 1)
        var cur = start + 1
        if (bs >> 0) & 1:
            self._p0 = cur
            cur += read_v62(self.buf, cur)[1]
        else:
            self._p0 = -1
        if (bs >> 1) & 1:
            self._p1 = cur
            cur += 8
        else:
            self._p1 = -1
        if (bs >> 2) & 1:
            self._p2 = cur
            cur += read_v62(self.buf, cur)[1]
        else:
            self._p2 = -1
        if (bs >> 3) & 1:
            self._p3 = cur
            cur += read_v62(self.buf, cur)[1]
        else:
            self._p3 = -1

    def source(self) raises -> Optional[String]:
        if self._p0 < 0:
            return None
        var pos = self._p0
        var p = read_v62(self.buf, pos)
        return String(read_utf8(self.buf, pos + p[1] + Int(p[0]))[0])

    def ts(self) raises -> Optional[Int64]:
        if self._p1 < 0:
            return None
        var pos = self._p1
        return read_i64(self.buf, pos)

    def tags(self) raises -> Optional[Utf8Array[Self.o]]:
        if self._p2 < 0:
            return None
        var pos = self._p2
        var p = read_v62(self.buf, pos)
        var payload = pos + p[1] + Int(p[0])
        var h = read_leb(self.buf, payload)
        var count = Int(h[0]) >> 2
        var es = 1 << (Int(h[0]) & 3)
        var table_base = payload + h[1]
        return Utf8Array[Self.o](self.buf, table_base, table_base + count * es, count, es)

    def values(self) raises -> Optional[NumArray[Self.o, DType.float64, 8]]:
        if self._p3 < 0:
            return None
        var pos = self._p3
        var p = read_v62(self.buf, pos)
        var payload = pos + p[1] + Int(p[0])
        var h = read_leb(self.buf, payload)
        return NumArray[Self.o, DType.float64, 8](self.buf, payload + h[1], Int(h[0]))

def read_telemetry_root[o: ImmOrigin, //](buf: Span[UInt8, o]) raises -> TelemetryAccessor[o]:
    return TelemetryAccessor[o](buf, root_offset(buf))
