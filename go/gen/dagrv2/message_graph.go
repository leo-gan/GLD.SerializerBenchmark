// Code generated by dagr (dagr/codegen/go/core.py); DO NOT EDIT.

package dagrv2

import "dagrv2/dagr"

type MessageAccessor struct {
	buf []byte
	pos int
	p   [8]int
	raw uint64
}

func NewMessageAccessor(buf []byte, pos int) MessageAccessor {
	a := MessageAccessor{buf: buf, pos: pos}
	for i := range a.p {
		a.p[i] = -1
	}
	es, ee := dagr.PackedBounds(buf, pos)
	cursor := es
	last := -1
scan:
	for cursor < ee {
		tagv, tb := dagr.ReadLEB(buf, cursor)
		fidx := int(tagv >> 1)
		// spec 07: tags are strictly ascending; anything else is the unknown tail
		// (or a spec-15 header's zero padding) and ends the scan.
		if fidx <= last {
			break scan
		}
		last = fidx
		raw := (tagv & 1) == 1
		_ = raw
		pl := cursor + tb
		switch fidx {
		case 0:
			a.p[0] = pl
			if raw {
				a.raw |= 1 << 0
			}
			cursor = pl + 1
		case 1:
			a.p[1] = pl
			if raw {
				a.raw |= 1 << 1
			}
			if raw {
				cursor = pl + 4
			} else {
				cursor = dagr.LebEnd(buf, pl)
			}
		case 2:
			a.p[2] = pl
			if raw {
				a.raw |= 1 << 2
			}
			if raw {
				cursor = pl + 8
			} else {
				cursor = dagr.LebEnd(buf, pl)
			}
		case 3:
			a.p[3] = pl
			if raw {
				a.raw |= 1 << 3
			}
			if raw {
				cursor = pl + 8
			} else {
				_, n := dagr.DecodePackedF64(buf, pl)
				cursor = pl + n
			}
		case 4:
			a.p[4] = pl
			if raw {
				a.raw |= 1 << 4
			}
			cursor = dagr.SkipBlob(buf, pl)
		case 5:
			a.p[5] = pl
			if raw {
				a.raw |= 1 << 5
			}
			cursor = pl + 1
		case 6:
			a.p[6] = pl
			if raw {
				a.raw |= 1 << 6
			}
			if raw {
				cursor = pl + 4
			} else {
				cursor = dagr.LebEnd(buf, pl)
			}
		case 7:
			a.p[7] = pl
			if raw {
				a.raw |= 1 << 7
			}
			cursor = dagr.SkipBlob(buf, pl)
		default:
			break scan
		}
	}
	return a
}

// BufferPos is where this node starts in the buffer — its identity in a graph.
func (a MessageAccessor) BufferPos() int { return a.pos }

// BufferBytes is the buffer this accessor reads — with BufferPos, enough to reopen
// the node through another package's accessor (a DataSink does, spec/11 §12).
func (a MessageAccessor) BufferBytes() []byte { return a.buf }

func (a MessageAccessor) FBool() (bool, bool) {
	pl := a.p[0]
	if pl < 0 {
		return false, false
	}
	return dagr.ReadBool(a.buf, pl), true
}

func (a MessageAccessor) FInt32() (int32, bool) {
	pl := a.p[1]
	raw := (a.raw>>1)&1 == 1
	if pl < 0 {
		return 0, false
	}
	if raw {
		return dagr.ReadI32(a.buf, pl), true
	}
	x, _ := dagr.ReadZigZagLEB(a.buf, pl)
	return int32(x), true
}

func (a MessageAccessor) FInt64() (int64, bool) {
	pl := a.p[2]
	raw := (a.raw>>2)&1 == 1
	if pl < 0 {
		return 0, false
	}
	if raw {
		return dagr.ReadI64(a.buf, pl), true
	}
	x, _ := dagr.ReadZigZagLEB(a.buf, pl)
	return int64(x), true
}

func (a MessageAccessor) FFloat64() (float64, bool) {
	pl := a.p[3]
	raw := (a.raw>>3)&1 == 1
	if pl < 0 {
		return 0, false
	}
	if raw {
		return dagr.ReadF64(a.buf, pl), true
	}
	x, _ := dagr.DecodePackedF64(a.buf, pl)
	return x, true
}

func (a MessageAccessor) FString() (string, bool) {
	pl := a.p[4]
	if pl < 0 {
		return "", false
	}
	s, _ := dagr.ReadUtf8(a.buf, pl)
	return s, true
}

func (a MessageAccessor) FStringBytes() ([]byte, bool) {
	pl := a.p[4]
	if pl < 0 {
		return nil, false
	}
	d, _ := dagr.ReadBytes(a.buf, pl)
	return d, true
}

func (a MessageAccessor) FBool2() (bool, bool) {
	pl := a.p[5]
	if pl < 0 {
		return false, false
	}
	return dagr.ReadBool(a.buf, pl), true
}

func (a MessageAccessor) FInt32_2() (int32, bool) {
	pl := a.p[6]
	raw := (a.raw>>6)&1 == 1
	if pl < 0 {
		return 0, false
	}
	if raw {
		return dagr.ReadI32(a.buf, pl), true
	}
	x, _ := dagr.ReadZigZagLEB(a.buf, pl)
	return int32(x), true
}

func (a MessageAccessor) FString2() (string, bool) {
	pl := a.p[7]
	if pl < 0 {
		return "", false
	}
	s, _ := dagr.ReadUtf8(a.buf, pl)
	return s, true
}

func (a MessageAccessor) FString2Bytes() ([]byte, bool) {
	pl := a.p[7]
	if pl < 0 {
		return nil, false
	}
	d, _ := dagr.ReadBytes(a.buf, pl)
	return d, true
}

func walkMessage(a MessageAccessor, seen map[dagr.SeenKey]bool) {
	sk := dagr.SeenAt(a.buf, a.pos)
	if seen[sk] {
		return
	}
	seen[sk] = true
	_, _ = a.FBool()
	_, _ = a.FInt32()
	_, _ = a.FInt64()
	_, _ = a.FFloat64()
	_, _ = a.FString()
	_, _ = a.FBool2()
	_, _ = a.FInt32_2()
	_, _ = a.FString2()
}

// ValidateMessage exercises every accessor path below a once, under recover, as Open does
// for the root: nil means reading this subtree is panic-free, malformed bytes
// yield dagr.ErrMalformed.
func ValidateMessage(a MessageAccessor) (err error) {
	defer func() {
		if r := recover(); r != nil {
			err = dagr.ErrMalformed
		}
	}()
	walkMessage(a, map[dagr.SeenKey]bool{})
	return nil
}

// OpenMessageGraph parses the framing word, then validates the whole reachable graph once
// (every accessor path is exercised under recover), so a buffer that passed Open is
// read panic-free by construction; malformed input yields dagr.ErrMalformed. A spec-15
// header is skipped. The buffer must outlive every accessor derived from it.
func OpenMessageGraph(buf []byte) (root MessageAccessor, err error) {
	root, err = OpenMessageGraphUnchecked(buf)
	if err != nil {
		return MessageAccessor{}, err
	}
	defer func() {
		if r := recover(); r != nil {
			root, err = MessageAccessor{}, dagr.ErrMalformed
		}
	}()
	walkMessage(root, map[dagr.SeenKey]bool{})
	return root, nil
}

// OpenMessageGraphUnchecked returns the root accessor after checking only the framing word —
// O(1), for buffers already known to be well-formed (a Go-written buffer, or one
// validated by an earlier Open).
func OpenMessageGraphUnchecked(buf []byte) (MessageAccessor, error) {
	root, err := dagr.RootOffset(buf)
	if err != nil {
		return MessageAccessor{}, err
	}
	return NewMessageAccessor(buf, root), nil
}
