// Code generated by dagr (dagr/codegen/go/serde.py); DO NOT EDIT.

package documentfrozengraph

import "dagrv2/dagr"

func storeDocumentMeta(n DocumentMeta, b *dagr.Builder) dagr.NodeStoreRef {
	if r, ok := b.BeginStoring(n.key()); ok {
		return r
	}
	var c0 int
	x0, has0 := n.Region()
	if has0 {
		c0 = b.StoreUtf8(x0, true)
	}
	if x1, ok := n.Version(); ok {
		b.StoreI32(x1)
	}
	if has0 {
		b.StoreForwardPointer(c0)
	}
	var bs uint64
	if _, ok := n.Region(); ok {
		bs |= 1 << 0
	}
	if _, ok := n.Version(); ok {
		bs |= 1 << 1
	}
	b.StoreBytes([]byte{byte(bs >> 0)})
	o := b.Cursor()
	b.FinishStoring(n.key(), o)
	return dagr.NodeStoreRef{Off: o}
}

func storeDocumentItem(n DocumentItem, b *dagr.Builder) dagr.NodeStoreRef {
	if r, ok := b.BeginStoring(n.key()); ok {
		return r
	}
	var c0 int
	x0, has0 := n.Sku()
	if has0 {
		c0 = b.StoreUtf8(x0, true)
	}
	if x2, ok := n.PriceMinor(); ok {
		b.StoreI64(x2)
	}
	if x1, ok := n.Qty(); ok {
		b.StoreI32(x1)
	}
	if has0 {
		b.StoreForwardPointer(c0)
	}
	var bs uint64
	if _, ok := n.Sku(); ok {
		bs |= 1 << 0
	}
	if _, ok := n.Qty(); ok {
		bs |= 1 << 1
	}
	if _, ok := n.PriceMinor(); ok {
		bs |= 1 << 2
	}
	b.StoreBytes([]byte{byte(bs >> 0)})
	o := b.Cursor()
	b.FinishStoring(n.key(), o)
	return dagr.NodeStoreRef{Off: o}
}

func storeDocument(n Document, b *dagr.Builder) dagr.NodeStoreRef {
	if r, ok := b.BeginStoring(n.key()); ok {
		return r
	}
	var c3 int
	x3, has3 := n.Items()
	if has3 {
		c3 = dagr.StoreNodeRefArrayPlain(b, x3, func(b *dagr.Builder, v DocumentItem) dagr.NodeStoreRef { return storeDocumentItem(v, b) })
	}
	var c2 dagr.NodeStoreRef
	x2, has2 := n.Meta()
	if has2 {
		c2 = storeDocumentMeta(x2, b)
	}
	var c0 int
	x0, has0 := n.ID()
	if has0 {
		c0 = b.StoreUtf8(x0, true)
	}
	if has3 {
		b.StoreForwardPointer(c3)
	}
	if has2 {
		b.StoreBidirectionalPointer(c2)
	}
	if x1, ok := n.Status(); ok {
		b.StoreI32(x1)
	}
	if has0 {
		b.StoreForwardPointer(c0)
	}
	var bs uint64
	if _, ok := n.ID(); ok {
		bs |= 1 << 0
	}
	if _, ok := n.Status(); ok {
		bs |= 1 << 1
	}
	if _, ok := n.Meta(); ok {
		bs |= 1 << 2
	}
	if _, ok := n.Items(); ok {
		bs |= 1 << 3
	}
	b.StoreBytes([]byte{byte(bs >> 0)})
	o := b.Cursor()
	b.FinishStoring(n.key(), o)
	return dagr.NodeStoreRef{Off: o}
}

// ToBytesDocumentFrozenGraph serializes the graph reachable from root (byte-identical to every
// other Dagr implementation for the same graph). maxSize sets the cycle back-
// reference placeholder width (0 → 2 MiB, i.e. 4 bytes) and must match across
// producers for byte-identity.
func ToBytesDocumentFrozenGraph(root Document, maxSize int) []byte {
	b := dagr.NewBuilder(maxSize)
	off := dagr.NodeOffset(storeDocument(root, b))
	b.StoreLEB(uint64(b.Cursor()-off) << 2)
	return b.MakeData()
}
