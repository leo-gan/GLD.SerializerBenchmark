# GENERATED by dagr/codegen/mojo/serde.py — do not edit by hand.
# Serializer (arena -> bytes) for graph 'StringsFrozenGraph' — plan 28 Phase 4.

from dagr_writer import Builder, NodeStoreRef, node_offset, resolved_ref, nil_ref, to_zigzag, leb_length
from strings_frozen_graph_arena import StringsFrozenGraphArena, Strings

comptime _VT_MAX = 1   # widest regular-node vtable (§1.19)


# Build a List[Int] of vtable entries from a varargs-free call site.
def _ilist(*vals: Int) -> List[Int]:
    var l = List[Int]()
    for v in vals:
        l.append(v)
    return l^


# Type-namespaced node identity for dedup / cycle keys: a bare packed handle
# collides across node types (index space is per-type), so fold in the type id.
def _nid(tid: Int, idx: Int) -> UInt64:
    return (UInt64(idx) << 8) | UInt64(tid)


def _store_strings[o: Origin[mut=False], //](mut b: Builder[_VT_MAX], a: StringsFrozenGraphArena, h: Strings[o]) raises -> NodeStoreRef:
    var _id = _nid(0, h._index())
    var _r = b.begin_storing_acyclic(_id)
    if _r.is_pending or _r.off != -1:
        return _r
    var _pr0 = True
    var _c0 = -1
    if _pr0:
        var _bi0 = h._index()
        var _offs0 = b.take_ints()
        for _k0 in range(len(a._arr_strings[_bi0].items) - 1, -1, -1):
            _offs0.append(b.store_utf8(a._arr_strings[_bi0].items[_k0], True))
        _c0 = b.store_ptr_table(_offs0)
        b.return_ints(_offs0^)
    if _pr0:
        _ = b.store_forward_pointer(_c0)
    var _bs: UInt64 = 0
    if _pr0:
        _bs |= UInt64(1) << 0
    _ = b.store_u8(UInt8(_bs & 0xff))
    var _o = b.cursor
    b.finish_storing_acyclic(_id, _o)
    return resolved_ref(_o)


def write_strings_graph(mut b: Builder[_VT_MAX], a: StringsFrozenGraphArena) raises -> Int:
    var _off = node_offset(_store_strings(b, a, a.root().value()))
    return b.store_leb(UInt64((b.cursor - _off) << 2))    # framing (no header)

def serialize_strings_graph(a: StringsFrozenGraphArena, max_size: Int = 2 * 1024 * 1024) raises -> List[UInt8]:
    var b = Builder[_VT_MAX](hint=len(a._arr_strings), max_size=max_size)
    _ = write_strings_graph(b, a)
    return b.make_data()
