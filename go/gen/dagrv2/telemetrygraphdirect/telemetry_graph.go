// Code generated by dagr (dagr/codegen/go/direct.py); DO NOT EDIT.

package telemetrygraphdirect

import (
	"iter"

	"dagrv2/dagr"
)

type TelemetryAccessor struct {
	buf []byte
	pos int
	p   [4]int
	raw uint64
}

func NewTelemetryAccessor(buf []byte, pos int) TelemetryAccessor {
	a := TelemetryAccessor{buf: buf, pos: pos}
	for i := range a.p {
		a.p[i] = -1
	}
	es, ee := dagr.PackedBounds(buf, pos)
	cursor := es
	last := -1
scan:
	for cursor < ee {
		tagv, tb := dagr.ReadLEB(buf, cursor)
		fidx := int(tagv >> 1)
		// spec 07: tags are strictly ascending; anything else is the unknown tail
		// (or a spec-15 header's zero padding) and ends the scan.
		if fidx <= last {
			break scan
		}
		last = fidx
		raw := (tagv & 1) == 1
		_ = raw
		pl := cursor + tb
		switch fidx {
		case 0:
			a.p[0] = pl
			if raw {
				a.raw |= 1 << 0
			}
			cursor = dagr.SkipBlob(buf, pl)
		case 1:
			a.p[1] = pl
			if raw {
				a.raw |= 1 << 1
			}
			if raw {
				cursor = pl + 8
			} else {
				cursor = dagr.LebEnd(buf, pl)
			}
		case 2:
			a.p[2] = pl
			if raw {
				a.raw |= 1 << 2
			}
			cursor = dagr.SkipBlob(buf, pl)
		case 3:
			a.p[3] = pl
			if raw {
				a.raw |= 1 << 3
			}
			cursor = dagr.SkipBlob(buf, pl)
		default:
			break scan
		}
	}
	return a
}

// BufferPos is where this node starts in the buffer — its identity in a graph.
func (a TelemetryAccessor) BufferPos() int { return a.pos }

// BufferBytes is the buffer this accessor reads — with BufferPos, enough to reopen
// the node through another package's accessor (a DataSink does, spec/11 §12).
func (a TelemetryAccessor) BufferBytes() []byte { return a.buf }

func (a TelemetryAccessor) Source() (string, bool) {
	pl := a.p[0]
	if pl < 0 {
		return "", false
	}
	s, _ := dagr.ReadUtf8(a.buf, pl)
	return s, true
}

func (a TelemetryAccessor) SourceBytes() ([]byte, bool) {
	pl := a.p[0]
	if pl < 0 {
		return nil, false
	}
	d, _ := dagr.ReadBytes(a.buf, pl)
	return d, true
}

func (a TelemetryAccessor) Ts() (int64, bool) {
	pl := a.p[1]
	raw := (a.raw>>1)&1 == 1
	if pl < 0 {
		return 0, false
	}
	if raw {
		return dagr.ReadI64(a.buf, pl), true
	}
	x, _ := dagr.ReadZigZagLEB(a.buf, pl)
	return int64(x), true
}

func (a TelemetryAccessor) TagsLen() (int, bool) {
	pl := a.p[2]
	if pl < 0 {
		return 0, false
	}
	_, blb := dagr.ReadLEB(a.buf, pl)
	c := pl + blb
	h, _ := dagr.ReadLEB(a.buf, c)
	return int(h), true
}

func (a TelemetryAccessor) TagsAt(i int) string {
	pl := a.p[2]
	_, blb := dagr.ReadLEB(a.buf, pl)
	c := pl + blb
	_, hb := dagr.ReadLEB(a.buf, c)
	p := c + hb
	for j := 0; j < i; j++ {
		p = dagr.SkipBlob(a.buf, p)
	}
	s, _ := dagr.ReadUtf8(a.buf, p)
	return s
}

type TelemetryTagsIter struct {
	a    TelemetryAccessor
	buf  []byte
	p    int
	i    int
	n    int
	walk bool
}

// TagsIter walks the array once, keeping its position between elements —
// O(n) for the whole array, where TagsAt(i) is O(i) per call.
func (a TelemetryAccessor) TagsIter() TelemetryTagsIter {
	n, lok := a.TagsLen()
	if !lok {
		n = 0
	}
	it := TelemetryTagsIter{a: a, buf: a.buf, n: n}
	pl := a.p[2]
	if pl < 0 || n == 0 {
		return it
	}
	_, blb := dagr.ReadLEB(a.buf, pl)
	c := pl + blb
	_, hb := dagr.ReadLEB(a.buf, c)
	p := c + hb
	it.p = p
	it.walk = true
	return it
}

func (it *TelemetryTagsIter) Next() (string, bool) {
	var v string
	if it.i >= it.n {
		return v, false
	}
	if !it.walk {
		v = it.a.TagsAt(it.i)
		it.i++
		return v, true
	}
	i := it.i
	it.i++
	_ = i
	v, _ = dagr.ReadUtf8(it.buf, it.p)
	it.p = dagr.SkipBlob(it.buf, it.p)
	return v, true
}

func (it *TelemetryTagsIter) Len() int { return it.n }

// TagsAll ranges over the array in one pass.
func (a TelemetryAccessor) TagsAll() iter.Seq[string] {
	return func(yield func(string) bool) {
		it := a.TagsIter()
		for {
			v, ok := it.Next()
			if !ok || !yield(v) {
				return
			}
		}
	}
}

func (a TelemetryAccessor) ValuesLen() (int, bool) {
	pl := a.p[3]
	if pl < 0 {
		return 0, false
	}
	_, blb := dagr.ReadLEB(a.buf, pl)
	c := pl + blb
	h, _ := dagr.ReadLEB(a.buf, c)
	return int(h) >> 2, true
}

func (a TelemetryAccessor) ValuesAt(i int) float64 {
	pl := a.p[3]
	_, blb := dagr.ReadLEB(a.buf, pl)
	c := pl + blb
	h, hb := dagr.ReadLEB(a.buf, c)
	mode := int(h) & 3
	p := c + hb
	if mode == 1 {
		return dagr.ReadF64(a.buf, p+i*8)
	}
	for j := 0; j < i; j++ {
		_, n := dagr.DecodePackedF64(a.buf, p)
		p += n
	}
	x, _ := dagr.DecodePackedF64(a.buf, p)
	return x
}

type TelemetryValuesIter struct {
	a    TelemetryAccessor
	buf  []byte
	p    int
	i    int
	n    int
	mode int
	walk bool
}

// ValuesIter walks the array once, keeping its position between elements —
// O(n) for the whole array, where ValuesAt(i) is O(i) per call.
func (a TelemetryAccessor) ValuesIter() TelemetryValuesIter {
	n, lok := a.ValuesLen()
	if !lok {
		n = 0
	}
	it := TelemetryValuesIter{a: a, buf: a.buf, n: n}
	pl := a.p[3]
	if pl < 0 || n == 0 {
		return it
	}
	_, blb := dagr.ReadLEB(a.buf, pl)
	c := pl + blb
	h, hb := dagr.ReadLEB(a.buf, c)
	mode := int(h) & 3
	p := c + hb
	it.p = p
	it.mode = mode
	it.walk = true
	return it
}

func (it *TelemetryValuesIter) Next() (float64, bool) {
	var v float64
	if it.i >= it.n {
		return v, false
	}
	if !it.walk {
		v = it.a.ValuesAt(it.i)
		it.i++
		return v, true
	}
	i := it.i
	it.i++
	_ = i
	if it.mode == 1 {
		v = dagr.ReadF64(it.buf, it.p)
	} else {
		x, _ := dagr.DecodePackedF64(it.buf, it.p)
		v = x
	}
	if it.mode == 1 {
		it.p += 8
	} else {
		_, n := dagr.DecodePackedF64(it.buf, it.p)
		it.p += n
	}
	return v, true
}

func (it *TelemetryValuesIter) Len() int { return it.n }

// ValuesAll ranges over the array in one pass.
func (a TelemetryAccessor) ValuesAll() iter.Seq[float64] {
	return func(yield func(float64) bool) {
		it := a.ValuesIter()
		for {
			v, ok := it.Next()
			if !ok || !yield(v) {
				return
			}
		}
	}
}

// Telemetry is a plain value record (spec 11 §4: no arena).
type Telemetry struct {
	Source dagr.Opt[string]
	Ts     dagr.Opt[int64]
	Tags   dagr.Opt[[]string]
	Values dagr.Opt[[]float64]
}

func restoreTelemetry(acc TelemetryAccessor) Telemetry {
	var rec Telemetry
	{
		if v, ok := acc.Source(); ok {
			rec.Source = dagr.Some(v)
		}
	}
	{
		if v, ok := acc.Ts(); ok {
			rec.Ts = dagr.Some(v)
		}
	}
	{
		if cnt, ok := acc.TagsLen(); ok {
			s := make([]string, cnt)
			it := acc.TagsIter()
			for i := 0; i < cnt; i++ {
				e, _ := it.Next()
				s[i] = e
			}
			rec.Tags = dagr.Some(s)
		}
	}
	{
		if cnt, ok := acc.ValuesLen(); ok {
			s := make([]float64, cnt)
			it := acc.ValuesIter()
			for i := 0; i < cnt; i++ {
				e, _ := it.Next()
				s[i] = e
			}
			rec.Values = dagr.Some(s)
		}
	}
	return rec
}

// RestoreTelemetry decodes one Telemetry (and its subtree) into plain values — the entry
// a DataSink uses for a record field that holds this graph's node (spec/11 §12).
func RestoreTelemetry(acc TelemetryAccessor) Telemetry {
	return restoreTelemetry(acc)
}

func storeTelemetry(n Telemetry, b *dagr.Builder) dagr.NodeStoreRef {
	buf, i := b.Take()
	buf, i = PutTelemetry(n, b, buf, i)
	b.Put(buf, i)
	return dagr.NodeStoreRef{Off: b.Cursor()}
}

func PutTelemetry(n Telemetry, b *dagr.Builder, buf []byte, i int) ([]byte, int) {
	before := len(buf) - i
	if x3, ok := n.Values.V, n.Values.Ok; ok {
		b.Put(buf, i)
		b.StorePackedFloatArray(nil, x3, true)
		buf, i = b.Take()
		buf, i = dagr.PutLEBAt(buf, i, 7)
	}
	if x2, ok := n.Tags.V, n.Tags.Ok; ok {
		b.Put(buf, i)
		dagr.StorePackedComplexArrayPlain(b, x2, func(b *dagr.Builder, v string) { b.StoreUtf8(v, false) })
		buf, i = b.Take()
		buf, i = dagr.PutLEBAt(buf, i, 5)
	}
	if x1, ok := n.Ts.V, n.Ts.Ok; ok {
		zz := dagr.ToZigZag(int64(x1))
		if dagr.LebLength(zz) < 8 {
			buf, i = dagr.PutLEBTaggedAt(buf, i, zz, 2)
		} else {
			buf, i = dagr.PutFixedAt(buf, i, uint64(uint64(x1)), 8)
			buf, i = dagr.PutLEBAt(buf, i, 3)
		}
	}
	if x0, ok := n.Source.V, n.Source.Ok; ok {
		buf, i = dagr.PutUtf8TaggedAt(buf, i, x0, 1)
	}
	return dagr.PutLEBAt(buf, i, uint64((len(buf)-i)-before))
}

// TelemetryGraphBuilder is the reusable form (spec 33 §4.3): one builder, reset per Build.
type TelemetryGraphBuilder struct {
	b *dagr.Builder
}

func NewTelemetryGraphBuilder() *TelemetryGraphBuilder {
	return &TelemetryGraphBuilder{b: dagr.NewBuilder(0)}
}

// Grow makes room for a record of n bytes up front, so the first Build does not
// have to double its way up from 64. A reused builder keeps its buffer.
func (w *TelemetryGraphBuilder) Grow(n int) { w.b.Grow(n) }

// Build serializes the value tree under root — byte-identical to the arena's ToBytes.
func (w *TelemetryGraphBuilder) Build(root Telemetry) []byte {
	w.build(root)
	return w.b.MakeData()
}

// BuildAppend is Build without the copy: it appends the record to dst and returns
// the extended slice. A caller that keeps dst between batches reuses its capacity,
// so steady-state encoding allocates nothing — this is the Go analogue of Rust's
// write_into (spec 33 §4.3).
func (w *TelemetryGraphBuilder) BuildAppend(dst []byte, root Telemetry) []byte {
	w.build(root)
	return w.b.AppendTo(dst)
}

// BuildBytes is BuildAppend with no copy at all: the returned slice aliases the
// builder's buffer and is invalidated by the next Build on it.
func (w *TelemetryGraphBuilder) BuildBytes(root Telemetry) []byte {
	w.build(root)
	return w.b.Bytes()
}

func (w *TelemetryGraphBuilder) build(root Telemetry) {
	b := w.b
	b.Reset()
	off := dagr.NodeOffset(storeTelemetry(root, b))
	b.StoreLEB(uint64(b.Cursor()-off) << 2)
}

// BuildTelemetryGraph is the one-shot form (spec 33 §4.2).
func BuildTelemetryGraph(root Telemetry) []byte {
	return NewTelemetryGraphBuilder().Build(root)
}

// RestoreTelemetryGraphValues decodes a buffer into plain value structs (no arena);
// malformed bytes yield dagr.ErrMalformed.
func RestoreTelemetryGraphValues(buf []byte) (v Telemetry, err error) {
	pos, err := dagr.RootOffset(buf)
	if err != nil {
		return Telemetry{}, err
	}
	defer func() {
		if r := recover(); r != nil {
			v, err = Telemetry{}, dagr.ErrMalformed
		}
	}()
	return restoreTelemetry(NewTelemetryAccessor(buf, pos)), nil
}
