# GENERATED by dagr/codegen/mojo/core.py — do not edit by hand.
# Lazy reader for graph 'MessageFrozenGraph' (regular-node scalars/utf8/data/enum/node-ref/arrays).

from dagr_reader import read_bitset, read_bool, read_f64, read_i32, read_i64, read_utf8, read_v62, root_offset


struct MessageAccessor[o: ImmOrigin](Copyable, Movable, ImplicitlyCopyable):
    var buf: Span[UInt8, Self.o]
    var pos: Int     # node start position (dedup key for eager restore)
    var _p0: Int   # position of field 0 (f_bool); -1 = absent
    var _p1: Int   # position of field 1 (f_int32); -1 = absent
    var _p2: Int   # position of field 2 (f_int64); -1 = absent
    var _p3: Int   # position of field 3 (f_float64); -1 = absent
    var _p4: Int   # position of field 4 (f_string); -1 = absent
    var _p5: Int   # position of field 5 (f_bool_2); -1 = absent
    var _p6: Int   # position of field 6 (f_int32_2); -1 = absent
    var _p7: Int   # position of field 7 (f_string_2); -1 = absent

    def __init__(out self, buf: Span[UInt8, Self.o], start: Int) raises:
        self.buf = buf
        self.pos = start
        var bs = read_bitset(buf, start, 1)
        var cur = start + 1
        if (bs >> 0) & 1:
            self._p0 = cur
            cur += 1
        else:
            self._p0 = -1
        if (bs >> 1) & 1:
            self._p1 = cur
            cur += 4
        else:
            self._p1 = -1
        if (bs >> 2) & 1:
            self._p2 = cur
            cur += 8
        else:
            self._p2 = -1
        if (bs >> 3) & 1:
            self._p3 = cur
            cur += 8
        else:
            self._p3 = -1
        if (bs >> 4) & 1:
            self._p4 = cur
            cur += read_v62(self.buf, cur)[1]
        else:
            self._p4 = -1
        if (bs >> 5) & 1:
            self._p5 = cur
            cur += 1
        else:
            self._p5 = -1
        if (bs >> 6) & 1:
            self._p6 = cur
            cur += 4
        else:
            self._p6 = -1
        if (bs >> 7) & 1:
            self._p7 = cur
            cur += read_v62(self.buf, cur)[1]
        else:
            self._p7 = -1

    def f_bool(self) raises -> Optional[Bool]:
        if self._p0 < 0:
            return None
        var pos = self._p0
        return read_bool(self.buf, pos)

    def f_int32(self) raises -> Optional[Int32]:
        if self._p1 < 0:
            return None
        var pos = self._p1
        return read_i32(self.buf, pos)

    def f_int64(self) raises -> Optional[Int64]:
        if self._p2 < 0:
            return None
        var pos = self._p2
        return read_i64(self.buf, pos)

    def f_float64(self) raises -> Optional[Float64]:
        if self._p3 < 0:
            return None
        var pos = self._p3
        return read_f64(self.buf, pos)

    def f_string(self) raises -> Optional[String]:
        if self._p4 < 0:
            return None
        var pos = self._p4
        var p = read_v62(self.buf, pos)
        return String(read_utf8(self.buf, pos + p[1] + Int(p[0]))[0])

    def f_bool_2(self) raises -> Optional[Bool]:
        if self._p5 < 0:
            return None
        var pos = self._p5
        return read_bool(self.buf, pos)

    def f_int32_2(self) raises -> Optional[Int32]:
        if self._p6 < 0:
            return None
        var pos = self._p6
        return read_i32(self.buf, pos)

    def f_string_2(self) raises -> Optional[String]:
        if self._p7 < 0:
            return None
        var pos = self._p7
        var p = read_v62(self.buf, pos)
        return String(read_utf8(self.buf, pos + p[1] + Int(p[0]))[0])

def read_message_root[o: ImmOrigin, //](buf: Span[UInt8, o]) raises -> MessageAccessor[o]:
    return MessageAccessor[o](buf, root_offset(buf))
