name: Benchmark CI

# Harness smoke checks (per language when those paths change) and analysis unit
# tests (when analysis/ or shared config/schemas change). Summaries and plots are
# generated locally and committed under docs/analysis/ for GitHub Pages — never
# here. workflow_dispatch runs all jobs for the chosen mode (smoke by default).

on:
  push:
    branches: [main, master]
    paths-ignore:
      - '**.md'
      - '**.txt'
      - '.gitignore'
      - 'docs/**'
      - 'mkdocs.yml'
      - 'site/**'
  pull_request:
    branches: [main, master]
    paths-ignore:
      - '**.md'
      - '**.txt'
      - '.gitignore'
      - 'docs/**'
      - 'mkdocs.yml'
      - 'site/**'
  workflow_dispatch:
    inputs:
      mode:
        description: 'Benchmark mode'
        required: true
        default: 'smoke'
        type: choice
        options:
          - smoke
          - all-single
          - full

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      csharp: ${{ steps.filter.outputs.csharp }}
      python: ${{ steps.filter.outputs.python }}
      rust: ${{ steps.filter.outputs.rust }}
      c: ${{ steps.filter.outputs.c }}
      javascript: ${{ steps.filter.outputs.javascript }}
      go: ${{ steps.filter.outputs.go }}
      analysis: ${{ steps.filter.outputs.analysis }}
    steps:
      - uses: actions/checkout@v4

      - name: Detect changed paths
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            csharp:
              - 'c-sharp/**'
              - 'config/benchmark_config.yaml'
              - 'schemas/**'
              - 'scripts/lib/**'
              - 'scripts/read-config.py'
              - '.github/workflows/benchmark-ci.yml'
            python:
              - 'python/**'
              - 'config/benchmark_config.yaml'
              - 'schemas/**'
              - 'scripts/lib/**'
              - 'scripts/read-config.py'
              - '.github/workflows/benchmark-ci.yml'
            rust:
              - 'rust/**'
              - 'config/benchmark_config.yaml'
              - 'schemas/**'
              - 'scripts/lib/**'
              - 'scripts/read-config.py'
              - '.github/workflows/benchmark-ci.yml'
            c:
              - 'c/**'
              - 'config/benchmark_config.yaml'
              - 'schemas/**'
              - 'scripts/lib/**'
              - 'scripts/read-config.py'
              - '.github/workflows/benchmark-ci.yml'
            javascript:
              - 'javascript/**'
              - 'config/benchmark_config.yaml'
              - 'schemas/**'
              - 'scripts/lib/**'
              - 'scripts/read-config.py'
              - '.github/workflows/benchmark-ci.yml'
            go:
              - 'go/**'
              - 'config/benchmark_config.yaml'
              - 'schemas/**'
              - 'scripts/lib/**'
              - 'scripts/read-config.py'
              - '.github/workflows/benchmark-ci.yml'
            analysis:
              - 'analysis/**'
              - 'config/benchmark_config.yaml'
              - 'schemas/**'
              - 'scripts/lib/**'
              - 'scripts/read-config.py'
              - 'scripts/run-all-benchmarks.sh'
              - 'scripts/verify-results.sh'
              - '.github/workflows/benchmark-ci.yml'

  csharp-benchmark:
    needs: changes
    if: >-
      github.event_name == 'workflow_dispatch' ||
      needs.changes.outputs.csharp == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run C# Benchmark
        run: |
          cd c-sharp
          ./scripts/run-benchmarks.sh ${{ github.event.inputs.mode || 'smoke' }}

  python-benchmark:
    needs: changes
    if: >-
      github.event_name == 'workflow_dispatch' ||
      needs.changes.outputs.python == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run Python Benchmark
        run: |
          cd python
          ./scripts/run-benchmarks.sh ${{ github.event.inputs.mode || 'smoke' }}

  rust-benchmark:
    needs: changes
    if: >-
      github.event_name == 'workflow_dispatch' ||
      needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Run Rust Benchmark
        run: |
          cd rust
          ./scripts/run-benchmarks.sh ${{ github.event.inputs.mode || 'smoke' }}

  c-benchmark:
    needs: changes
    if: >-
      github.event_name == 'workflow_dispatch' ||
      needs.changes.outputs.c == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install CMake
        run: sudo apt-get update && sudo apt-get install -y cmake build-essential

      - name: Run C Benchmark
        run: |
          cd c
          ./scripts/run-benchmarks.sh ${{ github.event.inputs.mode || 'smoke' }}

  javascript-benchmark:
    needs: changes
    if: >-
      github.event_name == 'workflow_dispatch' ||
      needs.changes.outputs.javascript == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: javascript/package-lock.json

      - name: Install dependencies
        run: |
          cd javascript
          npm ci

      - name: Run JavaScript Benchmark
        run: |
          cd javascript
          ./scripts/run-benchmarks.sh ${{ github.event.inputs.mode || 'smoke' }}

  go-benchmark:
    needs: changes
    if: >-
      github.event_name == 'workflow_dispatch' ||
      needs.changes.outputs.go == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23.x'
          cache-dependency-path: go/go.sum

      - name: Install protoc (optional regen of committed gen/pb)
        run: |
          sudo apt-get update
          sudo apt-get install -y protobuf-compiler
          go install google.golang.org/protobuf/cmd/protoc-gen-go@v1.36.1
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"

      - name: Run Go Benchmark
        run: |
          cd go
          ./scripts/run-benchmarks.sh ${{ github.event.inputs.mode || 'smoke' }}

  analysis-unit-tests:
    needs: changes
    if: >-
      github.event_name == 'workflow_dispatch' ||
      needs.changes.outputs.analysis == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install analysis package with test deps
        run: |
          cd analysis
          python -m venv .venv
          .venv/bin/pip install -U pip
          .venv/bin/pip install -e ".[dev]"

      - name: Run unit tests
        run: |
          cd analysis
          .venv/bin/pytest -q

      - name: Smoke read-config (master YAML)
        run: |
          PYTHONPATH=analysis/src python3 scripts/read-config.py --enabled-langs
          PYTHONPATH=analysis/src python3 scripts/read-config.py --lang-runners | grep -E '^(csharp|python|rust|c|javascript|go)\|'
          PYTHONPATH=analysis/src python3 scripts/read-config.py --mode-reps smoke
