# GENERATED by dagr/codegen/mojo/core.py — do not edit by hand.
# Lazy reader for graph 'TelemetryGraph' (regular-node scalars/utf8/data/enum/node-ref/arrays).

from dagr_reader import OwnedArray, PackedFloatArray, decode_utf8_list, read_i64, read_leb, read_utf8, read_zigzag_leb, root_offset


struct TelemetryAccessor[o: ImmOrigin](Copyable, Movable, ImplicitlyCopyable):
    var buf: Span[UInt8, Self.o]
    var pos: Int     # node start position (dedup key for eager restore)
    var _p0: Int   # tag position of field 0 (source); -1 = absent
    var _p1: Int   # tag position of field 1 (ts); -1 = absent
    var _p2: Int   # tag position of field 2 (tags); -1 = absent
    var _p3: Int   # tag position of field 3 (values); -1 = absent

    def __init__(out self, buf: Span[UInt8, Self.o], start: Int) raises:
        self.buf = buf
        self.pos = start
        var r = read_leb(buf, start)
        var cursor = start + r[1]
        var end = cursor + Int(r[0])
        self._p0 = -1
        if cursor < end:
            var t0 = read_leb(buf, cursor)
            if (t0[0] >> 1) == 0:
                self._p0 = cursor
                cursor += t0[1]
                var _l0 = read_leb(buf, cursor)
                cursor += _l0[1] + Int(_l0[0])
        self._p1 = -1
        if cursor < end:
            var t1 = read_leb(buf, cursor)
            if (t1[0] >> 1) == 1:
                self._p1 = cursor
                var raw1 = (t1[0] & 1) == 1
                cursor += t1[1]
                if raw1:
                    cursor += 8
                else:
                    cursor += read_leb(buf, cursor)[1]
        self._p2 = -1
        if cursor < end:
            var t2 = read_leb(buf, cursor)
            if (t2[0] >> 1) == 2:
                self._p2 = cursor
                cursor += t2[1]
                var _al2 = read_leb(buf, cursor)
                cursor += _al2[1] + Int(_al2[0])
        self._p3 = -1
        if cursor < end:
            var t3 = read_leb(buf, cursor)
            if (t3[0] >> 1) == 3:
                self._p3 = cursor
                cursor += t3[1]
                var _al3 = read_leb(buf, cursor)
                cursor += _al3[1] + Int(_al3[0])

    def source(self) raises -> Optional[String]:
        if self._p0 < 0:
            return None
        var t = read_leb(self.buf, self._p0)
        var payload = self._p0 + t[1]
        return String(read_utf8(self.buf, payload)[0])

    def ts(self) raises -> Optional[Int64]:
        if self._p1 < 0:
            return None
        var t = read_leb(self.buf, self._p1)
        var raw = (t[0] & 1) == 1
        var payload = self._p1 + t[1]
        if raw:
            return read_i64(self.buf, payload)
        else:
            return Int64(read_zigzag_leb(self.buf, payload)[0])

    def tags(self) raises -> Optional[OwnedArray[String]]:
        if self._p2 < 0:
            return None
        var t = read_leb(self.buf, self._p2)
        var payload = self._p2 + t[1]
        var _abl = read_leb(self.buf, payload)
        return OwnedArray[String](decode_utf8_list(self.buf, payload + _abl[1]))

    def values(self) raises -> Optional[PackedFloatArray[Self.o, DType.float64, 8]]:
        if self._p3 < 0:
            return None
        var t = read_leb(self.buf, self._p3)
        var payload = self._p3 + t[1]
        var _abl = read_leb(self.buf, payload)
        return PackedFloatArray[Self.o, DType.float64, 8](self.buf, payload + _abl[1])

def read_telemetry_root[o: ImmOrigin, //](buf: Span[UInt8, o]) raises -> TelemetryAccessor[o]:
    return TelemetryAccessor[o](buf, root_offset(buf))
