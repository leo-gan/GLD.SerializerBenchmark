# GENERATED by dagr/codegen/mojo/serde.py — do not edit by hand.
# Serializer (arena -> bytes) for graph 'MessageFrozenPackedGraph' — plan 28 Phase 4.

from dagr_writer import Builder, NodeStoreRef, node_offset, resolved_ref, nil_ref, to_zigzag, leb_length
from message_frozen_packed_graph_arena import MessageFrozenPackedGraphArena, Message

comptime _VT_MAX = 1   # widest regular-node vtable (§1.19)


# Build a List[Int] of vtable entries from a varargs-free call site.
def _ilist(*vals: Int) -> List[Int]:
    var l = List[Int]()
    for v in vals:
        l.append(v)
    return l^


# Type-namespaced node identity for dedup / cycle keys: a bare packed handle
# collides across node types (index space is per-type), so fold in the type id.
def _nid(tid: Int, idx: Int) -> UInt64:
    return (UInt64(idx) << 8) | UInt64(tid)


def _store_message[o: Origin[mut=False], //](mut b: Builder[_VT_MAX], a: MessageFrozenPackedGraphArena, h: Message[o]) raises -> NodeStoreRef:
    var _id = _nid(0, h._index())
    if _id in b.struct_lookup:
        return resolved_ref(b.struct_lookup[_id])
    var _before = b.cursor
    var _rawbits: UInt64 = 0
    var _pr0 = Bool(h.f_bool())
    var _pr1 = Bool(h.f_int32())
    var _pr2 = Bool(h.f_int64())
    var _pr3 = Bool(h.f_float64())
    var _pr4 = Bool(a._arr_message[h._index()].f_string)
    var _pr5 = Bool(h.f_bool_2())
    var _pr6 = Bool(h.f_int32_2())
    var _pr7 = Bool(a._arr_message[h._index()].f_string_2)
    if _pr7:
        _ = b.store_utf8(a._arr_message[h._index()].f_string_2.value(), False)
    if _pr6:
        if leb_length(to_zigzag(Int(h.f_int32_2().value()))) < 4:
            _ = b.store_leb(to_zigzag(Int(h.f_int32_2().value())))
        else:
            _ = b.store_i32(h.f_int32_2().value())
            _rawbits |= UInt64(1) << 3
    if _pr5:
        _ = b.store_u8(UInt8(1) if h.f_bool_2().value() else UInt8(0))
    if _pr4:
        _ = b.store_utf8(a._arr_message[h._index()].f_string.value(), False)
    if _pr3:
        _ = b.store_f64(h.f_float64().value())
        _rawbits |= UInt64(1) << 2
    if _pr2:
        if leb_length(to_zigzag(Int(h.f_int64().value()))) < 8:
            _ = b.store_leb(to_zigzag(Int(h.f_int64().value())))
        else:
            _ = b.store_i64(h.f_int64().value())
            _rawbits |= UInt64(1) << 1
    if _pr1:
        if leb_length(to_zigzag(Int(h.f_int32().value()))) < 4:
            _ = b.store_leb(to_zigzag(Int(h.f_int32().value())))
        else:
            _ = b.store_i32(h.f_int32().value())
            _rawbits |= UInt64(1) << 0
    if _pr0:
        _ = b.store_u8(UInt8(1) if h.f_bool().value() else UInt8(0))
    _ = b.store_u8(UInt8(_rawbits & 0xff))
    var _pb: UInt64 = 0
    if _pr0:
        _pb |= UInt64(1) << 0
    if _pr1:
        _pb |= UInt64(1) << 1
    if _pr2:
        _pb |= UInt64(1) << 2
    if _pr3:
        _pb |= UInt64(1) << 3
    if _pr4:
        _pb |= UInt64(1) << 4
    if _pr5:
        _pb |= UInt64(1) << 5
    if _pr6:
        _pb |= UInt64(1) << 6
    if _pr7:
        _pb |= UInt64(1) << 7
    _ = b.store_u8(UInt8(_pb & 0xff))
    _ = b.store_leb(UInt64(b.cursor - _before))
    var _o = b.cursor
    b.struct_lookup[_id] = _o
    return resolved_ref(_o)


def write_message_graph(mut b: Builder[_VT_MAX], a: MessageFrozenPackedGraphArena) raises -> Int:
    var _off = node_offset(_store_message(b, a, a.root().value()))
    return b.store_leb(UInt64((b.cursor - _off) << 2))    # framing (no header)

def serialize_message_graph(a: MessageFrozenPackedGraphArena, max_size: Int = 2 * 1024 * 1024) raises -> List[UInt8]:
    var b = Builder[_VT_MAX](hint=len(a._arr_message), max_size=max_size)
    _ = write_message_graph(b, a)
    return b.make_data()
