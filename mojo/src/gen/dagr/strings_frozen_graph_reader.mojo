# GENERATED by dagr/codegen/mojo/core.py — do not edit by hand.
# Lazy reader for graph 'StringsFrozenGraph' (regular-node scalars/utf8/data/enum/node-ref/arrays).

from dagr_reader import Utf8Array, read_bitset, read_leb, read_v62, root_offset


struct StringsAccessor[o: ImmOrigin](Copyable, Movable, ImplicitlyCopyable):
    var buf: Span[UInt8, Self.o]
    var pos: Int     # node start position (dedup key for eager restore)
    var _p0: Int   # position of field 0 (items); -1 = absent

    def __init__(out self, buf: Span[UInt8, Self.o], start: Int) raises:
        self.buf = buf
        self.pos = start
        var bs = read_bitset(buf, start, 1)
        var cur = start + 1
        if (bs >> 0) & 1:
            self._p0 = cur
            cur += read_v62(self.buf, cur)[1]
        else:
            self._p0 = -1

    def items(self) raises -> Optional[Utf8Array[Self.o]]:
        if self._p0 < 0:
            return None
        var pos = self._p0
        var p = read_v62(self.buf, pos)
        var payload = pos + p[1] + Int(p[0])
        var h = read_leb(self.buf, payload)
        var count = Int(h[0]) >> 2
        var es = 1 << (Int(h[0]) & 3)
        var table_base = payload + h[1]
        return Utf8Array[Self.o](self.buf, table_base, table_base + count * es, count, es)

def read_strings_root[o: ImmOrigin, //](buf: Span[UInt8, o]) raises -> StringsAccessor[o]:
    return StringsAccessor[o](buf, root_offset(buf))
