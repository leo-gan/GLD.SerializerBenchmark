// Code generated by dagr (dagr/codegen/go/arena.py); DO NOT EDIT.

package eventregulargraph

import (
	"strconv"
	"strings"

	"dagrv2/dagr"
)

type eventAttrValues struct {
	key   dagr.Opt[string]
	value dagr.Opt[string]
}

type eventValues struct {
	eventID    dagr.Opt[string]
	eventType  dagr.Opt[string]
	occurredAt dagr.Opt[int64]
	producer   dagr.Opt[string]
	attrs      dagr.Opt[[]EventAttr]
}

// Arena owns every node of one graph instance (struct-of-arrays storage; handles
// are indices into it, so the graph may be cyclic and share nodes freely).
type Arena struct {
	eventAttrs []eventAttrValues
	events     []eventValues
	root       dagr.Opt[uint64]
	id         uint32
}

var arenaIDs uint32

// NewArena creates an empty arena.
func NewArena() *Arena {
	arenaIDs++
	return &Arena{id: arenaIDs}
}

func (a *Arena) Root() (Event, bool) {
	if !a.root.Ok {
		return Event{}, false
	}
	h := Event{a: a, h: a.root.V}
	return h, true
}

func (a *Arena) SetRoot(h Event) { a.root = dagr.Some(h.h) }

func (a *Arena) ClearRoot() { a.root = dagr.None[uint64]() }

func (a *Arena) allocEventAttr() (int, uint64) {
	i := len(a.eventAttrs)
	a.eventAttrs = append(a.eventAttrs, eventAttrValues{})
	return i, dagr.Pack(0, i)
}

// NewEventAttr allocates a EventAttr with construct-time defaults.
func (a *Arena) NewEventAttr() EventAttr {
	idx, h := a.allocEventAttr()
	_ = idx
	return EventAttr{a: a, h: h}
}

func (a *Arena) allocEvent() (int, uint64) {
	i := len(a.events)
	a.events = append(a.events, eventValues{})
	return i, dagr.Pack(0, i)
}

// NewEvent allocates a Event with construct-time defaults.
func (a *Arena) NewEvent() Event {
	idx, h := a.allocEvent()
	_ = idx
	return Event{a: a, h: h}
}

// EventAttr is a value handle into an Arena (spec 09: generation<<40 | index).
type EventAttr struct {
	a *Arena
	h uint64
}

func (n EventAttr) Arena() *Arena { return n.a }

func (n EventAttr) Handle() uint64 { return n.h }

func (n EventAttr) Index() int { return dagr.UnpackIdx(n.h) }

func (n EventAttr) vals() *eventAttrValues {
	return &n.a.eventAttrs[dagr.UnpackIdx(n.h)]
}

func (n EventAttr) key() dagr.NodeKey {
	return dagr.NodeKey{Type: 0, Handle: n.h}
}

func (n EventAttr) IsValid() bool {
	return dagr.UnpackIdx(n.h) < len(n.a.eventAttrs)
}

func (n EventAttr) Key() (string, bool) {
	o := n.vals().key
	return o.V, o.Ok
}

func (n EventAttr) KeyBytes() ([]byte, bool) {
	o := n.vals().key
	return []byte(o.V), o.Ok
}

func (n EventAttr) SetKey(v string) { n.vals().key = dagr.Some(v) }

func (n EventAttr) ClearKey() { n.vals().key = dagr.None[string]() }

func (n EventAttr) Value() (string, bool) {
	o := n.vals().value
	return o.V, o.Ok
}

func (n EventAttr) ValueBytes() ([]byte, bool) {
	o := n.vals().value
	return []byte(o.V), o.Ok
}

func (n EventAttr) SetValue(v string) { n.vals().value = dagr.Some(v) }

func (n EventAttr) ClearValue() { n.vals().value = dagr.None[string]() }

func (n EventAttr) describe(sb *strings.Builder, seen map[dagr.NodeKey]int) {
	k := dagr.NodeKey{Type: 0, Handle: n.h}
	if ord, ok := seen[k]; ok {
		sb.WriteString("EventAttr#")
		sb.WriteString(strconv.Itoa(ord))
		return
	}
	seen[k] = len(seen)
	sb.WriteString("EventAttr#")
	sb.WriteString(strconv.Itoa(len(seen) - 1))
	sb.WriteString("{")
	sb.WriteString("key: ")
	if n.vals().key.Ok {
		sb.WriteString(strconv.Quote(n.vals().key.V))
	} else {
		sb.WriteString("nil")
	}
	sb.WriteString(", ")
	sb.WriteString("value: ")
	if n.vals().value.Ok {
		sb.WriteString(strconv.Quote(n.vals().value.V))
	} else {
		sb.WriteString("nil")
	}
	sb.WriteByte('}')
}

// String renders the node and everything reachable from it; a repeated node prints
// as `EventAttr#<ordinal>` so cycles terminate and sharing is visible.
func (n EventAttr) String() string {
	var sb strings.Builder
	n.describe(&sb, map[dagr.NodeKey]int{})
	return sb.String()
}

// Hash is a structural hash (FNV-1a of the canonical description): isomorphic graphs
// in different arenas hash equal.
func (n EventAttr) Hash() uint64 { return dagr.Fnv1a([]byte(n.String())) }

// Equals is cross-arena structural equality (cycle- and sharing-aware).
func (n EventAttr) Equals(o EventAttr) bool { return n.String() == o.String() }

// Event is a value handle into an Arena (spec 09: generation<<40 | index).
type Event struct {
	a *Arena
	h uint64
}

func (n Event) Arena() *Arena { return n.a }

func (n Event) Handle() uint64 { return n.h }

func (n Event) Index() int { return dagr.UnpackIdx(n.h) }

func (n Event) vals() *eventValues { return &n.a.events[dagr.UnpackIdx(n.h)] }

func (n Event) key() dagr.NodeKey { return dagr.NodeKey{Type: 1, Handle: n.h} }

func (n Event) IsValid() bool { return dagr.UnpackIdx(n.h) < len(n.a.events) }

func (n Event) EventID() (string, bool) {
	o := n.vals().eventID
	return o.V, o.Ok
}

func (n Event) EventIDBytes() ([]byte, bool) {
	o := n.vals().eventID
	return []byte(o.V), o.Ok
}

func (n Event) SetEventID(v string) { n.vals().eventID = dagr.Some(v) }

func (n Event) ClearEventID() { n.vals().eventID = dagr.None[string]() }

func (n Event) EventType() (string, bool) {
	o := n.vals().eventType
	return o.V, o.Ok
}

func (n Event) EventTypeBytes() ([]byte, bool) {
	o := n.vals().eventType
	return []byte(o.V), o.Ok
}

func (n Event) SetEventType(v string) { n.vals().eventType = dagr.Some(v) }

func (n Event) ClearEventType() { n.vals().eventType = dagr.None[string]() }

func (n Event) OccurredAt() (int64, bool) {
	o := n.vals().occurredAt
	return o.V, o.Ok
}

func (n Event) SetOccurredAt(v int64) { n.vals().occurredAt = dagr.Some(v) }

func (n Event) ClearOccurredAt() { n.vals().occurredAt = dagr.None[int64]() }

func (n Event) Producer() (string, bool) {
	o := n.vals().producer
	return o.V, o.Ok
}

func (n Event) ProducerBytes() ([]byte, bool) {
	o := n.vals().producer
	return []byte(o.V), o.Ok
}

func (n Event) SetProducer(v string) { n.vals().producer = dagr.Some(v) }

func (n Event) ClearProducer() { n.vals().producer = dagr.None[string]() }

func (n Event) Attrs() ([]EventAttr, bool) {
	o := n.vals().attrs
	if !o.Ok {
		return nil, false
	}
	s := o.V
	return s, true
}

func (n Event) AttrsLen() (int, bool) {
	s, ok := n.Attrs()
	return len(s), ok
}

func (n Event) AttrsAt(i int) EventAttr {
	s, _ := n.Attrs()
	return s[i]
}

func (n Event) SetAttrs(v []EventAttr) { n.vals().attrs = dagr.Some(v) }

func (n Event) ClearAttrs() { n.vals().attrs = dagr.None[[]EventAttr]() }

func (n Event) describe(sb *strings.Builder, seen map[dagr.NodeKey]int) {
	k := dagr.NodeKey{Type: 1, Handle: n.h}
	if ord, ok := seen[k]; ok {
		sb.WriteString("Event#")
		sb.WriteString(strconv.Itoa(ord))
		return
	}
	seen[k] = len(seen)
	sb.WriteString("Event#")
	sb.WriteString(strconv.Itoa(len(seen) - 1))
	sb.WriteString("{")
	sb.WriteString("event_id: ")
	if n.vals().eventID.Ok {
		sb.WriteString(strconv.Quote(n.vals().eventID.V))
	} else {
		sb.WriteString("nil")
	}
	sb.WriteString(", ")
	sb.WriteString("event_type: ")
	if n.vals().eventType.Ok {
		sb.WriteString(strconv.Quote(n.vals().eventType.V))
	} else {
		sb.WriteString("nil")
	}
	sb.WriteString(", ")
	sb.WriteString("occurred_at: ")
	if n.vals().occurredAt.Ok {
		sb.WriteString(strconv.FormatInt(int64(n.vals().occurredAt.V), 10))
	} else {
		sb.WriteString("nil")
	}
	sb.WriteString(", ")
	sb.WriteString("producer: ")
	if n.vals().producer.Ok {
		sb.WriteString(strconv.Quote(n.vals().producer.V))
	} else {
		sb.WriteString("nil")
	}
	sb.WriteString(", ")
	sb.WriteString("attrs: ")
	if n.vals().attrs.Ok {
		sb.WriteByte('[')
		for i, e := range n.vals().attrs.V {
			if i > 0 {
				sb.WriteString(", ")
			}
			e.describe(sb, seen)
		}
		sb.WriteByte(']')
	} else {
		sb.WriteString("nil")
	}
	sb.WriteByte('}')
}

// String renders the node and everything reachable from it; a repeated node prints
// as `Event#<ordinal>` so cycles terminate and sharing is visible.
func (n Event) String() string {
	var sb strings.Builder
	n.describe(&sb, map[dagr.NodeKey]int{})
	return sb.String()
}

// Hash is a structural hash (FNV-1a of the canonical description): isomorphic graphs
// in different arenas hash equal.
func (n Event) Hash() uint64 { return dagr.Fnv1a([]byte(n.String())) }

// Equals is cross-arena structural equality (cycle- and sharing-aware).
func (n Event) Equals(o Event) bool { return n.String() == o.String() }

func restoreEventAttr(acc EventAttrAccessor, a *Arena, seen map[dagr.SeenKey]uint64) EventAttr {
	sk := dagr.SeenAt(acc.buf, acc.pos)
	if h, ok := seen[sk]; ok {
		return EventAttr{a: a, h: h}
	}
	idx, h := a.allocEventAttr()
	seen[sk] = h
	var tmpKey dagr.Opt[string]
	{
		if v, ok := acc.Key(); ok {
			tmpKey = dagr.Some(v)
		}
	}
	a.eventAttrs[idx].key = tmpKey
	var tmpValue dagr.Opt[string]
	{
		if v, ok := acc.Value(); ok {
			tmpValue = dagr.Some(v)
		}
	}
	a.eventAttrs[idx].value = tmpValue
	return EventAttr{a: a, h: h}
}

func restoreEvent(acc EventAccessor, a *Arena, seen map[dagr.SeenKey]uint64) Event {
	sk := dagr.SeenAt(acc.buf, acc.pos)
	if h, ok := seen[sk]; ok {
		return Event{a: a, h: h}
	}
	idx, h := a.allocEvent()
	seen[sk] = h
	var tmpEventID dagr.Opt[string]
	{
		if v, ok := acc.EventID(); ok {
			tmpEventID = dagr.Some(v)
		}
	}
	a.events[idx].eventID = tmpEventID
	var tmpEventType dagr.Opt[string]
	{
		if v, ok := acc.EventType(); ok {
			tmpEventType = dagr.Some(v)
		}
	}
	a.events[idx].eventType = tmpEventType
	var tmpOccurredAt dagr.Opt[int64]
	{
		if v, ok := acc.OccurredAt(); ok {
			tmpOccurredAt = dagr.Some(v)
		}
	}
	a.events[idx].occurredAt = tmpOccurredAt
	var tmpProducer dagr.Opt[string]
	{
		if v, ok := acc.Producer(); ok {
			tmpProducer = dagr.Some(v)
		}
	}
	a.events[idx].producer = tmpProducer
	var tmpAttrs dagr.Opt[[]EventAttr]
	{
		if cnt, ok := acc.AttrsLen(); ok {
			s := make([]EventAttr, cnt)
			it := acc.AttrsIter()
			for i := 0; i < cnt; i++ {
				e, _ := it.Next()
				s[i] = restoreEventAttr(e, a, seen)
			}
			tmpAttrs = dagr.Some(s)
		}
	}
	a.events[idx].attrs = tmpAttrs
	return Event{a: a, h: h}
}

// RestoreEventRegularGraph eagerly materializes a buffer into a fresh Arena (shared and cyclic
// nodes materialize once).
func RestoreEventRegularGraph(buf []byte) (*Arena, error) {
	acc, err := OpenEventRegularGraph(buf)
	if err != nil {
		return nil, err
	}
	a := NewArena()
	a.SetRoot(restoreEvent(acc, a, map[dagr.SeenKey]uint64{}))
	return a, nil
}
